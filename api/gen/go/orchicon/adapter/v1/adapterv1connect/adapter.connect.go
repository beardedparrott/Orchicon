// Orchicon Runtime Adapter contract.
//
// This is the gRPC sidecar contract between the Orchicon control plane
// and pluggable execution runtimes (docs/04_Runtime_Adapter_SDK.md §3).
// It lives in a separate package from the public API
// (orchicon.adapter.v1) because it has a different audience (adapter
// authors vs. API clients), different stability guarantees, and a
// different auth model (mTLS vs. OIDC/API keys) — docs/07 §10.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: orchicon/adapter/v1/adapter.proto

package adapterv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/beardedparrott/orchicon/api/gen/go/orchicon/adapter/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RuntimeAdapterServiceName is the fully-qualified name of the RuntimeAdapterService service.
	RuntimeAdapterServiceName = "orchicon.adapter.v1.RuntimeAdapterService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RuntimeAdapterServiceRegisterProcedure is the fully-qualified name of the RuntimeAdapterService's
	// Register RPC.
	RuntimeAdapterServiceRegisterProcedure = "/orchicon.adapter.v1.RuntimeAdapterService/Register"
	// RuntimeAdapterServiceDeregisterProcedure is the fully-qualified name of the
	// RuntimeAdapterService's Deregister RPC.
	RuntimeAdapterServiceDeregisterProcedure = "/orchicon.adapter.v1.RuntimeAdapterService/Deregister"
	// RuntimeAdapterServiceHeartbeatProcedure is the fully-qualified name of the
	// RuntimeAdapterService's Heartbeat RPC.
	RuntimeAdapterServiceHeartbeatProcedure = "/orchicon.adapter.v1.RuntimeAdapterService/Heartbeat"
	// RuntimeAdapterServiceExecuteProcedure is the fully-qualified name of the RuntimeAdapterService's
	// Execute RPC.
	RuntimeAdapterServiceExecuteProcedure = "/orchicon.adapter.v1.RuntimeAdapterService/Execute"
)

// RuntimeAdapterServiceClient is a client for the orchicon.adapter.v1.RuntimeAdapterService
// service.
type RuntimeAdapterServiceClient interface {
	// Register announces the adapter with its kind/version/capabilities.
	// Control plane records a RuntimeAdapter row and begins a heartbeat
	// lease (docs/04 §2).
	Register(context.Context, *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error)
	// Deregister gracefully drains — new executions refused, in-flight
	// allowed to complete (docs/04 §2).
	Deregister(context.Context, *connect.Request[v1.DeregisterRequest]) (*connect.Response[v1.DeregisterResponse], error)
	// Heartbeat renews the adapter's lease and carries a health snapshot
	// (docs/04 §2, docs/03 §5).
	Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error)
	// Execute is one bidirectional stream per WorkerExecution
	// (docs/04 §3). Client messages (start/control/approval) go down;
	// adapter messages (telemetry/tool_call/checkpoint/result/error) come
	// up. All telemetry is dual-written: onto the stream (low-latency for
	// live UI) and onto NATS (durable) by the control plane
	// (docs/04 §4).
	Execute(context.Context) *connect.BidiStreamForClient[v1.ClientMessage, v1.AdapterMessage]
}

// NewRuntimeAdapterServiceClient constructs a client for the
// orchicon.adapter.v1.RuntimeAdapterService service. By default, it uses the Connect protocol with
// the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To use
// the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRuntimeAdapterServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RuntimeAdapterServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	runtimeAdapterServiceMethods := v1.File_orchicon_adapter_v1_adapter_proto.Services().ByName("RuntimeAdapterService").Methods()
	return &runtimeAdapterServiceClient{
		register: connect.NewClient[v1.RegisterRequest, v1.RegisterResponse](
			httpClient,
			baseURL+RuntimeAdapterServiceRegisterProcedure,
			connect.WithSchema(runtimeAdapterServiceMethods.ByName("Register")),
			connect.WithClientOptions(opts...),
		),
		deregister: connect.NewClient[v1.DeregisterRequest, v1.DeregisterResponse](
			httpClient,
			baseURL+RuntimeAdapterServiceDeregisterProcedure,
			connect.WithSchema(runtimeAdapterServiceMethods.ByName("Deregister")),
			connect.WithClientOptions(opts...),
		),
		heartbeat: connect.NewClient[v1.HeartbeatRequest, v1.HeartbeatResponse](
			httpClient,
			baseURL+RuntimeAdapterServiceHeartbeatProcedure,
			connect.WithSchema(runtimeAdapterServiceMethods.ByName("Heartbeat")),
			connect.WithClientOptions(opts...),
		),
		execute: connect.NewClient[v1.ClientMessage, v1.AdapterMessage](
			httpClient,
			baseURL+RuntimeAdapterServiceExecuteProcedure,
			connect.WithSchema(runtimeAdapterServiceMethods.ByName("Execute")),
			connect.WithClientOptions(opts...),
		),
	}
}

// runtimeAdapterServiceClient implements RuntimeAdapterServiceClient.
type runtimeAdapterServiceClient struct {
	register   *connect.Client[v1.RegisterRequest, v1.RegisterResponse]
	deregister *connect.Client[v1.DeregisterRequest, v1.DeregisterResponse]
	heartbeat  *connect.Client[v1.HeartbeatRequest, v1.HeartbeatResponse]
	execute    *connect.Client[v1.ClientMessage, v1.AdapterMessage]
}

// Register calls orchicon.adapter.v1.RuntimeAdapterService.Register.
func (c *runtimeAdapterServiceClient) Register(ctx context.Context, req *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error) {
	return c.register.CallUnary(ctx, req)
}

// Deregister calls orchicon.adapter.v1.RuntimeAdapterService.Deregister.
func (c *runtimeAdapterServiceClient) Deregister(ctx context.Context, req *connect.Request[v1.DeregisterRequest]) (*connect.Response[v1.DeregisterResponse], error) {
	return c.deregister.CallUnary(ctx, req)
}

// Heartbeat calls orchicon.adapter.v1.RuntimeAdapterService.Heartbeat.
func (c *runtimeAdapterServiceClient) Heartbeat(ctx context.Context, req *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error) {
	return c.heartbeat.CallUnary(ctx, req)
}

// Execute calls orchicon.adapter.v1.RuntimeAdapterService.Execute.
func (c *runtimeAdapterServiceClient) Execute(ctx context.Context) *connect.BidiStreamForClient[v1.ClientMessage, v1.AdapterMessage] {
	return c.execute.CallBidiStream(ctx)
}

// RuntimeAdapterServiceHandler is an implementation of the
// orchicon.adapter.v1.RuntimeAdapterService service.
type RuntimeAdapterServiceHandler interface {
	// Register announces the adapter with its kind/version/capabilities.
	// Control plane records a RuntimeAdapter row and begins a heartbeat
	// lease (docs/04 §2).
	Register(context.Context, *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error)
	// Deregister gracefully drains — new executions refused, in-flight
	// allowed to complete (docs/04 §2).
	Deregister(context.Context, *connect.Request[v1.DeregisterRequest]) (*connect.Response[v1.DeregisterResponse], error)
	// Heartbeat renews the adapter's lease and carries a health snapshot
	// (docs/04 §2, docs/03 §5).
	Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error)
	// Execute is one bidirectional stream per WorkerExecution
	// (docs/04 §3). Client messages (start/control/approval) go down;
	// adapter messages (telemetry/tool_call/checkpoint/result/error) come
	// up. All telemetry is dual-written: onto the stream (low-latency for
	// live UI) and onto NATS (durable) by the control plane
	// (docs/04 §4).
	Execute(context.Context, *connect.BidiStream[v1.ClientMessage, v1.AdapterMessage]) error
}

// NewRuntimeAdapterServiceHandler builds an HTTP handler from the service implementation. It
// returns the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRuntimeAdapterServiceHandler(svc RuntimeAdapterServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	runtimeAdapterServiceMethods := v1.File_orchicon_adapter_v1_adapter_proto.Services().ByName("RuntimeAdapterService").Methods()
	runtimeAdapterServiceRegisterHandler := connect.NewUnaryHandler(
		RuntimeAdapterServiceRegisterProcedure,
		svc.Register,
		connect.WithSchema(runtimeAdapterServiceMethods.ByName("Register")),
		connect.WithHandlerOptions(opts...),
	)
	runtimeAdapterServiceDeregisterHandler := connect.NewUnaryHandler(
		RuntimeAdapterServiceDeregisterProcedure,
		svc.Deregister,
		connect.WithSchema(runtimeAdapterServiceMethods.ByName("Deregister")),
		connect.WithHandlerOptions(opts...),
	)
	runtimeAdapterServiceHeartbeatHandler := connect.NewUnaryHandler(
		RuntimeAdapterServiceHeartbeatProcedure,
		svc.Heartbeat,
		connect.WithSchema(runtimeAdapterServiceMethods.ByName("Heartbeat")),
		connect.WithHandlerOptions(opts...),
	)
	runtimeAdapterServiceExecuteHandler := connect.NewBidiStreamHandler(
		RuntimeAdapterServiceExecuteProcedure,
		svc.Execute,
		connect.WithSchema(runtimeAdapterServiceMethods.ByName("Execute")),
		connect.WithHandlerOptions(opts...),
	)
	return "/orchicon.adapter.v1.RuntimeAdapterService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RuntimeAdapterServiceRegisterProcedure:
			runtimeAdapterServiceRegisterHandler.ServeHTTP(w, r)
		case RuntimeAdapterServiceDeregisterProcedure:
			runtimeAdapterServiceDeregisterHandler.ServeHTTP(w, r)
		case RuntimeAdapterServiceHeartbeatProcedure:
			runtimeAdapterServiceHeartbeatHandler.ServeHTTP(w, r)
		case RuntimeAdapterServiceExecuteProcedure:
			runtimeAdapterServiceExecuteHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRuntimeAdapterServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRuntimeAdapterServiceHandler struct{}

func (UnimplementedRuntimeAdapterServiceHandler) Register(context.Context, *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("orchicon.adapter.v1.RuntimeAdapterService.Register is not implemented"))
}

func (UnimplementedRuntimeAdapterServiceHandler) Deregister(context.Context, *connect.Request[v1.DeregisterRequest]) (*connect.Response[v1.DeregisterResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("orchicon.adapter.v1.RuntimeAdapterService.Deregister is not implemented"))
}

func (UnimplementedRuntimeAdapterServiceHandler) Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("orchicon.adapter.v1.RuntimeAdapterService.Heartbeat is not implemented"))
}

func (UnimplementedRuntimeAdapterServiceHandler) Execute(context.Context, *connect.BidiStream[v1.ClientMessage, v1.AdapterMessage]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("orchicon.adapter.v1.RuntimeAdapterService.Execute is not implemented"))
}
