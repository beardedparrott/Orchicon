// Orchicon FileEditService: durable file-edit ledger fetch + live stream.
//
// API-key auth rides the shared interceptor chain like every other service;
// the read policy mirrors ExecutionService's (a caller that can read an
// execution can read its file-edit ledger).

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: orchicon/api/v1/file_edit_service.proto

package apiv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/beardedparrott/orchicon/api/gen/go/orchicon/api/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// FileEditServiceName is the fully-qualified name of the FileEditService service.
	FileEditServiceName = "orchicon.api.v1.FileEditService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// FileEditServiceGetSessionFileEditsProcedure is the fully-qualified name of the FileEditService's
	// GetSessionFileEdits RPC.
	FileEditServiceGetSessionFileEditsProcedure = "/orchicon.api.v1.FileEditService/GetSessionFileEdits"
	// FileEditServiceStreamFileEditsProcedure is the fully-qualified name of the FileEditService's
	// StreamFileEdits RPC.
	FileEditServiceStreamFileEditsProcedure = "/orchicon.api.v1.FileEditService/StreamFileEdits"
)

// FileEditServiceClient is a client for the orchicon.api.v1.FileEditService service.
type FileEditServiceClient interface {
	// GetSessionFileEdits returns the file-edit ledger for one owner
	// (execution or Ask conversation), optionally resuming from a sequence —
	// the initial load + catch-up read for the GUI sidebar / TUI pane.
	GetSessionFileEdits(context.Context, *connect.Request[v1.GetSessionFileEditsRequest]) (*connect.Response[v1.GetSessionFileEditsResponse], error)
	// StreamFileEdits is the server-stream RPC that fans out new ledger
	// entries for one owner as they are persisted. event_id dedups across
	// reconnects; from_sequence resumes after a sequence.
	StreamFileEdits(context.Context, *connect.Request[v1.StreamFileEditsRequest]) (*connect.ServerStreamForClient[v1.StreamFileEditsResponse], error)
}

// NewFileEditServiceClient constructs a client for the orchicon.api.v1.FileEditService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewFileEditServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) FileEditServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	fileEditServiceMethods := v1.File_orchicon_api_v1_file_edit_service_proto.Services().ByName("FileEditService").Methods()
	return &fileEditServiceClient{
		getSessionFileEdits: connect.NewClient[v1.GetSessionFileEditsRequest, v1.GetSessionFileEditsResponse](
			httpClient,
			baseURL+FileEditServiceGetSessionFileEditsProcedure,
			connect.WithSchema(fileEditServiceMethods.ByName("GetSessionFileEdits")),
			connect.WithClientOptions(opts...),
		),
		streamFileEdits: connect.NewClient[v1.StreamFileEditsRequest, v1.StreamFileEditsResponse](
			httpClient,
			baseURL+FileEditServiceStreamFileEditsProcedure,
			connect.WithSchema(fileEditServiceMethods.ByName("StreamFileEdits")),
			connect.WithClientOptions(opts...),
		),
	}
}

// fileEditServiceClient implements FileEditServiceClient.
type fileEditServiceClient struct {
	getSessionFileEdits *connect.Client[v1.GetSessionFileEditsRequest, v1.GetSessionFileEditsResponse]
	streamFileEdits     *connect.Client[v1.StreamFileEditsRequest, v1.StreamFileEditsResponse]
}

// GetSessionFileEdits calls orchicon.api.v1.FileEditService.GetSessionFileEdits.
func (c *fileEditServiceClient) GetSessionFileEdits(ctx context.Context, req *connect.Request[v1.GetSessionFileEditsRequest]) (*connect.Response[v1.GetSessionFileEditsResponse], error) {
	return c.getSessionFileEdits.CallUnary(ctx, req)
}

// StreamFileEdits calls orchicon.api.v1.FileEditService.StreamFileEdits.
func (c *fileEditServiceClient) StreamFileEdits(ctx context.Context, req *connect.Request[v1.StreamFileEditsRequest]) (*connect.ServerStreamForClient[v1.StreamFileEditsResponse], error) {
	return c.streamFileEdits.CallServerStream(ctx, req)
}

// FileEditServiceHandler is an implementation of the orchicon.api.v1.FileEditService service.
type FileEditServiceHandler interface {
	// GetSessionFileEdits returns the file-edit ledger for one owner
	// (execution or Ask conversation), optionally resuming from a sequence —
	// the initial load + catch-up read for the GUI sidebar / TUI pane.
	GetSessionFileEdits(context.Context, *connect.Request[v1.GetSessionFileEditsRequest]) (*connect.Response[v1.GetSessionFileEditsResponse], error)
	// StreamFileEdits is the server-stream RPC that fans out new ledger
	// entries for one owner as they are persisted. event_id dedups across
	// reconnects; from_sequence resumes after a sequence.
	StreamFileEdits(context.Context, *connect.Request[v1.StreamFileEditsRequest], *connect.ServerStream[v1.StreamFileEditsResponse]) error
}

// NewFileEditServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewFileEditServiceHandler(svc FileEditServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	fileEditServiceMethods := v1.File_orchicon_api_v1_file_edit_service_proto.Services().ByName("FileEditService").Methods()
	fileEditServiceGetSessionFileEditsHandler := connect.NewUnaryHandler(
		FileEditServiceGetSessionFileEditsProcedure,
		svc.GetSessionFileEdits,
		connect.WithSchema(fileEditServiceMethods.ByName("GetSessionFileEdits")),
		connect.WithHandlerOptions(opts...),
	)
	fileEditServiceStreamFileEditsHandler := connect.NewServerStreamHandler(
		FileEditServiceStreamFileEditsProcedure,
		svc.StreamFileEdits,
		connect.WithSchema(fileEditServiceMethods.ByName("StreamFileEdits")),
		connect.WithHandlerOptions(opts...),
	)
	return "/orchicon.api.v1.FileEditService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case FileEditServiceGetSessionFileEditsProcedure:
			fileEditServiceGetSessionFileEditsHandler.ServeHTTP(w, r)
		case FileEditServiceStreamFileEditsProcedure:
			fileEditServiceStreamFileEditsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedFileEditServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedFileEditServiceHandler struct{}

func (UnimplementedFileEditServiceHandler) GetSessionFileEdits(context.Context, *connect.Request[v1.GetSessionFileEditsRequest]) (*connect.Response[v1.GetSessionFileEditsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("orchicon.api.v1.FileEditService.GetSessionFileEdits is not implemented"))
}

func (UnimplementedFileEditServiceHandler) StreamFileEdits(context.Context, *connect.Request[v1.StreamFileEditsRequest], *connect.ServerStream[v1.StreamFileEditsResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("orchicon.api.v1.FileEditService.StreamFileEdits is not implemented"))
}
