// Orchicon ApprovalService.
//
// The ApprovalService manages the human-in-the-loop approval gate for
// workflow steps. An APPROVAL step in a workflow blocks at
// approval_pending until a human reviews the upstream context (diff,
// summary, AC) and approves or rejects it. The decision is written to
// .orchicon/ files so downstream workers and loop-back steps can read
// the outcome.
//
// Future: notifications (SMS, email, browser), RBAC enforcement
// (which users/groups can approve at project/work-item scope).

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: orchicon/api/v1/approval_service.proto

package apiv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/beardedparrott/orchicon/api/gen/go/orchicon/api/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ApprovalServiceName is the fully-qualified name of the ApprovalService service.
	ApprovalServiceName = "orchicon.api.v1.ApprovalService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ApprovalServiceApproveStepProcedure is the fully-qualified name of the ApprovalService's
	// ApproveStep RPC.
	ApprovalServiceApproveStepProcedure = "/orchicon.api.v1.ApprovalService/ApproveStep"
	// ApprovalServiceListPendingStepApprovalsProcedure is the fully-qualified name of the
	// ApprovalService's ListPendingStepApprovals RPC.
	ApprovalServiceListPendingStepApprovalsProcedure = "/orchicon.api.v1.ApprovalService/ListPendingStepApprovals"
)

// ApprovalServiceClient is a client for the orchicon.api.v1.ApprovalService service.
type ApprovalServiceClient interface {
	// ApproveStep resolves a workflow step that is awaiting human approval.
	// The step must be in approval_pending status. On approval the step
	// transitions to succeeded; on rejection it also succeeds but carries
	// the rejection reason as data — a downstream LOOP_DECISION can branch
	// on the decision.
	ApproveStep(context.Context, *connect.Request[v1.ApproveStepRequest]) (*connect.Response[v1.ApproveStepResponse], error)
	// ListPendingStepApprovals returns unresolved approval_pending step runs
	// across the tenant, joined with project/work-item/workflow names for
	// display. Supports the same filter/sort/search pattern as other lists.
	ListPendingStepApprovals(context.Context, *connect.Request[v1.ListPendingStepApprovalsRequest]) (*connect.Response[v1.ListPendingStepApprovalsResponse], error)
}

// NewApprovalServiceClient constructs a client for the orchicon.api.v1.ApprovalService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewApprovalServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ApprovalServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	approvalServiceMethods := v1.File_orchicon_api_v1_approval_service_proto.Services().ByName("ApprovalService").Methods()
	return &approvalServiceClient{
		approveStep: connect.NewClient[v1.ApproveStepRequest, v1.ApproveStepResponse](
			httpClient,
			baseURL+ApprovalServiceApproveStepProcedure,
			connect.WithSchema(approvalServiceMethods.ByName("ApproveStep")),
			connect.WithClientOptions(opts...),
		),
		listPendingStepApprovals: connect.NewClient[v1.ListPendingStepApprovalsRequest, v1.ListPendingStepApprovalsResponse](
			httpClient,
			baseURL+ApprovalServiceListPendingStepApprovalsProcedure,
			connect.WithSchema(approvalServiceMethods.ByName("ListPendingStepApprovals")),
			connect.WithClientOptions(opts...),
		),
	}
}

// approvalServiceClient implements ApprovalServiceClient.
type approvalServiceClient struct {
	approveStep              *connect.Client[v1.ApproveStepRequest, v1.ApproveStepResponse]
	listPendingStepApprovals *connect.Client[v1.ListPendingStepApprovalsRequest, v1.ListPendingStepApprovalsResponse]
}

// ApproveStep calls orchicon.api.v1.ApprovalService.ApproveStep.
func (c *approvalServiceClient) ApproveStep(ctx context.Context, req *connect.Request[v1.ApproveStepRequest]) (*connect.Response[v1.ApproveStepResponse], error) {
	return c.approveStep.CallUnary(ctx, req)
}

// ListPendingStepApprovals calls orchicon.api.v1.ApprovalService.ListPendingStepApprovals.
func (c *approvalServiceClient) ListPendingStepApprovals(ctx context.Context, req *connect.Request[v1.ListPendingStepApprovalsRequest]) (*connect.Response[v1.ListPendingStepApprovalsResponse], error) {
	return c.listPendingStepApprovals.CallUnary(ctx, req)
}

// ApprovalServiceHandler is an implementation of the orchicon.api.v1.ApprovalService service.
type ApprovalServiceHandler interface {
	// ApproveStep resolves a workflow step that is awaiting human approval.
	// The step must be in approval_pending status. On approval the step
	// transitions to succeeded; on rejection it also succeeds but carries
	// the rejection reason as data — a downstream LOOP_DECISION can branch
	// on the decision.
	ApproveStep(context.Context, *connect.Request[v1.ApproveStepRequest]) (*connect.Response[v1.ApproveStepResponse], error)
	// ListPendingStepApprovals returns unresolved approval_pending step runs
	// across the tenant, joined with project/work-item/workflow names for
	// display. Supports the same filter/sort/search pattern as other lists.
	ListPendingStepApprovals(context.Context, *connect.Request[v1.ListPendingStepApprovalsRequest]) (*connect.Response[v1.ListPendingStepApprovalsResponse], error)
}

// NewApprovalServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewApprovalServiceHandler(svc ApprovalServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	approvalServiceMethods := v1.File_orchicon_api_v1_approval_service_proto.Services().ByName("ApprovalService").Methods()
	approvalServiceApproveStepHandler := connect.NewUnaryHandler(
		ApprovalServiceApproveStepProcedure,
		svc.ApproveStep,
		connect.WithSchema(approvalServiceMethods.ByName("ApproveStep")),
		connect.WithHandlerOptions(opts...),
	)
	approvalServiceListPendingStepApprovalsHandler := connect.NewUnaryHandler(
		ApprovalServiceListPendingStepApprovalsProcedure,
		svc.ListPendingStepApprovals,
		connect.WithSchema(approvalServiceMethods.ByName("ListPendingStepApprovals")),
		connect.WithHandlerOptions(opts...),
	)
	return "/orchicon.api.v1.ApprovalService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ApprovalServiceApproveStepProcedure:
			approvalServiceApproveStepHandler.ServeHTTP(w, r)
		case ApprovalServiceListPendingStepApprovalsProcedure:
			approvalServiceListPendingStepApprovalsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedApprovalServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedApprovalServiceHandler struct{}

func (UnimplementedApprovalServiceHandler) ApproveStep(context.Context, *connect.Request[v1.ApproveStepRequest]) (*connect.Response[v1.ApproveStepResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("orchicon.api.v1.ApprovalService.ApproveStep is not implemented"))
}

func (UnimplementedApprovalServiceHandler) ListPendingStepApprovals(context.Context, *connect.Request[v1.ListPendingStepApprovalsRequest]) (*connect.Response[v1.ListPendingStepApprovalsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("orchicon.api.v1.ApprovalService.ListPendingStepApprovals is not implemented"))
}
