# UPDATES.md

> Track record of what has been shipped, phase-by-phase.
| 231 | Chore | Frontend | Commit the WCAG-AA theme-contrast verification script that was accidentally left out of the theme-expansion feature run. `scripts/verify-contrast.mjs` (node, no deps) reads `frontend/src/index.css`, extracts every `[data-theme="..."]` block, and checks contrast for all 20 themes: foreground/background ≥7:1, muted-foreground/muted ≥4.5:1, muted-foreground/background ≥4.5:1, primary-foreground/primary ≥4.5:1, and destructive-foreground/destructive ≥4.5:1 — asserting exactly 20 themes and exiting non-zero on any failure. Run from the repo root: `node scripts/verify-contrast.mjs`. Verified passing against the current 20-theme stylesheet. |
| 230 | Bug fix | Frontend | Repair a corrupted one-liner in `frontend/src/components/app-shell.tsx` that broke the frontend build (33 `tsc` errors, shipped by the UI feature run via #413's `1d5bb1a23` "feat(mobile): final usability hardening"). The top-nav `DropdownGroup`'s menu-panel `className` contained a stray `"   "` quote artifact — `isOpen ? "opacity-100 visible translate-y-0" : "opacity-0 invisible translate-y-1 pointer-events-none "   "` — which opened an unterminated string and cascaded into TS1005/TS1381/TS17002 parse errors across the rest of the 2,719-char line (line 36). The whole dropdown `return` had also been collapsed into a single line by the same automated edit. Fix: close the class string correctly (`pointer-events-none")}`) so the hidden-menu behavior (`opacity-0 invisible pointer-events-none` when closed) is preserved; no other changes. Verified: `tsc -b` clean → `vite build` succeeds (2,754 modules). Note: the file is still a one-liner and could use a prettier pass; the 103 pre-existing eslint problems are separate (frontend eslint is not part of `make ci`, whose `lint` target is `buf lint` only). |
| 229 | Bug fix | Scheduler | Fix the run_context PR-URL merge wedging runs in an infinite `loop_decision: accepted` reconcile loop. `pollTaskStep` merged `pr_url` into the run's `run_context` with a raw `UPDATE workflow_runs SET ... version = version + 1` Exec while `run` was passed **by value**, so the caller's in-memory `run.Version` was never bumped (the `run.Version++` from the earlier #406 attempt mutated only the local copy). The subsequent `mark run completed` CAS (`WHERE ... version = <stale>`) then matched nothing → `db: not found` → the whole transaction rolled back — including the version bump and the run_context merge — leaving the version pinned and the run re-polling the same succeeded execution every pass (observed on prod runs `01M0ZV3903P6GAZ29DY8TFZG52` and `01M0ZYYF4WKQG7MFJR7R02SFZP`: `step-devops-pr` succeeded with `PR_URL: .../pull/403`/`/pull/407` in output but the run stayed `running`). Fix: `pollTaskStep` now takes `run *db.WorkflowRunRow` so the version propagates back to the caller, and the merge goes through the data-access layer (`db.UpdateWorkflowRun` — optimistic CAS returning the fresh row, `*run = updatedRun`) instead of raw SQL (also satisfying the no-raw-SQL invariant). Test call sites updated for the pointer signature. After this fix the pre-existing `effectivePRURL` stdout-scrape-first ordering (also from #406) correctly captures the reused/merged PR URL on the first pass, so the run completes. |
| 228 | Chore | Dev | Automate the develop version-bump on merge. The version a build reports comes from Git tags (`internal/version` + the Makefile's `git describe --tags`); the repo always intended one `v0.1.x` `develop` bump tag per merge so a rebuild reports the current version, but that was a manual step (`git tag -a v0.1.<next>`) that was being skipped — the tag history has holes (v0.1.291/292 never existed). New `.github/workflows/develop-bump.yml` triggers on `push` to `develop` and tags the exact new tip commit with the next `v0.1.x` (patch+1 over the highest existing `v0.1.x` tag), pushing it with `contents: write`. It is idempotent (re-runs / already-existing tags log-and-skip), serialized (`concurrency` group + recompute-from-remote + bounded retry) so concurrent merges or a racing human release cut can't produce two equal-version tags, and does NOT publish a release (a GITHUB_TOKEN tag push won't re-trigger `release.yml`, and its main-reachability guard skips non-main tags) — only the human's `develop → main` `release`-labeled merge (auto-release.yml) still cuts a release. Version stays monotonic across develop bumps and releases. Docs: developer.md and DOCUMENTATION.md no longer instruct the manual `git tag -a` step; the Makefile VERSION comment now names the develop-bump + auto-release tag sources. |
| 227 | Bug fix | Adapter | Cancel/abort actually stops the model session (the "terminated but still active" runaway). `CancelExecution` and `AbortRunInTx` only flipped the execution to `terminated` in the DB — the live opencode session kept generating (tool calls kept rising, tokens kept burning) until it natural-finished. New `opencode.Adapter.AbortExecution(ctx, execID, reason)` cancels the session's subscription context AND calls `client.Abort` so the model's current turn stops streaming; it is injected as an `abortSession` hook into both the execution service's `CancelExecution` (after commit) and the workflow service's `AbortRunInTx` (which now returns the IDs of the executions it terminates, so the abort handler can stop each live session). `docker ps` now also stops showing a lingering runtime container for a reaped run. Tests: `TestAbortExecutionNoLiveSession` (unknown exec is a safe no-op). |
| 226 | Feature | Adapter | Composite worktree tools live on EVERY runtime image + corrected steering + `/tmp` scope. **(A) Gate fix**: `RuntimeServeConfig` previously enabled the composite worktree tools (`batch_read`/`batch_grep`/`batch_write`, DB-less MCP sidecar running the daemon's bind-mounted `/usr/local/bin/orchicon`) only for dev images (`orchicon-runtime:orchicon-dev`) — so a run on the base image (`orchicon-runtime:local`) never got `orchicon-worktree` in its config and the model fell back to granular `read`/`glob`. The gate now applies to all runtime images (the sidecar is DB-less and the binary is present in every runtime container), so base/gui/dev workers all get the batch tools + the read/grep deny. **(B) Steering**: the canned `workerAgentPrompt` is rewritten so the batch tools are *the* file-access tools (read/write/edit/grep removed from the inventory, with an explicit "do NOT use `read`/`grep`/`write`/`edit` for file access; they are disabled in favor of the batch tools" discipline); the per-session `batchToolsDiscipline` block is appended to the system prompt for runtime-container executions; worker.md's Token-discipline section now says the same — resolving the former "use the normal read tool in batches" contradiction. **(C) Path scope**: the batch tools previously resolved only against the worktree base and rejected absolute paths, so a worker couldn't reach the mounted `/tmp` it uses for Go build/tmp work. `safeResolve` now permits paths under the worktree base OR `os.TempDir()` (with traversal/absolute-outside-root still rejected), so `batch_read`/`batch_grep`/`batch_write` can operate on `/tmp`. Plus the dimension-aware compaction gating (tokens+cost only, never tool-call/time — see the earlier compaction work) and a `scripts/container.sh` `runtime-stop` exit-code fix (the trailing `[ -z "$pid" ]` evaluated false on the stopped path and returned 1, breaking `make runtime-stop && make runtime-daemon`). Tests: base-image composite config, `TestBatchReadAllowsTempDir`, `TestExecutionSystemPrompt` (batch discipline only on runtime-container execs). |
| 225 | Fix | Workflow reconciler | The DAG-pass limit abort no longer swallows staged step-recovery. `reconcileRun`'s pass loop hit `maxDAGPasses` and did `return fmt.Errorf(...)`, which triggered the deferred `ttx.Rollback` and silently discarded every write the pass had already made — including a just-failed step's `recovering` transition and the appended `recoveryTriggers`. So a genuinely-recoverable step failure (e.g. the `step-sse` on SDLC run `01M0TJYNMEG95KV38XTPX8DFEP`, configured `summarize_restart, max_attempts:6`, budget-aborted) never fired `TriggerOnFailure` — `RecoveryID` stayed null, the run died "dead in the tracks", and the parallel `loop_decision` runaway (which is what drove the run to the pass limit in the first place) was the trigger. The `maxDAGPasses` branch now `break`s out of the loop instead of erroring, so the pass's accumulated progress AND the staged recovery triggers are COMMITTED and the deferred post-commit fire loop actually runs them (recovery is preserved, not rolled back). The bound still holds (we break after `maxDAGPasses` iterations, so no reconcile call pins a goroutine), and a run left `running` is re-scanned every heartbeat (`ListPendingWorkflowRuns` includes `running`) so it resumes where it left off. Note: recovery writes dropped by a NON-maxDAGPasses mid-pass error return (e.g. a transient DB error) remain out of scope. |
| 224 | Fix | Adapter/UI | Stop counting cache reads as budgeted work + show the REAL context window. **(A) Budget gate**: `budgetAccumulator`'s token gate was counting cache reads at full weight (`totalTokens` = prompt+completion+reasoning+cacheRead), so a cache-heavy code worker could trip `budget_abort:tokens` by merely re-sending context it already held — the SSE implement step on the SDLC run (`01M0TKQ8DBYD0550TBVVF2GA9X`) hit exactly this, aborting at ~1.5M total tokens of which ~77% was cache-read re-send, and the workflow died in the tracks with no recovery (`RecoveryID null`, worktree pruned). The gate now keys off `freshTokens()` (prompt+completion+reasoning, cache EXCLUDED) in both `fraction(dimTokens)` and `budgetBreached`; `totalTokens()` is kept for reporting/troubleshooting. Cache reads still govern spend via the cost gate, which prices them (discounted) — so money is still bounded, only "real work tokens" no longer get eaten by transport. Tests: `TestBudgetBreachedTokenFullWeight`→`TestBudgetBreachedTokenFreshWeight` (asserts cache excluded from the token gate AND that a cache-heavy session still breaches cost), `TestLadderTokenAbortAtLimit` now reaches the limit via fresh prompt tokens. **(B) Real context window**: the execution panel's Context card hard-coded `contextWindow = 200_000` (a conservative guess, never changed, wrong for every different model). The panel now resolves the actual model context window from the model that ran — usage records' provider+model matched against model discovery's `limits.context` (`useListOpenCodeModels`), with the `contextWindow` prop as an explicit override. When the window can't be resolved, the panel drops the fabricated %/denominator and labels the working set "model window unknown" (header shows "—") instead of guessing. **(C) Working-set vs cache-transport split**: the peak "working set" and Messages "token mix" bars previously included cache reads, inflating the % and double-counting (cache is re-sends of already-counted tokens). The bars now key off the PEAK FRESH row (prompt+completion+reasoning, cache excluded) so the mix sums to 100% coherently, and cache read/write are surfaced in a separate "Cache transport (cumulative)" block with a cache hit-rate % — the labeled cumulative figure no longer contradicts the peak bars (the 121k-vs-1.3M confusion). DOCUMENTATION.md's Execution-budget section updated. |
| 223 | Feature | Adapter | Per-tier compaction policy (`compact_tiers`) + calm-but-severe budget warnings + explicit `compaction.auto: false` so opencode is not a second compaction driver. The unified budget ladder previously compacted on EVERY non-abort tier across all four dimensions with no re-arm floor, so a re-send-heavy run could be collapsed repeatedly mid-flight (the "constant compaction" churn on high-cache runs). Now: (1) `parseBudgetSpec` reads a per-tier `compact_tiers` [warn, escalate, final] policy (default [false, true, true] — the earliest warn tier no longer compacts; escalate + final still do), honored by `maybeEnforceLadder`; (2) ladder compacts now share `canCompactNow()` — a re-arm latch (per-execution cap + min-turn floor) with the turn-count gate, so two triggers (a tier + the gate, or two tiers) can no longer collapse the session on consecutive steps; (3) the warning message copy is reworded to calm-but-severe (names the re-sent-context driver and gives the concrete remedy — batch round-trips, stop re-reading, deliver the delta) in both `defaultWarnMsgs` and the DB message columns; (4) the opencode config sets `compaction.auto: false` (opencode's own lossy auto-compaction is redundant/harmful alongside Orchicon's ladder). DB: `20260830000000_compact_policy_tiers_and_message_copy.sql` adds `budget_compact_{warn,escalate,final}_tier` columns to `tenant_settings` and rolls the new message copy onto rows that still hold the old default (customized messages preserved); `BudgetLadder`/`BudgetJSON`/`ApplyBudgetJSON` carry `compact_tiers`; Settings → Defaults → Execution budget gains three compaction checkboxes; a worker's `budget_overrides` can set `compact_tiers` per-worker (flows through `mergeBudgets`). DOCUMENTATION.md's Execution-budget defaults section updated. |
| 222 | Chore | Build | BuildKit apt cache mounts, buildx-gated with a graceful fallback. `scripts/container.sh build_image` now probes `docker buildx version` at build time: when buildx is present it builds the apt layers of `deploy/container/Dockerfile` and `deploy/runtime/Dockerfile` with `# syntax=docker/dockerfile:1` + `--mount=type=cache,target=/var/cache/apt` (a persisted cache so a re-run resolves `.deb`s instead of re-downloading) and exports `DOCKER_BUILDKIT=1`; when buildx is missing it silently falls back to a plain build of the stock (classic-builder-safe) Dockerfile — the build never hard-fails over a missing plugin, unlike the earlier take that required buildx and broke `make rebuild-dev` when `docker buildx version` didn't resolve. The cache-mount variant is generated transiently (`cached_dockerfile` → temp file in /tmp, removed after the build), so the shipped Dockerfiles stay classic-safe as a single source of truth. Requires the docker-buildx plugin only for the fast path: `docker buildx install` / `paru -S docker-buildx` (Arch) / `sudo apt install docker-buildx` (Debian). |
| 221 | Feature | Dev | One-command full rebuild per instance. New `make rebuild-dev` / `make rebuild-prod` (and the parameterized `make full-rebuild instance=dev|prod`) run the whole sequence in order: `make ci` (lint + proto regen + `go vet` + `go test ./...` + RLS check) → `make migrate-hash` (syncs `db/migrations/atlas.sum` for the Atlas CLI path) → `make container-rebuild` (builds the frontend via `force-fe=1` so `make build` embeds the fresh dist into the binary via `go:embed`, then builds the image) → `down`/`up` the instance. The DB migration itself is applied by the container at boot (`serve` → `migrate.Run`, `ORCHICON_MIGRATE_ON_BOOT` defaults true), so no separate `make migrate` is needed — and running it would conflict with the container-owned Postgres. Removes the manual remember-the-sequence step and the recurring "forgot `make migrate-hash`" drift. |
| 220 | Feature | Settings | Make the execution-budget ladder fully database-driven and editable in Settings → Defaults. The warn/escalate/final thresholds and message text — previously Go code constants (`defaultWarnFracs`/`defaultWarnMsgs` in `internal/opencode/compact.go`) mirrored as a stale copy in `frontend/src/lib/budget-defaults.ts` and as stale `0.5/0.75/0.9` placeholders in `routes/settings.tsx` — are now typed `tenant_settings` columns (`budget_*`, 5 gates + 12 thresholds + 12 messages), seeded to the front-loaded 25/50/75 + context-first copy, with existing gate values migrated out of the legacy `default_budget_overrides` jsonb (retained as the wire/API transport). `db.TenantSettingsRow` gains the typed `BudgetLadder`; `BudgetJSON()` serializes the columns to the budget JSON the adapters' `parseBudgetSpec` reads, and `ApplyBudgetJSON()` ingests a client's JSON into the columns with partial-update preservation. The scheduler builds the tenant budget JSON from the columns before `mergeBudgets` with a worker's `budget_overrides` (per-worker gates AND ladder overrides preserved). The frontend message mirror (`DEFAULT_WARN_MSGS`) and the stale threshold placeholders are removed — the Settings UI reads/writes the ladder from the API, which now always returns the DB ladder. This removes the code/frontend/JSON-override tri-source drift that left prod/dev showing the old 50/75/90 ladder. |
| 219 | Bug fix | Adapter | Tighten the worker decision-signal pipeline (probe sensitivity, fake-summary from inline seed echo, live todos, honest context numbers). **(A) Completion probe over-fired**: `maybeProbeCompletion` sent the "your response appears to have been cut off" probe the instant a `session.idle` arrived without a `ORCHICON WORKER SUMMARY` marker — but the opencode serve can emit idle a beat BEFORE the final text part (the one actually carrying the marker) flushes through the event stream, so a model still writing its long final response was interjected mid-token (and, worse, a model about to deliver the marker could re-plan instead). Now it first arms a **completion-probe grace** (`ORCHICON_COMPLETION_PROBE_GRACE`, default 3s): any mid-generation activity during the window cancels it (`resolveProbe`), and if the marker lands before the grace expires the run settles normally; the probe fires only for a session genuinely silent and markerless through the whole window. Probe sending extracted into `sendCompletionProbe`. **(b) Fake success from a seed-echo marker**: the execution that ended `succeeded` with a mid-sentence output and no real sign-off — the only `ORCHICON WORKER SUMMARY:` in its output was the recovery-seed instruction echoed verbatim as cross-referenced inline code (`finish with \`ORCHICON WORKER SUMMARY: failure\` reason \`...\``). `placeholderMarkerBody`/`placeholderSummaryBody` didn't recognize a backtick-wrapped decision word (`failure\`` ) as a placeholder, so `realDecisionMarkerIn`/`lastRealSummaryMarker` treated it as a real sign-off: the completion probe was suppressed and the workflow recorded success on the marker-less run. Both marker predicates (opencode + scheduler, kept in sync) now strip a leading backtick from the first word and treat a bare inline-code `` `success` ``/`` `failure` `` as placeholder echo — so a markerless-final run falls through to probe → fail instead of a hollow success. **(c) Todo list only appeared as an all-green wall at the end**: prompt-level `todoListBlock` retitled to "maintain it EVERY turn" with an explicit per-turn `todowrite` obligation ("emit a full replacement array after every tool call or turn boundary; write the plan before the first action") plus matching strengthened language in the after-compact reminder; test pinned in `workflow_prompt_test.go`. The UI plumbing (`GetExecutionTodos`→latest `todowrite` part, 2s poll) was already correct — the worker just never maintained the list. **(d) Usage numbers honesty**: the Context card now labels the big number **peak working set** (`max single-step total_tokens`, same value the % bar uses) and, when cumulative exceeds it, adds an explicit "Cumulative billed" line (total tokens + cost + cache-read re-sent each turn), so a provider-side million-token cumulative bill no longer reads as a UI bug next to a ~60k working set. Tests: `TestCompletionProbeDecision`/`TestSummaryWordIsSingleDecisionSignal` gained recovery-seed inline-code + backtick-example cases (and the real-marker-after-seed-echo case). Operational note: a pod-synthetic-progress fallback (derive a progress list from `step_start`/`tool_use` events if flash-class models keep ignoring per-turn `todowrite`) is tracked as a separate follow-up; this pass is prompt-level only. |
| 218 | Bug fix | Adapter | Audited every Settings → Defaults field for the same "claimed built-in default that isn't actually enforced" bug class as #217's budget fields. Result: everything else (stall no-progress/repetition windows, nudge tuning, execution-liveness-reaper grace/threshold, log rotation, max-concurrent-runs, Ask Orchicon's model fallback) already resolves an unset/zero tenant value to a real built-in default correctly (`realDefault; if dbValue > 0 { override }`, the same pattern `wallClockDeadline` established) — confirmed by reading the runtime code, not just grepping. Found one different, real bug in the same neighborhood: Settings claimed "0 = disabled" for the No-file-diff-window and Text-loop-window advisory stall checks, but `stallWindowsFromManifest` (`internal/opencode/progress.go`) used the same `> 0` guard as every other field, so `0` (an operator's deliberate "disable this" AND a never-configured tenant's default zero-value) was indistinguishable from "unset" — the built-in default always applied regardless, "0 = disabled" was aspirational and had never worked. Flipping the guard to treat `0` as disabled would have been WORSE (a fresh, never-configured tenant — the default state for anyone who hasn't opened Settings — would silently lose stall detection entirely). Fix: `0`/unset still applies the built-in default (unchanged, safe); a **negative** value (e.g. `-1`, unambiguous, no schema/proto/migration change needed since the column is already a signed `int64` throughout) now actually disables the check — the resulting `<= 0` duration is already gated on by every consumer in `progress.go`'s `check()` (`> 0` guards at the trip condition, the revival check, and the poll-interval calc), so no other code needed to change. Settings copy and DOCUMENTATION.md's Recovery stall parameters section updated to describe the real (now correct) semantics. Tests (progress_test.go had none for `stallWindowsFromManifest` before this): `TestStallWindowsFromManifestUnsetAppliesDefault`, `TestStallWindowsFromManifestPositiveOverrides`, `TestStallWindowsFromManifestNegativeDisables` (the last one exercises a full `progressMonitor.check()` end-to-end, not just the resolver). Also flagged, not fixed (bigger/different question, needs a product decision): `execution_reaper.go` and `server.go` both hardcode `tnt_dev` when reading tenant settings for their background loops (reaper tuning, log rotation) rather than reading per-tenant — every tenant's reaper/log-rotation behavior is actually governed by the dev tenant's settings. Consistent across two unrelated subsystems, so plausibly a deliberate single-tenant-deployment assumption rather than a bug, but worth a real answer if multi-tenancy is meant to be live. |
| 217 | Bug fix | Adapter | Make the compaction budget gate actually protect executions instead of silently doing nothing. Settings and DOCUMENTATION.md have long described `cost_usd`/`tokens` as falling back to built-in defaults when a worker/tenant leaves them unset — but `budgetBreached` (`internal/opencode/compact.go`) treated `nil` as "no gate configured" and `tenant_settings.default_budget_overrides` ships `{}` by default, so on a fresh install (or any tenant that never touched the Execution budget defaults) compaction never fired at all: sessions could run indefinitely, resending an ever-growing cached prefix on every turn (the "millions of cumulative cache-read tokens against a ~56k working set" symptom). **Real built-in defaults, calibrated from real telemetry**: `effectiveCostBudget`/`effectiveTokenBudget` now resolve an unset field to `defaultCompactCostUSD` / `defaultCompactTokens`, while an explicit `0` still opts a worker/tenant all the way out of that dimension (mirrors `wallClockDeadline`'s existing 0-disables convention). Pulled `usage_records` for the DeepSeek executions in the local prod-container DB (`docker exec` into `orchicon-cnt-prod`'s embedded Postgres) to ground these instead of guessing — this took TWO passes. **First pass** (all 1,350 executions, ~1 month of history): median cost/execution $0.0126, p95 $0.0951, max $0.6757 → set `defaultCompactCostUSD` to $2, kept `defaultCompactTokens` at 1,000,000. **Wrong**: `cache_read_tokens`/cache-aware `cost_usd` only started being computed at all with the `20260828000000_usage_cache_tokens` migration (2026-08-22T22:04:23Z, the SAME day) — ~55k of ~58k rows in that sample predate the instrumentation and read as cost≈0/cache_read=0 not because nothing happened but because nothing was measuring it yet. The "94% of executions never touch cache" conclusion was an artifact of that gap, not a real finding. **Second pass**, corrected: filtered to the 110 executions whose entire usage_records set falls after that cutoff (~20h clean window). Cost/execution: median $0.0126 (unchanged — this one happened to be sane even in the contaminated sample), p95 $0.0335, max $0.0508 → `defaultCompactCostUSD` recalibrated to **$0.50** (~10x the clean max — more margin than the first pass used, deliberately, since 110 executions over 20h of one model is a narrower, less trustworthy sample than a full month would be). Fresh tokens: p50 54.8k (matches the ~56k "active tokens" figure that started this whole investigation), p75 74.6k, p99 168k, max 172k → `defaultCompactTokens` recalibrated to **500,000** (~3x the clean max, down from 1,000,000 which was ~6x oversized against this evidence). Both constants' doc comments in `compact.go` record this two-pass history so a future recalibration doesn't repeat the same mistake against a longer time window that dilutes the pre-instrumentation contamination again. **New turn-count gate**: a `compact_max_turns` key (built-in default 8, new `StallField` on Settings → Defaults → Execution budget) forces a compact at least every N turns regardless of cost — the lever that actually bounds *how many times* the full session gets resent, which cost/token budgets alone don't control for a session that's individually cheap per turn but runs for dozens of turns (`effectiveCompactMaxTurns`, wired into `maybeCompact` in `internal/opencode/session_run.go` as a trigger independent of `budgetBreached`, sharing the existing min-turn re-arm floor so the two triggers can't loop each other). **Raised the one-shot cap**: `defaultCompactMax` (`internal/opencode/compact.go`) goes from 1 → 10 — with compaction now expected to recur periodically over a long session (the turn gate), capping at one meant everything after the first compact went right back to resending an unbounded prefix for the rest of the execution; the min-turn re-arm floor is what actually prevents a compact/step loop, not this ceiling. `default_budget_overrides`/worker `budget_overrides` is a free-form JSON blob already generically merged tenant→worker (`mergeBudgets`, `internal/scheduler/reconciler.go`), so `compact_max_turns` required no proto/DB schema change — just the new parse key, the Settings field, and the worker-creation page's example JSON. **`tool_call_count` is now a real hard abort**, closing the last gap in the same "promised but inert" family: Settings displayed "Max tool calls per execution. Empty = built-in default (100)" but nothing in the runtime ever counted or enforced it. `execStreamState` (`internal/opencode/adapter.go`) gains a `toolUses` counter incremented on every `evtToolUse` event — the real per-tool-call granularity (a single model turn/step can make several tool calls before its `step_finish`), distinct from the `steps` counter the compaction gates use. `effectiveToolCallLimit` (`internal/opencode/compact.go`, built-in default `defaultToolCallLimit` 100, explicit `0` disables) is checked by a new `checkToolCallLimit` (`internal/opencode/session_run.go`) on every `evtToolUse` event; unlike the compaction gates this is NOT soft — there's no "compact away" a tool call already made, so a breach hard-fails the execution (`tool_call_limit_exceeded`) via the exact same finish-then-Abort ordering `onStall`'s fatal branch already uses (record the true reason before the abort echo can land). Also fixed the worker-edit page's (`frontend/src/routes/workers_.$id.tsx`) example `budget_overrides` JSON, which used field names (`max_prompt_tokens`, `max_completion_tokens`, `max_cost_usd`) that `parseBudgetSpec` has never read at all — silently a no-op for anyone who copied it — now matches the real schema (same as the worker-creation page, plus `compact_max_turns`). Settings' tool-call-limit description updated to describe the real hard-stop semantics instead of claiming it wasn't enforced. Tests: `TestEffectiveToolCallLimit`, `TestCheckToolCallLimitAbortsAtBuiltInDefault`, `TestCheckToolCallLimitDisabled`, `TestCheckToolCallLimitNilStatsNoop`. Tests: `TestBudgetBreachedBuiltInDefaults`, `TestBudgetBreachedExplicitZeroDisables`, `TestEffectiveCompactMaxTurns`, `TestMaybeCompactTurnCountTriggerWithNoBudgetBreach`, `TestMaybeCompactTurnCountGateDisabled`; `TestBudgetBreachedNoGate` (which pinned the old no-default behavior) replaced and the cost-gate/no-breach tests updated to isolate their gate under test now that an unset field is live rather than inert. DOCUMENTATION.md's Execution budget defaults section updated to match (and its dangling reference to a never-committed `architecture-notes/compact-session-on-token-budget-breach.md` removed). Known gap intentionally left out of scope: the Ask Orchicon MCP tool docstrings for `create_work_item`/`update_work_item`'s `budgets` field (`internal/askorchicon/tools.go`) show a THIRD, also-unused field-name convention (`max_steps`, `max_cost_usd`) — but that `budgets` column lives on `work_items`, not worker `budget_overrides`, and nothing in the dispatch path (`mergeBudgets`/`parseBudgetSpec`) reads it at all; it appears to be an orphaned/never-wired field, a materially bigger question than a docstring typo, left untouched. |
| 216 | Bug fix | Scheduler | Unstick the orphan-branch sweep so it actually drains the dead-ref backlog. The orphan sweep (`sweepOrphanBranches` in `internal/scheduler/worktree_reconciler.go`) reclaimed a completed run's/step-run's provably-merged branch but **never cleared `worktree_branch` on the row** — `markPruned`/`markStepPruned` set `worktree_status='pruned'` + clear the path but keep the branch, so a swept row still matches the orphan query (`status='pruned' AND branch<>''`). With the query's `ORDER BY created_at ASC LIMIT 32`, the sweep then re-selected the SAME oldest 32 rows every scan — whose branches were already gone (idempotent no-op) — and permanently starved newer orphans. Observed state: 132 orphan step-run rows, 116 branches already swept, 16 newer branches never visited. Fix: after a successful `deleteBranch` (or a confirmed-already-gone branch — distinguished post-call via `branchExists` so a deliberately-skipped unmerged/protected/attached branch is NOT cleared), the sweep now clears the row's `worktree_branch` (`clearSweptRunBranch`/`clearSweptStepBranch`), so the query stops returning the row and the sweep walks forward one page per scan. Skipped branches (not provably merged) keep their row and are retried on a later scan — success-only semantics preserved. Extended `TestWorktreeSweepOrphanBranchOnCompletedRun` to assert the swept row's branch is cleared AND the orphan query no longer returns it. |
| 215 | Feature | Adapter | Enable opencode context-compaction pruning on every serve. `BuildConfigContent` (`internal/opencode/config.go`) now emits `"compaction": {"prune": true}` in the `OPENCODE_CONFIG_CONTENT` for BOTH the host serve and the runtime-container serve (the SDLC runs), so every worker session trims **old tool outputs** (accumulating `read`/`grep`/`bash` results) from the conversation as its window fills. Capability-safe by design: `prune` removes stale tool **RESULTS** only — it never disables/removes tool **definitions**, never collapses the conversation to a lossy summary (`compaction.auto` is left at opencode's default and NOT enabled), and the worker's working instructions/decisions survive. This attacks the observed high per-turn context (a ~52k working set across the SDLC runs) at its largest driver — the re-sent tool-output accumulation — without the quality cliff of aggressive lossy compaction. New `TestBuildConfigContentCompactionPruneEnabled` pins `prune:true` (and `auto` *not* set) on both serve paths. |
| 214 | Feature | Prompt engine | Turn-discipline contract in the shared stable prompt prefix: a new `## Step output — deliver the decision and the delta, not the journey` block (`db.stepOutputBlock`, wired into `StablePromptPrefix` in `internal/db/prompt.go`, so every worker and dispatch gets it) targets **turns-to-success** rather than per-output size — each tool call re-sends the whole accumulated conversation, so the NUMBER of turns dominates cost more than any single output (complements the existing `## Efficiency` batching block, which alone was observed insufficient: an architect step still made ~10 sequential single-file verify reads). Three directives: **(1) Gather context in one pass** — batch ALL reads/probes into a single tool round-trip before acting (`bash` chained with `&&`, one `read` over several paths, `grep`/`glob` over sequential reads); re-read only on a real failure. **(2) Deliver the decision + delta, not the verification** — design/review/decision steps output the ADR (Context → Decision → Consequences) and the exact file(s)/function(s) to change and why, not the read-by-read narrative that produced it; downstream steps inherit facts, not process. **(3) Verify once, then stop** — establish each fact once from a real command, never re-derive a prior step's / `facts_learned` record, and stop when acceptance criteria are met (no gold-plating / re-verification). New `TestCompositePromptStepOutputDiscipline` pins all three directives reach the workflow composite prompt. This is the prompt-contract half of a turns-to-success reduction; in-conversation tool-output summarization remains opencode-runtime territory (Orchicon only drives the session via `prompt_async` + SSE and does not mediate the conversation loop). |
| 213 | Bug fix | Runtime/UI | Make worker runtimes `go test`-ready and execution-token numbers truthful. **(A) `/tmp` is exec-capable**: the runtime container's tmpfs was mounted under Docker's `--tmpfs` **noexec default** (`internal/runtime/daemon.go`), so Go test binaries placed in TMPDIR failed `fork/exec ... permission denied` and the execution-guard shim under `/tmp/orchicon-guard-*` couldn't exec — spurring the "relocate GOTMPDIR → retry → clean" dance and stale `noexec` facts. The mount is now `/tmp:rw,size=<size>,exec` (no host-escaping change: the runtime user already has write+exec across the chowned ephemeral rootfs; noexec was incidental Docker defaulting). AGENTS.worker.md's environment baseline switched from "`/tmp` is noexec — relocate GOTMPDIR" to "`/tmp` is exec-capable; keep GOCACHE/GOTMPDIR in-worktree for tidiness only", and `RuntimeEnvironmentBlock` now states the exec-capable ground truth (so workers trust this over an older run's stale "noexec" fact). **(b) Usage cards go live**: `useGetUsage` scoped its TanStack query key to `executionId`/`taskId` (was `projectId`-only, so every execution collided on one cache entry) and the execution-detail stream now invalidates the usage query on each event — the Context card + Messages token bars refresh live instead of only on manual refresh. **(c) Peak-vs-cumulative honesty in the Messages card**: the token bars now show the **peak row's composition** (buckets of the largest single-step total — the same 54k the Context card shows) instead of cumulative sums that dwarf the working set (the "743k cache read sat next to 54k context" confusion); cumulative/cache-hit figures are relabelled and moved to a `Cumulative (transport)` footer. |
| 212 | Feature | Context | Context-by-reference wave: manifest handoff + delta execution history + on-disk per-step archive + run-context tool. **(A) Manifest renderer** (`contextfiles.RenderManifest`): project/work-item context is now a compact path+size manifest instead of inline-everything; small core files (<= `ManifestInlineMaxBytes`, 4KiB) are still inlined so a worker is never blind, larger files/dirs are listed with "read this on demand". Agnostic by design (inspects real paths/sizes via Stat/WalkDir); used by both composite builders. **(B) Delta step handoff** (`buildCompositePrompt`): Execution history re-embeds FULL detail only for the step's **direct upstreams** (DependsOn); every other completed step becomes a compact `✓✗ step [iteration] — <truncated summary>` index line + pointer to `.orchicon/<run>/steps/<stepId>.md`. Quadratically-growing prior-step summaries are gone; AC/instructions unchanged (D guardrail). **(C) Per-step archive**: `writeOrchiconFiles` writes a stable, never-overwritten `.orchicon/<run>/steps/<stepId>.md` (status/worker/summary/issues/touched) so any earlier step can be read/grepped from disk. **(D) `orchicon run-context <runID>`**: new CLI prints the run's `.orchicon` archive (`--grep`/`--steps`/`--summary`/`--limit`) — the one "grep the archive when you need older context" tool. **(E) Context budget rail**: execution sidebar now shows Cache-read / Cache-write / Reasoning splits AND the **peak-vs-cumulative fix** — the Context card + its % bar now key off the **max single-step total_tokens** (the model's real working set, bounded) instead of the cumulative re-send sum; cumulative "input consumed" is surfaced separately and explicitly labelled. |
| 211 | Feature | Context | Context-efficiency + sandbox-alignment pass (drives across 6 workflow runs to ~45k-72k context-per-call / ~99M tokens total down). **(1) Unblock run metadata**: `external_directory` now also allows the Orchicon-own `.orchicon/**` subtree so workers in isolated worktrees can read the `.orchicon/<run>/` summary/facts/issues the composite prompts them to read — the blocked-outside-worktree deny was burning a full tool call + retry per read. **(2) Scratch alignment**: the execution guard now allows `rm/mv/cp/chmod/chown/ln` inside `/tmp/orchicon` (the documented scratch dir), and the prompt's Efficiency block tells workers to prefer scratch/project paths instead of fighting a blocked path. **(3) Subagent off**: `permission.task` is denied (`taskToolDeny`) so opencode can't spawn subagents (each re-prepends its system prompt + re-carries parent history ≈ 2× context); prompt explicitly forbids delegation. **(4) Tool-output cap**: `capToolOutput` truncates a tool's output to `ORCHICON_MAX_TOOL_OUTPUT_BYTES` (default 128 KiB) before it reaches `OnToolCall`/UI. **(5) Context-inline caps**: `MaxInlineFileBytes` 256→64 KiB + cumulative `MaxInlineContextBytes` 384 KiB. **(6) KV-prefix-verified**: `StablePromptPrefix` byte-identical across a run's steps. Tests: permission asserts, `TestCapToolOutput`, `TestRenderCumulativeBudget`. |
| 210 | Bug fix | Scheduler | Reclaim orphaned local branch refs left by the worktree prune path. The prune pass only deletes a branch at the moment it reaps a **'ready'** worktree for a **completed** run (`pruneOne`/`pruneStepRunOne` early-return on `worktree_status != 'ready'`), so a branch that survived that instant — a run that completed after its worktree was taken down by an earlier pass, or a superseded/parallel step run pruned-early while a later iteration carried the merge — was never revisited and leaked as a dead `refs/heads/*` (observed: ~33 local refs, all merged into develop, on the dev repo). Fix: the WorktreeReconciler scan now ends with an **orphan-branch sweep** (`sweepOrphanBranches`) that lists terminal **completed** runs (and step runs of completed runs) whose `worktree_status='pruned'` but still record `worktree_branch`, and re-runs the existing proof-deletion gate (`deleteBranch` → `branchProvablyMerged`) for any that still exist locally, has no live worktree, and is provably merged. Success-only is preserved: a failed/aborted run's branch is never swept (a retry re-attaches to it). New DB queries `ListTerminalRunsWithPrunedBranches` / `ListTerminalStepRunsWithPrunedBranches`; new integration tests `TestWorktreeSweepOrphanBranchOnCompletedRun` (merged orphan reaped on scan) + `TestWorktreeSweepSkipsFailedRunBranch` (success-only guard). DOCUMENTATION.md + UPDATES.md updated. |
| 209 | Bug fix | Settings | Session TTL validator now honors the documented contract. `validateSessionTTLs` (`internal/settings/service.go`) rejected a refresh TTL at exactly the documented minimum (300s) whenever access TTL exceeded it — e.g. `(access=900, refresh=300)` returned "refresh token TTL must exceed access token TTL", contradicting the doc that refresh's range is independently `[300, 31536000]`. A refresh at the floor is a valid "use the minimum refresh lifetime" choice; only a refresh actively chosen **above** the floor must exceed access. Fix: enforce `refresh > access` only when `refresh > minSessionRefreshTokenTTLSeconds`. Tests pinned `(900,300)` passes, `(300,300)` passes (both at floor), and the existing `(900,900)` equal-and-below rejections still hold. |
| 208 | Bug fix | Scheduler | Survive opencode session-backend failures so a workflow run self-heals instead of wedging. **Dispatch self-heal** (`internal/opencode/adapter.go`): the session-setup loop distinguishes *infra* failures — serve process died (`connection refused`) or alive-but-poisoned (`CreateSession POST /session` HTTP 5xx, e.g. a store that errored `Failed to execute statement`) — from worker failures, and on an infra failure recycles the run's runtime container (bounded by `ORCHICON_SESSION_REPAIR_ATTEMPTS`, default 3), then re-dispatches on the freshly-built serve (the create handshake is the health gate; the on-disk worktree survives, so the work continues). Recycle, not in-place restart: the container's stable XDG data dir carries the poisoned store, so only a fresh container discards it — the fix for the two 2026-08-22/23 SDLC incidents (run `01M0NSHWJYWSDHGP12676YAN8X` refused; `01M0P08V7D8AY7KT8AEJ924DKR` 500s after a store error) that burned every retry through the same hole and left the run "running" for 45+ minutes with no Retry button. **Recovering-step never-limbo** (`internal/scheduler/workflow_reconciler.go`): a recovering step whose recovery-dispatch gate dead-ends (no recovery row ever, or a terminal `resumed` recovery whose seed never resolves) is now failed terminal after `ORCHICON_RECOVERING_STALL_TIMEOUT` (default 15m), so the run finalizes FAILED and Retry surfaces. Attempt budget + in-flight recovery holds untouched; `<1s` disables. Tests: `isInfraSessionError`/budget unit tests + DB-backed recurrence-stall gate tests (young-run holds, aged-run fails, disabled preserves, legacy poll). DOCUMENTATION.md (§Persistent Worker Sessions) + UPDATES.md updated. |
| 207 | Feature | UI | Rewrite the Settings page **User Guide** tab from a stale entity one-liner list into a real user guide: the big idea (orchestration vs execution), the typical end-to-end flow (project → worker → work item → workflow → run → watch/verify), scheduling/sequences, dependency & blocked status, failure/recovery + nudge-first stalls, and governance/visibility (policies, RBAC roles, audit, telemetry). A second "Settings explained" card walks each tab — Appearance, Defaults (models, stall params incl. nudges, budget defaults, liveness reaper, dispatch concurrency, log management), Session (token TTLs), Backups (cron/retention/directory + snapshots) — describing what each setting controls. High-level by design; points to DOCUMENTATION.md for detail. |
| 206 | Docs | Docs | Release-prep doc refresh for the current cycle. **AGENTS.developer.md** "Things you need to know" gains RBAC roles (identities bind roles carrying `entitlements`, immutable `admin` bypass, Roles CRUD + Manage Identity Roles in Admin), work-item/project archive + restore (terminal + childless, hidden from all views), and PR-link capture from the worker's `PR_URL:`/`PR_STATE:` summary lines with idempotent `orchicon backfill-pr` backfill onto completed runs. **AGENTS.worker.md** gains a tiny "Platform changes: keep Ask Orchicon in sync" rule (update `internal/askorchicon/tools.go` when adding/changing an RPC or capability) and stays otherwise minimal. **DOCUMENTATION.md** adds the `orchicon backfill-pr` command, work-item archive/restore, the RBAC roles & entitlements surface, a worktree/branch/PR-capture section (retry branch reuse + success-only branch deletion + PR chip), and the nudge-first, result-aware stall-detection model. **site/index.html** bumps the Cloudflare landing page's Latest-release bar + footer to v0.1.293. **README.md** "Last Release Changes" is replaced with the v0.1.293 concise summary (release notes this cycle use the summary, not the long UPDATES list). |
| 205 | Feature | Auth | First-admin via local IdP sign-up (replace the default `admin`/`admin` bootstrap). **Bootstrap is now purely opt-in**: `BootstrapLocalAdmin` (`internal/auth/bootstrap.go`) mints a credential ONLY when the operator pins BOTH `ORCHICON_LOCAL_ADMIN_USERNAME` AND `ORCHICON_LOCAL_ADMIN_PASSWORD`; either unset → a no-op (no default credential, no admin role, no generated password). The `localAdminDefaultUsername`/`localAdminDefaultPassword` constants and the `forceChange` path are deleted (a pinned password is never flagged); `ORCHICON_LOCAL_ADMIN_RESET=1` lockout recovery stays but also requires both envs pinned (no default fallback). **First-signup admin grant**: `POST /auth/signup` → `createLocalAccount` now grants the tenant admin role to the newly created identity when the tenant has no admin binding yet — inside the same tx as account creation, via the existing `ensureAdminRole`+`bindAdminRole` helpers. **Unified "first admin" semantics**: a new shared `grantFirstAdminRole` helper (tenant-scoped `pg_advisory_xact_lock`, mirroring the DAG-cycle trigger) is the single admin-provisioning path used by BOTH the local first sign-up and the external-OIDC first login (`oidcCallback` dropped its `created=true` gate) — exactly one tenant admin can exist regardless of which IdP ran first, whichever runs first wins. **Concurrency**: two simultaneous first sign-ups on a fresh plane produce exactly one admin (advisory-lock serialized). A fresh plane is bootstrapped by the operator creating their own admin via the Sign up link on first load. Tests reworked/added: `bootstrap_test.go` (no-env/partial-pin no-op, opt-in seed, reset-requires-pin), `signup_test.go` (first→admin, second→user, concurrent-first→exactly one admin), new `first_admin_test.go` (unified guard local-then-OIDC and OIDC-then-local). DOCUMENTATION.md (Troubleshooting → Admin Access & Lockout Recovery + local-dev bullet), `.env.example`, and UPDATES.md updated; no `admin`/`admin` default-credential references remain. |
| 204 | Bug fix | Schedules | Fix the Schedules History view missing runs + wrong ordering. History is now **run-driven** instead of derived from work-item status. Backend: `ListWorkflowRuns` gained optional `project_id`/`work_item_id` scope filters + `sort_by`/`sort_order` (empty `workflow_id` = all tenant runs, was previously rejected); `db.ListWorkflowRuns` filters by project/work item and sorts by the run's real `started_at` (`NULLS LAST`, cursor-ignoring for the started_at path); Ask Orchicon `list_workflow_runs` exposes the new filters. Proto regenerated (Go + Connect-ES). Frontend: HistoryView now fetches runs via `useListWorkflowRuns` (project-scoped, sorted by `started_at`, descending), resolves each run's bound work item + workflow, renders **one card per run** (`key=run.id`) with `RunStatusBadge`, and sorts by the run's actual `started_at` — so recurring fires (whose item re-armed to scheduled/recurring), prior runs of a multi-run work item, and in-flight runs all appear (previously only the latest terminal item showed, and a 2am-scheduled run floated above later runs because ordering used the item's next/future scheduled time). New pure model predicates `isHistoryRun` (has a real `started_at`) + `historyRunRanAt` (real start time) replace `isHistoryItem`/`TERMINAL_STATUSES`; `useListWorkflowRuns` converted to an options object (one caller updated). Upcoming/Running views unchanged. Unit tests updated for membership + ordering. DOCUMENTATION.md §Viewing Schedules updated. |
| 203 | Bug fix | Ask Orchicon | Fix the Stop button flicker + conversation scroll bounce after a turn completes while you stay on the screen. The restore effect (which re-attaches a stream slot when the server reports a running turn) read the conversations poll's cached `turn_in_flight` flag, which can lag the persisted reply by up to one poll cycle — the server removes the turn registry entry BEFORE writing the reply row. So after the completion effect cleared the slot (the acked reply appeared via the messages poll), the restore effect re-armed it from the stale flag, the completion effect cleared it again, and the two ping-ponged: the Stop button flickered (composer + sidebar) and every toggle re-stuck the scroll, bouncing the whole conversation. Fix: the restore effect now declines to re-arm a turn whose acked assistant message id is already present in the persisted messages — that id only ever exists once the reply is actually written (the assistant row is created at persist time, never as a placeholder at ack time), so the check exactly separates "genuinely still running" (no persisted row yet, re-arm correctly) from "done but stale flag" (row exists, skip). |
| 202 | Test | Seed data | Fix stale `TestArchitectSeedWorkflowsJSON`: it still asserted the two UI Architect workflows (`01KZA9H...`/`01KZA9M2...`, ref `w_se_architect_vision`) as canned and a 9-step shape with `edge_handles` on the entry step — neither matches the current seed, where those workflows were retired and the SDLC Architect templates have 10 steps with no stored `edge_handles` (derived from `depends_on` by the editor at load time). Updated to the current canned workflows (`w_se_principal_architect`), 10 steps, no `edge_handles` assertion; the retired UI Architect workflows remain covered by the `SeedDevWorkflows` retire pass. This unblocks `make ci` (previously failing in `internal/db`). |
| 201 | Bug fix | Ask Orchicon | Fix the Stop button flickering (disappearing/reappearing, unclickable) for the entire duration of a streaming reply. The completion effect (which clears the stream slot when the acked reply message appears) depended on `streams`, and `streams` changes on every streaming chunk — so while the model was responding the effect re-ran on every chunk, cleared the slot (`isStreaming` false), and the restore effect (also watching `streams`, seeing the server still `turn_in_flight`) re-armed it (`isStreaming` true). That continuous clear/re-arm oscillation flickered the Stop button (composer + sidebar) and never landed a click. Fix: the completion effect no longer depends on `streams` (removed from deps — it only needs `pendingReplyId`/`messages`/`activeConvId`), a new `liveStreamRef` guard prevents it from finalizing a slot while a local `runStream` is actively iterating (it finalizes only after the local stream ends, or for a restored server turn with no local stream), and the sent text is read from a `sentTextRef` instead of the stream slot so reply-failure copy still works. |
| 200 | Bug fix | Ask Orchicon | Simplify draft handling to "clear on send, restore only on failure". Replaced #330's sessionStorage draft-persistence (save-as-you-type, restore on conversation switch, clear-on-ack) with the straightforward mental model: the composer clears immediately when you send, and the text is put back in the box (and copied to the clipboard) only when the send actually fails — pre-ack failure (via `onSend === false`) or an acked turn whose reply later errored (the parent signals a `restoreDraft` prop with a changing token). No draft lingers across conversations or while a reply is in flight, and a failed send never forces retyping. sessionStorage draft persistence removed entirely. |
| 199 | Feature | Dev loop | Make `container-rebuild` a trustworthy one-command "rebuild dev/prod and test recent changes" flow. Previously `make container-rebuild instance=dev|prod` inherited the stamp-checked `fe-build` (only rebuilds the frontend when `frontend/src` is newer than `frontend/dist`), so a rebuild from a working tree older than the last build could **silently ship a stale frontend** — the exact cause of frontend fixes "going invisible after a rebuild" (the draft-preservation fix #330 not appearing). `container-rebuild` now always forces a fresh frontend build via a new `force-fe` Make variable (`force-fe=1` bypasses the stamp check in `fe-build`); plain `make build` keeps the fast stamp-checked Go-only loop (`make build force-fe=1` forces). DOCUMENTATION.md §Local Development Loop updated to document the dev/prod one-command flow. |
| 198 | Bug fix | UI | Ask Orchicon Stop survives a refresh / another tab or device. The server's in-memory turn registry now carries each running turn's acked assistant message id, and `Conversation` gains two computed read-time fields — `turn_in_flight` + `pending_assistant_message_id` — populated on `GetConversation`/`ListConversations` from the same authoritative source as the one-turn gate. The frontend reconciles its volatile stream slot against that state: when the server reports a running turn for the active conversation but the local slot is idle (refresh, or a turn started in another tab), it re-attaches the slot keyed to the server's pending id so the Stop button, the thinking indicator, and the existing 2s `ListMessages` completion poll all come back — no more "a reply is still in progress… stop it first" dead-end with no way to stop it. The conversations sidebar shows a pulsing dot + an always-visible Stop button on every running conversation, and the conversation list polls on a ~3s cadence while any conversation is running (idle = no polling), so running state stays live across tabs/devices and settles naturally once everything completes. A live local slot is never overwritten (server state only fills gaps). New DB-backed `TestConversationSurfacesRunningTurn` pins idle→running→released surfacing on both read paths; `TestTurnRegistry` pins the registry `get()` (running turn + pending id). Proto regenerated via `make gen`. DOCUMENTATION.md §Ask Orchicon updated. |
| 197 | Chore | Seed data | Remove the three demo/test canned workflow templates from the dev-tenant seeder and purge them from the prod instance: the Conflict-Aware Architect (`01KZB0CONFLICT000000000001`), `DevOps Per-Branch Lifecycle (Non-human)` (`wf_devops_per_branch`), and `DevOps Per-Branch Lifecycle (Non-repo, No-git)` (`wf_devops_per_branch_nogit`) entries are deleted from `internal/db/seed_workflows.go`. The engine features they exercised (conflict-value/conflict-chain gates, lazy Integrator chain, `w_se_integrator` worker) are untouched and remain available via the workflow editor. The workflow seeder gains a retire pass mirroring `retiredCannedWorkers`: `retiredCannedWorkflows` hard-deletes a removed template on boot only while it is still seed-managed (its current version id matches the original seed VersionID — a user-forked workflow is left untouched), so any instance that already carries the templates cleans itself on the next deploy. Seed-only tests removed (`devops_per_branch_seed_test.go`, `TestConflictAwareSeedWorkflowJSON`); new DB-backed `TestSeedRetiresRetiredWorkflows` pins the retire/preserve semantics. The three workflows (plus versions and an edit lock; 0 runs, 0 bound work items) were also hard-deleted from the prod `tnt_dev` database. |
| 196 | Feature | UI | Parallel board view: run/worktree/PR state surfaced on the board & list. Runs become visible on `WorkItemCard` via a new compact run footer (`RunFooter`/`PrLinkChip`) that shows each active run's branch (reusing the F1 worktree vocabulary) plus a PR link chip and a "+N concurrent" indicator when multiple runs are active — the board polls executions (joined on `taskId`/`workflowRunId`, MVP path, no new RPC) on the same 5s rhythm as the DAG so concurrent dispatches stay fresh; the tree rows get a compact branch + PR indicator and the F2 `BlockedChip` rendering is untouched. Every provisioned git-backed run links out to its PR: the per-branch DevOps worker authors `pr_url`/`pr_state` into the run's structured `run_context` JSONB, parsed server-side into new additive `WorkflowRun.pr_url/pr_state` (proto 19/20) at `runRowToProto` and mirrored to `WorkerExecution.pr_url/pr_state` (proto 29/30) at dispatch (same seam as the worktree columns, `internal/scheduler/reconciler.go`; new `db.PrFromRunContext`); when `pr_url` is empty the UI falls back to a deterministic `https://github.com/{owner}/{repo}/pull/new/{branch}` link from the project's cached git origin — new `projects.repo_slug` column captured at git-detection time in `WorktreeReconciler` (new `parseRepoSlug`/`detectRepoSlug`, provider-free) and exposed on the Project proto. PR links also added to the run-detail view (next to `WorktreeTiles`), the executions list rows, and the execution-detail footer. All theme-aware via `useDarkPalette`/muted chips. Two forward-only migrations (`projects.repo_slug`, `worker_executions.pr_url/pr_state`), schema.hcl + atlas.sum rehashed, proto regenerated via `make gen`. New tests: `parseRepoSlug` (all remote URL forms), `db.PrFromRunContext`, and frontend `prLinkForRun`/`prStateLabel`. |
| 195 | Feature | Scheduler | Blocked/waiting state surfaced in the reconciler + UI. A new persistent, system-managed work-item status `WORK_ITEM_STATUS_BLOCKED = 12` (`blocked`) replaces the silent park at both dependency-gate sites: the TaskReconciler flips a ready standalone task with unsatisfied deps `ready` → `blocked` (committed, never a silent requeue) and the sequence engine flips an on-deck child `pending` → `blocked` in `reconcileParent`. Clearing is automatic on the next scan — the standalone pass now reconciles `blocked` tasks (`db.ListBlockedTasks`, mirroring `ListReadyTasks`) and flips `blocked` → `ready` (dispatching in the same pass) once the gate satisfies; the sequence arming loop clears `blocked` → `pending` → arms. No dispatch ever proceeds from `blocked` (dispatch only starts from `ready`/`pending` past a satisfied gate). The server names the blocking edges on every read via `repeated WorkItemBlocker blocked_by = 32` (id/title/status), computed at read time (`db.ListUnsatisfiedDependencies`, predicate identical to `CheckDependenciesSatisfied` — only `succeeded` unblocks, so a failed/cancelled blocker leaves the dependent blocked and visible) and attached on Get/List/Update/Graph/Assign/Unassign/Reorder/ControlSequence responses. UI follows the status-meta pattern: distinct teal pill, `Blocked` filter option, maps to the Pending board column, `MANUALLY_UNMOVABLE_STATUSES` (system-managed, no drag-in), and the `BlockedChip` now reads the server's `item.blockedBy` when status is blocked (tooltip "Blocked by: …") with the detail page rendering a blockers list; the client-side advisory `computeBlockState` stays for the drag-drop gate. Status mapping round-trips (`statusToProto`/`validateStatus`); user input (`ValidateStatus`/board drops) rejects `blocked`. `blocked` routes to `pending` through reset/stop/resume like any non-in-flight status. Proto regenerated via `make gen`; DB `work_items.status` is free text — no migration. New DB-backed tests: standalone ready↔blocked park/clear/dispatch/no-dispatch/scan + workflow-bound skip, sequence park/clear/arm + failed-blocker stays blocked, stop/resume/refire reset of blocked children, kind-switch leaves blocked untouched; frontend meta tests pin the teal pill/column/filter/unmovable. DOCUMENTATION.md (status lifecycle, dependency gate, board) + UPDATES.md updated. |
| 194 | Feature | Dependency graph | Cycle detection + validation enforced on the work-item dependency DAG. The DAG invariant now lives at the persistence boundary: new forward-only migration `db/migrations/20260821000000_enforce_work_dag_acyclic.sql` adds the codebase's first trigger (`enforce_work_dag_acyclic`, `BEFORE INSERT OR UPDATE` on `work_item_dependencies`) — it rejects a self-loop (`from_id = to_id`) for every edge type (including `relates_to`), exempts `relates_to` from the reachability walk (a symmetric, non-ordering relationship), and uses a `WITH RECURSIVE` reachability check over `depends_on`/`blocks` edges to reject any direct or multi-hop cycle (`RAISE EXCEPTION` aborts the transaction, so invalid graphs never persist — covering the API RPCs, any future bulk-import path, and raw SQL). The trigger takes a per-project transactional advisory lock (`pg_advisory_xact_lock` on the tenant+project pair) before the walk, closing the concurrent-write TOCTOU race where two simultaneous inserts `A→B` + `B→A` could each walk an empty (uncommitted) graph and both commit a live cycle — the second writer now blocks until the first commits, re-walks, and is rejected. `db.CheckCycleWithRecursiveCTE` now filters the walk to `depends_on`/`blocks` only; `AddDependency` gates the app-layer cycle check on edge type (skipping it entirely for `relates_to` — with an existing `A depends_on B`, `B relates_to A` is no longer falsely rejected), both `AddDependency` and `replaceDependsOn` name the offending edge in the `FailedPrecondition` (`cannot add dependency X -> Y: would create a cycle in the work DAG`), and `mapDBError` maps the trigger's cycle rejection to `FailedPrecondition` when the concurrent serialization path rejects an app write. New DB-backed tests cover direct/multi-hop cycles (add + set-replace), `blocks` cycles, symmetric + mixed-case `relates_to` exemption, the create-path invariant, a trigger backstop test that raw-SQL cyclic/self-loop inserts are rejected and never persist while valid bulk inserts (incl. `relates_to`) succeed, and a deterministic concurrent-insert regression test (two transactions inserting `A→B`/`B→A`, barrier-verified on the advisory lock) proving the race is closed. `make migrate-hash` rehashed. DOCUMENTATION.md §Creating Work Items updated. |
| 193 | Feature | Auth | Local auth UX consolidation — one coherent local-login story. **Dev-login deleted end-to-end**: `ORCHICON_DEV_LOGIN`/`DevLoginAllowed` (config), `POST /auth/dev-login` + `devLoginRequest` + `ensureDevAdminBinding` + the `dev_login` capability flag (handlers), the `/auth/dev-login` public path (middleware), `startDevLogin`/`devLogin`/`AuthConfig.dev_login` (frontend), the login SPA's dev section, the container `AUTH_ENV` lines (scripts/container.sh), `devlogin_test.go`, and the dev-login tests in `tenant_mapping_test.go`/`config_test.go` — all gone; the OIDC callback's first-login admin grant now routes through the bootstrap helpers `ensureAdminRole`+`bindAdminRole`. **`ORCHICON_LOCAL_ADMIN_SEED` removed everywhere** (bootstrap guard, container.sh, .env.example, DOCUMENTATION.md): every local-mode plane (dev AND prod volumes) seeds `admin`/`admin` with `force_password_change=true`; a pinned `ORCHICON_LOCAL_ADMIN_PASSWORD` is used as-is with no forced change. **Don't-clobber refined to option B**: the `tenantAdminExists` skip is replaced by a credential-gap probe — an admin binding whose identity has no local credential (a dev-login-era volume) gets a credential provisioned for that EXISTING identity (identity + role binding preserved, `admin`/`admin` works after upgrade with no reset env); skip only when a credential already exists. **`Identity` proto gains `username = 10`** via a hash-free LEFT JOIN on `local_credentials` in `GetIdentity`/`ListIdentities` (never the hash; documented narrow credential-boundary exception). **Password management folded into identity edit**: the Admin Identities tab renders a Username column and the row-edit modal prefills the username + a new-password field wired to `SetLocalCredential`; the standalone "Set local password" block is deleted. `SetLocalCredential` stays `auth:write`-gated (admin-only; no self-service password change for non-admins). `.env.example`/DOCUMENTATION.md/UPDATES.md updated. New bootstrap test pins the credential-gap upgrade path; the no-clobber test flipped to the has-credential case. |
| 192 | Feature | Prompt engine | Guarantee every worker is told to work off a branch of `develop`: new shared `gitDisciplineBlock` in the stable prompt prefix (`db.StablePromptPrefix`, `internal/db/prompt.go`) — "work on a branch created off `develop`; never commit to/push to/PR into `main` or `develop` directly; use the workflow's branch if one exists" — so the branch rule can no longer be missing from a dispatch, even for custom workers whose own AGENTS.md carries no git content (previously it lived only in the canned workers' per-worker agents_md and the on-demand AGENTS.worker.md). The block joins identity/safety/efficiency/runtime-env in the byte-identical shared prefix, preserving the KV-cache reuse the prefix exists for. New `TestCompositePromptGitDisciplineForBareWorker` pins the rule in both the workflow composite and the standalone composite for a bare worker. |
| 191 | Docs | Prompt engine | Split the repo `AGENTS.md` into a thin router plus on-demand `AGENTS.worker.md` / `AGENTS.developer.md` so the 55 KB auto-loaded file (~14k tokens, the largest single contributor to the ~37.5k first-turn prompt on a DevOps run) is no longer injected into every opencode session. `AGENTS.md` now points Orchicon workers and human-facing/developer sessions to their own file (workers' instructions already ride their system prompt; the router adds ~150 tokens vs ~14k before). `AGENTS.worker.md` condenses the worker-only rules (branch discipline, token discipline, facts ledger, `.orchicon/` reads, verify-don't-assume, environment baseline, summary contract) and drops content the composite prompt already carries. `AGENTS.developer.md` holds the full dev workflow (git rules, security standards, verification, dev control, install/release, invariants, Ask Orchicon sync, UPDATES.md) read on demand by non-worker sessions. |
| 190 | Feature | Prompt engine | Reduce worker prompt/token overhead for local-model runs: (1) composite prompts now open with a byte-identical **stable prompt prefix** (`db.StablePromptPrefix`, `internal/db/prompt.go`) built only from shared constants — worker identity + safety rules + new efficiency directives + runtime environment — so llama.cpp's prefix-based KV cache is reused across all steps and roles of a run; role/task/history content moved AFTER the prefix. (2) New shared `## Efficiency` block gives every worker explicit **tool-output discipline** (compact `git status --short` / `git log --oneline -5` / single-line `gh` queries; grep `.orchicon/<run>/touched_files` instead of reading every file) and **tool-call batching** (multi-file reads, combined bash calls, grep/glob over sequential reads) with the guardrail that compact commands are still real commands — the "Verify, don't assume" contract survives. (3) Facts dedup: the embedded `## Facts learned (this run)` prompt block is removed — `.orchicon/<run>/facts_learned` is now the single source, with per-step attribution preserved via `FACTS LEARNED (from <step-name>): <fact>` (step name from the owning step run). (4) Summary cap: soft "under ~500 tokens" instruction in the summary contract + hard `capSummaryNarrative` guard (500 tokens, ~4 chars/token) that truncates narrative only, preserving the `ORCHICON WORKER SUMMARY:` routing word, all `FACTS LEARNED:` lines, and the `_issues` fallback substance; `extractFactsLearned` remains the single extraction path. (5) Safety rules moved from the canned workers' stored AGENTS.md into the shared prefix (seed marker bumped `orchicon.safety=v16`, `seedAgentsMD` strips them at persistence so nothing is duplicated per worker); compact git recipes baked into `gitBranchBlock`/`gitAwarenessBlock` for discovery-heavy workers. Standalone (non-workflow) dispatches get the same prefix. DOCUMENTATION.md updated. New tests: shared-prefix ≥1k tokens across two workers, efficiency+batching directives present, embedded facts block absent, cap preserves routing+facts, facts file step attribution (DB-backed). |
| 189 | Bug fix | Sequence engine | Fix "Resume button on a sequence parent does not resume the sequence": `scheduler.ResumeSequence` reused `reconcileParent` unchanged, so resuming a halted chain (parent failed, first non-succeeded child failed/cancelled) re-entered the engine at its halt point — the failed-child branch re-marked the parent failed in the same transaction, making manual RESUME a no-op. Resume now resets the first non-succeeded child to `pending` (when failed/cancelled) before the parent→running + `reconcileParent` call — byte-for-byte the same input change the manual "set failed child to pending" produces, so parent-level Resume ≡ child-level pending by construction; only the first child is reset (multi-failure chains stay parked per sibling), container children re-arm their subtree through the existing arm path, scan/auto-revive semantics untouched. Also removed the Start/Resume/Stop buttons from the work-item detail page (kept on the overview board + tree) per acceptance criteria. New DB-backed engine tests: halted-chain resume re-arms the failed child (re-run, not skipped), cancelled-child parity, container-child subtree re-arm, and a handler-level RPC wiring case for a failed-parent RESUME. DOCUMENTATION.md §Sequence runs Manual-control bullet updated (pre-existing "No manual 'resume sequence' action exists" drift cleaned). |
| 188 | Feature | Audit | Admin audit view filters (acceptance criterion #2 of the admin audit work item): `AuthService.ListAuditEvents` + `db.ListAuditEvents` gained an optional `start_time` (inclusive lower) / `end_time` (exclusive upper) window on `occurred_at` — absent bounds unbounded via the shared `'epoch'` sentinel (same convention as `usage.go`), keyset pagination still composes (cursor is unambiguous within the window). The Admin → Audit **Events** tab (`AuditEventsView` in `frontend/src/routes/admin.tsx`) now has a filter bar: action / actor / target-type / target-id exact filters plus from-to `datetime-local` inputs (browser-local values converted to UTC before they reach the RPC, since `occurred_at` is stored/compared in UTC — per the design-approver fact), Apply/Clear buttons, and the query is keyed on the filters so each combination refetches independently (`authKeys.auditEvents(filters)`). Ask Orchicon's `list_audit_events` tool mirrors the window with RFC3339 `start_time`/`end_time` strings. The policy-decision projection (`AuditDecisionsView` / `ListAuditEntries`) is untouched (criterion #3). New DB-backed `TestAuditServiceTimeRangeFilter` pins inclusive-start/exclusive-end/unbounded semantics; proto regenerated via `make gen`. DOCUMENTATION.md §Audit trail updated. |
| 187 | Bug fix | Audit | Close the transactional-outbox atomicity gap found in review: 11 RPCs previously wrote their audit row in a SEPARATE short tx after the mutation committed (a committed mutation could lack a row when the audit INSERT failed). The recovery engine now records `recovery.triggered`/`recovery.continuation_plan_approved`/`recovery.continuation_plan_rejected`/`recovery.task_marked_succeeded` inside its OWN transaction — `TriggerOnFailure`/`MarkTaskSucceeded`/`ApproveContinuationPlan`/`RejectContinuationPlan` take an `*audit.Entry` (the scheduler reconciler passes nil: system churn, D8); `recordAuditEntry` fills nil before/after snapshots from the observed status transition. Webhook `TestSubscription`/`ReplayDelivery` now create the delivery row + result + audit row in ONE tenant tx (`db.CreateDelivery`/`UpdateDeliveryResult`/`ReenqueueDelivery` accept a `deliveryWriter` shared by `*Pool` and `pgx.Tx`; TestSubscription holds the tx across the bounded 15s test POST). Documented best-effort short-tx deviations (no joinable tx — audit logged non-fatal): settings backup create/restore/delete (filesystem + full-DB-restore) and execution `SendExecutionMessage`/`ContinueExecutionSession` (live adapter session). New DB-backed recovery + webhook service-level audit tests pin one-row-per-mutation, zero-for-reads, the idempotent-trigger no-op writes nothing, and that the delivery committed with its audit row. |
| 186 | Test | Audit | Service-level audit test suite (design §5 item 3, previously the only gap): DB-backed tests per service asserting one `audit_events` row per mutation and zero for Get/List, through the real Connect handlers — workitem (create/update/delete + reads, atomic-rollback on validation reject), worker (create + publish + reads), project (create/update/archive + reads), workflow (create + reads), auth RPCs (api-key create/revoke with the D7 secret-leak check on the snapshot, identity/role create, role_binding.assigned, tenant.create scoped to the actor's tenant, List* reads zero), the auth HTTP endpoints (signup/login/logout/refresh each one row with the real auth_method, failed login zero), and Ask Orchicon (one `conversation.message_sent` per ChatStream and per InterjectConversationTurn via real connect streaming handlers — interject snapshots `superseded:true` — zero for GetConversation/ListMessages, one `conversation.turn_aborted` per Stop). |
| 185 | Feature | Audit | Actor-based audit trail on every mutating RPC + auth action (transactional outbox): new `internal/audit` package (`audit.Record` slots into the caller's tenant tx next to the existing outbox enqueues — an `audit_events` row exists iff the mutation committed) + `internal/db/audit.go` (`CreateAuditEvent` / `ListAuditEvents`, all SQL in the data-access layer per invariant #4). New forward-only migration adds `audit_events.auth_method` (`"oidc"|"apikey"|"local"|"signup"|"dev"|"refresh"|"system"`, `""` legacy-only; `make migrate-hash` rehashed). Actor resolution via `auth.ActorFromContext` (identity id + auth method + `actor_type=user` for any real actor, `system` only when no identity). Wired into every mutating Connect handler across workitem/worker/workflow/project/auth/policy/webhook/execution/recovery/settings/runtimeimage/approval/askorchicon (incl. the previously-missed ChatStream — one `conversation.message_sent` row per user send in the shared `startConversationTurnOpts` tx covers ChatStream + Interject; `AbortConversationTurn` audited in its own short tx) plus the six auth HTTP endpoints (login/logout/signup/refresh/dev/oidc; signup's row commits atomically inside `createLocalAccount`). `db.CreateTenant` refactored to accept a `pgx.Tx` so `tenant.created` commits atomically (audit scoped to the actor's tenant). Read-only calls write nothing; secrets never enter before/after snapshots (API-key hashes, credentials, tokens, prompt/step bodies excluded). New `AuthService.ListAuditEvents` RPC (keyset on `occurred_at,id`, action/actor/target filters, auto-mapped to `auth:read`) + Admin AuditTab "Events"/"Decisions" segment + `useListAuditEvents` hook + Ask Orchicon `list_audit_events` read-only tool. DB-backed `internal/audit` tests (commit/rollback/RLS/no-tenant). DOCUMENTATION.md §Audit updated. |
| 184 | Feature | DB | First general audit table: forward-only migration `db/migrations/20260815000000_audit_events.sql` adds `audit_events` — id (ULID), tenant_id, actor_identity_id (nullable FK → identities ON DELETE SET NULL — an audit row is immutable evidence, deleting an identity nulls the ref without losing the trail), actor_type (default `system`, matching policy_decisions), action (open text so the outbox `task.succeeded` vocabulary maps 1:1), polymorphic target_type/target_id (no FK — spans every entity kind), before/after jsonb DEFAULT '{}' (mutation snapshots), trace_id NOT NULL DEFAULT '' (OTel correlation, mirrors policy_decisions/telemetry_cost), occurred_at timestamptz DEFAULT now(). Four indexes (tenant+action+occurred_at, tenant+target, tenant+actor, trace_id) + the uniform `tenant_isolation` RLS policy with ENABLE+FORCE (fail-closed: 0 rows with `app.tenant_id` unset, cross-tenant INSERT rejected — verified as a non-superuser on a throwaway Postgres 15). No schema.hcl edit (stale past runtime_images — hand-authored per convention), `make migrate-hash` rehashed atlas.sum; all 55 up migrations apply cleanly on a fresh DB and `make rls-check` passes. DOCUMENTATION.md §Tenancy model updated (table now exists). No writer/RPC/frontend in scope — the recovery engine's outbox-based audit payload with `audit_event_id` is the intended future consumer. |
| 183 | Feature | Auth | Admin identity CRUD on the existing Admin → Identities tab: four new `AuthService` RPCs (`CreateIdentity` / `UpdateIdentity` / `SetIdentityStatus` / `DeleteIdentity`), all auto-gated to `auth:write` by the active RBAC interceptor (no anonymous bypass — the interceptor is mandatory in every mode). Create provisions `user` (subject = local-account handle, `^[a-z0-9][a-z0-9._@+-]*$`) or `service` (subject optional slug; synthetic `sa-<ULID>` when omitted) identities, duplicate `(tenant_id, subject)` → 409; Update renames display_name with optional optimistic concurrency (version mismatch → 404); SetIdentityStatus flips `active`↔`disabled` only (enum validated server-side against the unconstrained text column); Delete is a hard delete with two server-side guards — cannot delete your own identity (400) and an `active` identity must be disabled first (412 FailedPrecondition) — performed with cascade cleanup of role_bindings + api_keys + local_credentials in one tenant-scoped tx (those two tables have no FK to identities). No migration (status column existed, never written) and no outbox events (consistent with sibling AuthService mutations). Frontend: `IdentitiesTab` extended with create form, inline edit, Disable/Enable, per-row + bulk Delete (disabled rows only, confirm), search + status filter + select-all checkboxes + selection count per the AGENTS.md list-page pattern; "Set local password" section unchanged; four new hooks (`useCreateIdentity`/`useUpdateIdentity`/`useSetIdentityStatus`/`useDeleteIdentity`) invalidating `authKeys.identities()`. New DB-backed `identity_crud_test.go` (create/dup/validation, update+version-conflict, status transitions, self-guard, active-guard, cascade cleanup) + 4 RBAC mapping rows in `rbac_test.go`; DOCUMENTATION.md §Authentication documents the RPCs, status semantics, two-phase delete, and service-account flow. |
| 181 | Feature | Auth | Frontend route guards + login redirect: added a router-level auth guard (root-route `beforeLoad` in `frontend/src/auth/route-guard.ts`, deny-by-default with an explicit `/login` + `/auth/callback` public allowlist) so unauthenticated visitors are redirected to `/login` at navigation time — before any protected component renders (previously only the post-render AppShell effect bounced them). The guard preserves the intended destination in `?next=`; `login.tsx` `continueTo()` now SPA-navigates router-matching paths (keeps the in-memory token) and full-page-loads only server-only OP-bridge paths. Centralized a deduped `ensureSession()` bootstrap shared by the guard and `AuthProvider`: a fresh page load exchanges the HttpOnly refresh cookie for a new access token (refresh-on-boot), so live sessions survive reloads without re-login, and network failures resolve unauthenticated (land on `/login`, never an error boundary). AC3 (no anonymous `x-orchicon-tenant-id` fallback in the API client) verified already satisfied at base (PR #289). Unit tests for `ensureSession` (refresh-on-boot / failure / dedup / network-failure) and `requireAuth` (redirect + next, public allowlist, authenticated fast-path); DOCUMENTATION.md §Authentication updated. Sign-out regression fix: `logout()` now also fires the new `POST /auth/logout` endpoint (expires the HttpOnly refresh cookie server-side, `internal/auth/handlers.go`) and arms a signed-out flag that stops the guard from silently re-authenticating a signed-out user via the still-valid cookie — signed-out users stay signed out across SPA navigations and reloads (AC1), with unit tests pinning both the suppression and the cookie-clear contract. `POST /auth/logout` is credential-free by design, so `/auth/logout` was added to the `ResolveAuth` public-path allowlist (`internal/middleware/auth.go`) — without it the middleware 401'd the logout before the cookie clear ran and a reload after sign-out re-authenticated via the still-valid cookie; pinned by a middleware-level test (`TestResolveAuthAdmitsCredentialFreeLogout`). |
| 180 | Refactor | Auth | Completed the anonymous-bypass removal structurally: `api.Mount` now has exactly one auth behavior — `ResolveAuth` is applied unconditionally (no `AuthHandler != nil` tenant-only `ResolveTenant` fallback), so no future `Mount` caller can re-enable credential-less access; deleted the orphaned `internal/middleware/tenant.go` (`ResolveTenant`/`TenantHeader`/`DefaultTenantID`/`TenantFromContext` — the `x-orchicon-tenant-id` header is gone from the codebase entirely, never a substitute for identity); fixed the stale "when auth lands in Phase 9" comment in `internal/tenant` (tenant now comes from the resolved identity set by `ResolveAuth`); pinned AC2/AC3 at the mounted-API level with `TestMountRequiresCredential` + `TestMountTenantHeaderIsNotIdentity` (no credential → 401, header-only → 401) in `internal/api/api_test.go`. |
| 179 | Feature | Auth | OIDC claim-to-tenant mapping & membership (decision #178 consequence #1, auth surface): replaced the hardcoded `tnt_dev` in the auth paths with a config-driven deployment tenant — `Config.DeploymentTenantID` (`ORCHICON_DEPLOYMENT_TENANT_ID`, default `tnt_dev`, boot-validated: lowercase alnum + `-`/`_`, ≤63) is now the single source of truth consumed by the OIDC callback, the dev-login default, the embedded-OP local login, the embedded OP's identity-resolution fallback (`op.Config.TenantID` → storage), and `BootstrapLocalAdmin`. Boot provisioning is parameterized: `SeedDevTenant` inserts the configured tenant id (slug mirrors the id so the unique slug index can't collide) before auth mounts. Membership stays role-binding-based: first login upserts the identity into the deployment tenant + grants the tenant admin role (idempotent); cross-tenant membership is N/A in a single-tenant install (documented SaaS forward path). The dev-only `tenant_id` login override is retained (documented dev escape hatch). DOCUMENTATION.md §Tenancy model flipped to present tense + env-var table/.env.example updated. New DB-backed tests pin a non-default `ORCHICON_DEPLOYMENT_TENANT_ID=acme` landing the OIDC callback, dev-login, and local-login in `acme`; config tests pin the default/override/validation. |
| 178 | Docs | Tenancy | Tenant architecture decision (org-groups / deployment-scoped): each Orchicon deployment owns exactly one tenant — formalizing the existing single-tenant behavior (tenants table + tenant_id/RLS on every scoped table, admin-only CreateTenant/ListTenants, OIDC callback + dev-login converging on tnt_dev). The multi-tenant schema is retained unchanged as the SaaS forward path. Consequences for the three gated features: sign-up tenant targeting descoped to a config-driven deployment tenant id (ORCHICON_DEPLOYMENT_TENANT_ID, default tnt_dev) replacing the hardcoded literal across auth/MCP/recovery/scheduler/sequence/runtime/seed; audit-trail scoping by-construction via tenant_id + tenant_isolation RLS; identity provisioning keeps the per-tenant EnsureIdentityForSubject upsert with the tenant from deployment config. Documented in DOCUMENTATION.md → User Guide → Authentication → Tenancy model + full trade-off discussion in the work-item architecture note (architecture-notes/, ephemeral). |
| 177 | Docs | Docs | Flagged proposal for the AGENTS.md amendments (License note + password-standard rewrite): `PROPOSED-AGENTS-AMENDMENTS.md` ships the exact diff for the human to apply — a `## License` section verbatim-mirroring README.md/LICENSE, and a rewrite of the "Hashed at rest" standard to scope password storage to the embedded identity provider (bcrypt/argon2), never control-plane business logic. AGENTS.md itself is human-maintained and left untouched for the human to apply. |
| 176 | Feature | Auth | No-lockout admin bootstrap: added an explicit boot-time reset override (`ORCHICON_LOCAL_ADMIN_RESET=1`) that re-arms `BootstrapLocalAdmin` on a plane that already has an admin — the operator lost the credential and would otherwise be locked out. The reset overwrites only the local-account credential (identity + admin role binding preserved), keeps the seed's local-mode + embedded-OP guards (never fires in production), is explicit opt-in (never a default), and is NOT disabled by `ORCHICON_LOCAL_ADMIN_SEED=0`, so a locked-out operator always has a path back in (even on the prod dogfooding container, which runs the seed disabled). Documented the change/reset procedures: Admin → Identities now has a "Set local password" form (the `SetLocalCredential` `auth:write` RPC) for changing the admin password while logged in, and DOCUMENTATION.md §Troubleshooting → Admin Access & Lockout Recovery + `.env.example` cover the boot-time reset flow and the dev-login escape hatch. New DB-backed tests pin the reset semantics (overwrite-only, guards, SEED=0 override). |
> Read this before starting any work to understand the current state.
| 175 | Feature | Auth | OIDC is now the base auth path in every mode. Removed the local-mode anonymous bypass in both sites (`ResolveAuth`'s no-bearer dev-tenant fallback and `RBACInterceptor`'s no-identity local allow) so a request without a credential is 401/Unauthenticated everywhere; `NewRBACInterceptor`/`ResolveAuth` dropped the mode param. `ORCHICON_DEV_LOGIN` defaults to false (dev-login returns 403 when disabled; `.env.example` flipped, tests pin the gate). Config validation now requires an issuer (embedded OP or external IdP) in every mode — a plane with the OP disabled and no external issuer refuses to boot. Added a local-mode, opt-out, idempotent first-admin bootstrap seed (`BootstrapLocalAdmin` in `internal/auth/bootstrap.go`, `ORCHICON_LOCAL_ADMIN_USERNAME`/`ORCHICON_LOCAL_ADMIN_PASSWORD`/`ORCHICON_LOCAL_ADMIN_SEED`): seeds the embedded-OP login credential + admin role for a fresh plane, logs a generated password once on first boot, never clobbers an existing admin. New public `GET /auth/config` mirrors the plane's auth capabilities (`embedded_op`/`external_oidc`/`dev_login`, secret-free) for the login page; the SPA login page renders only the supported affordances, the frontend dropped the pre-login `x-orchicon-tenant-id` fallback in clients.ts, and the app shell redirects unauthenticated users to `/login`. Embedded-OP flow verified end-to-end (authorize → login bridge → local-login → callback → PKCE token exchange). |
> Read this before starting any work to understand the current state.
>
> **Lifecycle:** one row per PR, appended to the top (newest first). Row
> numbers are MONOTONIC — never renumber, the release-notes boundary derives
> from the previous release's copy. At release time the GitHub Release body
> is generated from these rows (scripts/gen-release-notes.sh). After a
> release, run `scripts/gen-release-notes.sh --trim` on the next develop
> merge to drop released rows and keep only the current cycle.

| # | Type | Phase | Summary |
|---|---|---|---|
| 173 | Feature | Auth | Embed zitadel/oidc v3 as an in-process OpenID Provider: the plane now serves discovery/authorize (code + PKCE)/token/userinfo/jwks on its own origin (`/.well-known/openid-configuration`, `/authorize` + `/authorize/callback`, `/token`, `/userinfo`, `/jwks`). ID tokens are signed ES256 with a key deterministically derived from `ORCHICON_AUTH_SIGNING_KEY` (HKDF-SHA256 → P-256; the JWKS publishes only the public point, never the HMAC secret), minted against a TTL'd in-memory `op.Storage`, and verified through the existing go-oidc RP path. The login bridge (`/auth/op/login`) authenticates the caller's existing Orchicon session cookie or bounces to the SPA `/login?next=` (same-origin only). The BYO-IdP RP flow is byte-for-byte unchanged: PKCE is capability-gated on the IdP advertising `S256` in discovery (`code_challenge_methods_supported`). Config: `ORCHICON_OP_ENABLED` (default true), `ORCHICON_OP_REDIRECT_URIS`, `ORCHICON_OP_ISSUER` (empty = dynamic from Host). Apache-2.0 notice ships in the distribution (`third_party/oidc/`, `orchicon notices`). |
| 172 | Bug fix | Workflow engine | Fix the work-item Start/Resume/Stop controls so they actually stop what they say they stop. chainStepIDs computed the loop re-run chain by slicing the canvas-ordered step array (only valid for linear workflows): with a parallel gate inside the loop body (Architect coding template) the gate was excluded, so a QA-failure loop-back re-created PR-Reviewer + QA iterations whose dependency was the never-superseded succeeded gate run - dispatching SSE, PR Reviewer, and QA concurrently (catastrophic fan-out). chainStepIDs now walks the DAG (dependents of the loop branch + ancestors of the loop decision), re-creating the gate. StopSequence previously parked only the node it was called on and never aborted runs: descendant containers stayed running/failed and kept arming children, and a FAILED parent auto-revived on the next scan - stopping an epic left the whole chain firing. STOP is now recursive: a parent parks its entire subtree (descendants to pending, schedules cleared, run bindings cleared) and ABORTS any in-flight workflow run (run to aborted, step runs to failed, executions to terminated) via a shared AbortRunInTx core reused by the AbortWorkflow RPC; a leaf stop parks only that item. ControlSequence now accepts any work item: parent START/RESUME re-fire/continue the chain, leaf START/RESUME fire the item's own bound workflow. SequenceControls now render on every work item (board, tree, detail page) with parent-vs-leaf gating. Regression tests: parallel-gate loop, cascade stop with abort, leaf-is-individual stop. |
| 171 | Bug fix | UI | Fix three UI bugs: (1) new-category dialog text now readable on dark themes — `CreateCategoryDialog` (shared by Ask Orchicon, workflow categories, and worker categories) gained `text-foreground` so the theme foreground wins over the native `<dialog>` UA `CanvasText` (was black-on-dark; verified 16:1 dark / 18:1 light); (2) theme toggle now switches between the user's actual light and dark themes — `theme-store` keeps separate `lightTheme`/`darkTheme` slots (migrating the legacy single key by the theme's own mode) so `toggleMode`/`setMode` apply the theme for the target mode instead of leaving a light theme id applied in dark mode (was a high-contrast light fallback); (3) mobile navigation — added a hamburger drawer below the md breakpoint in `app-shell.tsx` (shared `NavLinks`, closes on link, backdrop, or Escape) so phone-sized viewports can navigate the system. | |
| 170 | Bug fix | UI | Fix card text overflow & alignment on the work item page: board cards and the detail-page stat cards now wrap long unbroken tokens (workflow template names, runtime image tags, URLs, titles) inside the card via `overflow-wrap` instead of spilling out or forcing flex columns to min-content width; the detail header title, parent link, and the board meta row gained `min-w-0`/`shrink-0`/`truncate` so text, labels, and values stay inside card boundaries and align consistently. |
| 169 | Bug fix | UI | Fix four UI bugs: (1) category-creation text now legible on dark theme — added `text-foreground` to the shadcn/ui `Input` primitive and the `CreateCategoryDialog` textarea, fixing all Input usages app-wide plus the dialog's raw textarea; (2) conversation drag glitch eliminated — replaced in-place CSS transform with `DragOverlay` from `@dnd-kit/core` for a clean portal-mounted preview; (3) Schedules History elapsed-time clocks now correct — `HistoryCard` fetches the workflow run via `useGetWorkflowRun` and uses `run.startedAt`/`run.endedAt` instead of `updatedAt`; (4) Schedules History delete no longer destroys the work item — replaced `hardDeleteWorkItem` with `useRemoveSchedule` (clears `recurring_schedule` + unbinds `workflow_run_id` via `updateWorkItem` proto3 clear semantics, preserving the item's status). |
| 168 | Feature | UI | Add live elapsed-time timers to workflow run and schedules pages: shared `LiveDuration` component (`components/ui/live-duration.tsx`) accepts `number | Timestamp` for `startedAt`/`endedAt` and an optional `now` prop (page-level ticker avoids N intervals); shared `formatElapsed` in `lib/format.ts` (unified hours/minutes/seconds formatter). Run page shows a top-level timer card below the status line while active, persists final duration after completion. Schedules RunningCard shows a live timer badge (page `useNow` threaded through); HistoryCard shows a final duration badge. Fixed `historyRanAt` zero-duration trap (was returning `updatedAt` as start — now `createdAt`). HistoryCard label changed from "Ran" to "Started" for consistency. |
| 167 | Feature | Ask Orchicon | Conversation management in the Ask Orchicon pane: a pencil edit button on each conversation enables inline rename (Enter saves via the existing `useUpdateConversationTitle` hook, Escape cancels); a folder-shaped button next to the new-conversation button creates a conversation folder via the existing `CreateCategoryDialog`; conversations are draggable (`@dnd-kit`, grip handle) into folders, reassignable between folders, and droppable back to the Uncategorized section; and a created folder can itself be renamed (pencil, inline) or deleted (trash, moves its conversations to Uncategorized) after creation. `CategoryPage` extended with `"conversations"` and `useCategoryPreferences` gained a `noSeed` option so the conversations pane starts with zero folders (no "Software Development" seed). |
| 166 | Bug fix | Telemetry & Cost | Fix cost-explorer totals on the default telemetry page and rename the sidebar entry to "Telemetry + Costs". The Overview total (~5M tokens) disagreed with the Cost Explorer's per-model sum (~38M) because `TelemetryService.GetDashboard` defaulted to a last-24h window while `AIGateway.GetCost` is unbounded (frontend passes no window to either) — same data, two windows. Removed the 24h `start` default in `GetDashboard` so the Overview is all-time by default (zero-start flows to the SQL unbounded sentinel, exactly like GetCost), aligning the Overview with the Cost Explorer and the `/dashboard` lifetime tiles; only `end` still defaults to now. Renamed the `/telemetry` sidebar label to "Telemetry + Costs" (`frontend/src/components/app-shell.tsx`), updated the Overview "Cost by model" panel description (was "in the dashboard window", now "over the lifetime of the workspace"), and updated DOCUMENTATION.md §Telemetry & Cost. New DB-backed regression test `TestGetDashboardAllTimeTotalsMatchCostExplorer` (guarded by `ORCHICON_TEST_DSN`) seeds usage records spanning >24h and pins summary == GetCostTotal unbounded == per-model panel sum — verified it fails against the old 24h default (35M vs 38M) and passes with the fix. |
| 165 | Bug fix | Recurring schedule | Fix duplicate recurring badge + disable-leaves-stale-status: (1) `RecurringBadge` now renders only when the status pill is not already `recurring` (new `showRecurringBadge()` gate at all eight badge sites) — at rest the fuchsia RECURRING status pill is the single recurrence signal, while mid-run (status `running`) the badge still marks the occurrence. (2) The Update handler enforces a final-state invariant — `recurring` is derived state, valid only while a `recurring_schedule` is present: unchecking the toggle (empty-but-present schedule) with the dropdown still on `recurring` now clears the schedule AND demotes the item to `pending` (the old demote only fired when `status` was omitted, which the form never is); the inverse hole (a manual `recurring` pick with no schedule) is demoted too, and an explicit non-recurring status in the same request is honored as-is. Same invariant mirrored in the Ask Orchicon `update_work_item` tool (which never applied the kind-switch `ClearRecurringSchedule`), and `StopSequence` now parks a recurring sequence parent to `recurring` (cadence stays armed) instead of stranding it pending-with-schedule. Recurrence-after-completion behavior untouched (completion handlers bypass the handler). Tests: service disable/re-enable/inverse/keep/echo/scheduled paths, tool mirror paths, STOP-on-recurring-parent, `showRecurringBadge` unit cases. |
| 164 | Feature | Workers & workflows | Re-seed all six seeded workflow templates from their UI-published versions, now with an explicit `parallel` node between Senior SWE and PR Reviewer/QA (parallel fan-out into the single fan-in loop gate) plus the two new UI Development - Architect templates (vision workers `w_se_sse_vision`/`w_se_qa_vision`/`w_se_architect_vision`). QA steps in the three coding templates normalized to the canned `w_se_qa_engineer` (they referenced the user-created `usr_w_se_qa_engineer`); human-approval steps normalized to an empty worker ref (UI Human Architect's stale `w_se_ai_approver`). Workflow editor now clears a step's worker ref when the reviewer is switched to Human, so human gates never carry a leftover approver binding. `architect_seed_test.go` extended to cover all four Architect templates (10 steps each). |
| 163 | Feature | Workers & workflows | Review/QA step efficiency: (1) parallelize PR Review + QA in all five coding templates (simple, human, human-Architect, non-human, non-human-Architect) — both run concurrently after Senior SWE and fan into a single loop_decision gate that proceeds to the approval step (Code Approver or human) only when BOTH succeeded (any failure/`_decision: failure` loops the whole chain back to the implementer); the loop_decision handler now aggregates decisions across multiple upstream steps (fan-in) instead of reading a single dep. (2) Facts ledger: every step records `FACTS LEARNED:` lines in its `ORCHICON WORKER SUMMARY`; the composite prompt aggregates them into a `## Facts learned (this run)` section near the top and points workers at `.orchicon/<run>/facts_learned` first, so later steps inherit established facts (env gotchas, root causes, decisions) instead of re-deriving them. (3) AGENTS.md gained a "Worker environment baseline" section documenting the pre-existing noexec `/tmp` guard-test failures, base gofmt drift, whole-repo semgrep deny-list findings (with a scoped semgrep command), and the runtime-container supervisor/daemon-self-copy behavior. (4) The Ask Orchicon `update_settings` tool now exposes the tenant stall parameters (no_progress / no_file_diff / text_loop windows, repetition count + window) — previously settable only via the Settings UI. (5) New read-only `get_workflow_version` Ask Orchicon tool dumps a workflow version's steps JSON verbatim, so a configuration built in the UI can be adopted as the seed template (bake into `internal/db/seed_workflows.go`). |
| 162 | Feature | Runtime containers | Full Orchicon environment inside the `:orchicon-dev` runtime container: NATS v2.10 baked into `Dockerfile.dev` (curl install — the daemon's FROM-rewriting rewrite makes multi-stage COPY impossible; Dockerfile content hash rolls the stock seed forward → async auto-rebuild). The runtime supervisor (`internal/runtime/agent.go`) boots a disposable in-container control plane at start — `initdb`/`pg_ctl` (stable cluster under `/var/lib/orchicon-sandbox`, trust auth) → `nats-server -js` → `orchicon serve` on :8080 with sandbox env (`ORCHICON_TELEMETRY=none`, `ORCHICON_OPCODE_SESSION_TRANSPORT=0`, `ORCHICON_SANDBOX_PLANE=1` suppressing the serve's kill-orphans pgrep) — **self-gating** on binary presence (all four postgres server bins + nats-server; base/`:gui` images skip entirely, behavior identical to today), with a 15s `/healthz` watchdog re-booting via the idempotent path. The serve handshake reports `plane_enabled`; the daemon publishes `CreateResponse.PlaneURL = http://<container-ip>:8080` and the **run-start gate** (`EnsureServing`) now ALSO requires the plane's `/healthz` on the bridge IP (same 120s window) so a half-initialized plane can't pass. `RuntimeServeConfig(imageTag)` registers the built-in Orchicon MCP on dev images only — `ORCHICON_POSTGRES_DSN` (sandbox DSN) + tenant in the entry's environment, command forced to `/usr/local/bin/orchicon` (the daemon's bind-mount) — giving workers `orchicon_*` tools against the sandbox DB, never the host plane's. Fixed the architect-flagged `sandboxPgBinDir` self-gate defect (a partial bin dir no longer passes — extracted `sandboxPgBinDirComplete`, new regression test). Docs: AGENTS.md worker block + DOCUMENTATION.md §Sandbox plane. Timing/MCP-round-trip/no-DB-route verification deferred to the QA step. |
| 161 | Docs | Docs | Document the recurring schedules feature in DOCUMENTATION.md: `recurring` status added to the work-item status model (ER diagram + board columns + MCP status list), the `recurring_schedule` fields (`frequency`/`interval`/`days`/`start_date`/`start_time` + the `next_run_at` due-scan cursor), and the `RecurringFireReconciler` behavior — due items fire immediately and advance `next_run_at`, a recurring parent fires its children sequentially (no new items spawned), leaving `recurring` clears the schedule, a completed occurrence (success or failure) returns the item to `recurring`, and a failed occurrence never stops future cycles. Also updated the System Topology diagram + Key Architectural Patterns + Project Structure + Where-to-Find tables to include the `recurring_fire` reconciler. |
| 160 | Feature | UI | Schedules view recurring support: `recurrenceBadge` placeholder replaced with `formatRecurrence` (renders the schedule frequency, "One-time" for non-recurring); Upcoming merges RECURRING items chronologically via a unit-tested `upcomingSortTime` (moved into `schedules-model.ts` — next_run_at for recurring, scheduled_start_at otherwise), and a fuchsia `RecurringBadge` marks recurring items on Upcoming/Queued/Running/History cards so recurring schedules are visibly distinct. Tests: `upcomingSortTime` uses next_run_at, ignores scheduled_start_at for recurring, 0 when unset, chronological merge order. |
| 159 | Feature | UI | RECURRING status meta + Recurring badge: status pill switched from teal to fuchsia (distinct from the emerald SUCCEEDED family, shared hue with the badge); new compact `RecurringBadge` + `isRecurringItem()` helper (persistent `recurringSchedule` signal, so the badge doesn't blink off while an occurrence fires) surfaced next to the sequential `PositionBadge` on board cards, the tree, the Schedules page (Upcoming/Queued/Running rows), and the item detail header + chain-position card. Tests: fuchsia pill classes pinned, `isRecurringItem` true while firing. |
| 158 | Bug fix | UI | Fix recurring schedule checkbox immediately re-checking on uncheck: `useEffect` synced enabled state with `!!value` but an empty proto `RecurringSchedule` (emitted on uncheck) is truthy, so the checkbox re-checked and form fields never hid. New `isSchedulePopulated()` helper checks actual content (frequency, interval, days, startDate, startTime) instead of truthiness. |
| 157 | Bug fix | Workers & workflows | Revert the composite-prompt ownership overreach: the step-position blurb no longer tells a worker it "OWNS this work item end to end" and must "fix code or tests outside your specialty" — that framing let a DevOps Engineer use it as license to write Go code when the workflow happened to bind them to a backend task. It now reads neutrally: "This workflow has N steps and you are step M — Name. Follow your role for this step." (Deliberately neither says "own everything" — the overreach — nor "defer to other workers" — the earlier gating bug.) |
| 156 | Bug fix | Recurring schedule | Recurring items return to `recurring` when a bound run or sequence completes — success AND failure — instead of `succeeded`/`failed`. A failed occurrence is recoverable via the existing per-step recovery flow and never kills the cycle: the item stays `recurring` with schedule + `next_run_at` intact (recomputed from the schedule if the cursor was cleared mid-cycle), so the RecurringFireReconciler fires the next occurrence on schedule. Applied consistently across the workflow-reconciler run-completion success/failure paths, `failRunAtStart` (structural run failures like unresolvable runtime image / empty step DAG), and the sequence-reconciler completion + failure-chain paths (a failed recurring cycle halts the current chain but re-fires the next). Tests: leaf-run success + failure keep the item recurring with next_run_at preserved and re-fire the next occurrence; sequence-parent completion + failed-cycle tests assert the parent stays recurring and the next cycle re-fires. |
| 155 | Feature | Workers & workflows | Retire the UI-canned workers (`w_ui_developer`, `w_ui_design_architect`, `w_ui_qa_engineer`) in favor of Vision variants (`w_se_sse_vision`, `w_se_architect_vision`, `w_se_qa_vision`) — exact copies of the non-UI counterparts (`w_se_senior_software_engineer`, `w_se_principal_architect`, `w_se_qa_engineer`) plus the vision-capable `opencode-go/mimo-v2.5` model, extra UI/design-system/accessibility skills, and the Playwright visual-verification block. The seeder stops seeding the retired IDs and deletes any still-seed-managed instance on boot. Also fixed the workflow-prompt role framing that made specialized workers gate themselves out of work: the composite prompt no longer tells a worker to "focus on your specific role and let other workers handle their steps" — it now says the worker owns the work item end to end and must address reviewer/approver feedback even outside its specialty. Reviewer feedback now reaches the next worker in full (no 120-char truncation), the `.orchicon/<run>/issues` file is always written (falling back to `_summary`), and opencode `file_diff` telemetry is captured so the worker is explicitly told which files the previous session wrote and to read them before starting. |
| 154 | Bug fix | Recurring schedule | Fix the recurring-fire lifecycle test (`TestRecurringFireLifecycle_CompletionReturnsToRecurring`) which failed deterministically against a real Postgres: its fixture seeded a workflow with an empty step DAG, so `reconcileRun` failed the run at start and the item ended `failed` — the completion-handler fix it was meant to verify was never exercised and the committed suite was red. Now seeds a workflow with a real task step and flips the runtime-readiness gate on the test run so versions stay consistent. Also fixed a time-of-day flake in `TestRecurringAdvanceNextRunAt` (asserted next_run_at >30min out, which fails when the next 2-hourly occurrence is closer). |
| 153 | Bug fix | Recurring schedule | Fix recurring items marked `succeeded` after first run: completion handler checked `wi.Status == recurring` but `StartWorkflow` sets status to `running` at fire time, so the item was never `recurring` when the completion handler fired. Now checks the persistent `RecurringSchedule != nil` field instead. Same fix applied to failure path (clear schedule) and sequence_reconciler completion/failure chain. Lifecycle test exercises the full fire→complete cycle. UI fix: work-item detail shows `nextRunAt` for recurring items. |
| 152 | Feature | Recurring schedule | Recurring fire reconciler: scans `status='recurring'` items with `next_run_at` in the due window, fires via `StartWorkflowDirect` (leaf) or `StartSequence` (parent), and advances `next_run_at` to the next occurrence using optimistic version locking for idempotency. Workflow-reconciler completion handler keeps recurring items at `recurring` status (not `succeeded`) when `next_run_at` is set, and clears the schedule on failure. Registered as `recurring_fire` reconciler in server.go alongside `scheduled_run`. Tests: leaf fire + advance, sequence parent fire, non-due skip, idempotency, no-workflow skip, advanceNextRunAt unit, and mixed leaf+parent fire. |
| 151 | Feature | Sequence engine | Manual sequence control (START / RESUME / STOP) on any child-bearing work item: new `ControlSequence` RPC with a `SequenceAction` enum (start re-fires the chain from child #1 destructively; resume continues from the first non-succeeded child keeping history — for parked/halted chains; stop parks the parent → pending and clears the scheduled start so children run standalone, while an in-flight child finishes naturally). New `ResumeSequence`/`StopSequence` engine primitives (Stop needs no schema change), handler + status-gated validations, Ask Orchicon `control_sequence` tool, and a frontend Start/Resume/Stop button cluster on sequence-parent cards in the board and tree (confirm-gated Start, display-only gating with server validation). Tests: engine park/resume + in-flight-stop-inert + no-children guards, and RPC leaf/bound-run/active-run rejection + resume/stop wiring. |
| 150 | Bug fix | Ask Orchicon | Surface model/provider diagnostics in Ask Orchicon: a wiped Ask Orchicon model default silently fell back to the free model, whose rate-limit errors opencode never emits on the bus — every turn looked "stuck" and burned the full 120s stall window. Stall monitor now reads the tool input from `state.input` (the real opencode v1.x tool-part shape) instead of the absent `input`/`args` fields, so distinct commands no longer collapse to "8 repeats of bash|null" and trip a false repetition stall; stall + reply-timeout errors name the model and point at Settings → Default models; a loud log warning fires when the fallback free model is used; the frontend surfaces the effective model in the conversation header (amber badge + warning banner when the free fallback is active), names it in the thinking indicator and error bubbles, and wraps long model refs so the thinking bubble no longer truncates. |
| 149 | Bug fix | Ask Orchicon | Fix Ask Orchicon stalls: per-turn stall monitor (no-progress + tool-call-repetition) aborts a hung/looping model within ~2 min and persists a clear retryable error; new `InterjectConversationTurn` streaming RPC supersedes an in-flight turn (aborts the serve session so the model stops NOW, persists partial content as a plain message) — the chat nudge with an interrupt; token-guarded + TTL'd turn registry with a background sweeper (no orphaned/background turns, no permanently-stuck one-turn gate, stale-finalize race eliminated); frontend keeps the stream slot attached across a dropped ChatStream socket on an acked turn (Stop + interject stay; poll resolves completion) and enables the input while streaming so Send mid-reply interjects instead of failing with "another reply already processing"; handshake timeout default lowered 600s→60s. |
| 148 | Bug fix | Workers | Rework the UI canned workers (`w_ui_design_architect`, `w_ui_developer`, `w_ui_qa_engineer`) from "UI specialist" identities that gated themselves out of backend work into full-stack engineers/full-stack QA with deep UI expertise — seed Role/Skills/Behavior/AgentsMD now claim the whole stack, add an explicit "never refuse or gate work because it isn't UI" rule, keep the vision-capable model (`opencode-go/mimo-v2.5`), and safety marker bumped to v14 so existing workers roll forward. New identity regression test asserts full-stack framing + absence of the old limiting identity. |
| 147 | Feature | Workers | Replace the single context-switching canned AI Approver (`w_se_ai_approver`) with two dedicated seed workers: `w_se_design_approver` (static plan/design review, pinned to the Architect workflow's design-approval step) and `w_se_code_approver` (static done-ness verification of the completed implementation, pinned to the post-QA approval step). Both carry fixed review contracts — no predecessor-type inference, so re-running workflows with partial state no longer confuse the approver on its role. Seed tests split into per-approver review-contract tests; scheduler approval tests repointed to `w_se_code_approver`. Existing DB worker `w_se_ai_approver` intentionally left in place (the seeder never deletes) so in-flight workflows keep running; it stops being seed-refreshed. |
| 146 | Bug fix | Recurring schedule | Fix empty-clear guard, weekly empty-days, and create normalization for recurring schedules |
| 145 | Docs | Repo | Add AGENTS.md reminder that PRs must explicitly target `develop` (base defaults to `main` now that it is the default branch). |
| 144 | Chore | Repo | Flip GitHub default branch to `main`; update AGENTS.md + CLOUDFLARE_SETUP.md default-branch references. Branch protection unchanged on both branches. |
| 143 | Feature | UI | Add category folders for workers and workflows — localStorage-backed collapsible folder groups with rename, delete, description editing, and "New Category" dialog. All items seeded into "Software Development" on first load; folders collapsed by default. |
| 142 | Feature | DB | Forward-only migration adding recurring-schedule storage to `work_items`: `recurring_schedule` (JSONB, `{frequency, interval, days[], start_date, start_time}`) + `next_run_at` (TIMESTAMPTZ, computed next occurrence for the scheduler due-scan), with `COMMENT ON COLUMN` for both and a partial `idx_work_items_tenant_next_run_at` index on `(tenant_id, next_run_at) WHERE next_run_at IS NOT NULL`. Same table, so no new RLS policy; hand-written per the additive convention (patterned after `work_item_scheduled_start` / `work_items_sort_order`), `make migrate-hash` rehashed `atlas.sum`. Verified the migration runner applies it on a fresh DB with both columns + index present. |
| 141 | Feature | Work Item Status | Add RECURRING status to the full enum surface: proto enum (11), domain constant, validation, proto↔domain conversion, sequence reconciler, kind-switch guard, and all frontend status maps (teal pill, Pending column mapping, filter, detail page YAML editor, status dropdown). Includes regression tests for column mapping and terminal status guard. |
| 140 | Bug fix | UI | Schedules: fix missing upcoming/history entries for sequential workflow runs (and single runs started without a schedule). The sequence engine resets descendants to pending on fire and arms one child at a time — only the parent carries `scheduled_start_at`, so queued children never matched the `SCHEDULED` Upcoming query and completed children (and manual-start single runs) never matched History's `scheduledStartAt`-only predicate. Upcoming now derives a **Queued** section of pending children of active sequence parents (chain-ordered, with position badges) from the full project list; History now includes terminal items with a `workflow_run_id` (completed sequence children + single runs without a schedule) plus terminal sequence parents, with ran-time falling back to `updated_at`/`created_at`. Predicates extracted to `src/lib/schedules-model.ts` (pure, unit-tested against the real engine's verified state transitions). |
| 139 | Bug fix | UI | Ask Orchicon: make streaming state per-conversation (keyed `Record<convId, ConvStream>` with functional updaters) so a turn keeps running — Stop button, growing reasoning bubble, optimistic message and the thinking indicator survive navigating to other conversations and back, `isStreaming` correctly stays true for the conversation that is still processing (which blocks a duplicate send instead of hitting the server's "already one processing"), and it fixes the acked-stream-with-no-assistant-message crash when streaming a fresh account's first turn. The old single-slot state + "reset on conv switch" effect are removed; a deleted conversation's slot is dropped. |
| 138 | Feature | UI | Execution sessions: reasoning bubbles now use the shared chat `ReasoningBubble` (from `components/chat/`) so the runtime-session and chat session consoles get the same streaming-first reasoning UX as Ask Orchicon — auto-open + live char count while streaming, hide/show toggle when done — instead of each pane's divergent local copy. `RuntimeSessionPane` passes `streaming` (live phase) and `SessionChatPane` forwards each item's `live` flag. |
| 136 | Bug fix | UI | Ask Orchicon: widen active conversation view from the centered max-w-3xl column to the full main area (messages + input now span sidebar-to-conversations-panel), keeping the greeting load page centered. |
| 135 | Feature | UI | Ask Orchicon load page + mode awareness overhaul: larger greeting heading (text-4xl/5xl), mode toggle dropdown inside chat bubble on right, new conversations return to load page, full-viewport conversation view (conditional AppShell padding), dual-mode badge (Brainstorm/Orchicon) in header, Orchicon mode prompt aware of Brainstorm mode + project context, progressive streaming reasoning chunks with auto-scroll. |
| 134 | Feature | Workers | Rework the canned AI Approver (`w_se_ai_approver`) seed to decide based on WHO the previous step's worker was, not line-by-line code inspection: it identifies the previous worker via `.orchicon/<workflow-run>/worker` + the Execution history, reviews the PLAN only after a planner (e.g. Principal Software Architect) and the previous step's overall status (`success`/`failure`) + summary after an implementer (e.g. Senior Software Engineer, QA Engineer) — manager/Product-Owner style, no diff expectations. Safety marker bumped to v13 so the seed rolls forward to existing workers; seed tests updated + new decision-basis test. |
| 133 | Feature | UI | Ask Orchicon: add Brainstorm ⟷ Orchicon mode toggle (pill segmented control) next to send button — persists via conversation.mode, optimistic update, keyboard accessible (arrow keys), responsive (icon-only on mobile). |
| 132 | Bug fix | UI | Ask Orchicon: fix delete-active-conversation-while-streaming regression — reset guard now clears stale state on id→null transitions (skip only null→id). |
| 131 | Bug fix | UI | Ask Orchicon: fix greeting-state send clobbered by useEffect reset (track prev conv id, skip null→id transitions), fix optimistic user bubble ordering to render before streaming bubbles. |
| 130 | Bug fix | UI | Ask Orchicon: fix greeting-state first message silently dropped (stale closure on `activeConvId`), remove bogus `isStreaming` prop from AssistantBubble (tsc error), type SpeechRecognition constructor properly, and keep thinking indicator visible until first text chunk arrives. |
| 129 | Feature | UI | Ask Orchicon page overhaul + execution chat patterns: centered ChatGPT-style layout (max-w-3xl), greeting state ("What would you like to create today?"), shared chat bubble components extracted to `components/chat/`, streaming text/reasoning chunk rendering, auto-stick scroll, conversations panel on right (w-80). |
| 128 | Feature | UI | Nav + default route: Ask Orchicon is the front door — sidebar nav reordered (Ask Orchicon first, Dashboard moved down), `/` redirects to `/ask-orchicon`, Dashboard gets its own `/dashboard` path. |
| 127 | Feature | Ask Orchicon | Project directory context tool: Ask Orchicon (both modes) can now see a project's files via two read-only `orchicon_*` MCP tools — `orchicon_list_project_dir` (shallow one-level listing of a project's `project_dir` or a subdir; skips VCS/build noise, caps at 1000 entries, tags symlinks via Lstat without descending) and `orchicon_read_project_file` (bounded file read, default 256 KiB, clamped `max_bytes`, JSON envelope with `truncated`). Both resolve paths through one shared traversal-safe resolver, `contextfiles.ResolveWithin` (trim+length-bound, root normalized via Abs+EvalSymlinks, lexical containment rejecting `..`/absolute-out-of-root, full EvalSymlinks re-check rejecting symlink escapes while allowing in-root links, nonexistent targets keep lexical form). Works for projects found via the existing MCP project tools or one just created in-conversation; sessions stay directory-less by design. Always-run pure-fs unit tests cover `..`/absolute-out-of-root/symlink-file/symlink-dir/nested/root-symlink/empty/overlong/nonexistent cases; DB-backed tests prove the wiring end-to-end (list/read/bounds/no-project_dir/unknown project). |
| 126 | Feature | Ask Orchicon | Conversation mode model + per-mode prompts (brainstorm default): `ask_orchicon_conversations.mode` (forward-only, `ADD COLUMN IF NOT EXISTS`, `brainstorm`/`orchicon` text with brainstorm default — no backfill), proto `ConversationMode` enum + `Conversation.mode` (DB maps UNSPECIFIED/unknown→brainstorm, unknown ints rejected with `CodeInvalidArgument`), `SetConversationMode` RPC (the F4 task 9 toggle surface; create accepts optional `mode`), and per-mode persona prompts: Orchicon = governed platform expert byte-identical to the pre-Task-4 single `BuildSystemPrompt`, Brainstorm = open deep systems-thinking partner opening with "What can I help you create today?" (general design/coding/brainstorming in scope; DB role/skills/behavior are Orchicon-mode-only, `system_prompt`/Additional Instructions applies to both). The mode is read at turn-dispatch and applied per message via opencode's per-turn `system` field — toggling mid-conversation needs no session change or serve restart (the same opencode session persists; verified by a DB-backed test that switches modes between two turns on the same session id). MCP surface identical in both modes (serve/session-scoped, not per-turn swappable); model resolution unchanged. |
| 125 | Bug fix | UI | Fix sloppy reasoning-bubble grouping in worker executions: text/reasoning chunks are now keyed to a stable phase (history `step-N` from opencode step boundaries, live `live-N` from a monotonic counter) and grouped per (kind, phase) — `groupByPhase`/`groupPhaseGroups` in `sessionItems.ts` — so interleaved-streaming models (reasoning→text→reasoning…) render as ONE growing reasoning bubble plus ONE growing text bubble instead of chopped fragments, with no mid-stream regrouping across the 2s transcript flush; `mergeSessionItems` treats reasoning as one growing bubble, `transcriptItems` maps `step_start`/`step_finish` as no-render boundaries and groups history (terminal view converges with live), `liveItems` fixes the latent bug where live reasoning was never grouped (`live` tag omitted), and `RuntimeSessionPane` groups its rendered messages by phase with artifact absorption. |
| 124 | Feature | Runtime lifecycle | Warm runtime-container pool + deterministic run-start serve gate: containers are keyed by environment (image + project mounts), leased exclusively per run, and reset to pristine in the background between runs so dispatch never cold-starts a container or serve; a workflow run persists `runtime_ready` and the async ensure-serving pass proves the serve usable (L1: health + session-create) within `ORCHICON_RUNTIME_SERVE_READY_TIMEOUT` (120s) before ANY execution dispatches — a serve that can't come up fails the run at start instead of looping step recovery. The daemon self-copies its binary so `make clean` can't orphan the container mount (the root cause of recurring "no opencode serve available" dispatch failures), and the dead one-shot `opencode run` exec/stream/reconnect/signal/status machinery (~1050 lines) plus the `ExecutionReconnect*` tenant settings were removed. |
| 123 | Feature | Session transport | Stream + persist reasoning for Ask Orchicon: reasoning parts are unwrapped from the SSE bus in the live turn loop via the same `LegacyEventFromBus` mapping executions use, accumulated separately from the reply text (never folded into content), and persisted per turn on the assistant message (`ask_orchicon_messages.reasoning jsonb NOT NULL DEFAULT '[]'`, forward-only migration) — partial reasoning survives error/stop/timeout turns. `ChatMessage` gains `repeated string reasoning = 10` (delivered via the existing `ListMessages` poll, the F4 reasoning-bubble data path) and the `ChatStreamResponse` oneof gains a typed `ReasoningChunk` event (field 7, defined-but-not-emitted like `text_chunk`, for a future SSE surface). Additive: a model that emits no reasoning parts yields no events and an empty array; text/tool/error paths untouched. |
| 122 | Bug fix | Workflow engine | Un-wedge the loop_decision DAG and make force-progress skip only stuck steps. Root cause of a prod stall: a SUPERSEDED loop-decision iteration created in the SAME transaction as the active one shares `created_at`, and `runByID` ("last row wins" over `ORDER BY created_at`) could shadow the active pending run with the superseded succeeded one — the loop decision never dispatched, downstream never fired, and the run sat "running" until force-progress (which then skipped real work). Fixes: `ListWorkflowStepRuns` orders by `created_at, id` and `runByID` prefers non-superseded rows; the upstream-failed branch no longer spawns a duplicate iteration while one is already pending; and `ForceProgressWorkflowRun` now forces only in-flight steps and pending steps whose DAG deps are satisfied, leaving steps with unsatisfied deps PENDING so the reconciler dispatches them (it never skips a PR-merge step again). |
| 121 | Feature | Session transport | Ask Orchicon turn lifecycle matches executions' resilient pattern: `ChatStream` is now a send-and-ack RPC — the reply is collected on a request-independent detached context (survives tab close / disconnect) and delivered by polling `ListMessages` within the new `ORCHICON_ASK_REPLY_WINDOW` (default 30m); **Stop** is a first-class `AbortConversationTurn` RPC that aborts the serve session turn (session kept for the next message) and cancels the in-process turn registry; serve loss mid-reply re-attaches to the same session (watchdog preserves the data dir) or falls back to a fresh session seeded from the DB transcript; a serve down at send time fails fast after `ORCHICON_ASK_SERVE_DOWN_GRACE` (default 15s) instead of looping to the reply window; failed/timeout/stopped turns are persisted as error messages (`MessageMetadata.error`) with a Retry affordance that re-sends the most recent user message; one turn per conversation (FailedPrecondition while pending). QA round: the DB-backed turn tests now run against real Postgres (tenant injection in the abort handler test + real `buildSystemPrompt` content assertions instead of literals), and the conversations sidebar refreshes when a reply lands. |
| 120 | Bug fix | UI | Serve worker display names in the execution/run views: `WorkerExecution.worker_name` (proto 25) is LEFT JOINed from `workers` (tenant-scoped) by all five execution readers and rendered as `workerName || workerId` in the executions list, execution detail, run detail, and SessionChatPane — replacing the `w_se_`-prefix ID heuristic that fell back to raw IDs for adopted/user workers. Step-run summaries now also carry `_worker_name` for new completions. |
| 119 | Refactor | Session transport | Removed the legacy one-shot `opencode run` subprocess transport entirely: every worker execution and Ask Orchicon turn now runs as a persistent session on a serve, and a run that cannot get a session fails fast (failed_to_start → workflow recovery) instead of silently degrading. Added a supervisor serve watchdog (liveness-gated idempotent handshake + periodic health poll that restarts a wedged serve in place, preserving sessions via a stable XDG data dir) and made the daemon propagate serve-start errors instead of degrading. |
| 118 | Feature | Session transport | Ask Orchicon conversations now run as persistent sessions on the host opencode serve: first message creates the session (session_id persisted on the conversation), follow-ups prompt_async the same session, turn completion via session.idle, no per-message subprocess. |
| 117 | Bug fix | Sequence engine | Self-healed stale one-shot worker refs (binding a workflow now clears them), fixed schedule-time validation to use the post-update workflow, fixed drag-to-reorder navigation with document-level click suppression, added a chain-position card to the work-item detail page, and restyled the order badge to match the status pills. |
| 116 | Feature | Sequence engine | Closed the workflow-less scheduling gap: the edit form shows the schedule/start card for parents with children even without a workflow (labeled as a sequence, with the parent's own workflow annotated as ignored), and scheduling a workflow-less leaf is rejected at schedule time instead of silently never firing. |
| 115 | Bug fix | Sequence engine | "Has children" is now the sequence determinant: a parent with children routes to the sequence engine (validating its subtree) even with a stale workflow binding — no more silent individualized runs; the stale binding is cleared on fire. |
| 114 | Bug fix | UI | Fixed drag-to-reorder: the post-drag click no longer navigates into the item (tree + board), reorder works in the All-projects view (project_id derived from the items), and sequence children now show #N chain badges in both views. |
| 113 | Bug fix | Runtime lifecycle | Fixed leaked runtime containers from runs stuck "running" forever: fail empty-step-DAG / missing-version runs at start (container reaped, sequence chain halts), fail task steps whose work item or execution row was deleted instead of waiting forever, and reject empty-DAG child workflows at sequence schedule time. |
| 112 | Bug fix | Session transport | Hardened the session transport against truncated final turns: async fire-and-forget follow-ups (no dropped browser connections, reply collected on a request-independent context), completion-signal probe at session settle, truncated-final-turn detection, and a loop-decision re-ask budget that counts real re-asks. |
| 111 | Bug fix | Sequence engine | Fixed the scheduled-fire scan crash (SQL NULL into *string aborted every pass) and the mid-run reorder that armed a pending sibling ahead of an in-flight child. |
| 110 | Bug fix | Sequence engine | Added the reconcileParent sequence-parent guard, the Ask Orchicon reorder tool, and sequence UI polish (chips, badges, filter-aware tree reorder). |
| 109 | Bug fix | Sequence engine | Fixed tree drag-to-reorder to derive order from the true sort_order sibling rank; default display sort is now chain order. |
| 108 | Feature | Sequence engine | Added sequential multi-workflow runs: sort_order ordering, the sequence engine (fire/advance/halt/retry/park), schedule-time validation, and schedules/board/tree UI. |
