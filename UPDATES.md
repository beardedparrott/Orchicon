# UPDATES.md

> Track record of what has been shipped, phase-by-phase.
>
> **Lifecycle:** one row per PR, appended to the top (newest first). Row
> numbers are MONOTONIC — never renumber, the release-notes boundary derives
> from the previous release's copy. Rows up to and including the last
> release are trimmed by `scripts/gen-release-notes.sh --trim` on the first
> develop merge after a release. The GitHub Release body is NOT generated
> from these rows — it comes from RELEASE_HIGHLIGHTS.md (curated narrative)
> via scripts/gen-release-notes.sh; these rows are the internal
> engineering track record.

| # | Type | Phase | Summary |
|---|---|---|---|
| 291 | Bug fix | TUI/GUI parity | **An F-key now drops the tab's submenu and puts the keyboard in it — and it was already written THREE times, with the chord route the copy that had drifted. Along the way: the footer was lying about where the keyboard was, esc from the bar was dead, and ctrl+g was only a half-escape.** The operator: *“on certain screens, when hitting the F#, it automatically gains focus on the first submenu in the list and hitting enter doesn't bring down the submenu. Maybe we should make the action when you hit the F# key, it goes to the menu, but also automatically drops the submenu down and gains focus to that.”* **(1) THE ROOT CAUSE IS THREE COPIES OF ONE GESTURE.** “Select a tab” existed as the global **chord route**, `menuHandleKey`'s chord arm, and the **tab click**. The click and `menuHandleKey` both did `SwitchTo + openTabMenu`; **the chord route did `SwitchTo` alone.** So the same key meant two different things depending on whether a dropdown happened to be open — which is exactly why the report was *screen dependent*: with the menu SHUT, Enter went to whatever lay underneath. On **Ask** the conversations rail claims Enter while the rail is up (delivered behaviour, and the concrete case), a screen claiming keys (an open form, a latched search) claims it everywhere, and a **non-empty composer** takes it as send — none of which is the submenu. All three now call ONE `openTabWithMenu`, so they cannot disagree again. **(2) FOCUS GOES WITH THE MENU — AT THE SELECTION GESTURE, NOT AT EVERY MENU.** `openTabWithMenu` sets `focusTabs`, because with the keyboard on the bar `menuHandleKey` runs **ahead of every screen claim** and the arrows/Enter become unambiguously the menu's. It is deliberately **NOT** in `openTabMenu`: the composer's Enter/Space opens a dropdown with an empty buffer, and moving focus THERE would send the operator's next letter to the screen instead of the box — a real hazard, since space with an empty box opens the menu. The toggle is preserved: re-pressing the tab whose menu is down closes it (the click, `SwitchTo`'s same-tab path and `menuHandleKey` already agreed; the re-press still re-arms its streams). **(3) THE FOOTER WAS LYING, AND THAT IS WHY THE TRAP WAS INVISIBLE.** `footer.ComposerFocus` was set in **three places inside the MOUSE handler** and recomputed on the pass-to-screen path — **neither of which any keyboard path reaches**. So Tab (or an F-key, which now lands on the bar) left the footer claiming **“composer”** while the composer was blurred and typing went nowhere: the one piece of on-screen evidence of where the keyboard is said the exact opposite of the truth. It is now **derived in `setFocus`**, the single chokepoint every focus change passes through, and the three mouse copies plus the pass-to-screen recomputation are deleted. **(4) ESC FROM THE BAR WAS DEAD — and this change made that load-bearing.** With a submenu down, the first esc closes the menu (`menuHandleKey`, standard) so the SECOND had nowhere to go: the keyboard stayed on the bar with nothing selected below it and the operator's next letters ran **screen actions**. That is precisely what `TestFocusChordAndFallthrough` already pinned as *“esc must return focus to content”*, so esc from the bar now **disengages into the content** — the same place esc from the composer goes — with the **diff pane keeping precedence** exactly as delivered (its global route is further down, so esc still closes the pane first). **(5) `ctrl+g` WAS A HALF-ESCAPE.** It promises to *“leave whatever holds the keys”*, and a dropdown holds the keys, but it left the menu up: the composer had the caret while **Enter and the arrows still belonged to the menu** — one screen with the keyboard split in two. ctrl+g now dismisses the dropdown, which matters because it is the **documented way back to typing** (the footer and the help overlay both name it) and it is the path an F-key jump now needs. **A LOAD-BEARING DETAIL FOUND BY THE COMPILER:** the mouse path captured `wasOpen := m.MenuOpenID()` because the menu's **outside-click close** runs in the same gesture and erases the state the toggle reads (*“would turn click the open tab again into reopen — the menu could never close by mouse”*). Rather than pass that state around, the **tab click is now resolved FIRST**, before the outside-click close — safe because the dropdown renders BELOW the bar, so a press at `y == 0` can never be inside the panel or its header. One rule, one place. **TWO TESTS PINNED THE OLD STATE MACHINE AND WERE UPDATED, NOT WEAKENED:** `TestFocusChordAndFallthrough` asserted the one-press `esc → content` and now asserts the two-step (menu closes, then content) **plus** that the chord dropped the menu and took the keyboard; `TestComposerDraftSurvivesScreenSwitchAndFailedSend` kept every assertion — the draft still survives the switch — and now also pins that the chord's Enter belongs to the **menu** (it operates what is on screen rather than sending a message while a dropdown is visibly open), returning to the composer by the documented `ctrl+g`. **NEW TESTS:** the submenu drops and takes focus **for every tab** and Enter **selects and hands the keyboard to the content**; Enter beats **a key-claiming screen** (asserting the claiming screen never received the key) and beats **the Ask rail's conversation selection** — asserted on the **menu closing**, which is decisive and independent of which entry the Ask menu highlights, and is what the rail's own Enter would NOT do; the **re-press toggles**; **chord and click leave identical state** (tab, menu, focus); ctrl+g dismisses the dropdown and the next keystroke **reaches the composer**; the **footer follows the keyboard** both ways; and **esc from the bar disengages into content**. **PROVEN BY DISABLING:** restoring the OLD chord handler fails **seven** tests — `“f1: submenu not dropped (menuOpen=\"\", want \"ask\")”`, `“the chord must drop the submenu even when the screen claims keys”`, `“the chord must drop the Ask submenu”`, and the divergence itself, `“chord and click left different menus: \"\" vs \"execution\"”` — and removing just the focus-taking fails with `“the keyboard must be in the submenu it opened, got focus 1”` and `“the footer still claims the composer has the keyboard while a chord has put it on the bar”`. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side — requires an instance rebuild. |
| 290 | Feature | TUI/GUI parity | **The tab chords are F1–F7. The label printed at each tab IS the key (underlined individually), and the `alt+ ` modifier label is GONE — the bar now starts at the first tab.** The operator: *“Let's do F1-F7, remove the alt+ text and underline the F1-F7 individually. That works.”* **WHY THE MODIFIER WENT, AND WHY IT WAS ALWAYS A WEAKNESS.** `alt+ 1·Ask Orchicon` existed to explain a modifier the label did not name — five cells of chrome in front of the operator's own tabs, and the first thing to go when the bar ran out of room. **An F-key needs no explanation:** `F1` already names the whole key, so the printed label and the chord are one fact in two cases (`Chord` = `f1`, `Ordinal` = `F1`, and a test derives one from the other rather than restating a literal list — the literal-list mistake is what let the old ctrl+letter chords drift from the bar). **THE THREE CANDIDATES, ALL MEASURED through a pty and bubbletea's OWN parser (validated by its delivery of plain letters and of the chord under test):** **ctrl+`<digit>` does not exist as a key at all** — ctrl+1 arrives as ctrl+q, ctrl+2 as ctrl+@, and **ctrl+3 as ESC and ctrl+8 as BACKSPACE** (a control byte is `digit & 0x1f`, so 3 and 8 collide with escape and delete), with the disambiguating protocols (xterm `modifyOtherKeys`, kitty CSI-u) **not negotiated by bubbletea and returning nothing**; **alt+`<digit>` IS delivered correctly** — measured, alt+1 genuinely arrives as alt+1 — but **the operator's emulator claims it first**: *“I use Konsole and I bet a lot of other people do as well and when I hit alt+number it moves to a different terminal tab as opposed to actually affecting orch”*, and that happens at the **emulator** level, before any program sees the byte, so no binding here can recover it (the operator's own suggestion, `shift+<digit>`, is **also not bindable**: shift+1 arrives as the SHIFTED SYMBOL `!`, so binding the digits' shifted forms would break typing punctuation everywhere; measured alongside: ctrl+alt+1 → nothing, both modifier protocols → nothing); **F1 … F7 is delivered cleanly** — measured F1 → `f1` — one keystroke, not claimed by the emulator, and colliding with nothing in this program (no screen binds an F-key). **THE BAR-HEAD ASSERTION IS STRUCTURAL, NOT A SUBSTRING SEARCH.** The removal is pinned by requiring the bar's first painted glyph to be the FIRST TAB's label, which also fails if anything else is ever painted in there (a mode indicator, a title) — where `!strings.Contains(line, "alt+")` would pass on a bar that had simply moved the text somewhere else. **`composerBypassKeys` WAS A LITERAL LIST UNDER A COMMENT CLAIMING IT WAS DERIVED** — the fourth place the old chords were written down, and precisely how a chord change can leave the composer swallowing the key instead of switching tabs: the map now holds only the non-tab chords and an `init()` adds each tab's chord **from `Tabs`**, so a chord change is a one-line change. An F-key is safe to bypass (no textarea binds one), unlike the ctrl+e that once sat in this list and silently cost the composer its end-of-line. That bypass stays covered by `TestFocusChordAndFallthrough`, which presses **every** chord derived from `Tabs` while composing. **FOUND WHILE DOING IT — the chord-expressibility assertion rejected the key my own test helper built:** **bubbletea's `KeyType` values do NOT run in source order — `KeyF1 + 1` is `ctrl+shift+end`, not `f2`.** Six of the seven tabs were being fed the wrong key by a helper that looked obviously correct, and the failure surfaced as *“tab 1 chord \"f2\" is not expressible as a key event (got \"ctrl+shift+end\")”*. The helper now lists F1–F7 explicitly, with that fact recorded where the next person will hit it. **WIDTH:** the key-labelled tier needs **98 cells**, so at 80 columns the bar drops to **titles only** — the pre-existing tiering, unchanged by this work, and harmless because **the chords are global routes that work at every width**: a narrow terminal loses the reminder, not the binding (now stated where the tiers are chosen). Removing the prefix also relieved real width pressure — **restoring it re-overflows 80 columns** (`TestOverviewTabBarFitsAt80`). **GUI: CONSIDERED, NOTHING TO MIRROR** — the GUI has **no tab keyboard chords at all** (no `altKey` anywhere under `frontend/src`; its only `keydown` listeners are Escape/arrow handling inside modals and menus), so its tab nav is pointer-only. **TESTS:** the underline is asserted **per tab** against a spelled-out SGR sequence (spelling it out, not calling `underlineTabKey`, because a test that asks the helper under test for the expected text passes when the helper is a no-op); the chord is derived from the tab's own label and checked to be expressible as a key event; and `TestOverviewIsSecondDomain` no longer pins the labels as `\"1\"…\"7\"` — it derives each from **its tab's chord**, so the key and the label cannot drift. **PROVEN BY DISABLING EACH:** a no-op underline fails with *“tab Ask Orchicon: the key \"F1\" is not underlined in the bar”* **for each of the seven**, and restoring an `alt+ ` prefix fails with *“something is painted before the first tab (want the bar to start with \"F1·Ask Orchicon\")”* **plus** the 80-column overflow. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side — requires an instance rebuild. |
| 289 | Bug fix | TUI/GUI parity | **The provider token is a FIELD on the provider form (the `s` chord is gone), and Tab toggles the execution pane's two regions instead of leaving the operator locked in the chat. Reported alongside: `alt+<digit>` tab chords are blocked by Konsole, and `shift+<digit>` is NOT a modifier.** **(1) “We should get rid of the 's' to set a token on providers and have that as just another inline field in the edit/new.”** The token was a **SECOND form** reached by an `s` chord the hint had to explain, for a value that belongs to the **same object** — and setting it required the provider to exist **first**, so the two steps were never really independent. `s` no longer answers on the providers pane and `providerTokenForm` is **deleted** (a form nothing reaches is a knob nothing honours). The token is now a `KSecret` field on **BOTH** provider forms: **CREATE** — an **optional** field (auth mode `none` is legitimate and a blank must not block the create), with **one mutation issuing both RPCs in order** (create, then `SetProviderToken` against the provider's ref id, which **IS** its id for a custom provider — `providers.CreateCustom: ID: in.RefID`) so a failure of either surfaces as one failure of “create provider”; exactly the two calls the GUI makes. **EDIT** — **blank means LEAVE IT ALONE**: a `KSecret` is never read back, so an empty field carries no information, and treating it as “clear the token” would silently wipe the credential of anyone who edited only the base URL. The field's **placeholder says whether one is stored** (*“stored (CUSTOM_X_API_KEY) — type to replace”* vs *“none stored — will be stored as …”*), the one thing a masked field cannot tell the operator, answered from the secret **NAMES** the secrets list already fetches (never values — the API does not return them). **(2) “Once we have focus on an execution detail pane, we should allow tab to tab between the Execution details and the chat prompt. Currently once you go into a chat in an execution you are locked in it and can't get out.”** **The lock-in was REAL, and it had TWO causes that had to be fixed together:** the chat **CLAIMED Tab and then DROPPED it** (the composer treated `tab` as “nothing below me”), so the key vanished entirely — verified by probe: `handled=true`, `atComposer` unchanged — and where the **SHELL** won that race instead, it moved the **TOP MENU while the caret stayed in the chat**, which reads as stuck because typing still went into the chat. `esc` and `up` always worked, but nothing advertised them, and Tab is the gesture the rest of the app uses to move between regions. Fixed with an **optional, purpose-built `OwnsTab()` hook**: while the execution **DETAIL** is focused with an execution selected, Tab (and shift+tab) reach the screen, which toggles between the transcript and the message box. It is **deliberately NOT `FormOpen()`**, which the Tab chord also consults — FormOpen additionally makes the composer advertise a **form's** keys (`ctrl+s` / `esc`), which would be a lie over a transcript. **Scoped so nothing else changes:** the **list** still walks the tab ring (the navigation model) and only the Executions detail claims Tab; `up`/`down` still walk the transcript and **down past the last block still lands in the box**, so Tab's new meaning took no capability with it. This **REPLACES** a test that pinned Tab as *“the same ladder as down”* — the operator named both keys then and has since asked for Tab to be the pane toggle — and the replacement asserts **both directions** plus that the arrows kept their behaviour, because the missing half (**tabbing OUT of the chat**) is exactly what was reported. **(3) `alt+<digit>` is blocked by Konsole, and `shift+<digit>` cannot be bound — MEASURED.** The operator: *“I use Konsole and I bet a lot of other people do as well and when I hit alt+number it moves to a different terminal tab as opposed to actually affecting orch. Maybe shift+number?”* Probed through bubbletea's own parser (validated by delivering plain letters and the current `alt+digit`): **`alt+1` DOES reach the program as `alt+1`** — so the terminal claims it at the **emulator** level, before any program sees it — and **`shift+1` arrives as `!`**, the shifted SYMBOL, which cannot be bound without breaking punctuation typing; the disambiguating protocols (`modifyOtherKeys`, kitty CSI-u) return **nothing**. Also probed: `ctrl+alt+1` → nothing/`alt+ctrl+q`; **`F1` → `f1`** (delivered cleanly); `ctrl+1` → `ctrl+q` (the known-bad control-byte collision). **This is an OPEN DECISION for the operator** — the candidates are plain digits while the **tab bar** has focus (zero interception risk, uses the numbers, but two steps), or **F1–F7** (one keystroke, but fn-row and “F1 = help” caveats). **`alt+digit` remains bound meanwhile**, since it works wherever the emulator does not claim it. **Proven by disabling each:** restoring the composer's swallow fails with *“tab did not move focus OUT of the message box — the operator stays locked in the chat”*; returning `OwnsTab` false fails with *“the focused execution detail must own Tab — otherwise the shell moves the top menu while the caret stays in the chat”*; removing the provider token form fails with *“providers must not offer a separate token form”*. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side — requires an instance rebuild. |
| 288 | Feature | TUI/GUI parity | **The tab chords are `alt+<number>` — the number the tab bar prints — the numbers are underlined, and the modifier is named once at the bar's left (`alt+ 1·Ask Orchicon 2·Overview …`); the seven `ctrl+letter` chords are gone.** The operator: *“let's do alt instead and remove the current ctrl+letters we assigned and ensure we underline the numbers in the TUI and put 'alt+ ' before 'Ask Orchicon' so people know what to select.”* **THE CHORD IS NOW THE NUMBER ON SCREEN** — `Tab.Chord` is `alt+<ordinal>`, so the key pressed and the number printed beside the tab are **the same fact**. **WHY `alt` AND NOT `ctrl` — MEASURED, NOT ASSUMED.** The operator first asked for `ctrl+number`, so before implementing it I probed what a terminal **actually delivers**, through bubbletea's own parser (the same one the running program uses; the probe is validated by its delivery of plain letters and of the existing `ctrl+o`/`ctrl+t`). **`ctrl+<digit>` is not a distinct key**: `ctrl+1`→**ctrl+q**, `ctrl+2`→**ctrl+@**, `ctrl+3`→**ESC**, `ctrl+4`→ctrl+`\`, `ctrl+5`→ctrl+`]`, `ctrl+6`→ctrl+`^`, `ctrl+7`→ctrl+`_`, `ctrl+8`→**BACKSPACE** — because a control byte is `digit & 0x1f` and 3 and 8 collide with the escape and delete bytes. The protocols that would disambiguate (xterm `modifyOtherKeys`, kitty CSI-u) are **not negotiated by bubbletea and return nothing at all**. So `ctrl+number` is **undeliverable AND would hijack escape and backspace** — not a preference, a broken binding. `alt+<digit>` arrives cleanly as `alt+1` … `alt+7` (also measured), so the operator gets the numbers they asked for with a modifier that exists. **A side benefit:** `ctrl+e` was **also** the standard readline end-of-line in the composer and in form fields, so a tab chord on it was ambiguous; `alt+<digit>` collides with nothing. **What changed:** `Tabs` → `alt+1` … `alt+7`; the composer's bypass list now holds the tab chords **derived from `Tabs`** instead of a literal copy (the chords were previously spelled out in **four** places, which is how they could drift from the bar); the numbers are **underlined** (SGR 4/24) because they are keys, not decoration; the modifier is named **once at the bar's left**, derived from the chords; and it is part of the **numbered chrome**, so a width tier that drops the numbers drops the label too — a label explaining absent numbers is five wasted cells, and `TestOverviewTabBarFitsAt80` caught exactly that (the seven-tab bar measured **81 cells at 80 columns** once the label was unconditional). **`tabLabelStarts` now gets each label's column from the SAME pass that DRAWS the bar.** It used to recover them by searching the styled render for the plain label, which returns **-1 — a silently dead click target** — the moment the label stops appearing byte-for-byte, which underlining the number does; the columns are now computed as the bar is built, from the styles' **measured** padding rather than assumed cell counts. **Tests:** the chords are asserted against the bar's **own** modifier and each tab's ordinal (not a literal list) **and** that each chord is **expressible as a key event** — the failure mode that looks fine in a string; the bar names its modifier **before** the first tab; every number carries the **literal** underline sequence; and the centred row is asserted to be exactly `pad+bar+remainder`. Every call site that pressed a `ctrl+letter` to switch tabs now reads the chord from `Tabs` via a `tabChord(id)` helper, so the tests **follow the bindings** instead of restating them, and `TestTabBarCentered`'s hardcoded *“the bar has two pad columns before the first glyph”* was replaced by that exact comparison, since the first glyph is now the modifier label and the old constant no longer described anything. **One self-inflicted catch worth recording:** the underline assertion originally read `strings.Contains(raw, underlineDigits(tab.Ordinal))` — asking the helper under test for the expected text — so it **PASSED with the helper replaced by a no-op**. It now spells the SGR sequence out, and disabling the underline fails it. That is the **sixth** test this session found passing for the wrong reason, and it was found only by actually disabling the code. **Proven by disabling each:** a no-op underline fails with *“the number is not underlined”*; returning no modifier label fails with *“the bar does not name its modifier”* and *“the modifier label must precede the first tab”*. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side — requires an instance rebuild. |
| 287 | Bug fix | TUI/GUI parity | **A form's own keys are what the composer advertises, the secret name is validated before the form closes, and the provider auth modes are the server's own two — two of these share ONE root cause: the form accepted what the server would refuse, closed as if it had saved, and left the operator with nothing.** **(1) “There is no ctrl+s - save guidance in the composer under providers or secrets like other pages, nor a 'n' new or 'x' delete guidance.”** The composer shows the active screen's `HintLine`, which is a **BROWSING cheat-sheet**. On the providers pane it reads *“n: new custom · e: edit · t: enable/disable · s: set token · c: clear · x: delete”* — and it said **exactly that while a provider form held the keyboard**. In that state every one of those six chords is **INERT** (the form consumes every key) and the two that **DO** work, ctrl+s and esc, were not mentioned. So the hint was not incomplete, it was **WRONG**. Fixed in the **SHELL** (`App.refreshComposerHint`), not in each screen: the one place that already knows a form is open for **EVERY** screen — the same `FormOpen` probe the Tab chord uses — so a screen that adds a form later cannot forget. While any form is open the composer now names only the form's keys: `ctrl+s: save · esc: cancel · tab/↑↓: next field · enter: next field (or open a picker)`. The n/x half was **already present** and is now pinned by a test that renders the pane's hint for both providers and secrets. **(2) “When editing providers, I noticed for auth it says none, bearer, and api-key, yet in the GUI and for all current providers created, it actually says 'token'. This should be changed to match that.”** The form offered `none`, `bearer`, `api_key`. The server accepts exactly **TWO** values — `providers.AuthModeNone` (“none”) and `providers.AuthModeToken` (“token”) — and **rejects everything else** with `auth_mode must be "none" or "token"`. So **BOTH extra options were unusable** AND `token`, the value the GUI offers and every existing provider uses (live table: **2× token, 1× none, ZERO bearer/api_key**), **could not be chosen at all**. This is the **SECOND cause of report 4**: picking `bearer` produced a provider the server refused. The options are now built from the server's **exported constants** with the GUI's own label (*“token (auto-writes CUSTOM_&lt;REF&gt;_API_KEY)”*), and **`providers.ValidateAuthMode` is EXPORTED** so a test can run the **SERVER'S validator over the form's own options** — the same drift-proofing as `validSecretName`. A stored value outside `{none, token}` is **normalised to `none`** when the edit form opens, because a select whose Initial is not one of its Options fails validation with *“unknown option”* and the provider would be **UNEDITABLE by any route**. **(3) “I tried adding a secret and a provider in the TUI and saved it, yet they were never actually created.”** For a **SECRET** the cause is the name rule: the store requires **`^[A-Z][A-Z0-9_]+$` — UPPERCASE ONLY** — and the form's only check was `Required`. So `my_token`, the natural thing to type, **passed the form, the form CLOSED as if it had saved, and the server rejected it a moment later**: a closed form, no row, and a transient dock error. The form now validates with the secrets store's **OWN** validator (`secrets.ValidateName`), so the error is shown next to the field **before** the form closes, and the message says what is wrong in human terms rather than quoting the regex. For a **PROVIDER** it is cause 2. **The tests for this press the REAL KEYS** — `n` to open, characters to type, `ctrl+s` to save, through `Model.Update`, the same entry point the shell calls — and then run the command that comes back. The existing coverage drove the **FORMS** (`Set` the values, call `Submit` directly), which proves the form and proves **nothing** about whether ctrl+s ever reaches it; every one of those tests stays green while the operator's key goes nowhere — the **fifth** time this session a test was found pinning a path rather than the behaviour. The provider fixture used `AuthMode: "bearer"`, an invented value the server rejects, so it was pinning the old (wrong) option list; it now exercises `normalizeAuthMode`. **Tests:** the secret saves through the real keys and the form **closes** on success; a lowercase name is **refused with the form still open** and an error that says “uppercase”; a valid uppercase name still saves; a provider saves through the real keys; **every** auth mode the form offers passes the server's validator, `token` and `none` are offered, and `bearer`/`api_key` are **gone**; the create form's initial auth mode is one of its own options; and the pane hints for providers and secrets are rendered and asserted. **Proven by disabling each:** removing the secret-name validator fails with *“the form CLOSED on an invalid name — that is the defect: the operator sees a close and no row, instead of the reason”*. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). |
| 286 | Feature + Perf | TUI/GUI parity | **The executions page loads in ONE enrichment query instead of ~5,900, each row says what actually ran, and a ROLLING REFRESH WINDOW keeps every screen current — the answer to “are we currently implementing a rolling refresh window?” was NO.** **(1) “The initial execution page load is pretty slow. We need to find out how to make this close to instant.”** **Root cause, and I caused it:** `ListExecutions` enriched the page with **TWO QUERIES PER ROW** — a usage `SUM` and a load of the step run's `_prompt`. That was survivable while the list fetched one page of 100; once every list fetched itself **WHOLE** (the page concept is retired, row 283), the executions page pulled all **2,959 rows** and issued **~5,918 enrichment queries for a single screen load**. Measured against the live tenant: **2,959 rows × 2 = 5,918 queries before, 1 after** (the batched query itself runs in **47ms**). Two changes: `db.SumUsageForExecutions` sums the **whole page in one grouped query** — and is verified to return **exactly** what the per-row form returned, for every id, against the real database, because a batching change that silently mis-attributed a total would be worse than the slowness it fixed — and **`enrichSystemPrompt` is no longer called from the list**: it is a **detail-only field** (the TUI never reads it; no GUI list consumer does either — only the execution detail route and the worker detail, both via `GetExecution`), it loads **tens of kilobytes of JSON per row**, and the list was paying for it on every row of every page. `GetExecution` still enriches it, so the **detail is unchanged**. **(2) “The titles of the executions really don't tell me anything besides ID and status. I would like the workflow name and work item associated with it.”** The row already carried `WorkflowName` (populated by the query's `LEFT JOIN`) and **nothing was reading it**; the work item has **no proto field at all**, and the row's own `TaskName` is **never filled by any query** (declared, documented as “denormalised via LEFT JOIN at query time”, and neither the join nor the scan exists). Regenerating protos is not available here, so the title is composed **client-side** from the two things that *are* available: the execution's `WorkflowName` and the work item title from the **shared name index** (`names.go`) the runs pane already uses — no proto change, no extra query, same cache. Rows now read **`<workflow> · <work item>`** with Meta as **`<status> · <worker name>`**, degrading to whatever is known and falling back to the **id last**, because a blank line in a list is worse than an id — the operator cannot even tell which row it is. **(3) “when you send a message, nothing happens unless you move away from the execution and then come back ... We need this to be refreshing every few seconds. That also brings up a good point about all of the other screens. Are we currently implementing a rolling refresh window?”** **The answer was NO.** Before this the TUI contained **exactly one timer** (the clipboard toast): a pane could repaint **only** on a NATS event poke, and only **five** screens even subscribed. Everything else waited for an explicit `r`. **Why that produced the reported symptom:** a follow-up's reply is written to the **durable** transcript, and the live `execution.text` events that would poke the pane come from the TaskReconciler's **dispatch** callbacks — the **follow-up path does not go through them**. Nothing poked, nothing repainted, and the reply stayed invisible until the operator left the screen and came back, which re-fetched the durable side. A timer does not care which server path wrote the data. **The window** (`internal/tui/refresh.go`): one tick chain, armed at `Init` and re-armed by its own handler, refreshing the **active tab's visible source and its open detail** every **5 seconds** — the cadence the GUI polls at, so the two clients agree on freshness. **Event pokes are kept as-is**: they are what makes a running execution feel instant, and the timer is the safety net for everything the server does not emit. Deliberately **cheap and polite**: only the visible source (Control carries nine, and every list now fetches whole — reloading all of them every 5s would recreate the load problem just fixed); it **refuses while an editor or modal is open**, because repainting under a form the operator is typing into is worse than a stale row; and the cursor survives because `Table.SetItems` re-seats it **by id**. Every kit2 screen gets the hook **for free by embedding `Base`**, which is what makes this cover *“all of the other screens”* rather than the ones someone remembered to wire. **Tests:** the batched totals equal the per-row form for every id and absence means “no records” rather than zero (against the real DB); the title shows both names, degrades to what is known, is never blank, never leaks the proto enum, and the fetch populates it through the screen's own path (which required teaching the fake plane `ListExecutions` so the path is genuinely exercised); and the window refreshes, **re-arms**, follows the active tab, ignores a stale tick **without multiplying chains**, pauses under an overlay and **resumes afterwards**, and keeps running when a screen refuses. **Proven by disabling each:** reverting the title fails with the operator's own wording (*“the row still shows only its id”*); removing the re-arm fails with *“re-arm produced cursor.BlinkMsg, want a batch containing the next tick”*; the batched-vs-per-row equivalence is #1's proof. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side + API — requires an instance rebuild. |
| 285 | Bug fix | TUI/GUI parity | **The execution message box is a FIXED FOOTER now — it had been installed and INVISIBLE, twice, for two different reasons, which is why the fix comes with tests that assert VISIBILITY rather than existence.** The operator: *“I STILL don't see a chat prompt inside an execution in the TUI.”* **The box existed both times it was reported missing.** Before changing anything I verified it: for a 60-block transcript the composed body's **LAST line** was correctly `follow-up› Ask a follow-up — it continues this conversation — enter sends`. It was there. It was simply **never on screen** — and every assertion I had would have passed while the operator saw nothing. **(1) It was appended to the detail BODY**, at the end of the transcript. A real transcript is much taller than the pane and the **viewport opens at the TOP**, so the box sat ~65 lines below the fold. That is the wrong place for an input at all: anything inside a scrollable region can be scrolled away from the operator who needs it. **(2) Making it a footer was necessary and NOT sufficient.** The host `Panel` renders exactly `Height-2` content rows and **CLIPS the overflow from the TAIL** — which is where a footer lives — so the band was installed, correctly worded, and **discarded by the clip**. The pane's height budget had to account for every row `Detail.View` writes (title + fields + the body's separator and viewport + the footer's separator and band); the viewport now gets what is **LEFT**, which is what makes the band always visible. **What changed:** `screenkit.Detail.SetFooter` — a **fixed band below the scrolling region, outside it**, so the transcript cannot displace it, with its height budgeted in `ensureVP`; `kit2.Base.SetDetailFooter`/`DetailFooter` as the pane API; `installComposerFooter` — **one idempotent, state-driven call** whose placeholder names the act (message a live worker / ask a follow-up) and which **clears the band when another source's detail is showing**, so the prompt cannot linger over a flow view offering to message a worker; and it is installed from the **`onDetail` hook** — the moment the pane knows *which* execution it is showing, and **the only hook that fires on the ordinary fetch path**, which is the second half of the bug: the base writes the pane from a `detailMsg`, so a footer installed from the screen's own paint helper **never ran on a real fetch at all**. The composer is no longer in the body. **Tests** (`composer_visible_test.go`), all asserting the **RENDERED PANE** rather than the body: present with a 60-block transcript **and near the BOTTOM of the pane**; present for both a terminal and a live execution with the band reading `follow-up`/`message` as appropriate; present when there is **no transcript at all** (asking why is what an operator wants on a silent run); **absent from the BODY** (it would scroll away — the original defect); cleared when another source's detail shows; and the caret drawn only when the box actually holds the keyboard, so walking into it gives feedback. **Proven load-bearing by disabling the height budget:** the tests fail with the operator's exact words — *“the message box is NOT in the rendered pane — this is the operator's report (\"I STILL don't see a chat prompt inside an execution in the TUI\")”* — **while the footer is demonstrably installed** (`footer="follow-up› … enter sends"`). Installed **and** invisible, at the same time: that combination **is** the bug, and it is why the assertion had to move from “is it set” to “can it be seen”. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side — requires an instance rebuild. |
| 284 | Bug fix | DB / tests | **`DeleteProject` now cascades the telemetry tables it was silently leaving behind, DB-backed fixtures clean up after themselves, `SeedDevWorkers` takes a tenant instead of hardcoding `tnt_dev` — and the idempotency probe (the one cascade test with no `t.Cleanup`) got its own teardown after it was caught leaking in exactly the case it exists to test.** **(a) DeleteProject left the telemetry tables behind.** The cascade covered the whole WORK hierarchy — step runs, runs, versions, workflows, dependencies, work items — but **not** the telemetry tables, and **none of them declares a foreign key to projects**, so a missing delete was **silent** rather than an error. Measured on the live dev tenant after every project had been removed: **1,264 orphaned `worker_executions`, 150 orphaned `recovery_executions`, 9,959 orphaned `usage_records` and 132 orphaned `recurring_run_history` rows**, every one carrying a `project_id` that no longer existed. That is **not a test-only bug** — deleting a project in the GUI left all of it; the DB-backed tests, which create and drop projects constantly, left the rest. Nine tables carry a `project_id` and only ONE (`project_mcp_servers`) has a real FK, which is exactly why the delete has to be explicit. The cascade now runs leaves-first through **16 steps in one auditable table** (`execution_session_parts`, `usage_records`, `worker_executions`, `continuation_plans`, `recovery_step_runs`, `recovery_executions`, `recurring_run_history`, `workflow_step_runs`, `workflow_runs`, `workflow_versions`, `workflows`, `work_item_attachments`, `work_item_dependencies`, `work_items`, `project_mcp_servers`, the project), each scoped by **both** tenant and project, with a comment noting that a missing step is invisible by construction because the FKs that would catch it do not exist. Guarded by `TestDeleteProjectCascadesEveryOwnedTable`, which builds a project owning **one row of every kind the cascade is responsible for**, runs the real delete, and asserts nothing survives — so adding a table to the schema without adding it to the cascade shows up here rather than as orphans in production. **Proven load-bearing against the real database**: disabling the `worker_executions` step (keeping the param count valid) fails with *“worker executions SURVIVED DeleteProject (1 row(s)) — the cascade does not cover it, and with no foreign key to projects nothing else will ever clean it up.”* **(b) The fixtures clean up after themselves — 57 call sites.** A new `db.CleanupProject(t, pool, tenant, projectID)` registers a `t.Cleanup` that runs the SAME `DeleteProjectByID` the product uses, so a fixture's teardown is **exercised by every test that uses it** — if the cascade regresses, the fixtures start leaving orphans in the test tenant rather than somewhere nobody looks. Failure is **logged, never fatal** (a `t.Cleanup` cannot fail a test that already passed, and a teardown error should not turn a correct test red). It is one line at each call site, which is why it has **57 call sites against 54 creates**. **Audited for the same gap: every project-creating test file that COMMITS — and therefore can leak — already has a cleanup**; the shared `newSequenceTestEnv` helper covers the 44 sequence tests, and files with no cleanup reference all roll back and never commit, so they create nothing durable (`e2e_prompt_test.go` was a **false positive** from counting cleanup references alone). **(c) `SeedDevWorkers` takes the tenant.** It hardcoded `tnt_dev` in **five** places, so every test calling it wrote into the production tenant **unconditionally** — including from *other* tenants. The tenant is now a parameter, and the callers pass their own (`seedTenant` in the seed tests, `cfg.DeploymentTenantID` in the real bootstrap). That config value **defaults to `tnt_dev`**, so the production bootstrap path is behaviourally unchanged — verified. **(d) The idempotency probe leaked, and it was the one test without a teardown.** Found while proving (a): a deliberate-failure probe left a `zz_idem_probe_*` project behind, and a second probe from an earlier run was already sitting there. The cause is that `TestDeleteProjectIsIdempotent` — the test that asserts “deleting something already gone is not an error” — had **no `t.Cleanup` at all** and relied on its own two `DeleteProject` calls having succeeded, so any failure before them (or in the second one) skipped the cleanup entirely. **I had leaked those two myself while proving the fix**; they were removed (`DELETE 2`) and `tnt_dev` is back to **exactly one project**. The fix is a `t.Cleanup` running `DeleteProjectByID`; **proven by inserting a deliberate `t.Fatalf` immediately after the probe was seeded — the test now FAILS and leaves ZERO residue, where before it left a project behind.** **Final state:** `tnt_dev` has **zero orphans** across executions, recoveries, usage and run history. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). **NOTATED FOR LATER (operator's call — explicitly deferred):** after this branch merges, **audit the 80 new test files it added and prune the ones that only assert message wording or restate the type system**, keeping the behavioural ones. Rationale recorded: this session found **five** tests that had been *pinning defects* rather than catching them, which is the argument for pruning bad tests — not for dropping tests. The suite is **2,322 tests in ~16s** (and the frontend's 480 in ~3.5s), so the test suite is **not** what makes `full-rebuild` slow; the cost is in `container-rebuild` and `gen-check`. |
| 283 | Feature + Bug fix | TUI + DB | **The page concept is retired — every list fetches itself whole, so holding DOWN reaches the end — and the three cursors that contradicted their own ORDER BY are keyset cursors now; plus the test-fixture residue in `tnt_dev` is cleaned out (99 projects, 1,078 work items, 24 runs, 64 step runs, 39 executions).** **(1) \"I don't understand concept of 'pages'. It's mentioned in the TUI, yet I never page to see more work items. I just hold the down arrow and I should be able to see all of them. Same goes for any other item in the TUI. Pages are unnecessary imo.\"** They are right from the operator's side: the pane **IS** a scrolling list, and holding down must reach the end of it. `kit2.Base.loadSource` now **walks the RPC's cursors internally**, once, for every source, and **never surfaces a next-page token** — which is what removes *\"more pages: press f\"*: not a hidden key, but the **absence of anything left unfetched**. Retired with it: the base's `f` pager binding (it now **falls through** rather than refusing, because a refusal for a concept that no longer exists is noise — and because a screen may legitimately want `f`, as the execution screen's message-box stub does); the `(more: f)` / `(more pages: press f)` markers in `kit2.Table` and `screenkit.List`; and `f: more pages` in every hint line (ask, automation, control, enforcement, execution, overview). The load is bounded (`maxSourcePages`) purely so a server that never clears its token cannot spin inside one command — **a guard on a broken server, not a display limit**. A failure on a **later** page returns what was already fetched (a partial list beats an empty pane); only a first-page failure is an error. **(2) The cursors — the same defect class, fixed at every call site.** Fetching everything made the cursor bugs reachable for **every** list rather than just the pages an operator happened to press `f` into, so they are fixed at the source. All three had it: a bare `id > $n` cursor against a **different** ordering, so page 2 both **repeated** and **SKIPPED** rows. **`ListWorkItems`**: page 1 was chain-ordered (`sort_order NULLS LAST, created_at, id`) while later pages were ordered by id **alone** — measured on the live dev tenant (327 matching items, 200/page) it returned 200 + 14 rows with **9 duplicated and 122 MISSED**, i.e. the list was showing **205 of 327**; it is now a keyset on the **COALESCE**d `(sort_order, created_at, id)` tuple, exact in both directions and keeping NULL `sort_order` rows last — **re-measured with the new predicate: 200 + 127 = 327 distinct, 0 duplicated, 0 missed**. **`ListProjects` and `ListWorkers`**: default order is `created_at ASC` with an `id >` cursor and it is **not latent** — the live tenant has **499 project pairs and 1213 worker pairs** whose id order **DISAGREES** with their `created_at` order (an id is minted at create time while `created_at` is assigned by the database, and a bulk import or restored dump reorders the two); both are keyset cursors on the effective `(sort key, id)` tuple now, so an explicit `sort_by` still pages exactly. The pattern is the one **`ListExecutions` already used** — *\"This replaces the old bare `id > $n` cursor, which contradicted the default created_at DESC ordering and re-returned page 1 on page 2\"* — that commit fixed one call site; this fixes the **class**. **Tests** (`kit2/pages_retired_test.go`): every page is collected behind one screen fetch (the **last** item is present, which is the whole point of holding down); **no token reaches the table**, so no marker can render; a single-page source still takes one call; a later-page failure keeps the rows already fetched while a first-page failure is still an error; and `f` issues no load. **Proven by disabling each half**: capping the loop at one page fails *\"collected 10 items, want all 30\"*; re-propagating the token fails *\"the fetch reported a next-page token … the table holds a next-page token\"*. **(3) The fixture residue in `tnt_dev` — with a correction I caught before deleting on it.** My earlier report of **\"99 of 100 projects are fixtures\"** was **WRONG**, because I filtered by `project_dir LIKE '%.gotmp/%'` instead of \"not the real project\"; the true figure in `tnt_dev` was **626 projects, exactly ONE real**, and the largest group (`Parent Test` ×298) was created **2026-08-25** — it predated the batch I had been looking at. **I therefore re-audited before deleting and the counts then reconciled** (625 non-real projects, 1,078 items, 75 runs, 218 step runs, 39 executions). Deleted in dependency order inside **one transaction** and **backed up first** to `.gotmp/backups/cleanup_*.csv`: **99 fixture projects, 1,078 work items, 75 runs, 218 step runs, 39 executions, 1 mcp reference**. (`DELETE 99`, not 625, because the **remaining 526 fixtures live in OTHER tenants** — `tnt_validate_parent` ×304, `tnt_retry_failed` ×21, `tnt_force_progress` ×8, … — and **row-level security keeps them invisible to the real tenant**; the delete was correctly scoped `tenant_id='tnt_dev'`.) **One row was deliberately NOT deleted, and it is worth flagging:** `01KYQXGK8B9K3KM9NWKHD1SVH8`, an epic titled **\"Ask Orchicon\"** in status `ready`, whose `project_id` points at a project that **no longer exists**. It is **not** test residue — it was created **2026-07-29** at bootstrap, two minutes before the live epic `01KYQXRADTGKMHT7PD09MJ63HE` (now version 19, with dozens of children), so it reads as a **survivor of a project recreate/rename**, and it has no children, no runs and no executions and has not been touched since the day it was made. It is not mine to delete and it is a real-shaped record, so it is **left in place with its backup taken** (`.gotmp/backups/cleanup_orphaned_items_*.csv`) for the operator to judge. **Result:** `tnt_dev` is now **1 project, 363 work items, 359 runs** — the real `Orchicon` project only, and the API's `ListProjects` returns **count 1, not truncated**. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). |
| 282 | Bug fix + Feature | TUI/GUI parity + DB | **The execution transcript now collapses like the GUI's and the message box is INLINE at the bottom instead of a ctrl+s modal; and the 156 phantom adapters in the dev tenant are gone — because `CreateAdapter` was silently dropping the heartbeat a caller passed, which made every test adapter a permanent dispatch candidate.** **(1) The message box.** The operator: *“You said in executions 'f' was a follow up yet the interface says 'f' is more pages. I hit f and it brought up a follow up prompt. I typed in a question and hit ctrl+s to save it and nothing happened. This doesn't seem to be working and frankly is very weird interface wise. Not very intuitive. I would rather a chat box be at the bottom of the execution and you can click into there or gain focus with the down arrow key or tab and then type in your response and hit enter to send it. (Interjects should work the same way on live executions)”* — **TWO defects behind one report, and the first explains the confusion exactly.** `f` **WAS DOUBLE-BOUND**: the screen's follow-up box claimed it AND `kit2.Base` binds it to the pager whose own label — rendered in the detail pane's **title** — reads *“more pages: press f”*. The operator was reading the pane correctly; the pane was advertising the other meaning. And the modal was the wrong **shape** (a form to open with a chord, then submit with ctrl+s — which is why “nothing happened”: the gesture they reached for, enter, was not the submit). The box is now a **POSITION in the transcript, not a chord**: walk DOWN past the last block and you are in it; `enter` sends; `esc` leaves with the draft intact; `up` returns to the blocks. It is **ONE box for both acts**, decided by the execution's state rather than by which key was pressed — `SendExecutionMessage` while running, `ContinueExecutionSession` when terminal — which is the GUI's own composer (a single textarea whose placeholder changes) and the operator's *“Interjects should work the same way on live executions”*. `i` and `f` are retired to **explaining stubs** that land the operator in the box, because an operator who learned them deserves an answer rather than silence — and because the screen must stop claiming `f`. **(2) Collapsing.** *“The executions are incredibly long. In the GUI, all blocks are auto collapsed. We should be collapsing all conversation blocks, tool calls, etc. and a user can move down the line using tab or down arrow and hitting enter on a block should collapse or expand it.”* The transcript was one pre-rendered string, which **cannot be toggled by anything**. It is now a list of **BLOCKS with a cursor**: tool calls, thinking and artifacts start **COLLAPSED** (the GUI's rule — *“tool calls are compact collapsible cards, reasoning is collapsed”* — and its `useState(false)` default), prose and **ERRORS** stay open (collapsing the reason a run failed would hide the thing being looked for), and a **LIVE** block is always open because watching it arrive is the point of a live view. `enter` toggles the cursor's block; up/down and tab/shift+tab walk. The collapsed summary keeps the tool name **AND the first line of its output**, which is what makes a collapsed transcript scannable rather than merely shorter. Expansion is remembered **PER BLOCK KEY, not per index** — a live transcript appends blocks and a re-fetch renumbers them, so an index-keyed map would open the wrong block after the next event — and is cleared when the pane shows a different execution. The cursor **follows into view** (the pane scrolls to it), because a 17,574-part transcript is far taller than the pane and toggling a block you cannot see is the defect the run's step flow already had to solve. **(3) The adapters — the data was the symptom, and the cause was a one-column omission.** The operator: *“There are numerous opencode and orchicon adapters in my prod DB now. I am not even sure which ones are the real ones at this point. This needs to be cleaned up to preserve just the one orchicon and one opencode adapter that are the legit system ones and not from test data.”* `CreateAdapter`'s INSERT listed every field **except `last_heartbeat_at`** — declared on `AdapterRow`, passed by callers, and only **read back** from `RETURNING` — so a caller that set `LastHeartbeatAt: &now` silently got a row with **NULL**. That mattered far past cosmetics, because the dispatcher's candidate query treats NULL as **FRESH** (`last_heartbeat_at IS NULL OR last_heartbeat_at >= now() - ttl`), so every fixture created an adapter that could **never expire**; and since `selectAdapter` filters by free capacity and then breaks ties on the **adapter ID**, they entered the **same candidate pool** as the real adapter — **99 opencode candidates (1 real + 98 phantoms) and 59 orchicon (1 + 58)**. **The damage is in the data:** **993 real executions were dispatched onto `localhost:0` phantoms** — every one from 2026-09-06 to 09-09 succeeded, every one from **09-10 onward is `failed`/`failed_to_start`**, which is the point a phantom started being selected over the real adapter. Fixed in three parts: the INSERT persists the heartbeat; a new **`DeleteAdapter`** exists so a fixture *can* clean up (there was no way to remove an adapter row at all, which is why 156 accumulated); and the five dispatch fixtures now create their adapter through **one helper** that registers a `t.Cleanup` deleting it and deliberately does **not** set a heartbeat (a test adapter that heartbeats competes with the real one for the full 60s TTL; one left NULL still dispatches and is removed within the test's lifetime). **Cleanup applied** (dev tenant `tnt_dev`): **156** fixture rows deleted, backed up first to `.gotmp/backups/runtime_adapters_pre_cleanup_*.csv`; what remains is exactly the two legit system registrations, both **heartbeating**, and the candidate pool per kind is now **1**. **NOT changed, deliberately:** the 1634 executions whose `adapter_id` points at a row that no longer exists — only **13 distinct ids** are involved and one group (252 executions, 2026-08-30..09-03) was **already dangling before this delete**, so those references were never reliable history; the one consumer that resolves them (`server.go`'s session-transport lookup) explicitly falls through to worker-path resolution (*“never leave a mid-run RPC unresolved”*) and the DB has no FK, so nothing breaks and rewriting history to hide a past dispatch would be worse than leaving it true. **Defects found by tests while building (1) and (2):** the transcript's prose printed **twice** (its summary was its first line and its body contains that line) — caught by asserting the collapsed transcript is ~one line per block; a new action-bar row advertised `enter` as an **action key**, putting it in the action-by-key table so it fired on a prose block too — the exact *“enter did nothing”* experience the item exists to remove; and the transcript keys were gated on **`DetailID`**, which is set by the **detail landing**, so the composer was dead between selecting a row and the detail resolving, which reads as *“the chat box does not work”*. **Verified:** every fix proven by disabling it — reverting the collapse default fails with *“a tool block starts EXPANDED”*; removing the down-past-last-block landing fails with *“walking down past the last block did not reach the composer”*; reverting the prose fix fails with *“got 10 lines for 6 blocks”*; and reverting the `CreateAdapter` INSERT fails with *“the returned row lost the heartbeat the caller passed”* (run against the real database, creating and deleting its own row). gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). |
| 281 | Bug fix + Feature | TUI/GUI parity | **A step marked succeeded by the manual FORCE-PROGRESS escape hatch now says so, in the flow row and in the jump refusal — because it is the one case where a green status does NOT mean the work happened.** The operator asked the question that mattered: *“How can a Senior Software Engineer both be successful and yet have no execution? That doesn't make any sense. Every worker step that fires should have an execution behind it. Was it deleted?”* **The invariant is real — the ENGINE cannot produce that state.** Every writer of `StepRunSucceeded` is either `pollTaskStep` (for a step that HAS a linked execution) or a **non-worker kind** (`parallel` / `end` / `decision` / `loop` / passive `succeedStep`), and `dispatchStep` sets `worker_execution_id` and `status=running` in the **same transaction** that creates the execution — the code says so itself: *“Done inside the same transaction so a worker-step run never points at an execution that doesn't exist.”* **Three things DO produce it, read out of the live dev database rather than reasoned about.** **(1) FORCE-PROGRESS — the real cause in real project data.** All three “succeeded task step with no execution” rows in the Orchicon project carry `{"_forced": true, "_forced_at": …, "_forced_reason": "manual force-progress"}`. `ForceProgressWorkflowRun` — the manual escape hatch for a run wedged `running` — marks active non-terminal step runs succeeded with an `EndedAt` and **no execution ever created**, and annotates the result with `_forced` *precisely so a client can tell “this was a manual override, not a real execution success”* (the comment's own words). **Nothing in the TUI read that marker**, so a step that never ran rendered as an ordinary green step. The reconciler's own comment records the harm: *“a force-progress marked the DevOps PR step succeeded without ever dispatching it, and the PR was never merged.”* The marker is now read (`WorkflowStepRun.result` **is** in the proto and `stepRunRowToProto` copies it verbatim), the flow **row** says `no execution — succeeded by manual force-progress`, and the **refusal** says the step “was marked <status> by the manual force-progress escape hatch, without being dispatched”. **(2) DELETION — yes, it can happen.** `DeleteExecution` / `DeleteExecutionBatch` hard-delete the row (and audit `execution.deleted`), but **nothing clears the step runs that point at it**: live, **47 step runs** carry a `worker_execution_id` whose execution no longer exists, against **zero** `execution.deleted` audit events — so the deletions visible here did **not** come through the product path (45 of the 47 are test-fixture rows; the other 2 are a force-progress and a decision step, both in a “Force Test” project). The refusal now **separates the two situations it had been conflating**: a step that RAN and lost its link says the link is gone and the execution cannot be opened, rather than implying the step never ran. **(3) TEST FIXTURES — why the operator saw it at all.** The run he was reading (`01M2B8D9M0H8E59RKNADW9QH33`, the “Concurrent branch dispatch ticket” run) was written by `TestBranchWorktreeDistinctPerStepRun` in `internal/scheduler`, which **seeds `step-sse` as `StepRunSucceeded` with an empty result** so the reconciler will dispatch the parallel branches — it never runs the step. **The tell is in the row itself**: `succeeded` with `started_at` NULL, `ended_at` NULL, result `{}`, `worktree_status` `pending`. Those fixture rows live in the **real tenant DB** (`tnt_dev`) because `approvalTestPool` opens whatever `ORCHICON_TEST_DSN` points at and the tests clean up only the **pool**, never their rows — **19 test-fixture projects and 8 test-fixture runs** are sitting in the dev tenant, which is also what pollutes the Work Items and Executions lists. Also found: `internal/workflow/retry_failed_test.go` fabricates step runs with **random execution ids** (`db.NewID()`) — the source of the 45 dangling links — and `purgeScanTenant`'s tenant-wide `DELETE FROM worker_executions WHERE tenant_id = $1` is **rolled back**, so its comment claiming it “deletes residue” is wrong about its own effect (harmless, but misleading; the same class of defect as the `atlas.sum` churn — an environment artifact that should not be trusted as evidence). **Tests:** the forced marker read from the result, disclosed in the row, and named in the refusal; and its **absence** proven for empty / `{}` / an ordinary `_prompt` result / malformed JSON / `_forced: false`, so the disclosure cannot over-fire. Both directions proven by disabling the read (the row stops disclosing and reads as still-dispatching) and by forcing it always-true (the unforced test fails). gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). |
| 280 | Bug fix + Feature | TUI/GUI parity | **Five operator reports after a rebuild, each traced to a concrete defect — two of them measured against live rows (workflow run `01M2B8D9M0H8E59RKNADW9QH33`, execution `01M28MA4G8X1H345RZ9N08VEPP`) rather than guessed at.** **(1) \"I think it is showing the results in reverse order. It is showing the PR Reviewer and QA Engineer as first in the list.\"** The run's steps were sorted **ALPHABETICALLY BY STEP ID**. The live SDLC run's steps are `step-sse, step-parallel, step-branch-a, step-branch-b, step-loop`, so an id sort yields branch-a, branch-b, loop, parallel, sse — PR Reviewer, QA Engineer, Loop Decision, Parallel, Senior Software Engineer, which is the report **exactly**. No id encodes when a step runs, and the semantic ids (`step-devops-pr`, `step-approval`) sort just as arbitrarily. The server's own order (`created_at ASC, id ASC`) does not help either: every step run of a run is created in **one statement**, so they share a `created_at` and it collapses to id order too. The order now comes from the **WORKFLOW DEFINITION** — the version the run executed, its steps in flow order via the same Kahn sort the workflow view already draws (`flowOrder`), which is also what the GUI maps run status onto. A step the definition does not name is **appended rather than dropped**, and a version that cannot be read falls back to the run's arrival order — deliberately **not** to the id sort, which is the defect. **(2) \"hitting enter on the run immediately jumps to the execution. You should have to hit enter FIRST on the workflow run, THEN it moves to the detail pane and then from there enter should select steps.\"** This **reverses the previous turn's change**, which claimed `enter` unconditionally while a flow was drawn. That reasoning misread which enter the rows advertise: they describe what enter does **once the detail holds the focus**, and the first enter is what GIVES it the focus. Claiming the key from the list also broke the list, where enter is how a run is opened at all. Enter is a **two-stage gesture** again, and the test asserts **both** presses, because a test of the second alone would pass with the first broken. **(3) \"I can hit enter on the QA Engineer and PR Reviewer step/execution, but not the Senior Software Engineer.\"** Ground truth: `step-sse` is `status=succeeded` with `worker_execution_id` NULL — it genuinely has no linked execution, so the jump cannot work. But the refusal said it \"has not been dispatched\", which is **false** for a step that plainly succeeded and makes the operator distrust the status printed right beside it. The reason is now derived from the step's own state: a finished step is told it finished but has no linked execution *and where to look*, a blocked step is told it has not been dispatched, and a non-worker step (parallel marker, approval, loop decision) is **named as one that has no execution of its own** — for those a missing execution is expected, not suspicious. **(4) \"I don't think it is showing the entire execution. In fact, I don't think any of the executions in the TUI are showing the entire execution.\"** The transcript fetch asked for `Limit: 200` with **NO `before_seq`**, which the server answers with the **FIRST 200 parts** in sequence order (`db.ListExecutionSessionParts`: `ORDER BY seq` when `before_seq` is unset). The reported execution has **17,574 parts**, so the pane showed its opening 200 and nothing else — no late tool calls, no final answer, no follow-up reply. Every execution looked truncated **because every execution was**. The GUI solves this by asking for the **TAIL** (`beforeSeq: 9223372036854775807n, limit: 10000`, then `[...parts].reverse()` to chronological); the TUI now sends the **identical request** and reverses, so both clients agree about how much of a transcript is \"the transcript\" — the limit is the GUI's number rather than one of my own for exactly that reason. **(5) \"there is no context, token, or cost information being shown\"** and **\"I don't see the follow-up chat box to kick off additional questions to the worker. This feature is in the GUI and should be in the TUI.\"** The token/cost TOTALS were correct but being **BLANKED** by the session repaint (the composition defect fixed last turn); the CONTEXT panel had **no source at all**. The GUI is explicit about where its numbers come from and this reproduces it: `usage_records` is the authority (\"the row columns are write-never\"), the percentage is measured against the model's **real window**, and the numerator is the **PEAK single-step FRESH token count** (prompt+completion+reasoning, cache **excluded** — \"cache reads are re-sends of already-counted tokens\"), because the sum across turns grows without bound and would read as thousands of percent on a long run. The panel shows the model, the context %, a bar, the bucket breakdown (in/out/reasoning/cache read/total), the turn count and the cost, each line **omitted when its numbers are zero** so a block of zeroes never reads as \"it used nothing\". The window resolves through `modelpick.ContextWindow` — the same resolver the model picker uses — so there is no second, drifting table of windows, and the fetch follows the todo list's **staleness** rule so it cannot become the refresh loop that was just fixed. The follow-up box was a separate, simpler defect: `f` was bound and handled, but `actionsForSelection` returned **nil for a non-live execution** — so on a **finished** execution, which is the normal thing to open and exactly what a follow-up is FOR, the **entire action bar was empty** and the gesture was undiscoverable. Cancel is still offered only for a live execution; the bar now lists what the pane can DO, with each row explaining its own precondition. **Tests:** the authored order against the real SDLC step set with its real `depends_on` chain (a fixture with steps named a/b/c would have PASSED against the broken sort, which is why the live ids are used), the unnamed-step append, the arrival-order fallback, the malformed-JSON degradation, the two-stage enter, the reasoned refusals per step state, the tail request and its chronological reversal, the context panel's contents and its absence without usage, the peak-not-sum numerator, the bar's clamping (which also caught a **real bug**: `bar[:width]` slices BYTES and both bar glyphs are three bytes wide, so a 10-cell pane got 4 cells of bar), the panel surviving a session repaint, the staleness gate, the usage landing requesting nothing, and the follow-up being present in the bar. **Verified:** every fix proven by disabling it — removing the authored order reproduces the report's exact list `[PR Reviewer QA Engineer Loop Decision Parallel Senior Software Engineer]`, claiming enter unconditionally jumps on the first press, dropping the context panel empties it from the body, reverting the refusal wording makes a succeeded step claim it was never dispatched, clearing `before_seq` asks for the head again, and restoring the non-live early return empties the bar. gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side — requires an instance rebuild. |
| 279 | Bug fix + Feature | TUI/GUI parity | **Three operator reports after a rebuild: the step→execution jump did not land, the Executions pane flickered and showed stale detail, and text could not be selected or copied on any screen.** **(1) "hitting enter on a step is not taking you to the execution page for that step" had TWO independent causes, and fixing either alone leaves the report reproducible.** (a) **The key was gated on a focus the pane never asked for.** `handleActionKey` scoped the flow's keys to (runs pane + detail focused) — but the RUN detail is what DRAWS the step flow (cursor marker, and a literal "enter → execution" on every row) and it is drawn with the LIST still focused, so the advertised gesture took TWO presses: the first was kit2.Base's "enter toggles focus into the detail", and only the second jumped. An operator who pressed once and moved on was reading the pane correctly; the pane was lying. `enter` is now claimed whenever the pane HAS a flow (`runFlow.count() > 0`) and falls through to the base otherwise, so the generic activate-the-row gesture is untouched; the vertical keys keep the focus distinction, which is real there (with the list focused up/down move the list). (b) **The jump was reverted by the list landing.** A jump claims the pane (`ShowEntity`) so the row under the cursor cannot clobber the target — but the claim was CONSUMED by the first list landing, and the jump itself issues a list refresh, so its own refresh spent the claim; the next refresh (a live execution event pokes one continuously) then auto-loaded row 0 and slid the operator off the target. The claim is now held until the OPERATOR takes control: `loadDetail` clears it (what a cursor move or row click go through) while `RequestDetail` does not, which is how the jump asks for its own target without cancelling itself. Releasing it on the jump's detail landing would not work either — that detail can arrive BEFORE the list. **(2) "they are flicking repeatedly like they are refreshing poorly over and over" and "several of them still look like the old ones" were ONE closed loop plus the composition defect it fed.** The loop: `onDetail` (a detail landing) issued the todos fetch; the todo landing's handler called `RequestDetail`, which landed a detail, which issued the next todos fetch — detail → todo → detail → todo, one round trip per lap, every lap repainting the pane, with no base case. Fixed at BOTH ends: the todos fetch is now gated on STALENESS (a clock, not its own arrival) and a todo landing repaints from cache and requests nothing. The composition: `RenderSession` installed the WHOLE body from four fields of its own (id / session events / status), so every live repaint replaced the run's record with that stub — which is why some executions "still look like the old ones". The pane has three independent writers (the record, the todo list, the live session), so the body is now COMPOSED ON DEMAND from stored parts (`composeExecutionBody`) instead of replaced by whoever painted last, in reading order: todos (context for the transcript), record, transcript; absent parts contribute nothing. **(3) Select-and-copy on every screen.** `orch` runs with `tea.WithMouseCellMotion()`, which hands the terminal's every press, DRAG and release to the program and DISABLES the terminal's own text selection — so with no in-app selection to replace it there was nothing to drag with (native selection survives only as the per-terminal shift-drag bypass). The app now does the selection itself at the one layer that holds the text: the SHELL owns the frame, so it selects across chrome and panes on EVERY screen and no screen has to cooperate, addressing CELLS of the drawn frame — what is highlighted is exactly what is read. press·drag·release copies, and a "copied to clipboard" band confirms it on the shell's separator row (the one row the layout guarantees is blank). The write is OSC 52 — the terminal's own clipboard escape, the only mechanism that works from an alt-screen TUI, over SSH, and without a helper binary. Load-bearing details: a CLICK is never a copy (the press is not consumed and a release that did not move is a click, so the entire click surface is untouched; a selection needs movement); the walker goes cell-by-cell through the ansi parser so escape sequences (which occupy no cell) are skipped and a wide rune counts for both its cells (slicing by byte or rune index drifts on any styled line, which is every line here); the highlight re-arms reverse video after an SGR reset inside the span (a colour reset mid-selection would switch the visible selection off partway through); the toast REPLACES one row rather than splicing into one, because any escape sequence a splice dropped would take the themed background with it (the bleed-through the opaque-frame work exists to prevent) and it is truncated rather than wrapped, because lipgloss wraps to width and a wrapped toast would push rows down inside a fixed-height frame; the selection lives on a POINTER on `App` because App is a value model, so View and Update each receive a copy and only a shared pointer lets the frame the renderer painted be read back by the mouse handler that arrives after it; and the toast's timer carries the copy's sequence number so a second copy cannot be cleared by the first copy's tick. **Tests:** new `execution_pane_test.go` REPLACES the temporary probes, which had begun to encode the OLD behaviour (asserting a fetch on every round — which IS the loop): one press of enter jumps, with the fixture asserting the flow was drawn and the detail NOT focused so it cannot pass vacuously; enter is not hijacked with no flow; the jump target survives the list landing; a todo landing issues no follow-up work; the todos fetch is gated on staleness; the session and the record compose into one pane; the todo list leads the body; and a session paint leaves the pane alone rather than emptying it — plus the clipboard's cell walker over styled and wide-rune lines, the highlight preserving text and re-arming after a mid-span reset, a click copying nothing, horizontal / multi-row / backwards drags reading in reading order, the OSC 52 payload carrying the base64 selection, and the toast band's row/width/no-clobber/narrow-frame behaviour. **Verified:** every fix proven by disabling it first — reverting the enter gate fails with the pane still on "runs", reverting the claim fails with the pane clobbered, reverting the staleness gate fails with "every detail landing would re-fetch, which is the loop", and reverting the landing fails with "the todo landing produced follow-up work (kit2.detailMsg)"; gofmt clean, `go build ./...`, `go vet ./...`, full `go test ./...` green (65 ok, 0 failures). Report 3 had been WRITTEN BUT NEVER COMMITTED, so the operator's build had no selection at all — that one was work in progress rather than a regression. CLIENT-side — requires an instance rebuild. |
| 278 | Bug fix + Feature | TUI/GUI parity | **Recurring Items is reachable again (two screens had named a source "schedules" and the nav dedupe dropped the later one in silence); a workflow RUN is now a live step FLOW whose steps jump to their executions; and the Executions detail mimics the GUI.** **(1) Recurring Items was never removed — it became UNREACHABLE, and my own `d310e826` caused it.** `buildNavEntries` dedupes by slug (`if seen[cmd] { continue }`) and tabs are iterated in order — … Work, **Execution**, **Automation** … — so the First screen whose source slugs to a word wins and every Later screen's source is dropped WITHOUT A WORD. The new Schedules source on the Execution tab claimed "schedules", and Automation's Recurring Items (internally also named "schedules", the constant literally commented "also the slash-command slugs the shell generates") lost its menu entry while `/schedules` still resolved — to the wrong pane. Fixed by giving the Automation source the slug the GUI ALREADY uses for it: the GUI maps Recurring Items to `/recurring-items` and Schedules to `/schedules`, so the TUI now says the same thing (and the constant is renamed `srcRecurring` so the name matches its slug). Plus the GENERAL GUARD, because the dedupe is silent by construction: no two screens may share a source name; every source must reach the menu **on its own tab** (my first version of that test only checked the slug existed SOMEWHERE, which PASSES for a collision — it now asserts the owning tab); and both slugs are pinned to their GUI areas. Proven by restoring the collision: all three fail. **(2) A workflow RUN as a FLOW.** The run detail was a flat "step name (time)" list. It is now the same vertical flow the workflow view uses, so a run and its definition read with one visual language — with each row carrying what a RUN has: STATUS as a word and a glyph (● running, ✓ succeeded, ✗ failed, ⊘ skipped, ‖ blocked, ⧗ approval, ⟳ recovering, · pending, vocabulary named once in a legend), the ITERATION when a loop re-entered the step, RETRIES from the attempt count, the timing, and "enter → execution" so the gesture is discoverable from the row. Proto enum names never reach the pane. **ENTER ON A STEP GOES TO THAT STEP'S EXECUTION** — the cursor lives on the runs pane's detail (with the DETAIL focused up/down walk the steps, enter jumps; with the LIST focused the same keys still move the list, per the navigation model), the repaint is local so it costs no round trip, and the jump selects the execution AND requests its detail because the executions list is a page and a jump that silently does nothing is worse than no jump. **Loop iterations are GROUPED**: the active one represents the step and the superseded ones trail beneath marked as history — shown, but never what the cursor lands on, and the cursor is keyed on the STEP ID rather than a row index because the rows are rebuilt on every load. **Live**: the pane re-reads the run on an execution-events poke, and the cursor SURVIVES that reload by step, so a running workflow's flow updates under the cursor instead of jumping to the top on every event — which is exactly when the operator is watching it. **(3) The Executions detail mimics the GUI.** What already existed and was deliberately not rebuilt: the merged TRANSCRIPT (durable GetExecutionSession parts + live StreamExecutionEvents, phase-grouped through `chat.MergeSessionItems` — the same merge the GUI's SessionChatPane does) and the NUDGE (the `i` interject form, whose SendExecutionMessage is the GUI composer's own call). Added: (a) the run's FACTS — the worker's NAME with the id kept, the workflow name, the bound work item, the iteration, tokens, cost, the branch with its provisioning state and the PR with its state, each OMITTED when empty rather than rendered as a zero to interpret, with the output/error in the body since they are prose not signposts; (b) the worker's **TODO LIST** (`GetExecutionTodos`) with the marks the worker's own todos use, a done count and a `(!)` flag only on HIGH priority, completed rows dimmed rather than removed, placed ABOVE the output because it is context for reading it, re-read on the detail fetch and SURVIVABLE if it fails (it installs nothing and can never turn "open an execution" into an error); (c) the **FOLLOW-UP box** (`f` → ContinueExecutionSession), the complement of the nudge, as a separate chord because the two have different preconditions — `i` needs a LIVE session, `f` needs one that still exists — with the reply surfaced in the notice (the one surface the shell never truncates) and each key explaining itself when it cannot apply. **Tests:** ~25 new across `internal/tui` and the execution package — the nav uniqueness/tab-ownership/slug guards; the run flow's status/kind/retries/timing/glyphs with no enum leakage, one cursor marker on the right row, grouped iterations with the cursor unable to land on a superseded one, walking and clamping, surviving a live reload while showing the reload's new status, re-seating for a different run even when a step id coincides, the jump moving the pane and requesting the execution, a step with no execution refusing with a reason, the keys claimed only with the detail focused, and the detail body being the flow; the execution facts rendering with empties omitted, the todo marks/count/flag, no header with no todos, the cache's zero value and non-leakage, a failed todo fetch leaving the detail working, the follow-up submitting ContinueExecutionSession and surfacing the reply, `f` refusing off-pane and with no selection, and the nudge/follow-up precondition difference. **Verified:** every new predicate proven by disabling it first (the sequence-parent rule, the started_at filter, the todo section, the visibility/latch/tenant-id patterns earlier in this branch); gofmt clean, `go build ./...`, `go vet`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side — requires an instance rebuild. |
| 277 | Feature + Bug fix | TUI/GUI parity | **The four handoff items: Policies become a "coming soon" placeholder in both clients, the TUI's Decisions pane is removed, the TUI gains a Schedules section under Execution, and Workflow Runs show names instead of ids.** **(1) Policies — placeholder in BOTH clients.** "We never really ironed these out. They exist in the gui but the form is weird and I have never tested it. I would like both the TUI and the GUI to just say 'Coming soon...' for now." The TUI's Policies pane lists NOTHING, binds NO chords and renders the placeholder (its `fetchPolicies` no longer even issues a ListPolicies call — a request behind a "coming soon" pane is one nothing can act on), while the source stays REGISTERED so the section is still discoverable and nav-parity with the GUI holds. All three GUI policy routes (/policies, /policies/new, /policies/$id) render ONE shared `PoliciesComingSoon` component, so they cannot disagree about the state of the feature — the same reason the TUI shares `ComingSoonText` and the GUI's string is identical. The routes stay registered: the nav links to /policies and deep links exist in bookmarks, so an unregistered route would 404 instead of explaining itself. REMOVED with the surface rather than parked: the TUI policy editor (create/edit/publish/version-inspect), its form machinery, and the policy-only enum parse/name helpers — the operator's standing rule is "never ship a knob nothing honours", and a form nobody can reach is the same class of thing. The capability is untouched: ListPolicies / CreatePolicy / UpdatePolicyVersion / PublishPolicy / ListPolicyVersions all remain in the generated client. **(2) Decisions — removed.** "There is not a 1:1 for Decisions in the GUI." Confirmed: the GUI has NO Decisions nav entry (grep finds decisions only as policy context inside approvals.tsx), so the TUI's standalone pane was a second, worse copy of a panel the operator already has. Source, list, detail branch and chords are gone; `/decisions` disappears from the slash registry as a CONSEQUENCE (the registry is generated from the screens' Sources()), and the nav/command parity test's required list is updated with a note. The USEFUL half survives and is now pinned by a test: an approval's detail still renders the decision records for its step run. **(3) Schedules under Execution.** "We should be able to see upcoming, running, and finished just like the GUI and we should have delete operations, bulk delete operations, and a key that takes you to the workflow run." One SOURCE with three lenses cycled by `v` (the GUI's page is one view with three modes; three submenu entries would suggest three independent surfaces rather than three lenses): **upcoming** = SCHEDULED work items, server-filtered and paginated, EXCLUDING recurring (matching the GUI — they live on Automation → Recurring Items); **running** = bound in-flight runs PLUS active sequence parents, the GUI's predicate verbatim (ACTIVE_RUNNING_STATUSES + the sequence-parent extension), keyed by proto enum rather than number so the two cannot drift; **finished** = runs that actually RAN (a started_at), so a queued run is not history. DELETE means two things and the view decides which, exactly as the GUI offers "Cancel N" in one view and "Remove N schedules" in another: upcoming/running CANCEL the work item, finished REMOVES THE SCHEDULE from the bound work item — all three fields in ONE UpdateWorkItem, because clearing the run binding while auto_start_workflow stays true would let the backend re-fire a fresh run immediately (the rule the GUI's useRemoveSchedule documents). A one-shot run has no schedule to remove and both the action and the key say so. BULK reuses the shared kit2 mechanism, and the >1 threshold now lives in ONE place (`kit2.BulkIDs()` — the Work Items pane was migrated onto it too) rather than being restated per screen, which is what "a consistent way to handle bulk operations on all forms" actually requires; writes are sequential and a rejection is COUNTED. `g` is the jump to the run: it selects the run in the Runs pane AND requests its DETAIL, because the runs list is paginated and a jump that silently does nothing is worse than no jump. The hint line follows the view so the keys it advertises are the ones that will run. **(4) Workflow Runs show NAMES.** A WorkflowRun carries workflow_id / work_item_id and no name fields, so the client resolves them — exactly what the GUI's history view does with itemsById/workflowsById — into one cached index (30s TTL, the GUI's own refetch cadence) shared by the list AND the detail. The list reads `<workflow name> → <work item title>`, or `<workflow name> (one-shot)` when there is no bound item (a bare name reads as truncated data); the detail shows the name FIRST with the id kept alongside for traceability. BEST EFFORT by design: a failed half leaves the other resolved and never turns "list the runs" into an error, and every lookup falls back to the id, so the pane cannot get worse than it was. **Tests:** ~35 new across the enforcement and execution packages — the Policies pane lists nothing even when the plane HAS policies while opening no overlay for n/e/p/v; no "decisions" source exists while the approvals policy context still renders; each schedules lens's membership against the GUI's shapes (including that an idle active item is NOT running), the view cycle wrapping, a view change DROPPING the run bindings so a jump cannot target a row the pane no longer shows, `g` moving the pane and refusing with a reason when nothing has fired, the per-view delete writing the RIGHT RPC (DeleteWorkItem vs UpdateWorkItem, asserted for the cleared run binding and the false auto-start), the one-shot refusal, the >1 bulk threshold and rejection count, and the keys reaching through the screen's own dispatch; plus the runs names (both names in the list, the one-shot marker, the id fallback, the detail keeping names AND ids, a failed lookup leaving the pane working, and the index actually being cached). **Verified:** every new predicate proven by disabling it first (the sequence-parent rule, the started_at filter, the visibility/latch/tenant-id patterns earlier in this branch); gofmt clean, `go build ./...`, `go vet`, full `go test ./...` green (65 ok, 0 failures). Frontend: `tsc --noEmit` clean, eslint clean on the changed files (0 errors repo-wide), vitest 480/480 — node 22.14.0 lives at `.dev/tools/node/bin`, which is how this branch's frontend changes are verified. CLIENT-side — requires an instance rebuild. |
| 276 | Bug fix | TUI/GUI parity | **A slash command's alias is now findable and displayed, not just in `/help`.** "/quit is an actual slash command that shows up in the list. /exit works but doesn't get displayed in the slash command list as a valid slash command. We should fix that just for consistency." Both halves were broken, in two DIFFERENT places — which is exactly why the two surfaces disagreed: `/help` DID render aliases (`helpLines` → `aliasSuffix`), while the "/" PALETTE skipped every alias row outright AND matched its filter against the PRIMARY name only (`strings.Contains(c.Name, query)`); the query is the typed word WITHOUT its leading slash, so typing `/exit` → query `exit` → nothing matched `/quit` → the palette answered "no matching commands" for a command that RUNS perfectly. A command you can run but cannot see is the inconsistency. Two changes: (1) `SlashCommand.MatchesQuery(q)` matches the primary spelling OR any alias, so typing `/exit` surfaces the command it names — aliases still do NOT get their own row, because an alias is the same command and a second row would be a phantom entry with identical behaviour; (2) the palette ROW renders `c.AliasLabel()`, and `helpLines` now uses the SAME method instead of building the string inline, so the two surfaces cannot drift apart again. The alias is placed BEFORE the description deliberately — the row is truncated to the palette width and a long description is common, so appending the alias after it would let a wide row slice the alias off the end, which is the very thing being fixed — and the padding counts the alias, so a row with one does not push its description out of line with every other row. `/exit` is currently the registry's only alias, so the visible change is one row (`/quit  (alias: /exit)     exit orch (terminal state restored)`), but the mechanism is general: the next alias added is visible and findable by construction rather than by someone remembering to update a second list. Tests: typing the alias finds the command and the primary still does; a bare "/" lists everything; the row displays the alias with the description intact; the alias precedes the description; aliases do not duplicate rows; selecting the row found by its alias RUNS it; and `/help` agrees with the palette. **Verified:** each half proven by reverting it separately — the filter probe fails the findability tests, the display probe fails the row tests, and each leaves the other passing; gofmt clean, `go build ./...`, `go vet ./internal/tui/...`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side — requires an instance rebuild. |
| 275 | Feature + Bug fix | TUI/GUI parity | **Four more operator findings: Tab now owns an open form (resolving a conflict this branch had deliberately left open), the step editor's worker is a LOOKUP, changing a step's kind reveals that kind's fields, and every list gets one consistent bulk-select.** **(1) Tab in a form — the question settled.** "ONCE IN EDIT/NEW MODE using down/up OR tab/shift+tab should move through the edit items as opposed to the top menu bar on every screen. Once you ctrl+s to save or hit Esc to get out of the editing mode, tab/shift+tab now affects the top tab menu again." That is exactly the `FormOpen()` semantics router.go's Tab chord already implements — but TWO screens were not declaring it, and a missing method is not benign: the chord does `if fs, ok := FormOpen(); ok && fs.FormOpen()`, so with ok == false it falls through to `tabRingNext()`. **Work** therefore walked Tab OUT of an open editor instead of moving between its fields, and **Enforcement** was worse-hidden still — `handleFormKey` had handled `tab`/`shift+tab` for its overlay fields all along, but the shell intercepted the key first, so those fields were UNTABBABLE. Both now declare it (Enforcement only for `ovForm`, since a picker/viewer/confirm has no fields and must let Tab keep walking the menu). The STALE TEST is rewritten rather than deleted: `TestTabYieldsOnlyToAWindowedModalForm` asserted the opposite for an inline editor and passed for the whole life of that behaviour only because the method was MISSING — satisfied by the absence, not by design. It now asserts both halves through the real dispatch, and the guard that pinned the old uncertainty became a positive assertion. **(2) The step editor's worker is CHOSEN, not typed.** "we need to have a worker lookup as opposed to typing in an ID just like we do in work items." The ref is now a KPicker (value = the worker id the wire needs, label = the name a human reads, plus the lifecycle state), serving a task's dispatchee and a worker-backed approval's approver. The list loads WHEN THE FLOW VIEW OPENS, not when a step form does, because a picker with no options is worse than a text box; a FAILED load is survivable by design — it leaves the previous list alone and the field still accepts a typed id, so a worker-list outage cannot turn "open a step" into an error. **(3) Changing a step's kind now reveals that kind's fields.** "Currently they don't show up if it was a normal worker type beforehand." They did not because the specs were appended CONDITIONALLY on the kind the form was OPENED with — a form-SHAPE defect, not a rendering one. New kit2 primitive `FieldSpec.Visible`, a predicate over the form's current values; EVERY kind's fields now exist in every step form, gated on `kind`, so the reveal is live. Supporting contract: a hidden field is skipped by rendering, by Tab/arrow navigation and by validation (so a hidden Required field cannot block a save the operator cannot fix); `normalizeCursor` moves the cursor off a field a value change just hid; a hidden field's VALUE is retained so toggling kinds does not discard typing, and the submit still writes only the selected kind's keys while a preserved `recovery` block survives a switch to a loop (mergeStepConfig keeps what the fields do not model, and the loop does not consult it). **(4) One consistent bulk-select for every list.** "spacebar can do multi-select and Esc clears the multi-select and then the bulk options shows up only after you have more than one item selected." The mechanism lives in kit2 (`Table.Marks` + the Base's keys) so every screen gets the same gesture, the same clearing rule and the same threshold, instead of each inventing its own. Space marks and ADVANCES (a run of presses builds a selection; space on a marked row unmarks it) — reassigning a key that used to toggle focus into the detail, which made it redundant with enter and left no key for multi-select. Esc clears FIRST, then keeps its meaning. Bulk appears only above ONE (`len(MarkedIDs()) > 1`), and that single decision propagates to the bar, the hints and the key dispatch because all three are built from the same action list. Marks are keyed by ROW ID (a reload reorders and filters; an index would re-point the selection), PRUNED on every load, and dropped on a source switch. The confirm NAMES THE COUNT, writes are sequential, and a rejection is COUNTED — a partial bulk operation reports "3 of 4 — 1 were rejected" rather than claiming a sweep. First screen wired: Work Items (archive/delete + a discoverable clear). NOT offered: bulk status/reassignment, because each item carries its own acceptance review and worker binding. **Tests:** ~30 new — Tab/Shift+Tab move the field cursor with a form open and the tab does NOT change, then the bar again once it closes; Enforcement's overlay form is tabbable; the worker picker's options are ids-as-values/names-as-labels and a stored ref displays by NAME; the worker list sorts, labels without leaking the proto enum, and a failed load leaves the field usable; changing kind task→approval→loop→task reveals and hides exactly the right fields (through the FORM's own `VisibleFieldNames`, not a re-implementation — proven necessary, since re-deriving it in the test would have passed with `fieldVisible` disabled); the cursor never lands on a hidden field; a hidden Required field does not block submit; space/esc/gutter/threshold; the bulk confirm names the count; a bulk write with no client errors rather than panics (found by the test, fixed in the code); a partly rejected bulk archive reports "3 of 4"; reload pruning; source-switch clearing. **Verified:** every new behaviour proven by disabling it first (the visibility predicate, the >1 threshold); gofmt clean, `go build ./...`, `go vet ./internal/tui/...`, full `go test ./...` green (65 ok, 0 failures). CLIENT-side — requires an instance rebuild. |
| 274 | Bug fix + Feature | TUI/GUI parity | **Three operator reports: the Projects pane was swallowing every key on the Work tab, the workflow step editor became an explicit EDIT MODE, and multi-line fields now read as they look.** **(1) One stray assignment broke the whole Work tab.** `projectActions()` — called by `actionsForSelection()` to build the footer hints and key bindings — set `m.formLoading = true` as a SIDE EFFECT of a getter, and nothing ever cleared it (the only legitimate setters are the `prep*` helpers, whose returned cmd delivers the `*_formMsg` that clears it; `projectActions` returns no cmd). `ClaimsKeys()` includes `formLoading`, so the shell handed EVERY key to the screen before its own handling ran, producing all three reported symptoms at once: the Work submenu's up/down never reached `menuHandleKey`; Enter selected nothing (it fell to the pane's own "load detail"); and `shift+tab` — a global route, later still — was swallowed outright. It is exactly why the oddity was source-specific, as the operator suspected: `itemActions()` and `imageActions()` never set the flag. Fixed in two parts, because the ordering is what turned one line into a dead tab bar: the assignment is gone, and dispatch now handles the OPEN SUBMENU **before** the screen-claims gate (the menu is drawn on top and is what the operator just opened; a MOUSE click can also open it while a screen claims keys, since `dispatchMouse` runs at the very top of dispatch). `menuHandleKey` returns `handled=false` for keys it does not own, so typing still reaches the screen. Found on the way and deliberately NOT fixed: the Work screen does not implement `FormOpen()`, which every other screen does, so Tab walks out of an inline editor instead of advancing fields — resolving it means choosing between two conflicting operator asks both recorded in the tree (router.go's Tab chord + `worker_crud_test.go` vs `TestTabYieldsOnlyToAWindowedModalForm`), and that test passes today only because the method is MISSING; the conflict is documented and pinned by a test that fails if someone adds it without deciding. **(2) The workflow step editor is now an explicit EDIT MODE.** `e` on the Workflows pane enters it; inside, `enter` edits the cursor's step, `a` (and `-`) adds, `x` removes behind a confirm, `E` renames the header, `esc` leaves. Outside the mode the flow view is a read-only view and the step chords do NOTHING — which is what stops "edit step / add step / remove step" reading as top-level commands living outside an edit view (the operator's actual complaint). The mode is stated in the pane (`mode: EDIT — …`) and in the notice, because a mode the operator cannot tell they are in is worse than no mode. `n` now lands IN the mode: `defaultCreateWorkflow` returns the created workflow so the id can be selected via `SelectWhenLoaded`, and `flowEditPending` is consumed when that workflow's flow loads — a new workflow's first version is an empty DRAFT, so a read-only view gives the operator nothing to do. Hints switch to the mode's keys. **(3) Multi-line fields read as they look.** A focused multi-line field was edited on ONE horizontally-windowed row (prose scrolled off; you had to know ctrl+e to read your own text) — wrapping is now the DEFAULT for `KTextArea`/`KJSON`/`KYAML` on every form. Worse, an UNFOCUSED multi-line value was rendered RAW: `display()` returns the value verbatim, newlines included, into a string the host Pads to the pane width, so one field occupied several visual rows and shifted everything below it — now flattened with an explicit `↵` between logical lines plus an `(N lines)` marker. Formatted *properly* needed a NEW wrapper: the existing `wrapFormLine` wraps with `strings.Fields`, which COLLAPSES runs of spaces and so destroys pretty-printed JSON indentation; `wrapPreservingSpaces` leaves the text verbatim, prefers word boundaries and hard-splits an unbreakable token. The value's own newlines survive, rows are bounded at six (an unbounded render would push the rest of the form out of reach) with a stated `+N more lines hidden` marker, and ctrl+e remains the unbounded view. **Tests:** the Projects action builder must not latch a claim, and no source may claim keys merely by being selected (all three sources — the source-specific bug would have been caught by this); the claim still tracks REAL modal state so the assertion is not tautological; an open submenu outranks a screen that claims unconditionally; the menu leaves keys it does not own to the screen; the operator's whole seven-step sequence driven against the REAL Work screen; the mode's keys work inside it AND are inert outside it; `enter` follows the cursor; esc exits; `n` arms and the flow load consumes the mode; the mode reached THROUGH the screen's real `Update` (so a shell-level shadow cannot hide it); plus the multi-line suite (wrapping, indentation, soft/hard wrap, row bound + withheld count, unfocused flattening, `lineCount`, and that single-line fields are untouched). **Verified:** every new behaviour proven by disabling it first (the `formLoading` latch, the newline flattening, the tenant-id guard pattern) — each fails with the exact diagnosis and passes once restored; gofmt clean, `go build ./...`, `go vet ./internal/tui/...`, full `go test ./...` green (65 ok, 0 failures). Also reverted an unexplained mid-session modification to `db/migrations/atlas.sum` (nothing in the tree writes it and no atlas binary exists here). CLIENT-side — requires an instance rebuild. |
| 273 | Feature + Bug fix | TUI/GUI parity, Workflows | **The operator-reported parity batch, the loop-gate policy that replaced a tenant-id hardcode, and the retirement of two dead knobs.** Fourteen findings from live TUI/GUI use, plus one live engine defect found while investigating them. **(1) Budget warning TEXT was uneditable in the TUI.** The gates and the ladder THRESHOLDS had been expanded out of the budget blob, but the twelve per-tier MESSAGE strings (`warnings.messages.<dim>`, the words injected into a session when it crosses a threshold) had not — so an operator could tune WHEN the ladder fired but not one word the worker reads. Added 4 dims x 3 tiers as `KTextArea` fields (ctrl+e expands), mirroring the GUI's `BudgetWarningsEditor`, which had them all along. Semantics that matter: the server reads `messages.<dim>` as a WHOLE TRIPLE and an absent key means "keep", so a dimension is written only when at least one tier is non-blank and then all three go together tagged with the blanks; blanking one tier SILENCES it (the adapter assigns each string verbatim), and blanking all three leaves stored copy untouched. Whitespace-only counts as blank so a textarea newline cannot become the message. **(2) Every kit2 pane stole `shift+tab`** by cycling its own region ring, so the tab bar could not be walked backwards from any pane; and **(3) `left`/`right` rotated the tab bar instead of moving between the panes** — a defect that needed TWO fixes, because removing the global routes left a second path alive in dispatch's `focusTabs` branch, which is exactly where Tab and a tab click land. Arrows now hand the keyboard DOWN to the panes (no early return, so one press both grabs a pane and moves to it) and Tab/Shift+Tab keep the ring. A route named "toggle side rails" whose handler had always been `tabRingPrev` — and whose name the help overlay prints — is renamed. **(4) `p` on a recurring item looked dead**: the action was fine (`p` toggles `RecurringEnabled`) but `HandleMutation` reconciled only the LIST, so the DETAIL pane — a snapshot taken at selection — kept pre-mutation values. Detail now reloads on every successful mutation, and the pane and form say plainly that "Enabled" IS the pause flag. **(5) The workflow step editor still asked for raw JSON** — HEAD's proof: a `Config (JSON — recovery, other keys)` box written back WHOLESALE on submit, which for a task (whose config IS its recovery policy) meant renaming a step wrote `""` over the recovery policy all 43 live task steps carry. Every key the kind consumes is now a field, chosen from the reconciler's own config structs rather than a single "dual" flag, and config is MERGED key-by-key so anything the form does not model survives; an unparseable config is refused rather than discarded. Creating a workflow no longer takes a `Steps (JSON array)` blob — the version starts empty and is authored in the flow view, which was already the step editor. **(6) The loop-decision tenant-id hardcode is gone.** `if cfg.LoopBranch == "step-devops-pr" && (cfg.SuccessBranch == "step-l32ezp4b" \|\| cfg.SuccessBranch == "step-end")` rescued ONE of the THREE live terminal devops loops (SDLC Non-human matched; Human and Local-Models-Only did not, and kept re-asking until the re-ask budget failed the node — the wedge that needed force_progress). The "step-end" arm was a fossil carried by no row. Replaced by `config.on_missing_decision` (`reask` default / `success` / `fail`), additive because an absent key AND any value outside the vocabulary resolve to `reask`. Backfilled for existing instances by `20260924000000_backfill_loop_decision_missing_verdict.sql` (+ `_down`), whose predicate is STRUCTURAL (loop_decision, object config, exactly one dependency, that dependency IS the loop target) rather than a list of ids — measured read-only against the live plane at 25 steps across 25 versions and 11 workflows, correctly EXCLUDING every mid-workflow review loop where a re-ask genuinely helps. Seeds carry the key so new instances are right from birth. **(7) `max_reask` and `on_missing_decision` are exposed in both clients**; `success_value` / `failure_value` / `decision_field` deliberately are not — the verdict vocabulary is produced by the platform (`db.WorkerIdentityPreamble` plus 18 literal contract statements in the seeded prompts) and `firstWordAsDecision` normalizes exactly `success`/`failure` while passing any other word through VERBATIM, so pointing a gate at another word makes every verdict miss and every gate fail silently. **(8) `retry_delay_seconds` removed from the code** — the step-config key and the `recovery_executions` column were written and parsed, and read by nothing, because execution dispatch has no deferral mechanism (the only `next_attempt_at` machinery is for webhook deliveries). The COLUMN is retained (up migrations are additive-only) with `20260925000000_retire_recovery_retry_delay.sql` correcting its stale comment, and the false claim that `ORCHICON_RECOVERY_MAX_RETRIES` / `ORCHICON_RECOVERY_RETRY_DELAY_SECONDS` override the recovery defaults is corrected at source. **(9) Docs**: `worker.md`, `developer.md` and `AGENTS.md` now carry the standing rule that every Orchicon change considers BOTH clients (GUI + TUI), the platform-owned-contract rule, the never-ship-a-dead-knob rule, and a new key invariant (11) that engine behaviour is never keyed on a tenant's identifiers. **Tests:** ~40 new across `internal/tui/screens/control` (every warning-text field renders, seeds from the blob, round-trips, shares the `warnings` block with the thresholds, writes no block when untouched, one filled tier writes the whole triple, whitespace trims, prose with commas and quotes survives encoding), `internal/tui` (arrows do not cycle tabs EVEN FROM THE BAR — the existing test only ever ran composer-focused, which is why it passed throughout), `internal/tui/screens/execution` (no kind offers a JSON field; the field set follows each kind's real config struct; a rename keeps a task's recovery AND an unmodelled key; an approval's legacy `success_branch` survives; an unparseable config is refused with no write; clearing a branch deletes the key; the three contract keys stay out of the form; `max_reask` round-trips and deletes when blanked), and `internal/scheduler` (absent policy is the reask path for a verbatim production config, only exact strings select success/fail, the loop routing path holds no tenant step id — comments stripped, proven by reinstating the comparison). **Verified:** gofmt clean on every changed file, `go build ./...`, `go vet`, and the full `go test ./...` green (65 ok, 0 failures); both new migrations PARSE and PLAN against the real schema on Postgres 16 (`EXPLAIN` never executes), the backfill predicate was validated READ-ONLY against the live plane, and the down migration's replace logic was evaluated as a read-only expression and round-trips all four config shapes exactly. **Not verified:** the frontend edits are read-reviewed only (no `node` in the environment), the backfill UPDATE was never executed end-to-end (no disposable Postgres available; the only running instance is the live plane, which was read but never written), and `make full-rebuild` applies it at boot. `db/migrations/atlas.sum` was already stale before this change and is regenerated by `make migrate-hash`. |
| 272 | Feature + Bug fix | Ask Orchicon context management | **Ask Orchicon conversations can finally manage their own context: manual `/compact`, a proactive 95% gate, reactive overflow recovery, and an end to the phantom "budget".** Four operator-reported problems, one coherent slice across all four layers. (1) **A long conversation wedged permanently.** Native Ask is SESSIONLESS (`internal/orchicon/chatturn.go` re-sends the whole accumulated history as the prompt every turn), so once the history exceeded the model window EVERY subsequent send 400'd and the conversation was unrecoverable — observed live on conversation `01M26QAMTQQHX0BDMFJYPBBSN1` (1575 messages, ~1,016,584 prompt tokens against a 1,048,576 window, `This model's maximum context length is …`), which could not self-heal because the next turn re-sent the identical oversized prompt. (2) There was no way for a user to compact on demand. (3) Nothing measured prompt size at all, so no proactive trigger was even possible. (4) Ask sessions repeatedly reported being "almost at my budget" although NO budget was ever enforced on them. **Shipped:** (a) `CompactConversation` RPC (proto + all four generated layers, Go pb/connect + TS pb/connect) resolved through an OPTIONAL `scheduler.ChatCompactor` capability mirroring `SendTurnMessageWithAttachments` — the opencode adapter delegates to its own `SessionClient.Compact` (`/session/:id/summarize`, already live for workers), the native adapter reduce→summarize→archives in place, and an adapter without the capability returns an actionable error rather than silently declining. (b) A DETERMINISTIC reduce pass (`orchicon/askreduce.go`, no model call — cannot fail, hang or cost anything) that elides exactly what dominates a bloated prompt: inline image data URLs → an explicit marker, tool results → a 200-rune head + stated elision count, oversized tool-call ARGUMENTS → valid-JSON head (a `batch_write` carries a whole file body there), single text parts capped at 4000 runes; conversation TEXT is otherwise preserved verbatim, and roles, order and every tool_use/tool_result id pairing survive because an unpaired tool call is itself a provider 400. (c) Reactive recovery: `startTurnWithContextRecovery` retries the turn after reducing, with BOUNDED escalation (reduce all but the last 6 messages, then all) and no retry for a stage that reclaims nothing, so a hard limit can never become a retry loop — and every reduction is PERSISTED (memory + the on-disk history), which is what makes the repair permanent rather than a one-turn reprieve. Detection is textual and conservative (the DeepSeek/OpenAI-compatible, Anthropic and generic "reduce the length" phrasings); a miss means the turn fails exactly as before, so no unrelated error can trigger a lossy change. (d) Per-turn usage recording for native Ask (`askUsageSink`), which had NO equivalent before: the native bridge's usage sink was wired only on the worker-execution path (bound to an execution row), so zero `usage_records` rows carried a session id — the numerator the gate needs did not exist. It is attributed to the CONVERSATION (not an execution) with the adapter kind parsed from the model_ref, i.e. exactly the key `DeleteConversation` de-links. (e) The proactive gate (`orchicon/askpressure.go`, default 0.95, `ORCHICON_ASK_COMPACT_PRESSURE_FRAC`): measured prompt tokens against the model's REAL window, resolved the same way executions resolve theirs (`ListModels`, which merges probe-derived context, Ollama `/api/show` metadata and catalog enrichment) — DISARMED with a logged reason when either half is missing, per the platform's standing no-guess rule, so a model with no live window hint simply never auto-compacts (manual `/compact` and the RPC are unaffected); a transient `ListModels` failure is deliberately not cached so one blip cannot disarm the gate for a session. (f) `DeleteConversation` no longer leaks: `scheduler.ConversationHistoryPurger` (an optional capability, type-asserted off the RESOLVED client) hard-deletes the native history file + memory entry, and `db.ClearUsageSessionIDs` DE-LINKS usage in the delete transaction — the rows are KEPT because `usage_records` is the tenant's real spend ledger (Cost Explorer/Telemetry roll up from it), so deleting them would retroactively rewrite historical cost; an in-place UPDATE is also space-neutral. (g) The phantom budget, root-caused and killed: the serve-baked agent shell (`workerAgentPrompt`) was registered as the serve's agent AND default agent, and Ask conversations select no agent — so every Ask session inherited a prompt opening "You are an autonomous coding agent." and closing with tool-call-economy rules ("every extra tool call re-sends the whole conversation", "prefer the fewest tool calls that complete the task"). A model told it is a worker spending a per-call budget duly reported one. The SHARED shell is now session-kind-neutral (`sessionToolShell`, tool inventory only — no identity, no budget framing, no false "disabled" claim), the Ask persona states the truth positively ("## Session contract": no tool-call/token/cost/turn budget; the only bound is the single-turn time limit), and workers lose nothing because their identity and the SAME economy discipline come from their own composite prompt (`db.WorkerIdentityPreamble` + `db.efficiencyBlock`, plus `batchToolsDiscipline` for composite-tool runs). (h) `/compact` on both frontdoors: the TUI registers it in the ONE slash registry (so `/help` and the `/` palette inherit it) with the mid-turn refusal living on the controller (`CanCompact`) where it is testable and inheritable; the GUI composer gains a PURE, unit-tested parser (`lib/composer-command.ts`) whose grammar is deliberately identical to the TUI's `ParseSlash` (leading slash only, bare `/` is not a command, `\/` escapes, name ends at the first whitespace) and stays policy-free so an unrecognized `/word` still reaches the model instead of being silently swallowed. Both refuse while a turn is in flight, because compaction rewrites the very history a running turn is generating from; both surface the server's own detail line and the MEASURED context sizes (0 = unknown is omitted, never rendered as "0 tokens"); a declined compaction is not an error. **Tests (Go):** 30+ new across `askreduce_test`/`askreduce_live_test` (reactive recovery incl. an env-gated end-to-end run against a real wedged history — `ORCHICON_ASK_WEDGE_HISTORY`, operating on a TEMP COPY with a source-unchanged guard, because the recovery persists), `askpressure_test` (threshold resolution, live-window resolution + caching, non-caching of lookup failures, disarmed-without-measurement, disarmed-without-window, below-threshold, best-effort failure, purge clearing pressure state), `askusage_test`, `askcompact_test`, `chatturn_purge_test`, `asksession_contract_test` (the NATIVE path pinned: exactly one system block, verbatim, no native worker contract leak, cross-checked against `NativeStaticLayer` so the guard cannot pass vacuously), `agent_session_contract_test` (Ask prompt declares the contract; carries no worker/budget framing; worker composite prompt KEEPS its discipline), `session_shell_test` (the shared shell is neutral while still advertising the tool inventory; the built serve config carries it), plus TUI `/compact` parity tests and `TestCanCompactRefusesWhileStreaming`. **Verified:** `go build ./...` + `go vet` clean; `orchicon`/`askorchicon`/`opencode`/`scheduler`/`db`/all 17 `tui` packages green; `buf generate` produces ZERO drift (all four layers regenerate identically); frontend `tsc` clean, eslint clean, vitest 461/461 incl. 6 new parser tests. Node v22.23.2 was installed into the box to run the TS codegen + frontend gates (checksum-verified against nodejs.org). Deferred/noted: `make ci` and a container boot were NOT run (no `make` binary on this box, stack owned by PID 1) — see the PR body. |
| 271 | Bug fix | SDLC pipeline efficiency | **Quick Software Engineer can no longer report success on a pushed branch with no PR.** Live incident: the provider-conformance Quick Work run (`01M1X3NZEQVA5BAE89B6KP8AP3`) implemented, tested green, committed (`8ef736ad`) and pushed — then reported `success` with no `PR_URL:`/`PR_STATE:` lines, writing "the DevOps Engineer step creates the PR" in its output. Quick Work has exactly two steps (step-quick + step-end, pinned by `TestQuickWorkflowSeedDefinition`) — there is no DevOps step, so the branch sat unreviewable. Fix in the canned seed (`internal/db/seed_workers.go`, Quick worker `01M1ERH9921YS9S1QWGZV8D1VM`): the PR-reporting block now states the one-step reality explicitly (no downstream step exists; attributing PR creation to one is always false; missing/unauthorized `gh` is a failure of the worker's own step with the real command output), and the Completion gate now reads "unpushed work, or a pushed branch with no PR" as never-success with PR lines mandatory. Roll-forward is automatic (marker `single-step implementer` untouched, still present; seeder re-rolls records missing it). Tests: existing `TestQuickWorkerSeedDefinition` pins (`PR_URL:`, `PR_STATE:`) stay green. |
| 270 | Bug fix | Multi-adapter (ADR-0003/0005) | **Recovery resume is version/adapter-pinned — same worker ID can no longer silently switch adapters mid-recovery.** Live incident: the Architect step ran on worker v4 (`orchicon/...`, orchicon adapter) while the step resolves the active version (v3, `opencode/...`, opencode adapter); the first attempt died on a stream stall, recovery resumed, and four re-dispatches went out as v3/opencode into a dead serve (`no opencode serve available`), failing the run before the late v4 success landed. Root cause in two components: (1) the recovery engine published only `_recovery_worker_id` (worker ID, not version) with the adapter-compatibility check an admitted v0.2 deferral, while the scheduler's same-worker seed gate compares worker IDs — v3/v4 share one worker ID, so the flip passed as "same worker"; (2) the recovering re-dispatch rebuilt `_worker_version` from the step's active version instead of the dead execution's. Fix: resume publishes `_recovery_worker_version` + `_recovery_adapter` on both paths (step-run + standalone); `recoveryResultKeys` preserves them across re-dispatch; `recoveryDispatchReady` gains R7b (version moved → step fails loud with `worker version changed`; legacy rows without keys keep old behavior); the task re-dispatch pins `_worker_version` to the dead execution's version when the pin is present (approval path keeps gate protection, pin-out is a follow-up). Tests: seed-keys assertions on both paths, same-version pin through the race gate (phase 3), new `TestRecoveryGateWorkerVersionChangeFailsFast`. |
| 269 | Bug fix | Multi-adapter (ADR-0003/0005) | **Retired the dead execution spend columns: per-execution tokens/cost now served from usage_records everywhere.** The `worker_executions.token_usage` / `cost_usd` row columns were write-never (no production code ever updated them), so Ask agents reading `get_execution` reported 0 tokens while the detail card — computed from `usage_records` rows — showed real spend. New `db.SumUsageForExecution` (cumulative transport sum + cost per execution) now backs: the execution get/list API fields (best-effort enrichment, reads never fail for telemetry), the Ask `get_execution` tool, and the recovery capture snapshot + "failed after N tokens" summary (which always read zero before). Frontend sidebar drops the dead-column fallback (list rows were already gated on >0 and are now truthful). No proto or migration churn: field names are unchanged, now filled with real data; dropping the DB columns is a later Atlas migration, not this change. Test: `TestSumUsageForExecution` (seeded sums + missing-execution zeros). |
| 268 | Bug fix | Multi-adapter (ADR-0003/0005) | **Folded-think leak closed globally across the orchicon adapter + brainstorm prompt hardened for source-grounded work items.** (1) Executions had the same GLM folded-think leak the Ask work item is fixing, and it was never an ollama-only problem: every native provider wire that emits content deltas verbatim is now routed through the shared `thinkSplitter` — openai-compat (pre-existing), ollama native (`ollamaNativeStream`, new), anthropic `text_delta` (`anthropicStream`, new — gateways that inline chain-of-thought instead of thinking blocks), and commandcode legacy `text-delta` (`legacyStream`, new — the Go-plan fallback; the provider route delegates to the anthropic/openai clients so it inherits both). Same feed/drain pattern everywhere: split-tag holdback, unterminated-block drain to reasoning at Finish/message_stop, tag delimiters never emitted. The opencode transport needed no delta work (deltas are deliberately dropped; completed parts carry the durable record) but persisted completed `text` parts verbatim — `sessionRun` now segments folded blocks out of each completed part BEFORE `parseEvent` (clean output/UI/transcript in one shot) and persists each block body as its own reasoning part via the same reasoning path native parts use (no dedupe — folded blocks and native reasoning are distinct segments); a part carrying only thinking skips text handling; cross-part state covers blocks spanning parts with eager per-part emission so no end-of-run flush is needed. The Ask collector itself is untouched here (owned by the running Ask work item's worktree — no conflicting edits). (2) Brainstorm prompt (`agent.go`): ALWAYS ask clarifying questions before drafting work items; every item grounded in source-code truth via `list_project_dir`/`read_project_file` (files/lines verified, never invented); description + acceptance criteria NEVER light — detailed, mechanical, runnable blind by a true workflow run. Context is our friend. Tests: `ollama_think_test.go`, `think_wires_test.go` (anthropic + legacy single/split), `think_demux_test.go` (single/multi/spanning/unterminated/no-dedupe), `mode_test.go` pins the new prompt language. |
| 267 | Bug fix | Multi-adapter (ADR-0003/0005) | **Native adapter: the `max_steps` turn guillotine is removed — worker/tenant `tool_call_count` budgets are the only tool-call control (opencode parity).** The terminal turn cap (`ORCHICON_SESSION_MAX_STEPS`, default 25→100, plus the `budgets.max_steps` work-item override) punished honest tool-heavy work — 100 tool calls IS 100 turns, and two live SSE runs died at the cap doing nothing but singular reads — while genuine loops are caught far earlier by the progress monitor (repetition N-in-window, no-progress window, tool hang) and absolute cost is bounded by wall-clock. The guard is gone outright (no env knob, no work-item key — budgets stay on the worker/tenant ladder where every other budget and monitor lives): `steps` is retained purely for compaction pacing. Its replacement is the budget ladder's `tool_call_count` dimension, now evaluated natively at opencode parity — warn/escalate/final tier crossings latch with a one-shot log and never collapse context (the dimension is opted out of `compact_dims` by default; compacting on tool pressure would force yet more tool calls), while the abort tier fails the session terminal-direct as `budget_abort:tool_call_count` with no stall. Counting moved to the single correct point — emission in `drain` (opencode `evtToolUse` parity), so the abort fires BEFORE the over-limit call executes rather than a turn later on stale counts; unset falls back to the ladder default (100), explicit ≤0 disables. The model-facing prompt drops the `max_steps` wording for turn/tool-call budget economy, and the Ask Orchicon work-item `budgets` examples now show a real ladder key (`tool_call_count`). Tests: `loop_budget_test.go` (the guillotine contract) deleted, replaced by `compaction_tools_test.go` (abort at limit, no compact below limit, zero-disables, default-100 pinned); the QA guard and parity-matrix recovery tests now drive the ladder abort instead of the env step cap. |
| 266 | Bug fix | Multi-adapter (ADR-0003/0005) | **Native-adapter stream survival + honest context-window telemetry + inline-reasoning routing (operator-reported trio from live llama-server runs).** (1) `defaultHTTPClient` set `http.Client.Timeout: 10m` — a TOTAL deadline covering the entire request INCLUDING the streamed body; a long prefill (48k prompt tokens on a local llama-server) produces zero body bytes for longer than 10 minutes, killing a healthy generation mid-stream with `stream read: context deadline exceeded`. Fix: the total timeout is GONE — connect/headers are bounded by transport-level timeouts (dial 30s, TLS 15s, response-header 5m, a dead server fails fast), and the stream body is bounded by a new idle-read watchdog (`streamidle.go`): after the FIRST body byte, a silent gap past `ORCHICON_STREAM_IDLE_TIMEOUT` (Go duration, default 5m, "0" disables) aborts the read with typed `ErrStreamIdle` — pre-first-byte silence (the prefill) is exempt by design, bounded by the per-execution wall-clock budget. The watchdog wraps every provider response body in `postJSON` (all four provider families); a pump goroutine performs the blocking reads (single-reader contract), each delivery re-arms a generation-checked AfterFunc timer (never `Timer.Reset`'s stale-fire), and fire CLOSES the body to unblock the pump. Two live bugs found and fixed during QA of the wrapper itself: short consumer reads (bufio.Scanner starts small and grows) dropped the pumped chunk's tail — the remainder is now buffered and drained (short reads are legal io.Reader semantics); a BUFFERED pump handoff raced chunk-vs-EOF — select could serve the EOF and silently drop the final chunk (caught by the anthropic truncation test `TestQAAnthropicMessageStopWithoutReasonYieldsStopOther`, which now pins the fix; the handoff is unbuffered so the pump is at most one read ahead). (2) the execution sidebar showed "model window unknown" for custom native providers: the resolver chain (work-item declared window → opencode discovery `limits.context`) never matches a native provider, and the sourcing probe's decoder read only `context_length`/`max_model_len`/`max_input_tokens` — llama.cpp's `/v1/models` reports its trained window under `meta.n_ctx_train` (shape `{"data":[{"id":…,"meta":{"n_ctx_train":242576}}]}`) which decoded to `Context: 0` → `model_not_found`/`context_zero` → window unknown AND the session's compaction trigger disarmed. Fix: the probe decoder now reads `meta.n_ctx_train` (per-model, llama.cpp shape) and falls back to a top-level `n_ctx_train` (vLLM-style variant), pinned by `TestSourcingProbeLlamaCppMetaContext`; the sidebar's `resolvedContextWindow` chain gains the native provider's sourced models (via `useProviderModels(latest.usage.provider)`, the SAME enrichment the compaction trigger consumes — `emitTurnUsage` already splits canonical `native/<provider>/<model>` refs so segment 2 = the provider entry id) between the explicit prop and the opencode-discovery fallback, so the Context % bar denominates against the REAL 242k window instead of "unknown". (3) llama-server emits the model's chain of thought INLINE in `delta.content` wrapped in the `thin`…`big` tag pair (when `reasoning_format` is not enabled server-side), which rendered raw in the execution chat while Ask Orchicon's chat already surfaced thinking as bubbles. Fix: a streaming think-splitter (`thinksplit.go`) in the openai-compat client routes the enclosed text as ReasoningDelta (rendered as reasoning, Ask Orchicon parity) and passes everything outside as content — a cross-chunk-safe state machine that tolerates tag fragments split across SSE frame boundaries (holdback re-emitted on the next delta, drain at flush so a truncated final tag cannot swallow the response tail), with tag literals built by concatenation so tooling cannot rewrite them in source. Servers that emit a native reasoning field are unaffected (content never contains the pair — byte-identical passthrough). Tests: `thinksplit_test.go` (single-chunk, split-across-chunks, passthrough, partial-tag-never-emitted, unclosed-drain, multiple blocks, delimiter-drop, realistic llama.cpp turn shape), `streamidle_test.go` (idle abort after first byte, idle=0 no-op, EOF passthrough, per-chunk timer reset, short-read tail retention, postJSON wrap, env parsing incl. garbage→default; test helpers are Close-aware like real response bodies — a helper blocked in Read past Close deadlocked the deferred cleanup and hung the first run). Follow-up on the same branch (stall/budget parity audit): the repetition-count / tool-hang / text-loop / no-progress / nudge knobs from the settings page were VERIFIED already wired into the native engine on this branch — manifest carries all six windows (reconciler from tenant settings), `progress.go` implements the monitor at opencode parity with full test cover (`progress_test.go`: no-progress fatal, repetition tier-2 + reset, zero-usage tool loop, tool-hang advisory, fatal-parity), and the loop starts/feeds/drains it — the perceived gap is deployment lag, not code. The one real gap found: the native turn budget (`max_steps_exceeded`) resolved ONLY from `ORCHICON_SESSION_MAX_STEPS`/25 and ignored the work-item `budgets` JSON (the real opencode path has no step cap at all — only the sim hardcodes 3 — so the native guard is intentionally stricter). Fix: `maxStepsFromBudgets` resolves work-item `budgets.max_steps` (which rides the merged manifest payload, worker-over-tenant per key) → env → 25; bad keys fall through instead of disabling the guard; zero-value Sessions fall back instead of failing on step one. Tenant-level `max_steps` still cannot be set (ApplyBudgetJSON drops unknown keys — needs a typed ladder column + UI field; noted, not implemented). Second follow-up on the same branch: default turn budget 25 → 100 (one loop iteration is one MODEL turn and a tool-using engineer spends a turn per round-trip — two live SSE runs died at 25 doing nothing but singular reads; genuine loops are caught far earlier by the repetition/no-progress monitors and wall-clock bounds absolute cost, so the cap is only the catastrophic backstop; per-item override unchanged), and the native prompt Tool discipline section now states turn economy as a MUST-batch rule with an example (small local models ignored the soft wording; batch tools were always registered and advertised — host+MCP combined, no permission gate — the dead runs simply never called them). Pinned by `loop_budget_test.go`. Verified: full `go test ./...` green (twice — the buffered-handoff race fix and the format pass), `go vet`/`buf lint`/`gen-check`/synth-data green, fe `tsc`+eslint+vitest (367) green; rls-check skipped-with-grace (no reachable DB on this box; no schema changes in the diff). |
| 265 | Bug fix | Multi-adapter (ADR-0003/0005) | **Native adapter: tenant provider overrides reach chat dispatch (all provider kinds), and sessions can no longer settle as hollow successes.** Two operator-reported defects from the first live native-adapter runs (run `01M1NKZ1WYJTEKG3W580ZSZP9S`; exec `01M1NMC25W2F488YEVZZ7BMRPA`): (1) an ollama CLOUD BaseURLOverride in Settings → Adapters → Providers was silently ignored at chat time — every native session turn dialed the built-in default `http://localhost:11434` and failed with `connection refused`; root cause: `Registry.profile()` resolved BUILT-IN provider ids from the pure built-in table (`BuiltinProfile`, `OLLAMA_HOST` env) and never consulted the providers service, so the tenant override that the settings views correctly rendered (via `EffectiveProfile`) never reached the runtime client. Fix: `Registry.SetBuiltinOverridesLoader` — the providers substrate registers `EffectiveProfile` itself, and `profile()` resolves a built-in id as built-in default ⊕ stored tenant overrides (the SAME mapping the RPC views use), falling back to the pure built-in table when no row exists (and on loader error, logged — availability first, matching the custom-loader semantics; dispatch gates own enabled-ness). All four provider families route through the one hook: `ollama` (native /api/chat), `anthropic` (Messages), `openai`/`openrouter`/`opencode`/`opencode-go` (Chat Completions), `commandcode` (dual transport); tenant-CUSTOM providers already loaded through `SetCustomProfileLoader` and are untouched. The existing per-mutation `registry.Invalidate` (enable/disable/baseURL/tokens/visibility/models/delete) covers the cache — an override takes effect on the next dispatch; `OLLAMA_HOST` documented as lower precedence than tenant settings. (2) executions recorded `succeeded` while the model was still mid-monologue: three compounding defects, all absent the opencode adapter's completion-parity guards — (a) `openaiStream.flush()` SYNTHESIZED `StopStop` when a stream ended with no `finish_reason`/`[DONE]` (proxy drop / connection close), ollama mapped `done_reason:""` → stop, anthropic `message_stop` without a `message_delta` stop_reason → stop, legacycc `finishReason:""` → stop: a truncated/aborted turn was recorded as a completed one; (b) `StopLength` and `StopOther` were SUCCESS cases in the loop's settle switch (stale comment shows `StopOther` was half-intended as non-success): the hardcoded `MaxTokens: 4096` + a chatty reasoning model burning the cap on folded `<think>` monologue produced `finish_reason:"length"` mid-thought → hollow success with output ending mid-sentence — the EXACT artifact in the reported execution; (c) no decision-signal gate: the native loop settled ANY stop as success without the `ORCHICON WORKER SUMMARY:` sign-off the opencode transport requires. Fix (opencode parity, one contract three consumers): every stop-mapper now maps a missing end-of-response signal to `StopOther` (the honest terminal), and the loop's success gate (`internal/orchicon/completion.go` — `decisionMarkerPresent` via `realDecisionMarkerIn`/`placeholderMarkerBody`, kept in sync with `internal/opencode/session_run.go` + `internal/scheduler/reconciler.go`) requires a REAL marker before settling `stop`; a markerless settle-point fires the completion probe (`runCompletionProbe`, budget 2, source `completion_probe` — a fresh turn asking for the sign-off without restarting work; a probe reply carrying a real marker settles normally), and budget exhaustion fails honestly with `stalled:missing_decision_signal:completion_probe_no_response`; `StopLength`/`StopOther`/`StopError`/`StopContentFilter` are failures (`model terminated with stop reason …`) — a length-capped mid-monologue turn can never settle. `CommandCodeClient` audited: a pure router over Anthropic/OpenAICompat/legacy streams (all fixed beneath it) — no own stop synthesis. Tests: `qa_completion_test.go` (gate: probe→marker→success incl. probe-text-in-history, budget exhaustion → honest failure, real marker settles without probe, template-echo `<summary>` placeholder does NOT settle, StopLength/StopOther/truncated-marker fail; streams: openaicompat no-finish_reason → StopOther vs real stop → StopStop end-to-end over httptest, ollama done-reason mapping table, anthropic message_stop-without-reason → StopOther over httptest, legacycc mapping); `registry_override_test.go` (override wins / no-row fallback / loader-error fallback / custom ids never consult the hook / nil-hook pure built-in); DB-backed `TestProvidersRegistryEndToEnd` (settings row → `EffectiveProfile` → `Registry.Get` → built `OllamaClient.Host` carries the override, and clears back — green against the live dev Postgres); QA harness scripts the canonical sign-off on StopStop turns (`bare`/`markerSuffix` opt-outs for gate tests), five existing success tests updated to the marker-aware shape. Full `go test ./...` green; `make ci` pending at commit time. DOCUMENTATION.md: Providers section (built-in ⊕ overrides at chat time) + new §Native (orchicon-kind) session engine (decision-signal gate + no-synthesized-stop semantics). |
| 264 | Refactor | Multi-adapter (ADR-0003/0005) | **Worker-level `runtime_ref` is retired — `model_ref` is the single source of truth for the whole dispatch path.** The second, independently-settable source of adapter truth died: `resolveAdapterRowKind` (ADR-0005 D6) preferred the worker's `runtime_ref` over the model_ref's kind, and two live failure classes fell out of that duality — (1) misroute: the new-worker form defaulted `runtime_ref="opencode"` while the picker selected an orchicon model_ref, so native workers dispatched through the opencode adapter path (wrong capacity row, phantom requeue pressure); (2) black hole: the canned research trio's `runtime_ref` carried a runtime IMAGE TAG (`orchicon-runtime:web-research`) in a column dispatch read as an ADAPTER KIND — zero `runtime_adapters` rows match, so those workers could never dispatch. Fix: drop `worker_versions.runtime_ref` outright (Atlas migration `20260918000000_retire_worker_runtime_ref` + paired down, forward-only — the column carried no data the model_ref does not already express; every reader rewritten in the same change; dispatch semantics byte-for-byte match the pre-drop empty-runtime_ref fallback, so workers that never set runtime_ref see no change). `resolveAdapterRowKind` collapses to kind(model_ref) → legacy default; proto field 6 is `reserved` (never reused) in all four worker messages with generated Go+TS regenerated; worker service drops runtime_ref validation/storage on create/update (the two-field lockstep burden dies); the new-worker form loses the runtimeRef field + its ADR-0005-D6 derivation effect (picker governs dispatch through the model_ref itself); the research trio seeds lose the bogus tag; stale INSERTs in seed tests (which still listed the dropped column) fixed. ADR-0005 amendment (2026-09-04) records D6's retirement with the failure evidence and notes this as the prerequisite for the future human-configured model-failover feature (a fallback chain needs ONE canonical per-model identity to build ordered refs from); ADR-0003's runtime_ref invariant + watch item marked superseded/resolved. Test-side: adapter-seed regression test no longer passes a nil `*slog.Logger` (panics on the existing-kind Warn path — latent since it skipped without `ORCHICON_TEST_DSN`). Tests: `adapter_row_kind_test.go` rewritten around ref-derived kind (no runtime_ref input), retirement pinned by the rewritten `-workers-new.runtime-ref.test.ts` (no runtimeRef field/payload/derivation) + seed trio test (empty model_ref, no runtime_ref column). Verified: `go build ./...` + `go vet ./...` green; scheduler/server/worker/db suites green; DB-backed suites green against a fresh migrated DB (`-p 1`, created+dropped); migrations apply end-to-end via `tools/atlas-ci` on a disposable DB; `buf lint`/`buf generate` regenerated; `fe-lint`/`fe-test` (367) + `tsc -b` green; atlas.sum recomputed; synth-data + RLS gates pass. Pre-existing (not this PR): 3 DB-backed askorchicon stall-timing tests fail identically on clean HEAD on this box. Follow-up fix in the same PR, exposed by the operator's test of this branch: `api.Mount` took `Dependencies` BY VALUE, so its back-fills (`deps.ProvidersService = providerSvc.Service()`, and `ModelRefRegistry` when nil) landed on Mount's local copy — the caller's struct stayed nil forever and the native bridge's lazy ProviderResolver failed every orchicon-kind dispatch with "providers service not yet constructed (api.Mount)" (the lazy-resolve design was right; the by-value signature broke it; latent until this retirement made the native path live for the first time). Mount now takes `*Dependencies` (nil-guarded panic), server passes `&deps` so the resolver's capture observes the back-fill, regression-pinned by `TestMountBackfillsDependencies`. Second follow-up in the same PR, from the operator's retest (executions finishing in 1-2s with DSML token markup `<｜DSML｜tool_calls>…` as the final message, PR Reviewer loop ×3): tenant-custom provider profiles carried zero-value `Quirks{}` — `SupportsToolCalls:false` gated the ENTIRE tools array off the wire (openaicompat builds), so the tool-trained model improvised its native special-token tool-call format as plain text, the loop read a text-only finish, and sessions "succeeded" instantly. Custom profiles now set `SupportsToolCalls:true` (they are OpenAI-compat by definition and Orchicon sessions are inherently tool-driven), and defense-in-depth in the session loop: a provider reporting no tool capability fails the execution FAST pre-stream with an actionable message (never a silently tool-less wire), regression-pinned by `TestQANoToolsCapabilityFailsFast` (refusal is pre-stream — StreamTurn never called; transcript marked failed). Third follow-up in the same PR, from the operator's next retest (workers finishing with "tool registry not configured" — only memory/MCP tools present, no read/write/bash): ADR-0007's deferred tool-suite task lands — `internal/orchicon/hosttools.go` implements the ToolRegistry contract with the HOST suite (batch_read/batch_grep/batch_write/read/grep/write/edit/list/glob/bash/todoread) by REUSING the opencode sidecar's battle-tested composite engine (`internal/worktree` BatchRead/BatchGrep/BatchWrite — one containment boundary, one cap semantics; names + arg shapes sidecar-parity, write/edit use `filePath`), scoped to the execution's working dir (worktree when provisioned, else ProjectDir) with the project root READ-only (writes never land in the main checkout); `bash` runs `bash -c` in the working dir with a hard timeout (default 120s, max 600s, killed on expiry) and non-zero exits are RESULTS (stdout+stderr returned) so the model can course-correct; `glob` roots doublestar at the working dir; the bridge composes host + MCP via `combinedRegistry` (host names win, dedup by name, Execute routes by advertiser). Regression-pinned by `TestHostTools*` (defs shape + loop-parity gates, round-trip, grep/glob/list, bash incl. timeout + nonzero-exit-as-result, containment escapes refused) and `TestCombinedRegistryComposeDedupHostWins`. Fourth follow-up in the same PR, from the operator's live session on the rebuilt dev instance: (1) the session pane rendered native executions as ONE output blob — the native bridge persisted a NIL session-parts batch (`sessionStore(ctx, exec, nil)`, the deferred parts task never landed), so the DB transcript the pane consumes (`execution_session_parts`, the SAME durable surface the opencode adapter writes) was empty and terminal native executions fell back to accumulated-output rendering; the bridge now mirrors the session's crash-safe JSONL transcript into the DB live via a transcript observer (`sessionPartsRecorder`: batches under a mutex, flushes on a 2s ticker + final drain, best-effort end to end — a failed batch is dropped, the JSONL remains the durable record, persistence never breaks a session), converting every JSONL event class into the exact opencode part shape the frontend's `transcriptItems` parses (user_message{text,source}/text{part:{text}}/reasoning{part:{text}}/tool_use{part:{tool,callID,state:{status,input,output}}}/step boundaries/error{error:{message}}/session_info at open; tool calls ALSO emit the step-close/open boundary pair so the pane's phase grouping matches). Two live bugs fixed in the recorder itself: seq collisions — the DB's unique (tenant_id, execution_id, seq) + ON CONFLICT DO NOTHING silently DROPPED every part sharing a seq, so the recorder derives DISTINCT seqs (seq<<8 | subIndex per source event, resume-safe + replay-idempotent); tool-call/result pairing — ONE completed tool_use part per call carrying BOTH state.input (recorded at the call, held open) and state.output (merged when the matching result arrives; crash/cancel mid-call drains the held part with its input preserved; an orphan result flushes standalone) instead of separate input/output parts (duplicate bubbles) or an input-only part (output never recorded); the result decoder tolerates BOTH durable shapes (the loop marshals ToolCall without JSON tags — Go field names — while tests/older transcripts use snake_case; Go's unmarshal is case-insensitive but not underscore-insensitive, so the mismatch silently emptied ToolCallID and orphaned every result). (2) the host tool suite was scoped to the PROJECT dir (`NewHostTools(pd, manifest.ProjectDir)`) ignoring the provisioned run worktree — native bash/read/write ran in the MAIN checkout (wrong branch, worktree-hygiene violation; a scratch/out-of-repo cwd is where "git rev-parse → not a git repository" facts come from), now scoped to manifest.WorktreePath when provisioned mirroring the opencode adapter's executionDir(). Regression-pinned by `bridge_parts_test.go` (opencode-shape parity incl. todos-parser walk of part.state.input, strictly-increasing seqs, orphan+hold drain behavior, best-effort on store failure, pump lifecycle, observer-before-Run, and Close returning promptly both with and without a started pump — the original test pinned the old shape AND deadlocked 10m on a pump-less `<-r.stopped`). Live-verified on the dev instance after `make rebuild-dev`: native executions render as separate line items (reasoning → tool call with input+output → text per step, batch tools used) and the run branch/worktree provisioning confirmed adapter-agnostic. |
| 263 | Bug fix | Multi-adapter (ADR-0003/0005) | **Run-start runtime-serve gate is adapter-aware — native-only runs no longer gate on (or fail for) the opencode serve.** Observed live bug: a run whose workers were all native orchicon (`model_ref orchicon/deepseek/...`) armed with `runtime_ready=false` whenever a runtime daemon was wired, sat "waiting for dispatch…" while the ensure-serving probe demanded the container's opencode serve for 120s, then failed the run with "runtime opencode serve failed to become usable" — a dependency the native in-process engine never has. Root cause: the run-start gate (ADR-0009-era, built when opencode was the only adapter) was adapter-blind — `pending→running` stamped `RuntimeReady = !runtimeEnabled()` regardless of which adapter kinds the run's steps dispatch through, and `EnsureServing`/daemon `Create` hard-failed when the unneeded serve couldn't boot (its own 30s window) — the run never dispatched, so the step runs stayed "waiting for dispatch…" placeholders. Fix (single source of truth, ADR-0003): `WorkflowReconciler.runNeedsServe` resolves each task/approval step's model_ref exactly as dispatch does (step-pinned version → latest published; unresolvable worker ⇒ conservative opencode demand so nothing silently skips a container an opencode step might need); a run with no serve-dependent kind arms `runtime_ready=true` and stamps the `runtime.NoServeImage` sentinel over the resolved image — `EnsureForRun`, `EnsureServing`, and boot-time `Adopt` all no-op on the sentinel, so a native-only run creates no container at all (no serve, no 30s boot window, no leak); mixed runs (≥1 opencode step) keep the full gate byte-for-byte. The `RuntimeLifecycle` interface gains `ServeDependent(kind)` (opencode-only today, implemented by `runtime.Lifecycle`); `startEnsureServing` gains a serve-less guard (defense in depth). Dispatch itself unchanged (the gate is the only adapter-blind piece). Tests: `TestImageForRun` (sentinel stamping + no-real-tag collision), `TestRunNeedsServeNilRuntime`, `TestRunNeedsServeNonWorkerSteps`, DB-backed `TestRunNeedsServeAdapterKinds` (native-only=false, mixed=true, unresolvable=conservative true; skipped without ORCHICON_TEST_DSN). Full `go build ./...` + `go vet ./...` + scheduler/runtime/server/orchicon suites green. DOCUMENTATION.md serve-gate + lifecycle sections updated; delivers the core "warm only the needed adapter serves" slice of feature 01M18JX0PC2FB21ZKDT9TZ1W7E (concern 4; remaining mount/multiplexing scope narrowed in the work items). |
| 262 | Bug fix | CI gates | **CI gate parity: hermetic go-ci, enforcing rls-check, and the frontend gates wired into `make ci` — no adapter CLIs ever installed on CI runners.** GitHub CI had been red on every PR for a day (#475–#478 merged red; releases unaffected — release.yml never runs `make ci`) on two environmentally-coupled jobs, both green on the dev box by construction: (1) `internal/opencode` fail-fast tests need `exec.LookPath("opencode")` to FAIL to reach the "no opencode serve available" error they assert, but the dev box has the binary (worker containers mount `~/.opencode` — the intended design, single source of the CLI) while bare `ubuntu-latest` runners don't. Fix = a `resolveBinary` seam on the adapter (default resolver `resolveOpenCodeBinary` probes PATH then `~/.opencode/bin`, zero behavior change; `SetBinaryResolver` injects a stub in tests) — the multi-adapter pattern: claude code etc. get their own resolver + seam, nothing is ever provisioned in CI (the earlier `ORCHICON_SIMULATE_ADAPTER=1` idea was rejected: it's the FIRST line of Start and would bypass the exact fail-fast logic being tested). New tests: resolver-miss + nil-restores-default seam contract; stubbed fail-fast tests pass hermetically (verified with a stripped PATH/HOME — the exact CI condition — after reproducing CI's failure byte-for-byte locally). (2) `rls-check` was SKIPPING in CI (workflow comment claimed it enforced — no Postgres service existed) and is vacuous on an empty DB (zero tables = zero violations), so `go-ci` now runs a Postgres 16 service + `go run ./tools/atlas-ci` (the production engine `internal/migrate.Run` over the embedded `assets.MigrationsFS` — same tracking table, no atlas binary, no new deps) before `make ci` with DB_URL exported; verified against a fresh created-and-dropped database. (3) The 98 pre-existing `fe-lint` errors (in files recent frontend PRs shipped — fe-lint was never in `make ci`, so local and CI gates disagreed about what "done" means): swept to 0 errors — empty catches get explanatory comments, `any` probes become structural types (`WorkflowRunLike`/`ExecutionLike`/`WorkItemTimestamps` with a snake_case normalizer), proto payloads use the generated request classes (`PartialMessage<TenantSettings>` widened in the settings hook so per-tab partial saves type-check — int64 fields are BigInt), stale `@ts-ignore`/`as any` scaffolding removed where the generated types have long existed, one real bug fixed (DropdownGroup early-returned before its useEffect — a rules-of-hooks violation that could crash when nav visibility flipped mid-session), and `fe-lint` + new `fe-test` (vitest, 366 tests — 5 of which were silently failing since birth because vitest was in NO gate: sequence-controls assertions pinned pre-040b48962 semantics, ProvidersTab grepped for the deleted per-model immediate-save UI, ModelPicker grepped a placeholder copy-edit; tests updated to pin current shipped behavior) wired into `make ci` so local ≡ CI. vitest now excludes `tests/*.spec.ts` (Playwright's specs — they fail collection under vitest's default glob, 4 false-positive suite failures). eslint warnings (29) unchanged — not gate-failing. |
| 261 | Bug fix | CI gates | **No-synth-data audit scans git-owned files — stale test debris can no longer fail the build.** `make full-rebuild` failed with MockModelDiscoverer/MockMCPDiscoverer hits traced to `.gotmp/TestRecurringRunFailure…/001/.orchicon-worktrees/…` — stale worktree checkouts from past test runs (from the era when the ADR-0010 mocks still existed) under a gitignored scratch dir; `grep -R .` does not respect `.gitignore`, so the audit flagged dead debris as shipped source while `git grep` confirmed zero markers in tracked code. The scan list is now derived from git (`git ls-files --cached --others --exclude-standard`, NUL-safe read loop — `mapfile` cannot parse `-z` output), making `.gitignore` authoritative; a hard floor guard fails the audit when fewer than 100 files enumerate so a plumbing regression can never pass silently. Verified: debris-only scan passes; markers in tracked AND untracked source fail with exact path:line; test-file exclusion intact; sourcing sentinel unchanged. Same scan-scope flaw class as the test-file-exclusion fix in the audit's original PR. |---|
| 260 | Bug fix | SDLC pipeline efficiency | **QA Engineer: mandatory surface-impact check — UI verification no longer keys on diff file types.** Root cause (native usage/telemetry/budget run 2026-09-03): the QA step's trigger read "UI changes get visual verification via the Playwright loop" — a conditional that handed the worker the classification decision, and it classified a Go-only usage/budget-wiring diff as not-a-UI-change, verified everything at code level (build/vet/full tests/wire formats), never opened the browser, and reported success although the work item's criteria referenced budget display in the UI. The new workflow step 3 ("Surface-impact check — mandatory, before any verdict") decides explicitly whether the change affects anything user-visible (displayed data, numbers, budgets, costs, tokens, statuses, counts, lists, labels, forms) and states the invariant: **diff file types are not the test** — a Go-only diff feeding displayed values is UI-affecting; affected surfaces (or un-rulable ones) MUST be screenshotted via the Playwright loop and the displayed values confirmed against the criteria. Verdict contract: `success` now requires the surface-impact determination (what UI was verified, or why no user-visible surface was affected). Purpose/skills updated; old conditional trigger removed. New per-worker roll marker `qaSurfaceImpactMarker` re-rolls exactly the QA Engineer (exported `QASurfaceImpactMarker` for tests) — SSE/PR Reviewer/Architect keep `sdlcWorkhorseMarker`, so QA-only wording changes never re-roll them. Test-order hygiene fixed in the seed suite: `replaceCannedWorkerWithUserShell` now restores the canned worker via `t.Cleanup` (the usr_-prefixed shell used to leak into later tests and break canned-ID lookups), content tests call `resetWorker` first, and the workhorse test compares `budget_overrides->>'wall_clock_seconds'` (jsonb::text spacing made the old string-equality unsatisfiable). Same content applied out-of-band to the live tenant's QA Engineer (v4 roll-forward preserving model_ref) and QA Engineer - LocalModel (v7) via SQL, generated byte-identically from the seed by tools/qa-rollforward. |
| 260 | Chore | CI | **go-ci setup diet: 10m34s → ~3m target (pinned buf, plugin-only npm install, gen drift gate).** Measured per-step across four CI runs: the Go gate itself (`make ci` Go half) is a stable ~2m — the other ~8m was setup. Three root causes fixed: (1) buf was `go install ...@latest` per run (79s flat, plus unpinned-drift risk) — `make tools` now downloads pinned buf v1.72.0 (version + SHA256-verified tarball, x86_64 with a PATH fallback for other arches) into gitignored `bin/`, and `gen`/`lint` resolve `./bin/buf` when present; atlas install moved out of `tools` into the `migrate` target that actually needs it. (2) go-ci installed the FULL frontend node_modules (4–7m chronic, up from a 10s cache-race fluke) just to put two protoc plugins on PATH — the workflow now `npm install`s ONLY `@bufbuild/protoc-gen-es@1.10.1` + `@connectrpc/protoc-gen-connect-es@1.7.0` (exact package-lock versions, verified bin names) into a scratch prefix (~30s); BSR remote TS plugins were evaluated first and REJECTED on evidence: `bufbuild/es` only publishes v2.x (emits protobuf-es v2 code incompatible with the pinned v1 runtime) and `connectrpc/es` tops out at v1.6.1 ≠ locked 1.10.1, so local-npm plugins stay and bit-identity is trivially preserved. (3) The Go plugins in buf.gen.yaml were remote-UNpinned (BSR "latest" could silently change generated code) — pinned `connectrpc/go:v1.20.0` + `protocolbuffers/go:v1.36.12`, verified bit-for-bit against committed output with the pinned binary locally (the committed output was stamped v1.36.12, not go.mod's v1.36.11 — matching the stamp is what reproduces exactly). Job split mirrors the Makefile: `make ci-go` (lint, gen-check, vet, test, synth-data, rls-check — zero Node setup) runs in go-ci; `fe-lint` keeps its exact check name (no branch-protection churn) and absorbs `fe-test` (vitest), the two jobs now racing in parallel; local `make ci` = `ci-go fe-lint fe-test` unchanged for devs. New standing gate `make gen-check` (regenerate → `git diff --exit-code` on api/gen + frontend/src/api/gen) is part of ci-go, so generated-code drift fails CI loudly instead of shipping stale code. |
| 259 | Bug fix | Multi-adapter (ADR-0005 amendment) | **Legacy model refs can no longer brick worker saves (prod QA: phantom adapter kinds); validation unified with the picker; discovery freshness is invalidation-based.** Root cause: pre-namespace refs (free-form `provider/<slashed-model>`, e.g. `commandcode/deepseek/deepseek-v4-flash` — every canned worker's seeded ref) parse under the new left-greedy grammar to a PHANTOM adapter kind (head `commandcode`, never registered), so `validateAdapterChange` gated every save as an "adapter change FROM commandcode" and rejected the correct `opencode/commandcode/deepseek/deepseek-v4-flash-fast` ref (`commandcode` is only an orchicon-kind provider in the static catalog). Dev/prod divergence explained: identical binaries, but the gate is data-dependent (stored refs + tenant custom providers differ) and validation registries differed per path (settings/picker ran the CLI-aware registry — CLI discovery serves `commandcode` via the `opencode-cmd-provider` plugin — while worker validation kept a static catalog; `SetModelRefRegistry` had zero production callers). Fixes: (1) ADR-0005 amended + tests pinned — a current ref with an unregistered head is LEGACY DATA, not an adapter selection (adapter-change gate skipped; the new ref validates as a fresh selection), and an identical-ref re-save is a pure no-op (no grammar re-validation, whitespace-equal counts as identical; generalizes ADR-0004 D5 to the full legacy surface). (2) Atlas migration `20260917000000_canonicalize_model_refs` (+down) rewrites stored pre-namespace refs to canonical `opencode/<provider>/<model>` across worker_versions and both tenant-settings defaults — the kind cut is pinned in the SQL at authoring time (migrations run pre-server; frozen snapshot, never live-config-dependent); `adapter.NormalizeRef`/`NormalizeRefForMigration` are the Go twins for repair tooling; heals `SplitForServe`'s serve pair (legacy refs were attributing usage to `provider=deepseek`) and `resolveAdapterRowKind`'s dispatch row selection in the same stroke. (3) `api.go` installs the CLI-aware registry into the worker service (`SetModelRefRegistry` now has a production caller): worker validation ≡ settings validation ≡ the picker (builtin ∪ tenant customs ∪ live CLI ids); tenant-custom merge now uses the `adapter.ProviderKindExtender` seam (`Clone`-then-extend, copy-on-write) instead of a concrete-type assertion, with a union-overlay fallback so customs never silently vanish. (4) Model discovery: stale-while-revalidate (TTL expiry serves stale instantly, single-flight background refresh, cold loads wait on the in-flight probe instead of erroring, failure backoff = TTL), plus `Invalidate()` wired to every provider landscape mutation (custom provider CRUD, token save/clear, visibility save) so newly added models are usable immediately — the TTL answers "how often do we re-probe," not "when is new data usable"; the CLIProviderRegistry's own 10-min provider-id TTL is deleted (cache of a cache). Measured on the prod box: the probe costs ~1.2s wall / ~1.9s CPU for ~470 models across 7 providers. BulkUpdateWorkerModel now validates grammar per-worker (batch re-save of legacy-ref workers is a no-op for them, not a batch-wide rejection). |
| 258 | Chore | SDLC pipeline efficiency | **SDLC canned workers become time-boxed workhorses; Vision variants retired; sequential review/QA rewired.** Motivation: a live run ping-ponged 4+ hours on one task — ungrounded design prose, review/QA bouncing fixable findings back as reports, and a parallel PR+QA fan-out that races once both steps mutate code. Canned worker contracts rewritten around hard prompt time-boxes — Architect 20 min (plan grounded in code PROOF: file:line, insertion points, new files, closing numbered mechanically-executable step list; no ADR ceremony), SSE 45 min (executes the architect's plan, no re-planning; chunked-write discipline kept; build+focused tests per chunk; green+pushed before success), PR Reviewer 30 min (fixes all code bugs itself, only escalates genuinely-unfixable; semgrep once), QA Engineer 30 min (true UI + regression testing, fixes all bugs and drives to success; absorbed the Vision trio's Playwright visual-verification block). The prompt box carries ~33% grace via per-worker budget_overrides wall_clock fences (1500/3600/2400/2400s, merged over tenant defaults by scheduler mergeBudgets) — the box expires first, the fence is the emergency stop; cannedWorker gains BudgetOverrides plumbed into seedNewWorker + the roll-forward INSERT (nil preserves the current version's). Vision canned workers (w_se_sse_vision / w_se_architect_vision / w_se_qa_vision) retired: removed from cannedWorkers, added to retiredCannedWorkers (seeder deletes still-seed-managed instances on boot); their tests replaced with a retirement test + a base-QA-carries-Playwright test. Both canned SDLC templates re-baked from the operator's live sequential rewires (non-human v9: Architect → SSE → PR Reviewer → gate → QA → gate → DevOps → conflict gate → End; human v7: Architect → human Design Approval → SSE → PR Reviewer → gate → QA → gate → human Code Approval → DevOps → conflict gate — both loop back to the SSE; parallel fan-out removed, PR Reviewer precedes QA because both now write). Roll marker renamed sdlcFixForwardMarker/reviewFixProposalMarker → sdlcWorkhorseMarker so exactly the four SDLC workers re-roll on boot; lintBlock retired from the QA/PR seeds. The same worker content + fences were applied to the live dev tenant out-of-band via SQL (8 workers: 4 base + 4 LocalModel clones), preserving model_ref and version notes; the w_se_*_vision trio hard-deleted (no workflow-step refs, no live runs). Known live-instance bug found during the audit (NOT fixed in this PR): the prod fork of SDLC (Human) references QA worker id `w_se_qa_engineer`, which does not exist in prod (`usr_w_se_qa_engineer`) — the step fails dispatch on the next run until repointed. |
| 257 | Bug fix + Enhancement | Multi-adapter (ADR-0003..0010) | **Fix-branch QA rounds 2–4 with the operator: provider UX, validator/picker agreement, no-synthesized-data directive, in-box picker, self-healing probes.** Recorded as ADR-0010 (no synthesized data planes — live-truth sourcing, catalog as metadata-enrichment only, mocks deleted incl. MockMCPDiscoverer, live-usage-only, CLI-aware validation registry shared by picker + settings validator, in-box picker contract, plane-aware custom-URL resolution). Key fixes beyond row 256: (1) probe-or-nothing sourcing — a failed probe yields an empty visibly-degraded list with a diagnosable log line (exact URL + failure class), never the vendored snapshot; catalog demoted to metadata enrichment (never contributes model ids); MockModelDiscoverer + mockModels deleted outright. (2) MockMCPDiscoverer removed (last mock plane; well-known MCP list survives as a live-output convenience). (3) Legacy provider pills restored as OPTIONAL filters from live CLI discovery (All reset; red-cursor provider gate is orchicon-only) — and the static LEGACY set deleted; the CLI-aware registry (CLIProviderRegistry: static ∪ live CLI ids, TTL-cached) injected into settings validation (new SetValidationRegistry seam) and the gateway so freshly-saved CLI refs (opencode/deepseek/…) validate — root-cause fix for "provider not found". (4) Settings validator shadowing bug (local `s` shadowed the receiver). (5) In-box model selector (search → adapter pills → provider pills → model list inside one box; closed = selected-ref trigger or always-enabled search input; single CLI query feeds pills + list; no auto-open effects; no mid-flow panel closes). (6) Defaults tab: no auto-open fight, save errors surfaced (try/catch, inline role=alert), picker tiers re-seed on external value loads (blank-until-refresh bug). (7) Context-hint warnings key on CONTEXT only (output-max not warning-worthy); ollama /api/show enrichment now runs before the tags call (ollama CLOUD has no /api/tags); commandcode enrichment via alias-aware GetModelForProvider. (8) Custom providers: auth-mode selects styled (was a blank white box), plane-aware auto-resolution of loopback base URLs in container mode (ORCHICON_CONTAINER_MODE=1, gateway via UDP default-route detection, host AND port preserved after the port-dropping bug — QA round 4), plain-language UI notes (no docker jargon), and a self-healing probe that tries repaired candidates (gateway + /v1 + common ports) and PERSISTS the verified winner (custom-only, concurrent-change guard, audit-logged) so chat turns use the same fix. (9) Probe cache keys fold bearer + base URL. (10) Remaining feature tasks #12/#13/#14 rewritten with binding operator directives; #14 is now a ground-truth gate (standing CI audit fails the build on new synthesized-data fallbacks; human-gated live smoke with operator keys; corrected-contract conformance). ADR-0004/0006 amended; ADR-0010 records the directive. |
| 256 | Bug fix | Multi-adapter (ADR-0003..0006) | **Picker model-tier + provider sourcing integration fixes (human QA round 1 on the dev instance).** Four defects from live QA of the multi-adapter feature: (1) the model picker's model tier called opencode-CLI discovery for EVERY adapter — for the native `orchicon` kind the CLI can never emit the orchicon provider namespace, so the model list was structurally empty; the tier is now per-adapter (native → providers-service sourcing view: vendored catalog ⊕ credentialed probe ⊕ manual, projected into the picker shape; legacy adapters keep the CLI path, hooks run unconditionally per rules-of-hooks). (2) The adapter catalog seeded orchicon with provider id `command-code` while the provider layer/catalog/profile table use `commandcode` — `commandcode` is now canonical everywhere (seed, grammar comments, Go+TS mirror tests, ADR-0003/0004/0005 examples, Ask Orchicon tool description); the orchicon kind is additionally seeded with ALL built-in provider profiles (the native bridge serves them; validation previously rejected valid selections). (3) The sourcing probe sent no credentials (env-only) and ignored built-in base-URL overrides — the probe now carries the CredentialResolver's secret-first bearer, probes built-ins when their effective base URL differs from the built-in table (override endpoint authoritative), and the probe cache is keyed by provider⊕bearer-hash with InvalidateProbe dropping all credential variants (a token rotation can no longer be served a stale unauthenticated entry). (4) The Providers tab auto-saved hidden-models per checkbox click — visibility is now staged with an explicit Save/Discard pair (single update, ADR-0006 D4 operator flow). Session lifecycle: proactive refresh was armed only on interactive login, so every full page load drifted to a dead 15-minute access token and mutations surfaced raw `ConnectError: [unauthenticated]` — the bootstrap path now arms the proactive refresh (refresh-bootstrap and token-verify), and a definitively dead session clears the token/session store and lands on /login?next=… instead of an inline error string. MockModelDiscoverer fallback is now local-mode-only (a production plane without opencode serves an actionable Unimplemented, never 4 fake models) and `ORCHICON_OPENCODE_BIN` (the path the discovery error always advertised) is actually read as the binary override. |
| 255 | Chore | SDLC pipeline efficiency | **Fix-proposal requirement on reviewer/QA failure verdicts.** PR Reviewer + QA Engineer (+ QA Vision): when reporting `failure` on judgment-class findings, the verdict must include the specific fix the reviewer would apply — exact code change, chosen option among alternatives, invariants checked — marked `PROPOSAL — not applied`. Motivation: the first fix-forward-loop run showed the reviewer reproducing a blocker (typed-nil panic), mapping three candidate fixes, then discarding the analysis; the SSE re-derived the diagnosis from scratch next iteration (expensive). The proposal converts the loop into apply-and-verify. New per-family roll marker (`reviewFixProposalMarker`) re-rolls exactly PR Reviewer / QA Engineer / QA Vision on boot; SSE + Architect content unchanged (keep `sdlcFixForwardMarker`). Applied to the live dev tenant out-of-band (PR Reviewer, QA Engineer, PR Reviewer - LocalModel, QA Engineer - LocalModel) via SQL roll-forward preserving model_ref. |
| 254 | Chore | SDLC pipeline efficiency | **Trimmed SDLC (non-human) template + SDLC worker prompt hardening.** Canned non-human SDLC template drops the Design Approver and Code Approver approval steps (11 → 9 steps; Architect → SSE → QA+PR Review parallel → Loop → DevOps, matching the live forked template v8) — approver verdicts rarely fired the loop and each approval step carried its own back-edge into the SSE. Fix-forward contracts replace the reviewers' "YOU DO NOT MODIFY CODE" blocks: PR Reviewer + QA Engineer (and Vision variants) fix mechanical findings themselves (gofmt, imports, test harness), re-verify, and report success-with-notes; only judgment-class blockers (semantic bugs, security, design) report failure and loop back to the SSE — verdicts emitted via the `ORCHICON WORKER SUMMARY:` line. SSE (+ Vision) gains anti-stall discipline: recon time-boxed to ~15 min, files written in chunks (never one giant generation — today's dominant failure was `stalled:no_progress` killing long single-turn writes across Architect, Design Approver, and SSE), build+test after each chunk, facts_learned read-first, commit+push before success. Principal Architect (+ Vision) gains a mandatory completeness gate: no open decision may block implementation — every file-level, interface-level, and wiring-level decision stated (open questions recorded as `DECISION (revisitable):`), plus the same chunked-write discipline. Per-worker roll-forward marker (`sdlcFixForwardMarker`) re-rolls exactly these six canned workers on boot without touching the global fleet markers; the architect seed test now expects 9 steps for the non-human template (11 for human-approval). The same prompt content was applied to the live dev tenant out-of-band via SQL (not shipped here — the seeder roll-forward covers every future boot). Developer.md's stale `orchicon-postgres` backup command fixed (Postgres runs inside the instance container). |
| 253 | Chore | Release/Site | **Post-release housekeeping for v0.2.0: UPDATES trim + site footer version.** UPDATES.md trimmed of every released row (208–252 dropped, numbering untouched — the boundary derives from the previous release's copy of this file, so this is safe); the file now carries the next release cycle only. `site/index.html` footer version bumped v0.1.293 → v0.2.0 (the prep PR bumped the "Latest release" bar but missed the footer); `scripts/install.ps1` usage-comment example bumped v0.1.173 → v0.2.0 to match install.sh. Rides the first develop merge after the release per the trim contract, then a bare develop→main merge (NO release label — main's tree gets the site fix, Cloudflare Pages rebuilds, and auto-release.yml stays idle because the label gate doesn't match). The v0.2.0 GitHub Release itself published clean on the tag-push run (all 6 platform assets + GHCR images; the first workflow_dispatch attempt failed on a transient `npm ci` and was superseded). |
