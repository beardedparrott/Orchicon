# UPDATES.md

> Track record of what has been shipped, phase-by-phase.
> Read this before starting any work to understand the current state.

| # | Phase | Status | What landed |
|---|---|---|---|
| 102 | Context files confined to the project directory | done | v0.1.219 (follow-up): context paths that live OUTSIDE a project's `project_dir` are now rejected — they were pointless, since `project_dir` is the only directory guaranteed to be bind-mounted into the containers where workers run, and a path outside it is invisible to the worker (individual paths were only mounted at container-create time, so newly added ones silently failed). New shared `contextfiles.ValidateWithin(paths, projectDir)` (lexical: the path must be the project dir itself or a descendant; empty `project_dir` still applies the base `Validate` rules) wired into the Connect `UpdateProject`, `CreateWorkItem`, `UpdateWorkItem` handlers and the Ask Orchicon `create_work_item`/`update_work_item` tools — so the API/MCP surface can no longer persist an unreachable context path (rejected `InvalidArgument`). The FileBrowser's checkbox tree was already rooted at `project_dir`; the arbitrary-filesystem "Change/Set directory" browse button is now hidden in work-item mode (it couldn't change a project's root there and only offered the pointless out-of-project browse). Docs updated (DOCUMENTATION.md work-item context section). Tests: `TestValidateWithin` (inside/outside/empty-dir) + DB-backed `TestWorkItemContextFilesWithinProjectDirDB` (inside accepted, outside rejected on both Create and Update). Verified: `make ci` green and a browser check that the work-item context picker shows only the project-rooted tree while the project page keeps its "Change" root affordance. |
| 101 | Develop-first branching + UI worker rewrite + follow-up chat + context-file refresh + retry failed step | done | v0.1.219: five changes. **(1) `develop` is the new integration branch** (repo default, mirroring `main`'s branch protection: 1 approval, linear history, no force-push/deletes). Workers branch off `develop`, PR + merge into `develop` only, and never touch `main` — releases are made exclusively by the human merging `develop` → `main` (the auto-release workflow already fires only on `main`, so per-PR releases are gone). AGENTS.md/DOCUMENTATION/CLOUDFLARE_SETUP updated; pre-commit hook blocks `develop` too; PRs into `develop` never carry the `release` label and README's Last Release Changes is only maintained on the human's release merge. **(2) Canned UI workers rewritten** as an architect/developer/QA engineer who ALSO specializes in UI (skills preserved, tighter prompts, develop-first git workflow in every canned worker, seed marker `orchicon.safety=v9→v10`); the three UI workers now default to `opencode-go/mimo-v2.5` (per-worker `cannedWorker.ModelRef`, all published versions aligned) and are **deleted + recreated under their canned ids** (`RecreateSlugOwner`) so stale adopted ULID workers are purged (verified on prod: the `01KZ…` UI workers are gone, recreated as `w_ui_*`). **(3) Follow-up chat bubble appears immediately** on a completed execution: `ContinueSession` records the `user_message` (+ fresh `session_info`) into the durable transcript BEFORE the model replies, and `SessionChatPane` shows an optimistic bubble instantly (deduped against the transcript on settle). **(4) Context-file listings auto-refresh** — `useListProjectDir`/`useListDirPath` poll every 5s (the browser now shows the newest host files; the bind-mount view was already live, the frontend just never re-fetched), context-file saves invalidate the tree cache, work-item `context_files` changes trigger an immediate mount-manifest refresh via exported `project.NotifyProjectChanged`. **(5) `RetryFailedWorkflowRun` RPC** + "Retry failed step" button on a failed run view + Ask Orchicon tool: resets a failed run to `pending` (cleared `ended_at` via new `ClearEndedAt` DAO field), re-arms every active failed/skipped/blocked step run as `pending` (cleared result/execution/attempt/ended), flips the bound work item back to `running`, and the reconciler re-creates the runtime container and resumes from where it left off (succeeded steps kept). Also fixed a reconciler bug the retry exposed: a failed run now skips ALL non-terminal step runs (was only those iterated after the first failed step in `created_at` order). Verified: `make ci` green, DB-backed seed + workflow retry tests, and live E2E on dev/prod — ULID UI workers purged on prod, retry reset a real failed run (failed→pending→running→re-dispatch), failed-run steps show `skipped`, follow-up bubble appears instantly with the reply "Yes" recorded, and `ListProjectFiles` polls every ~5s. |
| 100 | Acceptance Review field on work items | done | v0.1.216: work items gain a first-class **Acceptance Review** field. New `work_items.acceptance_review` column (migration `20260808120000_work_items_acceptance_review.sql`, `ADD COLUMN IF NOT EXISTS`, forward-only), proto `WorkItem.acceptance_review = 27` + `UpdateWorkItemRequest.acceptance_review = 23` (optional), threaded through every work_items full-row SELECT/RETURNING + Scan in the data-access layer (Create/Get/List/ListDirectChildren/Update/ListReadyTasks + the two raw UnassignWorker SELECTs), plus `UpdateWorkItemFields.AcceptanceReview *string`. **Auto-population:** the `WorkflowReconciler` writes a deterministic markdown review in the SAME transaction as the item's terminal status flip (succeeded branch, failed branch, and the run-start image-resolution failure path). `buildAcceptanceReview` → pure `formatAcceptanceReview` aggregates the run's own step results (per-step `_summary`/`_decision`/`_issues`, the same data `buildRunNarrative` reads): succeeded steps with non-empty summaries → "What was delivered", failed/blocked steps → "Not delivered / needs attention", recovery episodes → "Recovery" section; skipped/empty/superseded steps are omitted; capped at 1 MiB. Deliberately NO LLM summarizer on the reconciler hot path (invariant #7 — no implicit model selection). **Mutability:** `ValidateAcceptanceReview` (trim + bound, exported), wired into the Connect `UpdateWorkItem` and the Ask Orchicon `update_work_item` tool (`tools.go` property + description added) so humans can correct/extend auto-generated reviews. **Frontend:** Acceptance Review card on the work item detail page (view = `<Markdown>`, edit = `<Textarea>`, empty state) + `acceptance_review` in the YAML view data + onSave mapping. Docs: DOCUMENTATION.md (work item section + MCP field table). Tests: `ValidateAcceptanceReview` unit, `formatAcceptanceReview` pure tests (completed/failed/recovery/empty fallback), MCP all-fields update round-trip extended. Verified: buf lint, codegen clean, `go build`/`go vet`/`go test` (only pre-existing `internal/guard` noexec-`/tmp` failures), frontend `tsc -b` + vite prod build, `make migrate-hash`. DB-backed tests need `ORCHICON_TEST_DSN` (skipped without Postgres in the workflow container). |
| 99 | Directories as context + work item context files | done | v0.1.216: **directories are first-class worker context, and work items carry their own `context_files` exactly like projects.** A new leaf package `internal/contextfiles` (one validator, one resolver, one renderer) serves both `projects.context_files` and the new `work_items.context_files`: paths may be files OR directories. In the worker prompt a **file** stays inlined (now capped at 256 KiB with a truncation note) and a **directory** is expanded into a bounded listing (up to 1000 entries, skipping VCS/build noise) with an explicit instruction to read every file and NOT open the directory path as a file — killing the `read <dir>: is a directory` failed-read note and the worker's "not a file" tool error. **Work items** gained the column (migration `20260808104050_work_items_context_files.sql`, threaded through every db query incl. the two raw UnassignWorker SELECTs and ListReadyTasks), proto fields (`WorkItem.context_files=26`, create=16, update=22 `optional ContextFiles` empty-list-clears), service validation/persistence, a `# Work item context` section in the composite prompt (workflow + standalone dispatch), FileBrowser cards on the new/edit/detail pages, a shared `contextMounts` helper + project-mount-manifest union so context paths are mounted into the single-container instance and per-workflow runtime containers, and Ask Orchicon/MCP `context_files` parity. Standalone dispatch no longer falls back to a worker-only prompt — `buildStandaloneComposite` renders worker + task + project context + work item context + instructions. Tests: `contextfiles` unit suite (validate/resolve/walk/render), DB-backed `TestWorkItemContextFilesDB`/`TestContextFilesColumnExistsDB`, standalone composite tests. Verified: buf lint, codegen, `go build`/`go vet`/`go test` (only pre-existing `internal/guard` noexec-`/tmp` failures), frontend `tsc -b` + eslint + prod build. DB-backed tests need `ORCHICON_TEST_DSN` (skipped without Postgres in the workflow container). |
| 98 | Cost explorer: work item name in workflow breakdown | done | v0.1.215: the "By Workflow" cost explorer breakdown under Telemetry now shows the bound work item's **name** as the primary run label instead of the raw run ID, with the ID demoted to a muted secondary (and falling back to the ID entirely for one-shot runs with no bound ticket). Server-side, `GetWorkflowRunCosts` (`internal/db/usage.go`) LEFT JOINs `work_items` on `workflow_runs.work_item_id` so each run row carries `work_item_id` + `work_item_name`, surfaced through new `WorkflowRunCost` proto fields 7/8 (codegen regenerated for Go + TS); the frontend `WorkflowCostPanel` (`frontend/src/routes/telemetry.tsx`) renders `workItemName || workflowRunId.slice(0,12)` as the primary label. Tests: new DB-backed coverage in `usage_test.go` asserting the joined name is returned and empty for one-shot runs. Verified: buf lint, codegen, `go build`/`go vet`/`go test`, frontend `tsc`, and end-to-end against live Postgres. |
| 97 | MCP: full work item field coverage | done | v0.1.214: every field the Connect API can set on a work item is now settable through the MCP. `orchicon_create_work_item` and `orchicon_update_work_item` expose the full mutable set — `budgets` (JSON validated), `context_window`, `workflow_id`, `scheduled_start_at` (ISO 8601 or "N minutes from now"), `auto_start_workflow` (opt-in), `runtime_image` — plus `project_id` reassignment (target must be active) and `workflow_run_id` (update-only); new `orchicon_assign_worker`/`orchicon_unassign_worker` tools mirror `AssignWorker`/`UnassignWorker` for `assigned_worker_ref`. The tools reuse the Connect service's shared validators (exported `ValidateTitle/Description/AcceptanceCriteria/Budgets/WorkerRef/Status`, `IsActiveRunStatus`, `WorkItemInProject`) so the two surfaces cannot drift, and replicate its downstream effects: setting a schedule flips status → `scheduled` (ADR-001, guarded against in-flight runs), `auto_start_workflow=true` clears any schedule and starts the bound run immediately via `workflow.StartWorkflowDirect` (real service call with its outbox event), and MCP mutations now enqueue `work_item.created`/`updated`/`kind_changed` outbox rows in the same transaction (invariant #3). `update_work_item` switched to pointer-based params ("unset" ≠ zero), returns the full post-write row, and fixes the stale status enum copy. Tool schemas (`tools.go` Properties) carry the new fields; the MCP `tools/list` exposes them and the agent system prompt auto-generates from the registry. Tests: DB-backed coverage for all new fields incl. budgets rejection, the status flip, auto-start clearing the schedule, project reassignment, workflow_run_id, and assign/unassign. Verified: buf lint, codegen clean, `go vet`, `go test` (whole suite green except the pre-existing `internal/guard` login-shell PATH failure present on `origin/main`), RLS gate, and an end-to-end `orchicon mcp` smoke test against live Postgres (create/update/assign/unassign round-trip with status flip + outbox rows confirmed in DB). |
| 96 | Schedules: running workflows + save-with-schedule status flip | done | v0.1.213: two bug fixes. **(1) Running view shows any currently-running workflow.** The Running tab previously only listed scheduled items that had started (predicate `scheduledStartAt && {4,5,9}.has(status)`), so a workflow started manually or via "Start immediately on save" left its ticket `running` with `workflow_run_id` set but `scheduled_start_at` NULL — an in-flight run was invisible. The predicate now requires only `workflowRunId && ACTIVE_RUNNING_STATUSES.has(status)` (RUNNING/CHECKPOINTING/RECOVERING — statuses only ever set for tickets bound to an in-flight run), and the card's start time/sort falls back `scheduledStartAt → updatedAt → createdAt` (`runningStartedAt` helper). Frontend-only (`frontend/src/routes/schedules.tsx`); interaction model, bulk Cancel, and empty state unchanged. **(2) Saving a schedule on a work item flips its status to `scheduled`.** `UpdateWorkItem` stored `scheduled_start_at` without touching status, so an edited item never appeared in Upcoming and never fired via `ScheduledRunReconciler`. The handler now sets `status = scheduled` when the request carries `ScheduledStartAt` (applied after the kind-switch plan so a non-schedulable kind switch that clears the schedule always wins), scoped to the single edited item — never a bulk flip. Guarded against in-flight runs (`running`/`checkpointing`/`recovering` keep their run status so the reconciler cannot re-arm a duplicate run) and the form's status echo is overridden. Docs updated (`DOCUMENTATION.md` §Viewing Schedules). Tests: new DB-backed `TestUpdateWorkItemScheduleFlipsStatusDB` (ready→scheduled, echoed-status override, sibling untouched, running-item guard, kind-switch precedence, clear path). Verified: buf lint, `buf generate` clean, `go vet`, `go test` (workitem + suite green; only pre-existing `internal/guard` failures from this container's noexec /tmp), frontend `tsc -b` + eslint, RLS gate against a live Postgres. |
| 95 | Schedules page: Running view | done | v0.1.212: the schedules page gains a third **Running** view between Upcoming and History. When a scheduled work item fires, `ScheduledRunReconciler` flips it to `RUNNING` (setting `workflow_run_id`) and it leaves Upcoming but isn't yet terminal — it disappeared from the page for the whole run. The new view shows exactly that gap: scheduled work items in an active, non-terminal, non-scheduled status (RUNNING/CHECKPOINTING/RECOVERING) with `scheduled_start_at` set, so users see what is in flight at a glance. Frontend-only change (`frontend/src/routes/schedules.tsx`): the `view` search param enum extends to `["upcoming","running","history"]`; `RunningView` mirrors the Upcoming/History interaction model exactly (search input, kind filter, sort toggle, select-all + per-item checkboxes, selection count, Bulk Cancel with the same cancel-to-cancelled semantics, empty state), and each card reuses the existing layout with StatusPill for live execution status and a RunChip to the bound workflow run. Data access follows the documented History pattern: `useListWorkItems` broad fetch (no status filter, pageSize 1000, 5s poll) derived client-side to the running set (`scheduledStartAt && {4,5,9}.has(status)`), isolated behind `useListWorkItems` so a future backend status-union filter can swap in with zero layout change. Verified: `make ci` green, typecheck/eslint/prod build, all 70 tests, and a live headless-chromium render of the toggle navigation. |
| 94 | Persistent worker sessions + live session chat | done | v0.1.211: worker executions run as **persistent opencode sessions** instead of one-shot `opencode run` subprocesses. A supervised, always-on host serve (`internal/opencode/servehost.go`) hosts the in-process population (standalone dispatches, follow-ups, Ask Orchicon later) against a dedicated data dir; per-workflow-run runtime containers host their own serve (`Cmd:"serve"` detached child, 0.0.0.0:4096, supervisor owns the password), reached **directly on the docker bridge** (`CreateResponse.ServeURL = http://<container-ip>:4096`) — no published port (a containerized plane can't reach host loopback; docker-proxy races a lazily-starting serve). One `SessionClient` (create / `prompt_async` / abort / permission auto-reply + `/event` SSE mapped to the legacy event shape, fed into the unchanged `parseEvent` pipeline). Completion is driven by `session.idle` (a user message spans many steps). Guardrails: fatal stalls + wall-clock abort the session; the advisory no-file-progress stall sends a **liveness probe** that revives on any post-probe activity (`recovered:liveness_probe`) and fails on no-response — resolving the analyst-false-positive class. **Durable transcript** `execution_session_parts` (RLS, cascade-delete) records every side of the session; the execution page is an **Ask-Orchicon-grade chat** (`SessionChatPane`: user right / assistant left, collapsible tool cards, auto-stick scroll, composer that nudges the live session with no new work item, Stop). New RPCs `SendExecutionMessage` + `GetExecutionSession`; Ask Orchicon gains a `send_execution_message` tool. Root-caused on live runs: container-plane→host-loopback routing, the serve hang on eager MCP connect (`SkipUserMCP` for the container serve — the `orchicon` docker-exec MCP entries wedge it), and serve cold-start (serveUsable session-create gate + 4× setup retry). `make ci` green; verified end-to-end: host serve + free model (PONG), false-positive repro (probe → revive → success), mid-run human message acted on by the worker, and the runtime-container session path (transcript via the container-IP serve). | Follow-ups run IN the session (`ContinueExecutionSession` — no new execution/work item; re-attach to the original session when its serve is reachable, else re-seed a host-serve session with the transcript as context; the exchange is recorded inline in the chat). The chat shows the full system prompt as the first (right-aligned, collapsible) bubble, full history when joining mid-run, and grouped streaming text/reasoning so a turn reads as one bubble; auto-stick scroll + transcript refetch on completion. gh now authenticates inside containers: the operator's token lives in the OS keyring (invisible to containers) — the host's effective token is injected as `GH_TOKEN` into runtime + main containers and `.config/gh`/`.local/share/gh` are mounted ro.

| 93 | Start immediately on save defaults to OFF (bug fix + feature) | done | v0.1.210: `auto_start_workflow` on work items is now **opt-in**, never the default. **Bug** — changing a work item's kind no longer kicks off a workflow run: the old post-commit auto-start guard only suppressed the run when the switch cleared the scheduled start (switch to a non-schedulable kind per ADR-WIT-2), so a switch between schedulable kinds (Task → Subtask) on an immediate (unscheduled) auto-start item still fired the run the user never asked for. `UpdateWorkItem` now suppresses auto-start on ANY kind switch unless `autoStartWorkflow=true` is explicitly sent in the same request (explicit opt-in still wins). **Feature** — `CreateWorkItem` defaults an unset `auto_start_workflow` to `false`, the DB column default flips to `FALSE` (migration `20260807120000_work_item_auto_start_default_false.sql`, `COMMENT` updated), and the edit form's "Start immediately on save" checkbox always opens unchecked — including legacy rows whose stored value is `true` — so saving an edit never starts a run by surprise. Scheduling is unaffected (`ScheduledRunReconciler` fires on `scheduled_start_at IS NOT NULL`, independent of the flag). Proto comments updated + codegen regenerated (Go + TS). Tests: `TestKindSwitchAutoStartDB` extended with the Task → Subtask case (no auto-start without explicit opt-in; explicit true still fires) and new `TestCreateWorkItemAutoStartDefaultDB` (unset→false, explicit true→true, explicit false→false); both verified DB-backed against real Postgres. |
| 92 | Work Item Tweaks (kind switching, expand/collapse all, parent picker revamp) | done | v0.1.209: **Proto** — `UpdateWorkItemRequest.kind = 21` (optional; unset = unchanged). **Kind switching** — a new shared `workitem.ResolveKindSwitch` (used by the Connect `UpdateWorkItem` handler AND the Ask Orchicon `update_work_item` tool so they cannot drift) resolves the hierarchy inside the same tenant transaction: switching to an Epic clears the parent; other kinds keep the current parent when it is strictly shallower or walk up the ancestor chain to the nearest shallower ancestor; an Epic switched to a non-Epic must pick a parent explicitly. Direct children that can no longer sit under the switched item are reparented to its resolved parent (they become siblings). Switching to a non-schedulable kind (Epic/Feature) clears the worker binding and scheduled start and demotes ready/assigned/scheduled to pending so `ListReadyTasks` can never dispatch a re-typed item. Running/checkpointing/recovering items and items with an active workflow run are rejected (`FailedPrecondition`). Outbox emits `work_item.kind_changed` (old_kind + new_kind) plus `work_item.updated` for every reparented child, all in one transaction. No migration (kind CHECK already existed). **Expand/Collapse all** (ADR-WIT-4) — buttons in the Work Items filter bar act on every row with children in both views; the preferences hook mutates the correct persisted set per view (board collapsed set, tree expanded set when unfiltered, tree collapsed set while a filter is active); pure `parentIds()` helper exported + tested. **Parent picker revamp** (ADR-WIT-5) — new `WorkItemParentSelect` component (searchable single-select popover, zero new deps) with color-coded kind badges, arrow-key navigation, Escape/click-outside, empty state; replaces the plain parent `<select>` on the create page and the edit page Parent card (candidates follow the SELECTED kind). Detail page gains a Type control with a confirmation describing the auto-resolution before saving. Tests: `ResolveKindSwitch` unit + DB-backed (walk-up, child reparent, schedulability cleanup, epic→non-epic explicit parent, running-item guard, outbox event), preferences `parentIds`, parent-select `filterParentOptions`. **Review fixes** — kind switch + project move now honors an explicit parent in the target project (the carried-parent guard no longer falsely rejects after `ResolveKindSwitch` validated it), rejects a "keep current parent" reparent when the current parent lives in the old project (and the walk-up itself refuses to cross projects), suppresses the post-commit auto-start when the switch cleared the schedule (an explicit `autoStartWorkflow=true` still wins), clears a stale parent in the create form when the kind changes, and the picker no longer displays a parent that is not a valid candidate for the new kind. New DB-backed tests: `TestKindSwitchProjectMoveDB`, `TestKindSwitchAutoStartDB`. |
| 91 | View and change parent from the work item edit page | done | v0.1.208: work item detail/edit page now shows the parent and lets you reparent. **Proto** — `UpdateWorkItemRequest.parent_id = 20` (optional; empty string = clear parent, unset = unchanged). **DB** — `UpdateWorkItemFields.ParentID *string` mirroring `WorkflowID` (empty clears to NULL), included in the update RETURNING/outbox payload (`parent_id` on work_item.created/updated events). **Validation** — the Create/Update depth rules were extracted into a shared `workitem.ValidateParent` (top-level = epic only; parent must be in the same (effective) project; child must be strictly deeper, which also makes reparenting cycle-safe) used by both Connect handlers and the Ask Orchicon `update_work_item` tool (`parent_id` property added). A cross-project move that carries a stale parent into the new project is rejected until the item is explicitly reparented there. **Frontend** — `work-items_.$id.tsx` gains a Parent card: view mode shows the parent's KindPill + title (linked, resolved from the project graph), edit mode shows a dropdown of candidate parents (same-project items at a strictly higher depth, excluding the item itself) rendered as `title (kind)`; candidates refetch from the edit project so the dropdown follows project reassignment; YAML save only sends parent_id when the YAML carries it (orphans/removed lines no longer error on save). `useListWorkItems` gained an `enabled` opt-out so the detail page only fetches candidates while editing. **Tests** — unit tests for the depth/top-level rules plus DB-backed `TestValidateParentDB` and `TestUpdateWorkItemReparent` (explicit reparent, deeper-parent/cycle rejection, clear rejection, cross-project guard, epic move, unknown-parent NotFound). Verified end-to-end against a live Postgres: API reparent flow, outbox payload, embedded frontend, and a headless-Chromium UI round-trip (reparent via dropdown, cross-project guard error surfaced). |
| 90 | Work Items page bug fixes (post-#207 merge) | done | v0.1.205: fixed the six regressions the #207 merge introduced. **(1) Filter semantics** — Status/Type multi-selects now treat an EMPTY selection as "show nothing" instead of "show everything": defaults are every option selected (Epic/Feature/Task/Subtask + the four recovery kinds; all ten statuses incl. Checkpointing), so unchecking every type or status shows an empty page on both Tree and Board. `filterItemsByKindStatus`/`buildTreeData` now require `kinds.includes(kind) && statuses.includes(status)`; `hasQuery`/`filterActive` only fires when the selection differs from the full option list (or a search is typed) so the default view doesn't dim rows or auto-expand the tree; `MultiSelect` empty labels updated to "No types"/"No statuses"; storage envelope version bumped 1→2 so stale v1 filter envelopes (old empty=all) fall back to the new defaults instead of rendering an empty page. **(2) Tree collapse stuck under filters** — while a filter/search was active the tree force-expanded ancestors and the chevron click was ignored ("stuck"); a new persisted `treeCollapsed` set (`orchicon.workItems.treeCollapsed.<projectId>`) records explicit collapses during filters so parents collapse AND the choice survives navigation. **(3) Board collapse** — board parents default EXPANDED again (the merge made them default-collapsed, so checking "Epic + anything" only showed epics: children were hidden); the board now persists `boardCollapsed` (empty = all expanded) and the parent chevron collapses/expands children and survives reload. Tests updated: preferences (new defaults, collapsed sets, v2 migration), filter semantics (empty = nothing, full-list default). Frontend-only: no proto/DB changes. |
| 89 | Work Items page: persisted views + multi-select filters + bulk moves | done | v0.1.204: **Tree view** — expand/collapse auto-saves per project (`orchicon.workItems.treeExpanded.<projectId>`), so the hierarchy restores when you come back. **Board view** — parents get expand/collapse controls (default collapsed, `boardExpanded`), and dragging one selected card moves the **whole selection** (multi-drag); dnd-kit `autoScroll` disabled so drops land on the visible column under the pointer (was silently moving cards to the wrong status near the viewport edge). **Both views** — Status/Type filters are now checkbox multi-selects (`components/ui/multi-select.tsx`, Radix) with OR-within-group semantics, saved immediately and restored per project; the last Tree/Board view is persisted as the default. **Bulk moves** — `batch-move.ts` shares one validation gate + `Promise.all` transport with partial-success toasts ("Moved 3 to Ready · Skipped 2: …") between board multi-drag and the selection toolbar's "Move to…" select. Iteration-2 fixes: optimistic cache writes now use `setQueriesData` with the trimmed prefix key (the bare `setQueryData` wrote to a phantom 3-element key the live 4-element list query never read — cards flashed back to origin); MultiSelect footer actions are Radix menu items (keyboard-reachable via arrow keys, previously plain buttons); status names title-cased in filter dropdowns/move menus/toasts to match the column headers (`titleLabel` on `StatusMeta`, pill stays lowercase). Tests: preferences parsing, array-filter semantics, title-label regression. Frontend-only: no proto/DB changes. |
| 1 | Foundation | done | Go module skeleton, Protobuf schema, Connect codegen, Atlas migrations, Docker Compose (PG+NATS+SigNoz+OTel), Makefile, Vite+React+tailwind shell |
| 2 | Projects slice | done | Full-stack Project CRUD: handler, data-access layer (pgx + tenant scoping), outbox relay, frontend list/detail/create forms |
| 3 | Realtime + infra | done | `orchicon dev` subcommand, outbox metrics, reconciler framework, OTel pipeline, NATS-streamed events, `useStream` hook with reconnect |
| 4 | Workers + WorkItems | done | Worker lifecycle service, WorkItem CRUD + DAG with cycle rejection, edit locks, frontend catalog + Kanban + dependency graph |
| 5 | Scheduling + adapters | done | TaskReconciler, RuntimeAdapterService + bridge, ExecutionService with streaming + controls, OpenCode CLI adapter, execution live view |
| 6 | Workflows | done | Workflow CRUD + versions, WorkflowReconciler (step DAG progression, gate eval, task handoff), React Flow visual editor, run view with live overlay |
| 7 | Recovery + Policy | done | Policy service + Rego engine (OPA v1), RecoveryService + RecoveryReconciler (6-step flow, auto-relax, L1→L3 escalation), frontend policy editor + recovery timeline |
| 8 | Telemetry + Cost | done | `correlation_id` propagation, TelemetryService (SigNoz proxy), AI Gateway usage dual-write, cost explorer with drill-down, embedded SigNoz UI |
| 9 | Auth + Webhooks + Polish | done | OIDC auth (dev + production), API key hashing, RBAC interceptor, webhook dispatcher with HMAC + backoff, BlobStore (local + S3), admin views |
| 10 | Project context files | done | `project_dir` + `context_files`, FileBrowser component, `ListProjectFiles` RPC, file injection into composite prompt |
| 11 | Feature adds | active | Ongoing feature additions for gaps identified after initial build-out |
| 12 | Bug fixes | active | Ongoing bug fixes and polish |
| 13 | Polish: docs + website | done | README and landing page overhaul (--clean flag, commands, installed files, custom LICENSE). AGENTS.md restructured with concise Git Workflow, Phases, UPDATES.md tracking. Playwright MCP added for browser testing. |
| 14 | Single decision signal | done | v0.1.179: workflow routing now reads ONLY the `ORCHICON WORKER SUMMARY: success\|failure` word — the old `_decision:`-line extraction and the `_issues:` → forced-failure hard rule are gone (they caused infinite loops when a worker's prose like "non-blocking, not `_issues:`" was substring-misparsed as blockers). `_issues:` is now line-start-only and informational (run view + `.orchicon/issues`). Composite prompt, re-ask prompt, AI Approver seed, and DOCUMENTATION.md updated to the single-signal contract; unit tests cover the exact output shapes that previously looped. Root-caused on dev run `01KZ55262Z2QSPV17NQZPSV8PZ` (4-iteration loop, aborted). |
| 15 | Sanctioned /tmp/orchicon scratch dir | done | v0.1.179: workers writing scratch outside the project (e.g. screenshots to /tmp/opencode) hit "The read tool can't access" because `external_directory: deny` hard-blocked it. `external_directory` is now object-form — catch-all `*` deny + a single precise `"/tmp/orchicon/**": "allow"` carve-out. The runtime supervisor pre-creates `/tmp/orchicon` (runtime user), and the composite prompt's runtime-environment block tells workers it's the sanctioned ephemeral scratch location. The pattern deliberately excludes the supervisor socket, guard shims, and `/tmp/opencode-data-*` auth.json copies. Verified E2E against the real opencode runtime with a free model: reading `/tmp/orchicon/*` succeeds, reading any other `/tmp` path is denied. |
| 14 | Telemetry fixes | done | SigNoz iframe now conditional (avoids app-in-frame when degraded). Telemetry hooks poll every 10s while degraded. Cost Explorer: fixed rollup enum, per-task API lookups for names, working drill-down. Credits tab with provider/model spend. /signoz reverse proxy in Go server. |
| 15 | Telemetry root cause | done | Rewrote SigNozClient to query ClickHouse directly via SQL (bypasses SigNoz v0.132 query-service which uses a different API with mandatory auth). Uses ClickHouse HTTP interface at port 8123 with JSONEachRow format. |
| 16 | OTel log export | done | Added OTel log exporter (otlploggrpc) to pipe adapter stderr into ClickHouse signoz_logs. Telemetry logs tab now shows execution stderr correlated to traces. |
| 17 | Workflow editor polish | done | Uniform node sizing (min-h-[76px]), Clone workflow button, Save Draft fix (immediate cache seed after new version), Delete version (draft-only, with guard), YAML code view toggle with connections format (from_<pos>/to_<pos>). |
| 18 | Infrastructure fixes | done | composeDown force-removes orphan containers on start/stop. composeUp self-heals by pruning leftovers before starting. composeDown outputs stderr to visible logs. |
| 19 | Telemetry proxy + workflow context | done | /signoz SigNoz proxy: removed the asset/css substring rewrites that double-prefixed the SPA's already-relative ./assets/ paths (blank iframes); only the document <base href> is rewritten now. Workflow composite prompt: replaced the single "Upstream context" section with a chronological "Workflow context" timeline that inlines per-step results — TASK workers' full output (truncated to 16K with ORCHICON WORKER SUMMARY cross-ref) + extracted summary + per-work-item _recovery_summary, RECOVER steps' strategy + trigger reason + narrative, WORK_ITEM/PROJECT step titles, plus a "→ Next task (you are here)" block naming the dispatch target. Verified end-to-end on the Story Time workflow. |
| 20 | Telemetry log streaming | done | opencode adapter smart-parses the leading [ERROR]/[WARN]/[INFO]/[DEBUG] token on stderr lines (defaults to INFO) so the full transcript populates the logs tab at INFO instead of only on hard errors. New slog→OTel bridge (internal/telemetry/slog_bridge.go) wraps the control plane's JSON handler with a MultiHandler that fans every record into the OTel LoggerProvider alongside the local log file — outbox, reconciler, dispatch, workflow run progressed, task dispatched, opencode step started/finished, and recovery events all flow into ClickHouse. LoggerProvider resource attribute orchicon.tenant_id=tnt_dev is set so the tenant-scoped QueryLogs filter doesn't drop control-plane records. Fix in SigNozClient.QueryLogs: ClickHouse logs_v2.timestamp is UInt64 nanoseconds (was being passed to time.UnixMicro which expects microseconds → "seconds out of range" on every record). |
| 21 | SigNoz auth + frontend fix | done | PR #132 (v0.1.118): Added SIGNOZ_IDENTN_IMPERSONATION_ENABLED=true and SIGNOZ_USER_ROOT_ENABLED=true env vars to avoid SigNoz enterprise login screen in embedded iframe. Disabled auth in signoz-config.yaml. PR #133 (v0.1.119): Replaced signoz/signoz:latest (enterprise) with signoz/signoz-community:v0.132.2 to work around "failed to validate config user" crash. Ran signoz-schema-migrator to create required ClickHouse databases. Fixed frontend lint (hooks after early return, any type). |
| 22 | TypeScript build fix | done | PR #135 (v0.1.120): Added `as string` type assertions to payload property reads after changing type from `any` to `Record<string, unknown>`. `Record<string, unknown>['text']` returns `unknown` which is not assignable to `string` — the build was failing with "Type '{}' is not assignable to type 'string'". |
| 23 | Pre-commit hook docs | done | AGENTS.md now documents the pre-commit hook that blocks commits on main/master. Includes the full script so agents can recreate it if missing. |
| 24 | SigNoz impersonation password | done | Fixed `orchicon-dev` failing Signoz's `IsPasswordValid` (needs uppercase, number, symbol). Changed to `Orchicon-Dev-42!`. Re-added impersonation env vars and root user config to docker-compose.yml. SigNoz UI now runs without login page (no-auth embedded iframe). |
| 25 | NATS healthcheck fix | done | `nats:2.10-alpine` doesn't ship the `nats` CLI binary, so `nats server check jetstream` healthcheck always failed. Added `-m 8222` to enable NATS HTTP monitor and switched healthcheck to `wget http://127.0.0.1:8222/healthz`. |
| 26 | SigNoz proxy gzip fix | done | Browsers send `Accept-Encoding: gzip`; Go reverse proxy's ModifyResponse received compressed bodies. `rewriteSigNozHTML` silently failed on gzip bytes — iframes got un-rewritten HTML with `<base href="/">`. Fixed by decompressing gzip before rewriting and clearing Content-Encoding/Content-Length headers. Also fixed Vite dev proxy to route `/signoz` through the Go binary (not directly to SigNoz) so the rewrite applies in dev mode too. |
| 27 | SigNoz DSN + telemetry rework | done | Added `SIGNOZ_TELEMETRYSTORE_CLICKHOUSE_DSN` env var — query-service was defaulting to `[::1]:9000` (connection refused). Switched to `signoz-community:latest` (v0.133.0). Fixed tagType enum by adding `scope` via ALTER TABLE to fix OTel collector trace export. Fixed SigNoz iframe explorer paths. Moved iframes to top of panels, compacted StatCards, added scrollbars. Added `scripts/install-local.sh`. |
| 28 | Execution UI fixes + features | done | v0.1.126: Fixed assistant bubble text truncation (70vh scroll container, word-break on all bubbles). Fixed project context files (listed absolute paths instead of relative, added directory headings). Added copy-to-clipboard buttons on assistant/result/artifact cards. Added follow-up chat with inline conversation persistence (`conversation jsonb` column, `CreateFollowUpExecution` RPC). Restored work item creation for follow-ups so AI processing works, writing responses back to the original execution's conversation. Added execution list search/filter/sort/bulk-delete. Added recovery step configuration UI (max_retries, retry_delay_seconds with DB columns). Bounded recovery retries with `countTaskChainRecoveries` (default 5 max → L3 escalation). Default to free model when worker has no model_ref. Added cache-control headers to embedded SPA. Added `is_follow_up` flag to hide follow-up executions from main list. Fixed `DeleteWorkflowVersion` SQL and multiple `is_follow_up` scan mismatches. |
| 29 | Unified page patterns | done | v0.1.127: Added ArrowLeft back buttons to all detail pages (projects, work items, workers, workflows, policies, recovery, dependency graph). Added Code/Detail toggle with EntityYamlView + Clone button to projects, work items, and workers detail pages. Added bulk delete with select-all checkboxes to projects, work items, workers, workflows, and recovery list pages. Added status + decision point filters to policies list. Created reusable `EntityYamlView` component and batch-delete API hooks. Converted all list pages from grid to vertical list format matching executions. Switched `context_files` from relative paths to absolute paths end-to-end — updated validation (`validateContextFiles` now requires `filepath.IsAbs`), `listDirectory` returns absolute `FileTreeEntry.Path`, worker dispatch uses paths directly with backward-compat fallback, and FileBrowser removes the `projectDir` display prefix hack. |
| 30 | Bound-run dispatch (PR-A) | done | v0.1.131: Added `work_item_id` field to `StartWorkflowRequest` and `WorkflowRun` proto + DB migration (nullable `work_item_id`, `bound_worker_ref` on `workflow_runs`). `WorkflowReconciler.dispatchStep` branches on bound runs: when `run.WorkItemID` is set and no canvas markers are upstream, operates on the bound work item directly. One-shot runs unchanged — verified via curl E2E. |
| 31 | Loop decision step (PR-B) | done | v0.1.132: Added `StepKindLoopDecision` to proto/domain/reconciler. Loop decision inspects upstream step result: success → forward; failure → loop back (up to `max_iterations`). Iteration tracking via `iteration` + `superseded_by` on `workflow_step_runs`. Frontend adds Loop Decision palette node (cyan accent, Repeat2 icon), max_iterations config in PropertiesPanel, and cycle-detection relaxation for loop back-edges. Exhaustion → run fails → opt-out recovery (invariant #8). |
| 32 | Scheduled start + binding (PR-C) | done | v0.1.133: Added `scheduled_start_at` + `auto_start_workflow` to work_items. `ScheduledRunReconciler` fires pending bound runs. WorkItem create/update accepts workflow_id. Immediate auto-start on save when applicable. |
| 33 | Template/one-shot types + content-aware loop | done | v0.1.134: Added `type` column to workflows (one_shot/template) with full-stack support — create form type dropdown, filtered list views, restricted palette for templates. Loop decision now parses `_decision` from reviewer output (success/failure), re-asks up to 3x with amended prompt if no signal found. |
| 34 | Hugging Face models script | done | v0.1.136: Added `scripts/hf-latest-models.sh` — fetches latest AI models released today or this week from Hugging Face API with paginated results, pipeline_tag distribution summary, clean output to `/tmp/`. |
| 35 | Worker prompt fields refactor | done | v0.1.139: Replaced single `system_prompt` with four structured fields (Role, Skills, Behavior, AGENTS.md) across proto, DB, Go backend, and frontend. Added pre-canned workers with model_ref defaults. Removed auto-recovery trigger — recovery now via explicit `recover` canvas steps. Loop decision triggers recovery on upstream failure. Added scheduled status (`scheduled` → `running`) for work items. Stall detection improved with `text_loop` signal. Canvas handles colored by direction (emerald input, amber output). Project directory browser separate from context files. YAML code view editable with save support. Delete buttons on all workflow versions. Re-schedule support via `workflow_run_id` clearing. |
| 36 | System prompt delivery + run state | done | v0.1.141: Three bug fixes. (1) The OpenCode adapter was setting `OPENCODE_SYSTEM_PROMPT` as an env var, which opencode v1.x does not read — the composed worker prompt (Role+Skills+Behavior+AGENTS.md) was silently dropped. Switched to writing a custom `orchicon-worker` agent into `OPENCODE_CONFIG_CONTENT` (JSON) and selecting it with `--agent`, which is the mechanism opencode actually consumes. (2) The workflow reconciler kept re-applying `mark step ready` to a superseded step run (a `loop_decision` re-ask creates a fresh run with `SupersededBy` set on the prior run). Every 200ms the optimistic-concurrency check failed with `db: not found`, wedging the whole run. Now every pass skips `SupersededBy != ""` and any run that has already been moved past `pending` in the active `runByID` map. (3) The run view's loop-back edges and `loop_decision` handles are now colored/animated like the editor canvas, the `LOOP_DECISION` kind constant is now correctly 8 (matching the proto), and `RunStepNode` displays the recorded loop decision outcome inline (`re-ask reviewer` / `decision made` / `max iterations reached`). |
| 37 | HF models script: pagination fix + optimization | done | Fixed critical pagination bug in `scripts/hf-latest-models.sh`: the Hugging Face API uses cursor-based pagination (via the `Link` response header), but the script was using `offset` which is silently ignored — every page returned the same 100 models (claimed "500 models" was only 100 unique, duplicated 5×). Replaced with cursor-based pagination parsing the `Link` header's `rel="next"` cursor. Also replaced per-model jq subprocess loop with a single jq call for speed. Exit code fix: unknown options now exit 1 instead of 0. |
| 38 | HF models script: robustness + error handling | done | v0.1.143: Reworked `scripts/hf-latest-models.sh` with several robustness improvements — pagination now uses the full next URL from the `Link` header (extracting the URL between `<` and `>` angle brackets) instead of parsing/reconstructing the cursor, making it resilient to changes in API URL structure. Added HTTP status code checking with specific handling for 429 (rate limit), 5xx (server errors), and non-200 responses. Added network error detection (curl exit code). Added tool dependency check (curl, jq) with helpful error messages. Added macOS `date` fallback (`-v-7d`). Error messages now go to stderr. Stats display shows `—` instead of zero values for cleaner output of new models with no downloads. |
| 39 | System prompt delivery + loop + recovery + version management | done | v0.1.141–v0.1.142: Comprehensive fix batch — (1) System prompt delivery: switched from broken `OPENCODE_SYSTEM_PROMPT` env var to `OPENCODE_CONFIG_CONTENT` with a custom agent, which opencode actually reads. (2) `--dir` flag was silently dropped because it was appended after `exec.CommandContext` — moved before command creation. (3) Loop decision now blocks downstream steps during re-ask/re-enter/upstream-failure by marking itself RUNNING and creating chain runs instead of succeeding instantly. (4) Recovery step marks itself RUNNING when triggered so the poll phase tracks it instead of the run failing immediately. (5) `_decision` extraction handles any delimiter format. (6) Worker purpose field shown in composite prompt. (7) Worker detail page: consolidated Versions card with expandable rows, Make Active / Edit (auto-revert to draft) / Delete buttons in top bar. (8) DeleteWorkerVersion, GetWorkerVersion, SetActiveWorkerVersion, RevertWorkerVersionToDraft RPCs. (9) Human-readable worker names + iteration display in execution views. (10) Run view edges visible with className fallback, emoji support, raw/markdown toggle. (11) PR Reviewer/QA Engineer prompts updated with clear role delineation. (12) AGENTS.md updated for README.md table format. |
| 40 | Inline step recovery | done | Replaced the DAG-level `recover` step kind with per-task-step recovery config. `pollTaskStep` routes failures by strategy (retry/summarize_restart/human_escalation/stop). Removed `StepKindRecover` from dispatch and polling. Recovery engine still used for `summarize_restart` but called inline from `pollTaskStep`. Frontend: Recovery palette entry removed; recovery strategy + max_attempts merged into TASK step properties panel. |
| 41 | Workflow context + .orchicon files | done | Decision signal parsed from summary prefix (`ORCHICON WORKER SUMMARY: success|failure — text`) instead of separate `_decision:` markers. Touched files extracted from `diff --git` lines in output. `.orchicon/<run_id>/` files written after each execution — workers read prior step results from disk. Prompt simplified: role + task + project + instructions only. No workflow context timeline. Per-step results (decision, summary, worker, files) displayed inline on run view. Stale `_decision` / `_issues` leak fixed across loop iterations. `recovery_policy_ref` removed end-to-end. Worker edit flow and config default persistence fixed. |
| 42 | Comprehensive documentation | done | Replaced old 11-file `/docs/` directory with single comprehensive `DOCUMENTATION.md` at project root. Covers architecture, technologies, project structure, installation, usage, development, deployment, env vars, troubleshooting, and more with Mermaid diagrams and full directory tree. Updated README, AGENTS.md, and landing page to reference new docs. |
| 43 | Human-in-the-loop APPROVAL step kind + worker-backed approval + AI Approver | done | v0.1.149: Full approval system — APPROVAL step kind with native loop-back (no separate loop_decision node), human approve/reject via UI, worker-backed approval via AI Approver worker, DevOps Engineer worker, Principal Software Architect seed, worker identity (Role/Skills/Behavior/AGENTS.md) in composite prompt, topological step numbering, iteration context and execution history in prompts, bulk approve/reject on Approvals page, editable worker purpose field, auto-start workflow on save, lock button that disables pan/select/drag, MiniMap top-right resizable, and 20+ bug fixes across the board. |
| 44 | Settings & Cost by Workflow | done | v0.1.150: Preferences renamed to Settings with Defaults sections (default worker model, default Ask Orchicon model, recovery stall parameters) stored per-tenant in Postgres (tenant_settings table) with GetSettings/UpdateSettings RPCs. Adapter reads stall thresholds from settings at dispatch time with env-var fallback. Dispatch fails if both worker model_ref and tenant default are empty. Cost Explorer adds "By Workflow" tab showing cost per workflow run with per-step breakdown via new GetWorkflowCosts RPC (JOINs usage_records → work_items → workflow_runs). |
| 45 | Ask Orchicon + MCP + fixes | done | v0.1.151: Ask Orchicon conversational agent (proto, Go backend, ToolRegistry with 25+ tools, streaming ChatStream, frontend /ask-orchicon route). MCP server subcommand (`orchicon mcp`) registered in opencode config. MCP config auto-injection: BuildConfigContent reads user's `~/.config/opencode/opencode.json` and merges MCP servers into OPENCODE_CONFIG_CONTENT for every worker execution and Ask Orchicon chat. Dynamic adapter capabilities (BuildCapabilitiesJSON) reflects MCP servers + model providers from user's config. Seed data moved from SQL migrations to Go code (internal/db/seed_workers.go) — all 6 canned workers (Senior SWE, PR Reviewer, QA Engineer, Principal Architect, DevOps Engineer, AI Approver) defined as Go structs with published status. AGENTS.md overhaul: removed destructive E2E cleanup checklist, added backup-first protocol. Bug fix: worker-backed approval dispatch (step.Ref vs config.worker_ref mismatch) was falling through to human approval branch — stuck in approval_pending forever. New RetryStepRun RPC + "Retry step" UI button to re-dispatch stuck step runs without aborting the whole workflow. |
| 46 | Dogfooding dual-instance setup | done | v0.1.152: Added production-like Orchicon instance for dogfooding (using Orchicon to build Orchicon). New `docker-compose-prod.yml` with ports offset +1 from dev, `orchicon serve` subcommand (server with embedded frontend, no compose management), `scripts/dev-prod.sh` (full lifecycle manager for the prod instance), `make dev-prod-*` targets, and DOCUMENTATION.md section documenting the dual-instance workflow with database copy instructions. |
| 47 | Worker model + prompt polish | done | v0.1.153: Fixed model_ref default from `opencode/deepseek-v4-flash-free` to `opencode-go/deepseek-v4-flash` across seed, model definitions, and gateway. Principal Architect: writes architecture summaries to `architecture-notes/`, added Git workflow, removed stale docs/ refs. Senior SWE: checks architecture-notes before coding. Dual-instance warning on all workers. PR Reviewer + QA Engineer: bold "YOU DO NOT MODIFY CODE" guardrails. Seed function now syncs agents_md/role/skills/behavior on existing workers every boot. Frontend: fixed kindFilter TS errors, added client-side kind filtering. Signoz schema-migrator pinned to v0.144.6 with pull_policy:missing. |
| 48 | DevOps Engineer PR & merge fix | done | v0.1.155: Moved PR & merge instructions from PR Reviewer to DevOps Engineer. DevOps now PRs and merges immediately on approval without asking, deletes branch after merge. AGENTS.md updated with branch deletion step. |
| 49 | Worker safety controls | done | v0.1.157: Post-incident response to the 2026-07-30 `/home` wipe (QA Engineer ran `rm -rf /` inside a python TUI, invisible to opencode's bash deny rules). Added an OS-level execution guard (`internal/opencode/guard.go`) that shims dangerous binaries (`rm`, `sudo`, `dd`, `mkfs*`, `fdisk`, `parted`, `shred`, `wipefs`, LVM, `chmod`/`chown`/`mv`/`cp`/`ln`) on every worker's PATH — destructive commands are refused even from inside subprocesses, and file operations are scoped to the project dir. Hardened the opencode `permissionRules()` deny list. Added a safety lint for PR Reviewer and QA Engineer based on Semgrep (`.orchicon/semgrep_orchicon.yml` + `.semgrepignore`, written into every project; workers run `semgrep scan --config .orchicon/semgrep_orchicon.yml --error .` — no shell script, fully cross-platform) so they catch bugs/security issues automatically instead of hunting manually. All canned workers now carry a "Safety rules" AGENTS.md block (destructive commands, destructive "security testing", and scope creep banned); the seed rolls a new published worker version forward via a versioned marker when the safety content changes so updates reach edited workers. Lightened PR Reviewer and QA Engineer prompts (proportionate testing, blockers-only review, no destructive "security tests"). Added `serve --detach`/`--stop` so the control plane can be started without blocking an agent/script session. |
| 50 | Grafana telemetry stack | done | v0.1.160: Replaced SigNoz/ClickHouse with the Grafana stack — Tempo (traces), Loki (logs), VictoriaMetrics (metrics), Grafana UI. OTel collector fans out OTLP to all three; `TelemetryService` (`internal/telemetry/grafana_client.go`) queries native APIs (Tempo `/api/search`, Loki LogQL, VM PromQL) with tenant-scoped filters and the same degraded-graceful semantics. Grafana runs with `serve_from_sub_path` and is proxied same-origin under `/grafana` (plain reverse proxy, no HTML rewriting). Frontend Telemetry page embeds kiosk-mode Grafana Explore deep-links per datasource. Compose: 7 services; ports Grafana 3002/3003, Tempo 3200/3201, Loki 3100/3101, VM 8428/8429. Metric instruments dropped units to keep canonical `orchicon_*` names (Prometheus exporter would suffix them). Loki severity filtering via `severity_number` label. Removed `signoz-config.yaml`, `clickhouse-cluster.xml`, signoz proxy + tests. |
| 51 | Single-container deployment | done | v0.1.162: New `orchicon container` subcommand — a PID-1 process supervisor (`cmd/orchicon/container.go`) that spawns postgres → nats → Grafana telemetry plane (Tempo/Loki/VictoriaMetrics/OTel collector/Grafana) → control plane with readiness gates, per-component log prefixing, crash restart with exponential backoff, and graceful SIGTERM handling. Writes the embedded container configs (deploy/container/configs/*) into the data dir with @DATA_DIR@ substituted. New `deploy/container/Dockerfile` (multi-stage from the official images onto a Postgres Debian base) + `.dockerignore`. `ORCHICON_TELEMETRY=none|embedded|remote` controls the telemetry processes (none ≈96 MiB, embedded ≈384 MiB — verified). Verified end-to-end in the container: healthz, /grafana proxy, embedded dashboard, traces→Tempo, logs→Loki, metrics→VictoriaMetrics. Tempo OTLP receivers offset to 14317/14318 (collector owns 4317/4318); Loki gRPC listener on 9096 (9095 collides in one namespace). |
| 52 | Container workflow + data preservation | done | v0.1.163: Phase C on the same branch. `scripts/container.sh` manages dev/prod single-container instances (dev `-p 8080:8080 -p 3002:3000`, prod `-p 8091:8080 -p 3003:3000`) with `make container-*` targets. Data preservation: instances reuse the existing compose-stack postgres volumes (`orchicon_postgres-data` / `orchicon-prod_postgres-data`), so dev/prod DBs survive the switch; the container's postgres runs as the data dir's owner (uid 70 — alpine compose volumes) so preserved volumes are readable; the script hard-refuses to start while the matching compose postgres is running (two postgres on one data dir corrupts it) with an `ORCHICON_PG_VOLUME=fresh` escape hatch. Verified: copied the live dev postgres volume, ran the container against it, and the real work-item data served through the API. Release workflow builds + pushes the container image to ghcr.io/beardedparrott/orchicon (vX.Y.Z + latest) on every tag. |
| 53 | Compose removal (single container only) | done | v0.1.163: Deleted the Docker Compose deployment workflow entirely — `orchicon dev` subcommand + `cmd/orchicon/dev.go`, `deploy/compose/`, `scripts/dev.sh`/`dev-prod.sh`/`install-dev.sh`/`install-prod.sh`, the `orchicon-dev`/`orchicon-prod` binary-name convention (`prod_defaults.go`), and Makefile compose/dev/dev-prod/build-dev/build-prod targets. `serve.go` refactored to own its PID/log state + `withFrontend` (new `serve_state.go`); `procattr_{unix,windows}.go` renamed from `dev_procattr_*`. The single container is now the only full-stack deployment: `scripts/container.sh up/rebuild/down dev|prod` + `make container-*`. `scripts/container.sh rebuild [dev|prod]` = stop → build → start. Installers (`install.sh`/`install.ps1`) now target the container instances + `serve --stop` instead of compose. Both dev + prod instances converted to containers and verified (healthz, Grafana, data preserved via the compose-era Postgres volumes). Full doc sweep (DOCUMENTATION, AGENTS, README, landing page). |
| 54 | Scoped container mounts + host-user ownership | done | v0.1.163: Replaced the whole-$HOME mount with a scoped set — opencode config/data + the project dirs/files from a plane-written manifest (`/var/lib/orchicon/project-mounts`, refreshed from `projects.project_dir` + `context_files` every 30s; new `internal/server/project_mounts.go`). `scripts/container.sh sync-mounts [dev|prod]` applies a saved project-dir change (Docker can't add mounts to a running container, so it compares the manifest to the live container and recreates it). Control plane + worker subprocesses run as the host user (`ORCHICON_HOST_UID/GID/HOME` from the script), so worker-created files are owned by the operator, not root; postgres stays uid 70, telemetry root. Data-dir root + backups/blobs chowned to the host user. |
| 55 | Workflow runtime containers | done | v0.1.164: Pure per-workflow execution — one short-lived container per active workflow run (Azure Pipelines agent model), created at run start, killed at terminal. `orchicon runtime-daemon` (host, owns the Docker socket, narrow validated unix-socket API), `orchicon runtime-supervisor` (container PID 1, streams opencode runs), `orchicon runtime-client`. Security: runtime runs as the host uid with the rootfs chowned to it — no root process, full ephemeral-FS control (pip/npm/mise/uv installs), project dirs written as the host user, host opencode data mounted read-only with worker state redirected to ephemeral XDG_DATA_HOME. Toolchain baked at build (python/node/bun/mise/uv/gh); dpkg refuses non-root so apt is build-time only. Lifecycle wired into the WorkflowReconciler (ensure on running, reap at terminal) + a 30s orphan-reap sweep. Execution guard moved to internal/guard (leaf package) and built in-container. Adapter dispatches `opencode run` into the container when the task belongs to a workflow run; wall-clock timeout signals the in-container child. Verified E2E: full wf_coding_template run (free model) dispatched through the container as uid 1000 with the guard active, all steps succeeded, container reaped at completed. |
| 56 | Runtime daemon socket robustness + container hygiene | done | v0.1.166: The daemon socket is now bind-mounted as a directory (`/tmp/orchicon-runtime` → `/var/run/orchicon-runtime`), so a daemon restart (which recreates the socket file) no longer stale-mounts the control plane's connection — a file-bind used to strand the plane with no daemon and stop the runtime sweep. The daemon now removes stopped/crashed runtime containers before recreating them for active runs (previously "name already in use" blocked recovery) and runs an age-based orphan sweep (default 24h, `ORCHICON_RUNTIME_MAX_AGE` / `ORCHICON_RUNTIME_SWEEP_INTERVAL`) as a backstop for containers leaked while the plane is down. Verified E2E: daemon restart mid-session keeps the plane connected; a stopped runtime is recreated; a 5s/5s sweep reaps aged containers and the plane re-adopts active runs. |
| 57 | Mid-workflow rebuild recovery (execution liveness) | done | v0.1.166: A rebuild mid-workflow previously killed the in-flight execution AND left the run/step/execution stuck `running` forever (the stall monitor + wall-clock deadline were in-process, lost on restart, and no reconciler re-checks running executions). Fixes: (1) an **execution-liveness reaper** (boot + 30s sweep) fails executions orphaned by a plane restart or lost runtime container with `execution lost: control plane restarted or runtime container gone` and transitions the work item to failed, so recovery re-dispatches; (2) the adapter **self-heals** — it ensures the runtime container exists before every dispatch (daemon createRuntime now retries past "name already in use"/"removal in progress" conflicts), so a recovery re-dispatch can't race ahead of the adopt sweep and exec into a missing container; (3) daemon kill treats "removal already in progress" as gone. Verified: liveness endpoint (alive true/false), fake-orphan running execution failed by the reaper with the right message, and container destroy → adapter detects → recovery re-dispatches (the full pipeline retry E2E was otherwise confounded by intermittent free-model gateway failures — a pre-existing external flakiness, not runtime code). |
| 58 | Instance-scoped runtime containers (multi-instance orphan fight) | done | v0.1.168: Dev and prod share one runtime daemon, but each plane's adopt sweep only knew its own DB's active runs — so prod's sweep reaped dev's runtime containers as "orphans" every 30s (and dev's reaped prod's), a perpetual fight that left active runs without a runtime. Runtime containers are now labeled `orchicon.instance=dev\|prod` (from `ORCHICON_INSTANCE`, set by container.sh); the daemon's List endpoint scopes to the instance and each plane's adopt reap only sees its own containers; the age-based orphan sweep stays global as the backstop. Verified: with both instances up, dev's two active runs' containers persisted across multiple adopt cycles (Up 2m+) while prod's orphan-reap count stayed 0. |
| 59 | One-command install + runtime image shipping | done | v0.1.170: `orchicon install` subcommand — one command that pulls the published images (skips the pull when already local), ensures the host runtime daemon (spawns it detached if down), creates/starts the single-container instance (same scoped mounts + ORCHICON_INSTANCE as container.sh), waits for healthz, and prints connection/management info. The installers (`install.sh`/`install.ps1`, `--no-setup` to skip) run it after dropping the binary. The release workflow now also builds + pushes `ghcr.io/beardedparrott/orchicon-runtime` (was local-only), and the runtime daemon defaults to the GHCR image. Bug fix: the container image's postgres user is uid 70 (matching compose volumes) and `postgresUIDGID()` never returns root — a fresh install's initdb previously failed with "could not look up effective user ID 70"/"cannot be run as root". Verified E2E: `orchicon install` created a healthy container from a clean slate (fresh initdb), reported an existing one idempotently, and respawned the daemon when down. |
| 60 | Windows WSL2 install | done | v0.1.173: Reworked `scripts/install.ps1` so the Windows one-liner (`irm https://orchicon.dev/install.ps1 | iex`) provisions/detects WSL2, verifies Docker inside the distro, downloads the **Linux** release binary and installs it inside WSL, then runs `orchicon install` there (pull images, start the runtime daemon, launch the single-container instance) and prints the Windows-visible URLs (`http://localhost:8080` control plane, `http://localhost:3002` Grafana). New `-NoSetup` flag; `-Uninstall` stops the container instances + removes the binary (distro left intact); `-Clean`/`-ForceClean` mirror the Linux semantics inside WSL. The runtime layer (daemon, unix socket, container mounts) is POSIX-only and runs under WSL2 on Windows — no native port; `install.go`/`runtime.go` unchanged. UI `project_dir` is entered as its WSL path (`/mnt/c/...`). README/DOCUMENTATION/landing page/AGENTS.md updated. Not verified on real Windows — shipped as-is for testing. |
| 61 | Ask Orchicon on the Orchicon MCP | done | v0.1.174: Proper MCP support. `orchicon mcp` is now tenant-scoped (`ORCHICON_MCP_TENANT_ID`), echoes the client's MCP protocol version (opencode 1.18 sends `2025-11-25` and its SDK rejects a downgrade), emits a non-nil `inputSchema.properties` (the SDK rejects `null`), returns tool failures as `isError` results, and handles `ping`. `BuildConfigContent` registers the built-in Orchicon MCP by default in the `OPENCODE_CONFIG_CONTENT` for in-process worker executions and the Ask Orchicon chat, so the model drives Orchicon tools natively as `orchicon_*` MCP tools — the chat's string-protocol tool_call hack and keyword pre-execution are removed (the tool_use callback now only relays). Runtime-container executions skip the Orchicon MCP (isolated sandbox, no Postgres route — documented). Ask Orchicon prompt gains an About-Orchicon primer and a fresh enabled-projects context block. Tool registry expanded: list/get workflow runs, update/delete worker, list/publish/deprecate/set-active worker versions, archive_project, delete_work_item, list/get policies, list recoveries; create/update project + work-item schemas aligned with what the tools parse. Verified E2E: `orchicon mcp` round-trip against the dev DB and a free-model opencode run calling `orchicon_list_projects` via MCP returning real project data; `make ci` green. |
| 62 | Execution success semantics + runtime line caps + workflow-bound work item status | done | v0.1.175: (1) **No silent "succeeded" with empty output** — the runtime path's `bufio.Scanner` line caps were 64KB (local path: 1MB); opencode emits a whole model response as one line, so a large final answer (e.g. a PR review) was dropped along with every event after it (`ErrTooLong` permanently kills the scanner), and the execution came back `succeeded` with no output → loop_decision saw no `_decision` and re-asked until the run failed. Runtime caps raised to 1MB (`internal/runtime/agent.go` + `client.go`) and the adapter now tracks `step_start`/`step_finish` and downgrades a clean exit with an unpaired step to a failure (`execution ended before the final model step completed (model response stream truncated or event dropped)`). (2) **Work item bound to a workflow run tracks the run** — every task step operates on the shared bound item, and each step's success used to flip the whole item to `succeeded`; task steps are now polled on their OWN execution (`worker_execution_id`) instead of the shared item's status, the item stays `running`/`assigned` while the run is in flight, and the reconciler sets `succeeded`/`failed` only at run completion. (3) Reaped two stale pre-reap-logic workflow-runtime containers and terminalized their orphaned July-30 runs (step runs referenced executions that no longer exist) so the adopt sweep stops recreating them. Verified E2E on dev: single-step free-model workflow — item stayed `assigned` mid-run, execution `succeeded` with 5500 chars of output (no false positive), run `completed`, item `succeeded`, runtime container reaped; unit tests for the step-tracking and the >64KB line. `make ci` green. |
| 63 | Runtime adapters mounted, never baked | done | v0.1.176: Orchicon no longer ships the runtime adapter CLI (opencode) in its container images — the operator installs it on the host and it is **bind-mounted** into the main + runtime containers at runtime (`~/.opencode`, read-only, bin on PATH). Licensing-safe for future adapters (Claude Code / Codex): the product mounts the operator's own install, never redistributes the CLI. Changes: removed the `curl opencode.ai/install` steps from `deploy/container/Dockerfile` + `deploy/runtime/Dockerfile`; `scripts/container.sh` + `cmd/orchicon/install.go` mount `~/.opencode` (ro) into the main container and `orchicon install` now hard-requires opencode on the host (clear error + install hint); `cmd/orchicon/container.go` prepends `$HOME/.opencode/bin` to the control plane's PATH; the runtime daemon mounts `~/.opencode` (ro) into every runtime container + puts it on PATH; the supervisor's allowlist is a named set (`runtimeBinAllowlist`, `internal/runtime/agent.go`) so `claude`/`codex` are one-line additions; the adapter resolves opencode via PATH or `~/.opencode/bin`. Bug fix alongside: container creation was racing — the reconciler's `EnsureForRun` and the adapter's self-heal `Create` both called `docker run` for the same workflow name, one removed the other's container mid-setup and the exec failed with `exit status 1`; `createRuntime` is now serialized with a daemon mutex. Docs updated (README, DOCUMENTATION.md, AGENTS.md, landing page) to make the mount-never-bake policy explicit. Verified E2E on dev: images contain no opencode; a single-step free-model workflow ran through the **mounted** opencode in the runtime container (execution `succeeded` with real output, run `completed`, container reaped, zero create conflicts). `make ci` green. |
| 64 | Tunable, conservative execution-liveness reaper | done | v0.1.176 (follow-up): the execution-liveness reaper used to fail an execution on a **single** not-alive probe with a hardcoded 60s fresh-execution grace — and the liveness probe can false-negative on a transient docker/socket hiccup, so a healthy actively-working execution got killed and forced into recovery (observed live: a working architect execution reaped mid-run, then stall-flagged `stalled:no_progress`). New tenant settings under **Settings → Execution liveness reaper**: `execution_reap_grace_seconds` (default 60) and `execution_reap_consecutive_failures` (default 3) — an execution is now reaped only once it's older than the grace window AND has been reported not-alive for that many consecutive sweeps (counter resets on any alive answer or fresh execution; pruned when the execution stops running). Env overrides `ORCHICON_REAP_GRACE_SECONDS` / `ORCHICON_REAP_CONSECUTIVE_FAILURES`. Proto + codegen + migration (`20260802220000_execution_reaper_settings.sql`) + DB/service layer + frontend settings card; unit test for the consecutive counter. Applied + verified on dev (columns default 60/3), `make ci` green. |
| 65 | Exec stream transport resilience (reconnect, not kill) | done | v0.1.176 (follow-up): a broken control-plane ↔ runtime-supervisor exec stream used to fail the execution — the daemon teardown SIGKILL'd the child on any request-cancel and the supervisor's disconnect watchdog killed it on client EOF, so a single transient socket/docker blip destroyed a healthy execution (the same class of bug as the architect reap). Now: the adapter retries the exec stream for the same `exec_id` (`execution_reconnect_attempts`, default 3, with backoff), the supervisor keeps the child running through a `execution_reconnect_grace_seconds` (default 60) window instead of killing on disconnect, and a re-attach resumes the run in place (session-level broadcast fans output to all attached clients; a per-client disconnect detector starts the grace the moment a client drops). Explicit termination (wall-clock deadline, abort, plane shutdown) still kills promptly via an explicit `SIGKILL` signal on ctx-cancel. New settings under **Settings → Execution transport resilience**; env overrides `ORCHICON_RECONNECT_ATTEMPTS` / `ORCHICON_RECONNECT_GRACE_SECONDS`. Proto + codegen + migration (`20260803000000_execution_reconnect_settings.sql`) + DB/service layer + frontend fields + manifest/adapter plumbing; daemon no longer SIGKILLs on disconnect; supervisor refactored (`internal/runtime/agent.go`) with re-attach + grace + orphan-kill. Unit tests: re-attach resumes stream, orphan killed after grace, re-attach after exit reports final state, >64KB line survives. Verified: `make ci` green; E2E single-step free-model workflow still succeeds end-to-end through the mounted opencode. |
| 66 | Stop steps completing off stale/foreign executions + fix result propagation target | done | v0.1.176 (follow-up): two bugs found investigating run `01KZ2X0WJF2NH7W4W6EJNWAP4W` where the DevOps step "succeeded" in 2ms with no execution and showed the architect's summary. **(1) Stale-execution fallback:** `pollTaskStep` fell back to `GetLatestExecutionForTask(workItemID)` when a task step had no execution link — but the bound work item is shared across steps and re-run across workflow runs, so the "latest execution" could be a different step's or a previous run's execution (here: an hours-old architect run), marking a never-run step `succeeded` and satisfying downstream dependencies on a lie. Fix: a task step with no execution link now **waits** (returns false) instead of falling back. **(2) Wrong-result propagation:** `propagateStepRunResults` located the step run by `result LIKE '%_work_item_id":%<task>%' ORDER BY created_at DESC LIMIT 1` — every step run is seeded with the same `created_at`, so the LIMIT 1 was arbitrary among the steps sharing a bound work item, and the architect's summary/decision got written onto the DevOps step's card. Fix: it now keys by `worker_execution_id`, which unambiguously identifies the step that ran the execution. Both fixes in the reconciler; verified `make ci` green + single-step free-model E2E still succeeds. Underlying shared-work-item dispatch race (only one of two simultaneously-ready steps gets an execution) is a separate, deeper design issue noted for a follow-up. |
| 67 | Fix cross-connection self-deadlock in step recovery + stale-exec re-trigger + lost-dispatch hang | done | v0.1.176 (follow-up): run `01KZ30ER243X3VT8HW8QQNGQPB` wedged after the SSE step failed on a provider-side "Unexpected server error". The execution failed correctly (v0.1.175 step_finish detection), but recovery deadlocked the reconciler: `pollTaskStep` called `TriggerOnFailure` synchronously inside its own reconcile transaction, and `TriggerOnFailure` opens its own tx on a separate connection — when the same pass had already re-dispatched the recovering step and written the shared work item row, the child tx blocked on the pass's own row lock while the pass synchronously waited on the child's return. Postgres cannot detect that cross-connection self-deadlock (B waits on A's lock; A appears idle-in-transaction because it's blocked in Go), so the workflow reconciler goroutine wedged holding the tx: run stuck `running`, step stuck `recovering`, three pile-on connections blocked, runtime container never reaped (terminalization lives inside the stuck pass) and kept alive by the 30s adopt sweep. **Fixes** (`internal/scheduler/workflow_reconciler.go`): (1) recovery triggers are now collected (`recoveryTriggerReq`) and invoked **after** the transaction commits — both `pollTaskStep` (summarize_restart) and `dispatchStep` (loop_decision) sites — closing the lock cycle; (2) `dispatchStep` now clears the stale failed `worker_execution_id` when re-dispatching a recovering step so `pollTaskStep` waits for the inline dispatch's replacement execution instead of re-triggering recovery (attempt 1→2→3→terminal) on the old failure in the same pass; (3) a 15s `dispatchLinkGrace` on the empty-link wait routes a lost dispatch (inline DispatchTask failed / adapter down) to recovery instead of hanging the step as `running` forever — also fixing a pre-existing hang on a lost first dispatch. Verified: `make ci` green; rebuilt dev container cleared the deadlock; the stuck run recovered live — step-sse re-dispatched, new execution `01KZ340R...` succeeded with real output, PR Reviewer (previously the empty-succeed step) ran a real execution and succeeded, DAG progressed to QA. Test run then aborted via `AbortWorkflow` (container reaped, execution terminated). |
| 68 | Work items = shared input references; step runs = the execution + recovery unit | done | v0.1.176 (follow-up): the agreed model from the bug-2 discussion — a workflow-bound ticket is a passive **input reference**, not a mutable per-step claim. `StartWorkflow` sets it `running`; it stays `running` for the whole run and only reaches `succeeded`/`failed` at run end. `dispatchStep` no longer writes `assigned_worker_ref`/`workflow_step_id`/`prompt_context`/`status` onto the ticket; dispatch is scoped per step run (`TaskDispatcher.DispatchTask(ctx, taskID, stepRunID)`), the composite prompt + worker live on the step run's result (`_prompt`, `_worker_id`, `_worker_version`, read by `reconcileOne`/`startExecution`), and `reconcileOne` drops the ticket `ready`-gate + `assigned` flip for step-run dispatch. Parallel steps on one ticket each get their own execution — **bug 2 (the shared-work-item dispatch race) is eliminated, not serialized**. Loop decisions and upstream prompt context read from the upstream **step run's** result (the shared ticket's `_decision`/`_summary` are no longer the source of truth); `transitionWorkItemOnResult` no longer writes per-step results onto a run-bound ticket. **Recovery is per failing step run**: `TriggerOnFailure` gains a stepRunID, idempotency is per failed execution (`GetActiveRecoveryForExecution`), the ticket is never flipped `recovering`/`ready` for run-bound failures, resume writes `_recovery_summary` onto the step run (resolved robustly from the failed execution's run+step, not the transient exec link), the dispatch section's recovering-gate consults active-recovery-per-step-run, and `buildCompositePrompt` injects the recovery narrative into the replacement execution's prompt. At run end the ticket's `results` carry `_run_narrative` (per-step summary/decision/issues + recovery episodes). **Also fixed a pre-existing run-terminal-state bug**: a `Failed`/`Blocked` step set `anyFailed` but didn't clear `allSucceeded`, so a run whose only step failed was wrongly marked `completed` and the ticket `succeeded` (exposed by the single-step recovery test). Verified E2E on dev: (a) two parallel steps bound to one ticket both dispatched, ran real free-model executions, and succeeded; run completed, ticket `succeeded` with `_run_narrative`; (b) a single-step run with a failing worker ran its full recovery cycle (recovery row per failed execution, step re-dispatched, `_recovery_summary` on the step run), failed again, and the run/ticket correctly ended `failed` with the recovery episode in the narrative. `make ci` green. |
| 69 | Fix AI-approver double dispatch, stuck approval steps, and rejection-loop fan-out | done | v0.1.177: run `01KZ43ZTTFRTA2VZ7FTV56Z007` (custom workflow with an AI approver) exposed four issues in the worker-backed approval path under the new work-item-as-input model. **(1) Double dispatch:** the approval step creates a dedicated "Approval: ..." work item (status `ready` + assigned worker), and the standalone TaskReconciler scan dispatched it in parallel with the workflow's inline step-run dispatch → TWO approver executions for one step; once the run completed, the orphaned "ready" ticket was dispatched AGAIN as a ghost execution (failing because its runtime container was already reaped). Fix: the scan's `reconcileOne` now skips ANY item with a `workflow_run_id` (workflow steps are dispatched exclusively by the inline step-run path). **(2) Stuck approval step:** `pollTaskStep`'s approval branch polled the approval WORK ITEM's status, which under the run-bound model never tracks the approver's execution → the step sat `running` forever after the approver succeeded. Fix: approval steps now poll their OWN execution (unified with task steps); the approve/reject decision routing stays in Phase 0. **(3) Rejection fan-out:** `approvalReenter` left the superseded approval step `succeeded`, so the success-branch step (step-b) dispatched in the same pass while the loop also re-ran. Fixes: `depsSatisfied` treats a superseded run as NOT satisfying deps, and `approvalReenter` re-creates the approval step as a pending run (superseding the old with the real new-run id instead of a dangling `db.NewID()`), so the loop re-approves before the success branch fires. **(4) Rejection storm at max_iterations:** Phase 0 did `return failStep(...)` — an early return before the commit, so the deferred rollback undid the failed mark and every pass re-failed the same step forever (infinite "step failed" loop, run never terminalized). Fix: mark the step failed and continue the pass so the completion check fails the run. Verified E2E on dev (free model): approve path ran exactly 3 executions and completed; reject path looped through 4 iterations with step-b PENDING throughout, then failed cleanly at max_iterations (no storm); the user's stuck run self-healed (approval completed with decision `success`, step-sse resumed). `make ci` green. |

| 70 | Runtime Images: self-service image builder + per-work-item image selection | done | v0.1.178: New **Runtime Images** feature — a sidebar page (list + build form with a live Dockerfile preview that doubles as an advanced raw-Dockerfile editor + a streaming Deploy button) defining and building the container images workers execute in, via the runtime daemon's new `GET/POST/DELETE /v1/images` endpoints. The daemon always rewrites the Dockerfile's `FROM` to the base image and injects the `org.orchicon.runtime-base=true` label (inherited through `FROM`), which is also the container-create gate (label-carrying local images accepted without registration; anything else refused — verified debian refused, pyside6-gui accepted). Stock `:gui` (`deploy/runtime/Dockerfile.gui`, headless Qt/tkinter/X11 libs) and `:dev` (`deploy/runtime/Dockerfile.dev`, Go 1.26/Node 22/buf/atlas + baked PostgreSQL 15 for dogfooding — a worker boots a disposable non-root in-sandbox Postgres and runs the full `make ci` DB path) variants ship alongside the base; `ORCHICON_RUNTIME_IMAGES` adds operator stock images (base always included). `runtime_image` on a work item (backend-stamped to base when unset) selects the container: run-start resolution (template → bound item; one-shot → WORK_ITEM markers, all must agree or the run fails at start via `resolveImageFromValues`), stored on the run, threaded through `ExecutionManifest.RuntimeImage` → adapter self-heal. Composite prompts carry a machine-generated **Runtime environment** block (image name + rootless no-apt install patterns + `apt-get download`/`dpkg-deb -x`/`LD_LIBRARY_PATH` escape hatch). Delete removes the DB row + local Docker image (gated on no active run). Ask Orchicon gained list/get/create/delete runtime-image tools. Bug fixed during E2E: `BuildRuntimeImage`'s mark-building + final-outcome transactions were rolled back instead of committed, leaving images stuck `draft` after a successful build. Verified E2E on dev: UI-built `pyside6-gui` image (PySide6 offscreen + tkinter run inside), a template-bound workflow run resolved and dispatched into `pyside6-gui:latest` (execution succeeded with real output, run completed, container reaped, work item succeeded), label gate worked, delete cleaned both DB + image. `make ci` green.
| 78 | Worker-backed approvals without work item creation | done | v0.1.186: Worker-backed approval steps no longer create per-step "Approval: …" work items — the Work Items page clutter is gone while the approval behavior is unchanged. `dispatchStep` (approval/worker branch) now mirrors the TASK-step pattern: it resolves the run's shared ticket (recovering `_work_item_id` → upstream WORK_ITEM markers → the run's bound item), builds the composite prompt against it, and writes the full approval record onto the **step run** (`_work_item_id`, `_prompt`, `_worker_id`, `_worker_version`, `_upstream_worker`/`_upstream_summary`/`_upstream_files`/`_ac`, `_decision:"pending"`, recovery narrative preserved, stale `worker_execution_id` cleared) before dispatching `DispatchTask(ticket, stepRunID)`. The Approvals page was already step-run-based and is unchanged; approval executions now appear under the ticket's title. The failure/retry path is approval-aware: `pollTaskStep`'s retry branch never clones the ticket for approval steps (same `_work_item_id`, attempt incremented, re-dispatch on the same step run), and the dispatch section's recovery-in-flight gate now covers approval steps (summarize_restart waits). Phase 0's decision fallback no longer lets a stale `_decision` left on a shared ticket by a prior run/step override the step run's real decision. Constraint introduced (same as TASK steps): a worker-backed approval on a one-shot run with no bound ticket and no upstream WORK_ITEM marker fails with a clear message. **Also fixed a pre-existing headless-serve crash**: `server.go` passed a typed-nil `*runtime.Lifecycle` into the `RuntimeLifecycle` interface (non-nil to the reconciler), so the deferred `EnsureForRun`/`ReapForRun` nil-dereferenced the receiver and crashed the whole plane on the first workflow run without a runtime daemon; the wiring now declares the interface type and the reconciler guards typed-nil runtimes (`runtimeEnabled`). Unit tests for the ticket-resolution/result-shape/decision-precedence helpers, the nil-runtime guard, plus DB-backed integration tests (skipped without `ORCHICON_TEST_DSN`) proving zero work-item creation across dispatch and retry. Verified E2E on headless serve with the real opencode runtime + free model: a worker-backed approval run completed with the work item count unchanged (epic + ticket only), zero "Approval:" rows, step run `succeeded` with `_decision: success`. Docs updated. `make ci` green.
| 71 | Single-signal workflow decision + sanctioned /tmp/orchicon scratch dir | done | v0.1.179: (1) workflow routing reads ONLY the `ORCHICON WORKER SUMMARY: success\|failure` word — the old `_decision:`-line extraction and the `_issues:` → forced-failure hard rule caused infinite loops when worker prose like "(not `_issues:`)" was substring-misparsed as blockers (run `01KZ55262Z2QSPV17NQZPSV8PZ` looped 4× then aborted). `_issues:` is now line-start-only + informational; composite prompt / re-ask / AI Approver seed updated to the single-signal contract. (2) `external_directory` is now object-form: catch-all `*` deny + a precise `"/tmp/orchicon/**": "allow"` scratch carve-out (pre-created by the runtime supervisor, advertised in the runtime-env prompt block) so workers can read back their own screenshots/logs without weakening the sandbox; the pattern deliberately excludes the supervisor socket, guard shims, and `/tmp/opencode-data-*` auth.json copies. Verified E2E against the real opencode runtime (scratch read succeeds, other /tmp denied). |
| 72 | Stalled executions now recover (hard-kill) + wall-clock default + ctx fix | done | v0.1.180: run `01KZ5G3JPR4R18NJRTJFKX3CXH`'s PR Reviewer hit `stalled:no_progress` and never recovered — `OnStall` marked the execution `unhealthy`, but `pollTaskStep` only treats `failed`/`failed_to_start`/`terminated` as terminal-failure, so the step sat `running` forever, zero `recovery_executions` rows were created, and the subprocess leaked in the runtime container for 48+ minutes. Fix: a genuine hang/loop stall (`no_progress`/`text_loop`/`repetition`) now hard-kills the subprocess (local `Process.Kill`; runtime SIGKILL via the supervisor) so the execution lands in `failed` through the normal `OnResult(false)` → recovery path; `no_file_progress` stays advisory (a reviewer may legitimately not touch files — the SSE worker flagged it yet succeeded). The stall reason is threaded into the terminal error. **Wall-clock**: `budget_overrides.wall_clock_seconds` (default now 3600 when absent; explicit 0 disables) kills the subprocess even mid-output, but the terminal DB writeback used the deadline-exhausted ctx → `context deadline exceeded` → execution stuck `running` forever (verified live). The deadline is now applied only to the subprocess/exec context; the callback ctx stays clean so `failed` + recovery always land. Verified E2E on dev with a real free-model workflow: 3 executions each killed at the exact 60s deadline, each `failed`, recovery created (`step_recovery`, resumed), 2 re-dispatches, run failed cleanly at max_attempts. |
| 73 | Execution budget defaults as a tenant setting (per-worker override) | done | v0.1.180: consolidated the per-execution budget ceilings — `tokens`, `cost_usd`, `wall_clock_seconds`, `tool_call_count` — into a dedicated **Settings → Defaults → Execution budget** section (`tenant_settings.default_budget_overrides` jsonb column). The tenant value is the default; a worker's own `budget_overrides` always override it per-field via a new `mergeBudgets` at dispatch (worker wins each key over the tenant default), and empty tenant fields fall back to the built-ins (1M tokens, $10, 3600s wall clock, 100 tool calls). The 3600s wall-clock default closes the "no hard stop at all" gap for empty `{}` budgets (the PR Reviewer ran 48+ min unchecked). Replaced the earlier scoped-in `stall_wall_clock_seconds` column/migration with `default_budget_overrides`. `ORCHICON_STALL_WALL_CLOCK_SECONDS` env override remains. Verified end-to-end: API Update→Get round-trip, worker-override-wins unit test (`TestMergeBudgets`), UI renders the section in Chrome, `make ci` green. |
| 74 | Rotating log management + disk hygiene | done | v0.1.181: (1) **Rotating, size-bounded serve logs** — a run-away component previously grew `.dev/logs/orchicon.log` unbounded (observed 300+ GB). New `internal/logging` `RotatingWriter` (size + time rotation, retention/max-files pruning, live reconfig via `Apply`); the detached serve child owns the file, dup2's it onto fd 1/2 (re-pointed each rotation) so panics/stray prints stay in the log, and fails loudly if it can't open it. New **Settings → Defaults → Log management** fields (`log_directory`, `log_max_size_mb`, `log_roll_interval_hours`, `log_retention_days`, `log_max_files`) + migration + DB/service layer + frontend card, live-applied by the server every 5s with `ORCHICON_LOG_*` env-var fallbacks (defaults 100MB/24h/7d/7). Instance containers pin Docker's `json-file` driver to `max-size=100m`/`max-file=7`. Fixed a pre-existing edge where an empty `default_budget_overrides` string broke `UpdateSettings` (invalid jsonb). (2) **Container image + Go cache hygiene** — `scripts/container.sh build` and the runtime daemon's custom-image build now prune dangling images after each build (repeated builds had accumulated ~229 dangling images / ~240GB); the daemon's age sweep now runs once immediately at start so crash-leftover `orchicon-runtime-*` containers are reaped within seconds, not after the first interval tick. New `make clean` / `cache-check` targets clear the Go build cache + `bin/`. (3) **Telemetry retention** — Loki had NO retention (logs accumulated forever in the instance volume); added `retention_period: 336h` + compactor to the embedded `loki.yaml` and pinned Tempo `block_retention: 336h` (14d, matching VM's 30d). (4) **One-time cleanup + `make clean-docker`** — removed the legacy SigNoz/ClickHouse images (~5.6GB, replaced by the Grafana stack in v0.1.160), orphaned compose-era/tmp volumes (~3.2GB), stray test containers, the stale pre-single-container `bin/orchicon-dev`/`orchicon-prod` binaries, and 151MB of anonymous volumes. `make clean-docker` prunes dangling images + stopped containers + unused volumes regularly. Unit tests for the RotatingWriter; E2E-verified live settings apply (log moved dirs via API) + Loki `retention_enabled: true` / `retention_period: 2w` confirmed in a running container; `make ci` green. |
| 75 | Advisory stalls no longer fail healthy executions + SSE progress guidance | done | v0.1.182: run `01KZ7Q9DBQ1A490Y9PHSHGMX7T` (execution `01KZ7S2V28JXDK92M4S3AR1G34`) — the Senior Software Engineer worker tripped `stalled:no_file_progress` 15 min in (no file writes during a long analysis phase), got stamped `unhealthy` mid-flight, and still succeeded. Two-part fix. **(1) Advisory stalls no longer fail executions:** the stall monitor's `no_file_progress` signal now trips into a revivable `warned` state instead of the one-shot `fired`; the execution keeps running with a non-terminal `stalled` health notice + reason, and the monitor emits `OnRecovered` (new `ExecutionCallbacks` method) which flips health back to `healthy` and clears the notice the moment a `file_diff` lands again — the window can then trip again. `OnStall` gained a `fatal` bool (`isFatalStall` computed in the adapter): only genuine hang/loop signals (`no_progress`/`text_loop`/`repetition`) still hard-kill + mark the execution `unhealthy` + fail the work item. Also fixed the stale-`error_message` bug: a succeeded execution no longer carries `stalled:no_file_progress` (cleared on revival and on success). **(2) SSE worker prompt:** the Senior Software Engineer seed gained a "Make progress visible" block — write incrementally (scaffold/partial implementations) and persist something concrete after each meaningful phase, because Orchicon monitors health from file-modification activity; seed marker bumped `orchicon.safety=v5`→`v6` so the context rolls forward to existing workers. Docs updated (README, DOCUMENTATION.md, AGENTS.md); unit tests cover the advisory revival (trip → file_diff → recovered → re-trip). `make ci` green. |
| 76 | Build version tag sync | done | v0.1.184: the version embedded at build time came from `git describe --tags --abbrev=0` on the local checkout, and since `git pull` does not fetch tags while the auto-release workflow creates the canonical release tag on GitHub at merge time, a local rebuild could embed an older version than the merged code (observed: a rebuilt prod instance reported v0.1.181 for merged v0.1.183 code). The Makefile now runs `git fetch --tags` as a best-effort `fetch-tags` prerequisite on `build`/`run` (offline builds fall back to local tags) and resolves the version at recipe time (recursive `VERSION ?= git describe ...`), so a rebuild embeds the merged release version; `make build VERSION=v0.1.x` overrides the tag explicitly. GitHub-release binaries/GHCR images already embed the exact `ORCHICON_VERSION` and are unaffected. AGENTS.md documents the fetch-tags behavior. `make ci` green. |
| 77 | Stuck-run reconciliation + index integrity + force-progress | done | v0.1.186: prod run `01KZ86NYYSVZWM2A9VQF1X24GT` wedged "running" for 8h. Root-caused to a **corrupted `worker_versions_worker_version_idx`** (hard host sleep mid-write): the `=` lookup used the index and returned zero rows for `w_se_principal_architect` (v5 published, exists), so when the DevOps step succeeded the reconciler's dispatch of the downstream architect step failed "load worker version … db: not found", `reconcileRun` errored and **rolled back the whole pass — undoing the DevOps step's terminal mark**, leaving it "running" with a succeeded execution underneath. Then the **manager `dequeue` busy-looped forever** on the single requeued key (captured `now` once, re-appended the not-ready key endlessly): ~150% CPU, the workflow reconciler's advisory-lock connection (PID 231) stopped renewing, and no run was reconciled again. Three fixes: **(1) `dequeue` bounded to one pass** (single not-ready key returns ok=false instead of spinning — the CPU bug, regression-tested) + `maxDAGPasses` cap on the DAG loop. **(2) step-dispatch worker-resolution failure now fails that step** (clear reason, run terminalizes, recovery/retry re-drives) instead of erroring + rolling back the entire pass. **(3) index-integrity sweep** — boot + every `ORCHICON_INDEX_CHECK_INTERVAL` (default 6h), validates every user btree index with amcheck `bt_index_parent_check` and rebuilds corrupt ones with `REINDEX INDEX CONCURRENTLY`; **ForceProgressWorkflowRun** RPC + "Force next step" button on the run view + Ask Orchicon tool (`_forced: true` markers, terminates still-running linked executions). `make ci` green; dequeue regression tests added. |
| 79 | Execution page shows the real per-step system prompt | done | v0.1.187: the execution detail page's "System prompt" card read the shared work item's `prompt_context`, which is a **shared input reference that carries the FIRST step's composite forever** (never mutated per-step — by design, `workflow_reconciler.go:985`). In a workflow run, every execution displayed "You are a DevOps Engineer" (the first step's role) regardless of the actual worker — a pure display bug; the model itself was dispatched with the correct per-step prompt. Fix: added `WorkerExecution.system_prompt`, populated server-side from the **linked workflow step run's `_prompt`** (the exact composite the execution was dispatched with) via new `db.GetWorkflowStepRunByExecution` (matches on `worker_execution_id`), with the work item's `prompt_context` kept only as a fallback for legacy/non-workflow dispatches. Verified against prod data: `GetWorkflowStepRunByExecution('01KZ96TSMAHWW9HNR4SAH9FAVE')` returns the Senior Software Engineer step run's `_prompt` ("experienced full-stack engineer"), not the DevOps ticket. `make ci` green. |
| 81 | Avoid full runtime image rebuilds on every build | done | v0.1.190: **stock runtime images are no longer rebuilt (or churned) on every build, and custom-image Deploy is idempotent.** (1) **Binary no longer baked** — removed `COPY orchicon` from `deploy/runtime/Dockerfile`, the `cp bin/orchicon deploy/runtime/orchicon` steps from `scripts/container.sh` + `release.yml`; the runtime daemon now bind-mounts **its own executable** (`os.Executable()`, resolved at start, `Daemon.ExePath`) read-only at `/usr/local/bin/orchicon` in every runtime container (the mount is a hard dependency of the `orchicon runtime-supervisor` entrypoint — a missing executable fails the create with a clear error, never a silent skip), so rebuilt binaries reach new containers with no image rebuild. (2) **Stock builds version-gated** — `container.sh` computes `org.orchicon.runtime.version` labels (`<app-version>-<sha256-12 Dockerfile>`; `:gui`/`:dev` embed the base's version for the cascade) and skips each variant when the existing image's label matches (`runtime_image_needs_rebuild`), with `FORCE_RUNTIME=1` / `ORCHICON_FORCE_RUNTIME_REBUILD=1` bypass; release.yml tags runtime builds with the same label. (3) **Custom Deploy short-circuits** — new `runtime_images.built_version` column (migration `20260805120000_runtime_image_built_version.sql`, no backfill → one safe rebuild after upgrade), `StatusOnly` flag on `db.UpdateRuntimeImageFields` so build-flow transitions stop bumping `version` (only spec edits bump — `version` is now the "spec changed" signal), `BuildRuntimeImage` skips the daemon entirely when `status=ready && tag!='' && built_version==version` (single "already up to date" log line + final response, no build, no prune), passes `SpecVersion` to the daemon which injects `LABEL org.orchicon.runtime.spec-version=<n>` into every custom build, and stamps `built_version=version` on success (failure leaves it stale → rebuild on retry). **Concurrent Deploys are serialized** — the read→mark-building transition runs under `SELECT … FOR UPDATE` (`db.GetRuntimeImageForUpdate`) so two simultaneous Deploys can't both `docker build` the same tag (the StatusOnly mark doesn't bump version, so version-OCC alone couldn't gate it; the lock loser re-reads `building` and fails fast). Proto + codegen: `BuildRuntimeImageResponse.skipped`; frontend Deploy shows "already up to date" on skip (keeps the server's log line, no overwrite). The stale gitignored `deploy/runtime/orchicon` artifact (left by the pre-change `cp`) was deleted and excluded from the runtime build context (`deploy/runtime/.dockerignore`); `make clean` removes both context copies. Docs updated (DOCUMENTATION.md §Workflow Runtime Containers / Runtime Images / Build, AGENTS.md, README). Verified: `go build/vet/test ./...`, `make gen`, `buf lint`, updated `rewriteDockerfileBase` tests (spec-version label injected/omitted). Docker-based verification (two back-to-back `make container-build`, label inspect, live Deploy short-circuit) NOT run — this environment has no Docker daemon; the label-gate logic in `container.sh` is exercised by inspection of the generated commands. |
| 80 | Architecture-notes housekeeping | done | v0.1.188: `architecture-notes/` added to `.gitignore` and the leftover architecture note (`keep-worker-type-approvals-without-work-item-creation.md`) removed from the repo so worker-generated design summaries can never be committed and confuse future workers. The canned DevOps Engineer worker AGENTS.md (seeded) gained a "Clean up architecture notes (before PR & merge)" final-step instruction — remove any leftover architectural documents from the repo and working tree (git rm tracked ones, delete untracked leftovers) before creating the PR. Seed marker bumped `orchicon.safety=v6`→`v7` so the updated context rolls forward to existing seeded DevOps Engineer workers. README Last Release Changes updated. |
| 82 | Canned stock runtime images (editable rows) | done | v0.1.191: **the shipped stock runtime images are no longer a separate read-only section — they seed as normal, editable `runtime_images` rows (`source='stock'`) on every boot, exactly like canned workers.** (1) **Migration** `20260805200000_runtime_image_source.sql` adds `runtime_images.source` (`'stock'`/`'custom'`). (2) **`runtimeimage.SeedCannedImages`** (boot, mirrors `SeedDevWorkers`; dev/prod instances each seed their own DB) creates one row per daemon-reported stock variant (base/`:gui`/`:dev`) with the shipped Dockerfile template + a self-consistent `# orchicon.seed=<sha12>` marker as the `dockerfile_override`; `seedState` classifies a row as seed-pristine only when its body hashes to its own marker — current seeds reconcile in place, stale seeds roll forward (`version` bumps, `built_version` lags → **async auto-build**, which prunes the previous tag version), and user-edited bodies are **never touched** (user edits survive seed bumps like edited canned workers). Rows are created ready (`built_version=version`) when the local image is already current (container.sh/installer built it pre-boot), else auto-built; failed auto-builds are not retried every boot. (3) **`container.sh` skip rule** — `runtime_image_needs_rebuild` also skips any image carrying `org.orchicon.runtime.spec-version` (a tenant-owned daemon build from an edited canned row), so an edited+redeployed canned image survives `make container-rebuild`; the existing label gate is unchanged. (4) **Daemon `GET /v1/images`** now returns per-tag `org.orchicon.runtime.version` / `spec-version` labels (`ImageList.Infos`) for the seeder's reconciliation. (5) **`BuildRuntimeImage` refactored** into a shared `buildCore` (RPC streams logs; seeder auto-build discards) — same row-lock serialization, skip gate, and built_version stamping. (6) **Delete protection** — `DeleteRuntimeImage` never `docker rmi`s the daemon's base image or stock rows (deleted canned rows re-seed next boot). (7) **Frontend** — list page merges stock rows into the main list (stock badge + spec version/built_version), separate stock section removed; detail page shows version/built_version/source; `ListAvailableRuntimeImages` de-dupes stock tags covered by ready rows. Unit tests for `resolveVariant`, `seedState`, `seedVersion`, `stockImageCurrent`. Docs updated (README, DOCUMENTATION.md, AGENTS.md). |
| 83 | Canned UI workers + Playwright-ready dev image + worker prompt/clone fixes | done | v0.1.191: (1) **Canned UI workers** — three UI-focused workers (`w_ui_design_architect` / `w_ui_developer` / `w_ui_qa_engineer`) are seeded on boot with UI-development profiles (design systems/tokens/accessibility, design-system-driven implementation, visual/a11y/responsive QA) + a shared **Browser automation (Playwright)** AGENTS.md block: the runtime container has no root process so Chromium must launch with `--no-sandbox`, via a project `scripts/browser.mjs` helper the worker creates on first use. (2) **`:orchicon-dev` runtime image** now preinstalls Playwright + headless Chromium into `/ms-playwright` (chowned to uid 1000, `PLAYWRIGHT_BROWSERS_PATH` set); the canned stock-dev row rolls forward + auto-builds on next boot. (3) **Bug fix: prompt edits vanished.** Editing Role/Skills/Behavior/AGENTS.md saved into a `system_prompt` string that `db.UpdateDraftVersion` never persisted (its SET clause omits system_prompt), so edits silently disappeared on reload. The four fields are now first-class API fields (`role`/`skills`/`behavior`/`agents_md` on `WorkerVersion` + `CreateWorkerRequest`/`UpdateWorkerVersionRequest`/`CreateWorkerVersionRequest`); the service stores them as the source of truth and recomposes `system_prompt` (legacy `system_prompt`-only clients still work); the frontend sends/reads the structured fields directly with a regex fallback for legacy versions. (3b) **Bug fix: worker clone failed.** Code-view Clone reused the source slug → `workers_tenant_slug_idx` violation. `CreateWorker` now dedupes slugs per tenant (`-2`, `-3`, … via new `db.WorkerSlugExists`), and Clone sends a `-clone` slug and copies the version fields. (3c) **Bug fix: canned-worker drafts force-published on boot.** The seeder's "publish any stray drafts" step promoted a user's in-progress draft every restart; it now only promotes a draft when the worker has **no published version left** (and only the latest one, with `current_version` following) — user drafts alongside a published version stay drafts. DB-backed tests (`TestSeedLeavesUserDraftUntouched`, `TestSeedPublishesLatestDraftWhenNoPublishedVersion`) guard both paths; E2E verified a reboot leaves a user draft draft. Verified E2E: UI workers seeded published with structured fields; editing a draft Role persists + round-trips after reload; cloning creates `ui-developer-clone` with copied model/prompt; `:dev` image contains playwright + chromium. |
| 84 | Canned seeder adopts empty slug-owner workers (prod UI workers now have their text) | done | v0.1.192: prod's UI workers were created manually (ULID ids) before they were canned, so the seeder's worker INSERT collided on `workers_tenant_slug_idx` and **aborted the entire seed batch** — the canned `w_ui_*` workers never appeared and the manual shells stayed empty (their content had been lost to the prompt round-trip bug). `seedWorker` now resolves the target by canned id, else by slug: a worker owning the canned slug that is an **empty shell** (all prompt content blank) is **adopted in place** — its id is preserved (workflow step refs stay valid) and the existing needSync path fills its v1 with the canned profile (role/skills/behavior/agents_md + safety/Playwright blocks); a worker with real content keeps the slug untouched (skipped quietly via `errSeedSkipWorker`). `SeedDevWorkers` now logs-and-continues past a failing worker (`errors.Join` at the end) instead of aborting. DB-backed tests added for adoption (`TestSeedAdoptsEmptySlugOwner`), skip-on-content (`TestSeedSkipsCustomizedSlugOwner`), plus the existing draft-preservation tests. Verified E2E on prod: the 3 empty ULID UI workers kept their ids and gained the full canned profiles; no `w_ui_*` duplicates; the 2 workflow_versions referencing the UI Developer still resolve. |
| 85 | Approval deep links include the workflow ID segment | done | v0.1.195: the Approvals page rendered broken run deep links (`http://localhost:8080/workflows//runs/{run_id}`) because `ApprovalItem` carried no workflow id and the frontend passed an empty segment. `workflow_id = 12` added to the proto; `listApprovalItems` SELECT/Scan populates it from the existing `workflow_runs` join (the inner join guarantees a non-empty segment); both approvals.tsx links now render `/workflows/{workflow_id}/runs/{run_id}`. Codegen regenerated (Go + TS). Verified: `make ci` green, QA confirmed both links render the correct URL. |
| 86 | Expanded light & dark theme palette (incl. Gruvbox Light) | done | v0.1.196: 28 themes (14 light + 14 dark) selectable from the theme picker. **Light themes darkened** — all 10 existing light themes moved from background L 97–100 % to 93–96 % (tinted primaries darkened where white button text failed WCAG AA) so workflow nodes/edges stop washing out in bright rooms. **Dark themes lightened** — all 10 existing dark themes moved from background L 7–8 % to 10–11 % and muted-foreground raised 58 % → 62 % for bright-lighting readability. **8 new themes**: Gruvbox Light (warm parchment `#fbf1c7` background, deep Gruvbox blue `#2b6064` primary — spec-corrected tuple), Paper, Ash, Porcelain (light set); Graphite, Dusk, Forest, Cocoa (lighter darks, 13–15 % background). Light-mode `--kind-*` canvas edge accents darkened to pass ≥3:1 non-text contrast on the darkest new light background; dark-mode set unchanged. `themes.ts`/`index.css` token blocks transcribe the contrast-verified design spec; persisted theme ids stay valid. Docs updated (README, DOCUMENTATION.md, UPDATES). |
| 87 | Schedules page (upcoming + history views) | done | v0.1.197: new **Schedules** page showing all scheduled work items in chronological order with their next runtimes, plus a history of items that already ran. Frontend-only (no proto/DB changes): `frontend/src/routes/schedules.tsx` renders an agenda timeline grouped by local day (Today / Tomorrow / weekday date) for the **Upcoming** view (default, `status = WORK_ITEM_STATUS_SCHEDULED`, 5s polling) and a reversed rail for **History** (`scheduled_start_at` set + terminal status, client-side filter since ListWorkItems accepts one status, 30s polling) — both deep-linkable via `?view=` search params. Cards link to `/work-items/$id` and `/workflows/$id` (plus `/workflows/$id/runs/$runId` in History); the standard filter bar (search / project / schedulable-kind / run-time sort asc-desc, select-all + "N of M selected", bulk Cancel for Upcoming via soft delete / bulk Delete for History) mirrors executions/work-items. Polish: live clock (`role="timer"`, pulse dot, pauses on `visibilitychange`), countdown chips with descriptive aria labels, stats strip (upcoming / next run / due today), kind dots reusing the work-items palette, and a "One-time" frequency slot reserved for the future recurring-schedule feature (`recurrenceBadge` is the only helper that will change). Also added `refetchInterval` option to `useListWorkItems` and `min-w-0` on the app-shell content column (fixes horizontal scroll on small viewports). Route registered in `routeTree.gen.ts` (generated); nav entry added after Work Items. Docs updated (README, DOCUMENTATION.md, UPDATES). Verified: `npm run build` + `npm run routes` clean, no new lint errors, `make ci` green except pre-existing `internal/guard` noexec-tmpdir env failures, and browser verification (Chrome) at 375/768/1280px in light (gruvbox-light) + dark (midnight) themes with seeded scheduled/history items — links, filters, bulk cancel, empty states, and run-chip all working, no console errors. |
| 88 | Work Items board fixes | done | v0.1.201: Board drag-and-drop now uses `DragOverlay` so cards float across columns (was snapping back due to CSS transform on original DOM element). Fixed hierarchy rendering: orphaned children (items whose parent is in a different column) now render as root-level nodes instead of disappearing. Removed `min-w-[280px]` from columns so all 7 board columns share the viewport width evenly. Removed the dependency graph page and filter bar link (the DAG is still available via the API for block-state chips). |
| 85 | Real browser-based UI verification for workers | done | v0.1.193: **UI workers can now actually LOOK at the browser.** The `:orchicon-dev` runtime image already shipped Playwright + Chromium, but a worker's script couldn't `require('playwright')` from an arbitrary project dir — ESM `import` ignores `NODE_PATH` and the global npm package wasn't resolvable (verified: CJS `require` + `NODE_PATH` launches Chromium as uid 1000 + screenshots fine; ESM `import` fails). Fixes: (1) `Dockerfile.dev` adds `ENV NODE_PATH=/usr/local/lib/node_modules` so `require('playwright')` resolves globally. (2) The canned UI workers' AGENTS.md Playwright block is now a full **VISUAL verification** protocol: start the app in-container, `launch()` (--no-sandbox), navigate at desktop + mobile viewports, `shot()` to `/tmp/orchicon/<n>.png`, **read the screenshot back** (that's how the model sees the UI), iterate against the AC; helper is `scripts/browser.cjs` (CommonJS) with `launch()`/`shot()`. (3) Seed marker bumped to v8 AND `safetyBlock` now emits the marker from the `seedSafetyMarker` constant (it was hardcoded v7, so the content marker never matched the roll-forward gate). (4) Fixed an adoption follow-on: a previously-adopted worker (canned slug under a ULID id) was being skipped as a "user worker" once filled, so seed updates never rolled forward to it — `workerIsSeedManaged` (content carries `orchicon.safety=`) now keeps adopted workers in the sync set; `TestSeedKeepsSyncingAdoptedWorker` guards it. Verified: dev + prod UI workers carry v8 + the VISUAL block; a real headless Chromium screenshot runs in the image as uid 1000. |
| 86 | DevOps cleanup empties notes dirs instead of deleting them + covers design-notes | done | v0.1.200 (same release as the browser-verification work): the DevOps Engineer worker's pre-PR cleanup step told workers to "remove architectural documents from the repo and working tree", which workers implemented by `rm -rf`-ing the whole `architecture-notes/` directory (the user wanted only the FILES gone) — and `design-notes/` (created by the UI workers) was never cleaned at all. The instruction now explicitly says to **delete the FILES inside both `architecture-notes/` and `design-notes/`, keeping the directories** (`git rm` tracked, unlink untracked, never remove the folder). Seed marker bumped v8 → v9 so the updated context rolls forward to the canned DevOps Engineer (and all canned workers). Both dirs were already gitignored. |
