// Orchicon AskOrchiconService.
//
// Provides the conversational "Ask Orchicon" agent: streaming chat,
// conversation management, attachment upload, and agent configuration.
// The agent can create, read, update, and delete any Orchicon entity,
// answer questions about the platform and user data, diagnose failures,
// and create project directory scaffolding.

// @generated by protoc-gen-connect-es v1.7.0 with parameter "target=ts"
// @generated from file orchicon/api/v1/ask_orchicon_service.proto (package orchicon.api.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { AbortConversationTurnRequest, AbortConversationTurnResponse, ChatStreamRequest, ChatStreamResponse, CreateConversationRequest, CreateConversationResponse, DeleteConversationRequest, DeleteConversationResponse, GetAgentConfigRequest, GetAgentConfigResponse, GetConversationRequest, GetConversationResponse, GetModelCapabilitiesRequest, GetModelCapabilitiesResponse, ListConversationsRequest, ListConversationsResponse, ListMessagesRequest, ListMessagesResponse, UpdateAgentConfigRequest, UpdateAgentConfigResponse, UpdateConversationTitleRequest, UpdateConversationTitleResponse, UploadAttachmentRequest, UploadAttachmentResponse } from "./ask_orchicon_service_pb.js";
import { MethodKind } from "@bufbuild/protobuf";

/**
 * Conversation CRUD.
 *
 * @generated from service orchicon.api.v1.AskOrchiconService
 */
export const AskOrchiconService = {
  typeName: "orchicon.api.v1.AskOrchiconService",
  methods: {
    /**
     * ListConversations returns conversations for the calling tenant,
     * ordered by updated_at descending (most recent first).
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.ListConversations
     */
    listConversations: {
      name: "ListConversations",
      I: ListConversationsRequest,
      O: ListConversationsResponse,
      kind: MethodKind.Unary,
    },
    /**
     * GetConversation returns a single conversation by id.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.GetConversation
     */
    getConversation: {
      name: "GetConversation",
      I: GetConversationRequest,
      O: GetConversationResponse,
      kind: MethodKind.Unary,
    },
    /**
     * CreateConversation creates a new conversation. Optionally accepts
     * an initial message to start the conversation immediately.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.CreateConversation
     */
    createConversation: {
      name: "CreateConversation",
      I: CreateConversationRequest,
      O: CreateConversationResponse,
      kind: MethodKind.Unary,
    },
    /**
     * DeleteConversation deletes a conversation and all its messages.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.DeleteConversation
     */
    deleteConversation: {
      name: "DeleteConversation",
      I: DeleteConversationRequest,
      O: DeleteConversationResponse,
      kind: MethodKind.Unary,
    },
    /**
     * UpdateConversationTitle updates the title of a conversation.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.UpdateConversationTitle
     */
    updateConversationTitle: {
      name: "UpdateConversationTitle",
      I: UpdateConversationTitleRequest,
      O: UpdateConversationTitleResponse,
      kind: MethodKind.Unary,
    },
    /**
     * ListMessages returns messages for a conversation, ordered by
     * created_at ascending (oldest first).
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.ListMessages
     */
    listMessages: {
      name: "ListMessages",
      I: ListMessagesRequest,
      O: ListMessagesResponse,
      kind: MethodKind.Unary,
    },
    /**
     * ChatStream sends a message (with optional attachments) to a
     * conversation and returns immediately with a TurnStarted ack carrying
     * the assistant message id under which the reply will be persisted. The
     * reply is collected on a request-independent context (bounded by the
     * reply window) and delivered by polling ListMessages — a browser
     * disconnect or tab close never cancels or loses it. The TextChunk /
     * ToolCallResult / ErrorChunk / DoneSignal events are retained for a
     * future SSE surface but are not emitted by the current implementation.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.ChatStream
     */
    chatStream: {
      name: "ChatStream",
      I: ChatStreamRequest,
      O: ChatStreamResponse,
      kind: MethodKind.ServerStreaming,
    },
    /**
     * AbortConversationTurn aborts the in-flight turn on a conversation's
     * opencode session (the Stop button). The session itself is kept alive
     * for the next message — only the running turn is cancelled, and the
     * collector persists a user-initiated-stop error message under the acked
     * assistant message id. Idempotent: aborting a conversation with no
     * running turn is a no-op.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.AbortConversationTurn
     */
    abortConversationTurn: {
      name: "AbortConversationTurn",
      I: AbortConversationTurnRequest,
      O: AbortConversationTurnResponse,
      kind: MethodKind.Unary,
    },
    /**
     * UploadAttachment uploads a file attachment for use in a message.
     * Returns a URL that can be referenced in subsequent ChatStream calls.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.UploadAttachment
     */
    uploadAttachment: {
      name: "UploadAttachment",
      I: UploadAttachmentRequest,
      O: UploadAttachmentResponse,
      kind: MethodKind.Unary,
    },
    /**
     * GetAgentConfig returns the Ask Orchicon agent configuration for
     * the calling tenant.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.GetAgentConfig
     */
    getAgentConfig: {
      name: "GetAgentConfig",
      I: GetAgentConfigRequest,
      O: GetAgentConfigResponse,
      kind: MethodKind.Unary,
    },
    /**
     * UpdateAgentConfig updates the Ask Orchicon agent configuration.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.UpdateAgentConfig
     */
    updateAgentConfig: {
      name: "UpdateAgentConfig",
      I: UpdateAgentConfigRequest,
      O: UpdateAgentConfigResponse,
      kind: MethodKind.Unary,
    },
    /**
     * GetModelCapabilities returns the capabilities of a specific model
     * (text-only vs multi-modal), so the frontend can adapt the UI.
     *
     * @generated from rpc orchicon.api.v1.AskOrchiconService.GetModelCapabilities
     */
    getModelCapabilities: {
      name: "GetModelCapabilities",
      I: GetModelCapabilitiesRequest,
      O: GetModelCapabilitiesResponse,
      kind: MethodKind.Unary,
    },
  }
} as const;

