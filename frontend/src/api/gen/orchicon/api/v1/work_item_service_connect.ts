// Orchicon WorkItemService.
//
// WorkItem CRUD + dependency DAG management + worker assignment
// (docs/07_API_Specification.md §3.2). All mutations write through the
// transactional outbox (docs/09_Database_Schema.md §6) and use
// optimistic concurrency via the version column (docs/09 §5).
// Dependency traversal uses a recursive CTE
// (docs/09_Database_Schema.md §11).

// @generated by protoc-gen-connect-es v1.7.0 with parameter "target=ts"
// @generated from file orchicon/api/v1/work_item_service.proto (package orchicon.api.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { AddDependencyRequest, AddDependencyResponse, ArchiveWorkItemRequest, ArchiveWorkItemResponse, AssignWorkerRequest, AssignWorkerResponse, ControlSequenceRequest, ControlSequenceResponse, CreateWorkItemRequest, CreateWorkItemResponse, DeleteWorkItemRequest, DeleteWorkItemResponse, DismissIdeaRequest, DismissIdeaResponse, GetDependencyGraphRequest, GetDependencyGraphResponse, GetWorkItemRequest, GetWorkItemResponse, GetWorkItemRunHistoryRequest, GetWorkItemRunHistoryResponse, HardDeleteWorkItemRequest, HardDeleteWorkItemResponse, ListIdeasRequest, ListIdeasResponse, ListWorkItemsRequest, ListWorkItemsResponse, PromoteIdeaRequest, PromoteIdeaResponse, RemoveDependencyRequest, RemoveDependencyResponse, ReorderWorkItemsRequest, ReorderWorkItemsResponse, RestoreWorkItemRequest, RestoreWorkItemResponse, UnassignWorkerRequest, UnassignWorkerResponse, UpdateWorkItemRequest, UpdateWorkItemResponse } from "./work_item_service_pb.js";
import { MethodKind } from "@bufbuild/protobuf";

/**
 * @generated from service orchicon.api.v1.WorkItemService
 */
export const WorkItemService = {
  typeName: "orchicon.api.v1.WorkItemService",
  methods: {
    /**
     * CreateWorkItem creates a new work item (epic/feature/task/subtask)
     * within a project. Depth is constrained to 4 levels.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.CreateWorkItem
     */
    createWorkItem: {
      name: "CreateWorkItem",
      I: CreateWorkItemRequest,
      O: CreateWorkItemResponse,
      kind: MethodKind.Unary,
    },
    /**
     * GetWorkItem returns a single work item by id.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.GetWorkItem
     */
    getWorkItem: {
      name: "GetWorkItem",
      I: GetWorkItemRequest,
      O: GetWorkItemResponse,
      kind: MethodKind.Unary,
    },
    /**
     * GetWorkItemRunHistory returns the per-fire run-history ledger for a
     * recurring work item, newest first (fire status, start/end, execution
     * ids, outputs). Used by the recurring-item detail view (4.3) and API.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.GetWorkItemRunHistory
     */
    getWorkItemRunHistory: {
      name: "GetWorkItemRunHistory",
      I: GetWorkItemRunHistoryRequest,
      O: GetWorkItemRunHistoryResponse,
      kind: MethodKind.Unary,
    },
    /**
     * ListWorkItems returns a page of work items for a project, optionally
     * filtered by parent (tree) or status (Kanban).
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.ListWorkItems
     */
    listWorkItems: {
      name: "ListWorkItems",
      I: ListWorkItemsRequest,
      O: ListWorkItemsResponse,
      kind: MethodKind.Unary,
    },
    /**
     * UpdateWorkItem applies a partial update with optimistic concurrency.
     * Only mutable fields are written (field-mask semantics — docs/07 §5.4).
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.UpdateWorkItem
     */
    updateWorkItem: {
      name: "UpdateWorkItem",
      I: UpdateWorkItemRequest,
      O: UpdateWorkItemResponse,
      kind: MethodKind.Unary,
    },
    /**
     * DeleteWorkItem soft-deletes a work item (sets status to cancelled).
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.DeleteWorkItem
     */
    deleteWorkItem: {
      name: "DeleteWorkItem",
      I: DeleteWorkItemRequest,
      O: DeleteWorkItemResponse,
      kind: MethodKind.Unary,
    },
    /**
     * HardDeleteWorkItem permanently removes a work item. Cascades to
     * dependencies (work_item_dependencies rows where this item is either
     * the from or to side are deleted). The outbox emits a
     * work_item.purged event. Use this for irreversible removal — the
     * soft DeleteWorkItem is preferred for normal cancellation.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.HardDeleteWorkItem
     */
    hardDeleteWorkItem: {
      name: "HardDeleteWorkItem",
      I: HardDeleteWorkItemRequest,
      O: HardDeleteWorkItemResponse,
      kind: MethodKind.Unary,
    },
    /**
     * AddDependency adds an edge to the work DAG. Cycles are rejected at
     * admission (docs/02 §2.2, docs/09 §3.2).
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.AddDependency
     */
    addDependency: {
      name: "AddDependency",
      I: AddDependencyRequest,
      O: AddDependencyResponse,
      kind: MethodKind.Unary,
    },
    /**
     * RemoveDependency removes an edge from the work DAG.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.RemoveDependency
     */
    removeDependency: {
      name: "RemoveDependency",
      I: RemoveDependencyRequest,
      O: RemoveDependencyResponse,
      kind: MethodKind.Unary,
    },
    /**
     * GetDependencyGraph returns the full DAG (nodes + edges) for a
     * project. Used by the frontend's read-only React Flow graph (docs/10).
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.GetDependencyGraph
     */
    getDependencyGraph: {
      name: "GetDependencyGraph",
      I: GetDependencyGraphRequest,
      O: GetDependencyGraphResponse,
      kind: MethodKind.Unary,
    },
    /**
     * AssignWorker binds a Worker (id + version) to a Task/Subtask.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.AssignWorker
     */
    assignWorker: {
      name: "AssignWorker",
      I: AssignWorkerRequest,
      O: AssignWorkerResponse,
      kind: MethodKind.Unary,
    },
    /**
     * UnassignWorker removes the worker binding from a Task/Subtask.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.UnassignWorker
     */
    unassignWorker: {
      name: "UnassignWorker",
      I: UnassignWorkerRequest,
      O: UnassignWorkerResponse,
      kind: MethodKind.Unary,
    },
    /**
     * ReorderWorkItems renumbers sort_order for the siblings under parent_id
     * (empty = top level) to the given order, in one transaction. Display
     * sort (ListWorkItems sort_by) never mutates sort_order — only this RPC
     * does. Safe to call while a sequence is running: the sequence cursor is
     * derived from sort_order at reconcile time, so a mid-run drag shifts
     * only future arming.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.ReorderWorkItems
     */
    reorderWorkItems: {
      name: "ReorderWorkItems",
      I: ReorderWorkItemsRequest,
      O: ReorderWorkItemsResponse,
      kind: MethodKind.Unary,
    },
    /**
     * ArchiveWorkItem hides a terminal work item (succeeded/failed/cancelled/
     * skipped) from every normal view. Blocked when the item has children.
     * Reversible via RestoreWorkItem. Audited as work_item.archived.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.ArchiveWorkItem
     */
    archiveWorkItem: {
      name: "ArchiveWorkItem",
      I: ArchiveWorkItemRequest,
      O: ArchiveWorkItemResponse,
      kind: MethodKind.Unary,
    },
    /**
     * RestoreWorkItem returns an archived work item to the active views,
     * back to the terminal status it was archived from (not pending).
     * Audited as work_item.restored.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.RestoreWorkItem
     */
    restoreWorkItem: {
      name: "RestoreWorkItem",
      I: RestoreWorkItemRequest,
      O: RestoreWorkItemResponse,
      kind: MethodKind.Unary,
    },
    /**
     * ListIdeas returns a page of idea-state work items (automation-produced
     * items awaiting triage, feature 5.1) with their automation provenance
     * (spawned_by + spawned_by_run_id) and a read-time spawned_by_title badge.
     * Scoped to status='idea' by construction — it shares the 4.1 idea gate
     * with ListWorkItems(IdeaScope=only), so membership can never diverge.
     * idea_state_scope=REJECTED reads the rejected graveyard instead:
     * dismissed idea spawns (status='cancelled' WITH automation provenance),
     * retained so rejected ideas stay visible, readable history — and so an
     * automation's dedupe gate can never re-propose something a human
     * already rejected.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.ListIdeas
     */
    listIdeas: {
      name: "ListIdeas",
      I: ListIdeasRequest,
      O: ListIdeasResponse,
      kind: MethodKind.Unary,
    },
    /**
     * PromoteIdea approves an idea: it transitions the idea-state work item
     * to a normal pending work item (leaves idea state, becomes queryable in
     * the normal Work Items scope with normal status semantics, and can be
     * planned/scheduled/run through the existing pipeline). Provenance fields
     * are retained for display. Audited as work_item.promoted. PromoteIdea is
     * the ONLY sanctioned path out of idea state (the generic update path is
     * gated for idea items).
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.PromoteIdea
     */
    promoteIdea: {
      name: "PromoteIdea",
      I: PromoteIdeaRequest,
      O: PromoteIdeaResponse,
      kind: MethodKind.Unary,
    },
    /**
     * DismissIdea discards an idea: it transitions the idea-state work item
     * to cancelled (the soft-delete/cancel terminal, consistent with
     * DeleteWorkItem), so it leaves idea state and drops out of every active
     * view. Provenance is retained as a record of where it came from. Audited
     * as work_item.dismissed.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.DismissIdea
     */
    dismissIdea: {
      name: "DismissIdea",
      I: DismissIdeaRequest,
      O: DismissIdeaResponse,
      kind: MethodKind.Unary,
    },
    /**
     * ControlSequence drives a sequence parent manually (START / RESUME /
     * STOP). A parent with children IS a sequence run; these explicit
     * gestures are what the engine's derived cursor cannot infer on its own:
     *   - START re-fires the chain from child #1 (destructive — every
     *     descendant resets to pending); validations + in-flight guards run
     *     server-side.
     *   - RESUME continues from the first non-succeeded child (keeps state).
     *   - STOP parks the chain (parent → pending, schedule cleared) so
     *     children can be run standalone.
     *
     * @generated from rpc orchicon.api.v1.WorkItemService.ControlSequence
     */
    controlSequence: {
      name: "ControlSequence",
      I: ControlSequenceRequest,
      O: ControlSequenceResponse,
      kind: MethodKind.Unary,
    },
  }
} as const;

