// Orchicon FileEditService: durable file-edit ledger fetch + live stream.
//
// API-key auth rides the shared interceptor chain like every other service;
// the read policy mirrors ExecutionService's (a caller that can read an
// execution can read its file-edit ledger).

// @generated by protoc-gen-connect-es v1.7.0 with parameter "target=ts"
// @generated from file orchicon/api/v1/file_edit_service.proto (package orchicon.api.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { GetSessionFileEditsRequest, GetSessionFileEditsResponse, StreamFileEditsRequest, StreamFileEditsResponse } from "./file_edit_pb.js";
import { MethodKind } from "@bufbuild/protobuf";

/**
 * @generated from service orchicon.api.v1.FileEditService
 */
export const FileEditService = {
  typeName: "orchicon.api.v1.FileEditService",
  methods: {
    /**
     * GetSessionFileEdits returns the file-edit ledger for one owner
     * (execution or Ask conversation), optionally resuming from a sequence —
     * the initial load + catch-up read for the GUI sidebar / TUI pane.
     *
     * @generated from rpc orchicon.api.v1.FileEditService.GetSessionFileEdits
     */
    getSessionFileEdits: {
      name: "GetSessionFileEdits",
      I: GetSessionFileEditsRequest,
      O: GetSessionFileEditsResponse,
      kind: MethodKind.Unary,
    },
    /**
     * StreamFileEdits is the server-stream RPC that fans out new ledger
     * entries for one owner as they are persisted. event_id dedups across
     * reconnects; from_sequence resumes after a sequence.
     *
     * @generated from rpc orchicon.api.v1.FileEditService.StreamFileEdits
     */
    streamFileEdits: {
      name: "StreamFileEdits",
      I: StreamFileEditsRequest,
      O: StreamFileEditsResponse,
      kind: MethodKind.ServerStreaming,
    },
  }
} as const;

