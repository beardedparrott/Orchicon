# wrangler.toml — CloudFlare Pages build config for orchicon.dev
#
# This file tells CloudFlare Pages how to build the static site.
# Source of truth: site/ (HTML + CSS). The build command copies the
# canonical install scripts from scripts/ into the deployed bundle, so
# `curl -fsSL https://orchicon.dev/install | bash` and
# `irm https://orchicon.dev/install.ps1 | iex` serve the same content
# as the repo's scripts/install.sh and scripts/install.ps1.
#
# Configure the GitHub integration in the CloudFlare dashboard
# (Workers & Pages → orchicon-site → Settings → Builds):
#   - Build command:        bash scripts/build-site.sh
#   - Build output dir:     site
#   - Root directory:       (leave blank = repo root)
#   - Environment variables: none required
#
# If you change the build approach, mirror it here and in
# CLOUDFLARE_SETUP.md (single source of truth for the deploy recipe).

name = "orchicon-site"
compatibility_date = "2024-09-23"
pages_build_output_dir = "site"

[build]
command = "bash scripts/build-site.sh"
